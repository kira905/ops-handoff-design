# 长会话的自动交接与接力

> **这篇文档讲什么**：一个长期运行的 agent 会话**撞上上下文上限**时，怎么把活**自动**交给一个新会话接着干。它回答四件事：**为什么不用"自动压缩"顶替**（摘要会反复发生、层层衰减，而操作级细节最先被抹掉）、**交接靠什么载体才可核查**（一份全文交接档 + 一台可重建的台账 + 一条给人看的标题轴，三者谁是权威源）、**一次交接要过哪十二道闸门**（其中"权限不降级"要分**静态段与运行时段两段断言**）、**以及无人值守时谁来替它写档**（在把会话尾部发出去之前的**那道唯一能拦外发的筛**）。
> **版本**：v1.10（**已于 2026-09-22 随本仓 tag `v1.10` 双端发布**）
> **素材基准日**：设计定稿 2026-09-20 → 生产实测 2026-09-22（含一次事故与一次口径反转，均如实写进正文）
> **占位符说明**：`<DSH 主目录>` = agent 的数据主目录；`<工作区根>` = 工作目录；`<宿主主目录>` = 使用者主目录；`steward-*` = 本仓 `02` 号文档既定的管家组件族占位名；`A 机 / B 机` = 两台长期运行的工作站（A = 日常开发机，B = 常驻稳态机）。**组件一律用职责名**（"接力器""上下文看门""调度器""动作登记表""模块登记表""只读面板""会话标题插件"）；文中的"所有者"= 这套体系的拥有者本人。
> **数字口径（重要）**：本文全部阈值与实测数字（软限 300K / 硬限 450K / 间隔 20 分钟 / 失败 2 次 / 标题字节预算 59B 等）**来自特定环境下的特定观测，环境相关、勿照抄**。它们证明的是"这门尺子长什么样、该怎么被质疑"，不是给你的参数。

---

## 0. 先看结论

1. **"提醒"与"新会话"之间那一步才是缺口。** 监控早就知道你会话快满了，但它只能**提醒**；而"开一个新会话、把在途的活交过去"这件事一直是人做的。**自主推进的主线一到上限就断在人这里**——断点不在检测，在动作。
2. **别用自动压缩顶替交接。** 压缩摘要是**一次性 LLM 生成 + 固定模板**，你无法指定"保留什么"；而路径、命令、校验和、单据编号这类**操作级细节最容易被抹掉**。更糟的是它会**反复发生**（每次越线再压一次）⇒ 摘要的摘要，信息层层衰减，且被压掉的内容**模型自己不知道**。
3. **交接档是唯一权威载体，其余一律是缓存或路标。** 台账（谁在跑、今日几次、深度几棒）**丢了只影响计数，不影响事实认定**；"这次交接到底发生过没有"的权威源是**追加写的审计流水**。把可重建的缓存当权威源，是这套设计里最贵的一次返工。
4. **标题不是装饰，是"主线有没有断"的唯一人眼信号。** 所以它值得被当成一个**有规范、有词表、有字节预算**的对象来设计——本文 §4 记录的两次实测与一次 59 毫秒事故，全都发生在标题上。
5. **闸门防的是"异常"，不是"次数"。** 一条主线接 5 次、6 次是**正常**的（原设计里的"单日 3 次 / 深度 3"因为与别的闸门互斥而被**当场删除**，见 §6.2）。**接力次数是健康指标，不是要压的数字。**
6. **权限必须"两段断言"，而且两段缺一不可。** 只判配置不算数——真正危险的是"**配置说 A、实际发出去的是更宽的 B**"这种静默放宽。所以既要判**设计期的源码常量与配置是否漂移**，也要在**新会话建好之后回读它真正继承到的那一份**。
7. **"读不到"绝不许当成"没问题"。** 安全闸门的空态（读不到、取值未知）**一律按最坏处理**；权限投影没就绪时要**宽限重试**（不许把"还没生成"判成"不一致"，否则每次尝试都会留下一个被归档的会话，而归档**不可逆**）。
8. **没有明确的"下一步"就不许开新会话**（fail-closed）。这是整套机制的止损线：宁可停在原地提示人，也不许凭空造出一个不知道要干什么的新会话。
9. **最危险的一步是"把会话尾部发给模型"。** 无人值守通道要靠模型写交接草稿，而**内容一进第三方请求体就不可撤回**⇒ 那道过滤必须在**组装请求之前**，而不是"写盘之前"（§8.2）。
10. **"看起来在工作"与"在工作"必须能被区分。** 每一条静默失败都对应一个具体的可见性缺口：提醒发出去了但没人动、额度用完了没人知道、降级之后不再触发、候选面长期为空——这些都得**各自有指标**，否则整条机制可以长期零动作而没人发现。

---

## 1. 问题：长跑会话撞上上下文上限

### 1.1 缺口在哪一步

把这条链拆开看，缺的从来不是检测：

| 环节 | 典型现状 | 谁在做 |
|---|---|---|
| 检测 | 上下文看门定期扫全部会话，越过软限 / 硬限就投一条提醒 | 自动化（**已有**） |
| 提醒文案 | "建议现在收尾 / 告诉所有者该开新会话" | 自动化（**已有，但没告诉人该跑什么**） |
| **交接动作** | **写交接档 + 开会话 + 改标题 + 改绑在途工单 + 提示所有者** | **人（这就是缺口）** |
| 新会话 | 由所有者手动开，再把交接档指给它 | 人 |
| 兜底 | 宿主自带的上下文压缩（阈值很高，日常根本触不到） | 宿主 |

一句话：**自动化已经会喊"你快满了"，但它不会接班。**

### 1.2 为什么不让自动压缩顶替

结论是**保留压缩作最后兜底、但不把它当交接手段**，四条理由：

1. 摘要是**一次性生成 + 固定模板**，**无法指定保留什么**——而路径 / 命令 / 校验和 / 单据编号 / 踩过的坑这类操作级细节，恰恰最容易被摘要抹掉；
2. 它会**反复发生**（每次越线再压一次）⇒ 摘要的摘要，**信息层层衰减**；
3. 被压掉的内容**模型自己不知道**（磁盘上的原始日志还在，但模型不会去翻）；
4. 对比之下，文档交接是**全文**：可核查（搜索 / 比对 / 校验和）、可修改、可跨会话跨机、**不衰减**。

> **一条实测画像**：某次全机盘点里，几十个会话中最高用量约 **43%**，**无一触及宿主自带的自动压缩线** ⇒ 那套自动压缩在真实使用中**从未跑过**。这提醒一件事：**别把"有个兜底机制"当成"问题已解决"**——它可能离触发线还差一倍。

### 1.3 分水岭：内容必须由"最懂的那个"来写

交接档里最有价值的两段是"**我们聊到一半的设计**"与"**待所有者拍板的事**"——这些**只存在于对话里，脚本读不到**；而当前会话的 agent 手握全部上下文，写得比任何摘要都准。

**这就是"保质"与"走流程"的分水岭**：自动化可以替你去开会话、改标题、改绑工单，但**不能替你写"下一步"**。所以主路径由 agent 自己发起——**它收到提醒、自己动手**，而不是"监控发现越线就替你开一个新会话"。

---

## 2. 目标与非目标

### 2.1 目标

- **G1 无人工干预的完整交接**：到软限时自动完成——写档 → 开会话 → 注入开工指令 → 在途工单改绑 → 旧会话停手并回报 → **由接手会话补标"已交接"**。
- **G2 主线在会话列表里可辨**：新会话以固定前缀开工，旧会话由接手方改标。
- **G3 零新建工单**：交接**不产生"交接卡"**，不污染工单池。
- **G4 失败可见、可停、可回滚**；**没有明确下一步就不开会话**（fail-closed）。
- **G5 无人值守也能接力**（第二阶段）：只对"正在执行的工单会话"生效（见 §8.1）。

### 2.2 非目标（明确不做）

- ❌ 不建"交接卡" / "接手卡"；
- ❌ **不替代所有者拍板**：遇到"选 A 还是选 B"只能出报告、攒清单等人；
- ❌ 不改宿主的压缩阈值，也不改监控的软限 / 硬限（本机制只改**提醒文案与随后动作**）；
- ❌ **不做跨机接力**（跨机换机仍是另一条链的事）；
- ❌ 不自动改记忆 / 技能 / 生产代码（接力只"接着干活"）；
- ❌ **不替"已经收手的会话"开会话**——这一条是**阻断级**的，它救过一次事故（见 §8.1）。

---

## 3. 三个载体：谁是权威源

### 3.1 分工

| 载体 | 角色 | 丢了会怎样 |
|---|---|---|
| **交接档**（`<工作区根>/交接-<主线名>-<日期>.md`） | **唯一权威的交接内容**（全文） | 新会话没有指引 ⇒ 链直接断 |
| **交接台账**（`<DSH 主目录>/…-relay.json`） | **可重建缓存**：运行键 / 当日计数 / 深度 / 两个窗口期状态 | 计数归零（**可能多发**），**不影响事实认定** |
| **审计流水**（`<DSH 主目录>/audit/<年-月>.jsonl`，追加写） | **"这次交接发生过没有"的权威源** | 追溯断档 |
| 标题轴（会话标题里的前缀） | **给人看的路标** | 人找不到接力关系 |

> **一条最贵的返工**：早期版本把"接力是否已发生"判在读台账 / 扫文件上，而另一处同一件事早就认定了"权威源 = 审计流水"——**同一事实两处口径**。修法是把台账**明确降级为可重建缓存**，判定一律回到审计。

### 3.2 交接档的七段结构与"结构契约"

七段：① 头部（机器可读的键值行）② 一句话现状 ③ 已完成（带证据路径）④ 在途 / 待验收 ⑤ **下一步（编号有序列表）** ⑥ 待所有者拍板 ⑦ 路径速查 + 红线。

**判据不是"人读着还行"，而是可执行的契约**：

```
段标题逐字固定：   `## 1. 头部` … `## 5. 下一步` …      （按行前缀匹配）
第 5 段合格判据：   段内至少 1 行匹配「有序列表首项且非空」
主线名取值顺序：    ① 头部 `mainline:` → ② 文件名段 → ③ 都取不到 ⇒ 拒绝接力
不合格处置：        写档但**不开新会话**，改为提示所有者
```

**必须配反例单测**：段标题写成顿号版、第 5 段只有无序列表、头部缺主线名字段——三种都**必须判不合格**。

### 3.3 档内容禁止清单（四类）+ 一道最要紧的筛

交接档是"全文留档 + 进私有备份仓 + 无人值守时由模型自动生成"的载体，而它的保留策略是"**不自动删**"⇒ 一旦写进敏感内容，它会**随备份扩散并同步到另一台机器**，且**没有自动清理**。所以四类内容禁写：

| # | 禁止类 | 写入前自检 |
|---|---|---|
| ① | **凭据值** | API key / token / 密码 / 凭据文件内容——**只许写"路径名"或"键名"，绝不写值** |
| ② | **内网地址** | 非回环 IP / 内网主机名 / 内部端口（`127.0.0.1` 与 `localhost` 除外） |
| ③ | **他人隐私** | 第三方姓名 / 联系方式 / 个人身份信息 |
| ④ | **会话原文** | 不粘贴会话全文；引用只写**结论 + 出处** |

**落地四处**（这是"由三处扩成四处"的一次修订）：**(a)** 档模板固定带一行"本档禁写"；**(b)** 无人值守草稿器**写盘前**跑正则粗筛，命中即**弃用草稿、降级为只写事实档**（不是"改一改再写"）；**(c)** 草稿校验清单里加这一条；**(d) ⚠️ 最要紧的一处：把同一套筛子**前移到"组装请求之前"**——见 §8.2。

### 3.4 一致性的自愈

三载体必须**每次进入自检时同时校准**："锁在而运行键缺 ⇒ 补；运行键在而锁丢 ⇒ 清"，且**只针对本链那一个对象**（不许误清并行的其它链）。这条自愈规则存在的理由很实在：人工删过锁之后，如果没人校准，最容易出现的是**"无人值守通道永远不再触发"这类静默故障**。

---

## 4. 标题轴：接力在界面上怎么被看见

### 4.1 三条轴与固定形态

| 轴 | 标记 | 谁判 | 含义 |
|---|---|---|---|
| 进程轴 | 【运行】 | 会话标题插件（按回合边界自动） | 回合正在跑 |
| 任务轴 | 【完成】/【待续】 | agent | 活闭环 / 收手但没闭环 |
| **接力轴（本机制新增）** | 【续】/【已交接】 | 接力器 / 标题标记工具 | "我是接手方" / "这条主线的活已交出去" |

规范写死为以下五步（**接力轴永远在最后，主线名永远是尾段**）：

```
① 旧会话收尾：        【完成】<主线名>                 ← 只标完成，不动接力轴
② 接力命令运行：      （旧会话标题保持不动）            ← 旧会话到此停手
③ 新会话创建：        【续】<主线名>                   ← 创建时直接命名
④ 跑起来之后：        【运行】【续】<主线名> → 【完成】【续】<主线名>
                                                       ← 插件自动叠状态前缀，正常形态
⑤ 新会话开工第一步：  改掉**源会话**标题为【已交接】<主线名>   ← 替换、不是叠加
```

**为什么由接手方补标**：① 源会话已近上限，少一次改名就少一次调用；② "已交接"是**事实发生之后**的标记，由"确实开工了"的那一方确认更准；③ 源会话可能在交接后立刻停手甚至被归档。

### 4.2 两次实测否掉了"单前缀"方案

原设计想让新会话只带一个前缀 `【续】<主线名>`。**实测做不到**：改名之后只要再跑一个回合，标题插件的回合边界逻辑就会**无条件重写**标题，把它变成 `【运行】【续】…`、收尾再变 `【完成】【续】…`（它**不看置顶标记**）。

⇒ 结论反转：**接受"状态轴在前、接力轴在后"的固定双层形态**，并把它当成正常形态写进文档——**不要当成覆盖故障去修**。

### 4.3 一次 59 毫秒的丢轴事故（本机制最有价值的一条）

双层形态定下来之后，生产上一直**没有真正达成**。原因很隐蔽：

- 接手会话创建时，标题**只有** `【续】<主线名>`（还没有状态前缀——状态前缀是回合边界才叠的）；
- 而标题插件的重写逻辑走的是**单层剥离**：它把开头的 `【…】` 当成"旧状态前缀"剥掉；
- 于是**接力后的第一个回合边界**，`【续】` 被当成状态前缀**吃掉** ⇒ 实测**59 毫秒内接力轴丢失**（标题事件从第 3 次跳到第 7 次）。源会话的"已交接"同样被剥成了"完成"。

**修法**：把"剥离"升级为**分层解析**——**状态轴照旧重写、接力轴原样保留**。

**代价是一条连带的教训**：光改插件不够——**两个配套判据也必须同步升级**。原来它们判"标题**以**【已交接】**开头**"，而新形态是"前缀段**包含**【已交接】"（例如 `【完成】【已交接】X`）⇒ 不改判据，过滤会**静默失效**（表现为"通知还投给已经停手的源会话"）。这是"改了写入方、忘了判定方"的经典形态。

### 4.4 字节预算：两道闸，取最紧的一档

标题有**两道**长度闸：① 宿主的标题字节上限（约 **80**）；② 标题插件自己还有一道**固定**闸（**64**）——它**与前缀无关**，且**两条分支（普通会话 / 工单会话）都会截**。

⇒ 口径写死：**基名上限 = min(80 − 前缀实际字节, 64)**，而**新会话与"已交接"标记一律取最保守的 ≤ 59 字节**（最终形态带 `【完成】【续】` = 21 字节前缀 ⇒ 80 − 21 = 59，是全局最紧的一档）。

> **一次差点踩中的坑**：如果按"动态计算实际前缀"实现，会算出 71B / 65B 两个值——它们**都超过插件的固定 64B 闸**，写进去的长标题会被**再截一次**，正好落进本机制自警的"接力前后标题不一致"。⇒ **动态计算只用于"是否超 64"的判定，实际写入值一律 ≤ 59。**

---

## 5. 接力动作链与顺序铁律

### 5.1 七步

```
① 写交接档（七段，头部含主线名 / 源会话 / 生成方）
② 跑接力器（一条命令）
   ├─ a) 权限闸门（fail-closed，见 §7）
   ├─ b) 创建新会话  → newSessionId
   │      └─ 成功后**立刻**写一条"归属依据"审计行（见下）
   ├─ c′) 运行时回读新会话的真实权限（含宽限，见 §7.3）
   ├─ c) 改名【续】<主线名>
   ├─ d) 投递开工指令（队列模式）
   └─ e) 回写档头（链标识 / 接力时刻）
③ 新会话开工第一步：改源会话标题为【已交接】<主线名>（一条命令、幂等）
④ 新会话第二步：跑一次进入自检（会消费标记并清台账窗口期状态）
⑤ 在途工单改绑（由接手方做；改绑前后各核一次当前持有者）
⑥ 旧会话停手并回报
```

### 5.2 三处顺序不能调换

1. **先写档、再跑链**——档是唯一权威内容，链失败时档依然有用（人工兜底通道只写档不开会话）；
2. **先校验权限、再改名**——否则不一致时被归档的是一个**从未改过名**的会话，不会留下"挂着接力标题却从未开工"的僵尸标题；
3. **先判定闸门（含计数预占）、再建锁、最后创建会话**——锁改成"按源会话分"之后，**不同源会话可以并行**；若闸门不在锁之前，并行会直接穿透"单日额度"这类全局计数。

**一条"归属依据"的设计**（值得单独说）：创建会话成功之后**立刻**写一条审计行（只含"新会话 id / 链 id / 源会话 id"）。它的理由是：原来"这个新会话是哪条链造的"只有**宽限窗口**才会产生的审计行作依据——而投影就绪的常态路径**从不写它** ⇒ 在那几十毫秒里进程被杀（重启 / 强制结束都是常态），这个空会话就**没有任何载体能归属到本链**，会静默残留。

### 5.3 链标识

每次交接生成一个链标识（形如 `relay-<源会话id>-<年月日时分>`），**同时写进**：审计行、台账、交接档头部、开工指令文本、工单改绑备注、日报行。

一条链跨 **4 个组件 6 个动作**（创建 / 改名 / 投递 / 改绑 / 补标 / 记账）——**链标识是唯一串联键**，出问题时按它一次捞全。

---

## 6. 闸门：防异常，不防次数

### 6.1 十二道闸门

| # | 闸门 | 判据 / 默认 | 可否放宽 |
|---|---|---|---|
| 1 | 软限阈值 | 沿用监控既有阈值，本机制不动 | 不动 |
| 2 | **速率闸** | 同一**主线**两次交接间隔 ≥ 20 分钟（计数载体 = 审计流水里**只取成功**的那些；**读取范围 = 当月 + 上月**） | 可调 ≥5 分钟 |
| 3 | **失败闸** | 同一主线**连续失败 ≥2 次** ⇒ 停止交接 + 🔴 报警（与"某一次失败"分开记） | **不可放宽** |
| 4 | 成本闸 | 按 token 预算（不按次数）；**默认值为 0 = 不启用**，要启用须显式设值 | 可调 |
| 5 | **单飞锁** | 互斥键 = **源会话**（不是链标识——链标识每次新生成，天然不同，挡不住并发）；原子建锁，失败即本轮跳过 | **不可放宽** |
| 6 | 深度 | **只记不卡**（接得多 ⇒ 提示"该拆主线"）；仅一个**保险丝**防死循环 | 保险丝可调 |
| 7 | **去重键** | `relay|<源会话id>`——一个源会话只许交一次（天然唯一、不误挡重试） | **不可放宽** |
| 8 | 无"下一步"段 / 主线名取不到 | **不开新会话**（fail-closed，改提示所有者） | 不可放宽 |
| 9 | **权限不降级** | 源会话档位 < 基线 ⇒ **不开新会话**（两段断言见 §7） | 不可放宽 |
| 10 | 配置不可读 | 配置读不到 / 解析失败 ⇒ **拒绝交接**（**不许**当成"全放行"） | 不可放宽 |
| 11 | **动作总闸** | 两个开关（agent 侧 / 定时侧）**初值一律 = 关** | 开关操作 |
| 12 | 触发源额度 | 主路径的触发信号有**硬天花板**：每会话每档 24 小时 1 条 + 池级每日上限 | 不改（属监控既有口径） |

**闸门命中必须有"三件套"**（这是一次补全：原表只写了"判据"，没写"命中后做什么"）——**动作**（不建会话）+ **审计行**（带拒绝原因）+ **退出码** + **可见性**（日报里"闸门拒绝原因 Top"）。**只写判据不写动作，就是把口径留给实现期即兴发挥。**

**一个真实的死条款**：速率闸与失败闸原写"按**同一条链**计"，而链标识**每次交接都是新的** ⇒ **永无第二个样本**，两道闸**永久失效**。修法：一律改成按**主线**计（与去重键"按源会话"对称），并让主线名比较**只走一处归一化**（去空白 / 全角半角冒号归一 / 大小写不折叠）——否则一个尾随空格就能让两道闸同时静默失效。

### 6.2 次数不是敌人（一次口径反转）

原设计有"单日 3 次 / 深度 3"两道闸。所有者的原话是：**"一条主线跑起来，续一次根本完不成，续五次六次都是正常的。"**

⇒ 当场删除这两道闸，并把**接力次数改判为健康指标**。同时消除了一处**互斥**：原表里"24 小时冷却"与"单日 3 次 / 深度 3"**四项不可能同时通过** ⇒ 表本身自相矛盾。

**教训**：闸门设计的第一件事是**问它到底在防什么**——防"异常"可以收紧，防"次数"往往是在防一件正常的事。

### 6.3 谁能发起：两类被明确禁止

| 发起者 | 能做什么 | 违规处置 |
|---|---|---|
| 当前会话的 agent（主路径） | 收到提醒后自行交接 | 记拒绝审计行 |
| 定时动作（无人值守） | 替无人值守的会话写档 + 交接 | 同上 |
| 其它脚本 / 人对 CLI | **只允许**预览与"只写档不开会话" | 直接拒绝（参数错） |
| **禁止：工单执行会话自己发起** | ❌ | 记拒绝（该走定时通道，**不是**不给它交接） |
| **禁止：子代理会话发起** | ❌ | 记拒绝（它会绕过主线的单主约束） |

**为什么禁这两类**：主路径的本质是把"开新会话"这个动作**开放给每一个 agent**（包括被外部内容诱导的会话）。工单执行会话**恰恰是无人值守通道的目标人群**——它该被"替它交接"，不该自己开新会话；子代理是父会话的内部零件，让它开新会话会绕过"一条主线只有一个主"的约束。

---

## 7. 权限不降级：一次"两段断言"的设计

### 7.1 危险方向只有一个

接班的新会话通常继承**部署默认权限**。这里唯一危险的方向是：**源会话被判"够了 ⇒ 放行"，而新会话实际拿到更宽的权限**——一次**静默放宽**。

顺带一个具体坑：权限档位**绝不能按字符串比较**。字典序下 `read-only` 排在 `danger-full-access` **之前**（`r` > `d`），照字面写"档位 < 基线 ⇒ 拒绝"，会**正好反转**，把"低权限 ⇒ 拒绝"变成"低权限 ⇒ 放行"。⇒ 比较**一律走序表**（只读 `0` < 工作区写 `1` < 完全访问 `2`）。

### 7.2 静态段与运行时段

被保护的对象**既不是文件、也不是可外部只读的环境变量**（注入发生在启动链里，agent 子进程里根本读不到）⇒ 写死为**两段断言 + 两个读点，缺一不可**：

| 段 | 时机 | 比对对象 | 读点 | 失败语义 |
|---|---|---|---|---|
| **静态段** | 设计期 / 单测期（**不在交接运行时执行**） | **配置键 vs 启动链各注入点源码里的字面常量** | 源码文本（按文件类型分组正则匹配，允许引号与空白变化） | **任一处取不到 ⇒ 判不一致**（绝不把"匹配不到"当"通过"）；不建会话 + 审计 + 退出码 5 |
| **运行时段** | 建会话**之后** | **新会话真正继承到的那一份**（回读会话投影） | 会话列表接口的权限投影字段 | 不一致 ⇒ **归档该会话 + 不投递指令** |

**为什么两段都要**：静态段只抓"**配置与源码常量漂移**"，它**代表不了"这次实际生效值"**（外部环境变量覆盖、启动参数分支都会改变结果）；只有运行时段读到的才是**真实生效值**。

**一处必须写进清单的边界**：清单范围要**逐处点名**，而不是"去全库找注入点"。实测还发现过第 5 处注入（属于**另一个组件的安全模式**，值为只读）——若按"全库找"理解，它会让静态段**恒判不一致 ⇒ 主路径彻底停摆**。

### 7.3 "读不到"必须宽限，不许判死

新会话建好之后，权限投影**可能还没就绪**（创建与列表之间的可见延迟）。此时有三态都算"读不到"：会话不在列表窗口 / 没有该投影 / 投影取值为空。

写死口径：**每 30 秒重试一次、至多 10 分钟**；宽限内**既不投递也不归档**；宽限状态与重试计数**落在锁文件的一个子对象里**（不另造状态文件）；审计**只在进入与退出各写一条**（不是每轮一条）。

**为什么宽限内不许归档**：那会把"投影还没生成"误判成"权限不一致"，而**每次尝试都会留下一个被归档的会话**——污染会话面，且归档**不可逆**。

**超宽限仍读不到 ⇒ 才走 fail-closed**：归档 + 不投递 + 退出码 5 + 审计带被归档会话 id；若**该会话已被外部归档**，则**跳过归档动作**、改为只报警（因为"对已归档会话再归档一次"的幂等性未验）。

一处容易忽略的连带影响：宽限窗口把"创建 → 改名"之间的窗口从**秒级拉长到最长 10 分钟**，而这段时间里进程被杀（重启 / 守护进程接管 / 强制结束都是常态）会留下**无标题空会话**⇒ 必须给它一条**独立的孤儿判据与只报不收拾的处置**（见 §10.1）。

---

## 8. 无人值守通道

### 8.1 候选面：两个前置，缺一不可

主路径依赖"当前会话的 agent 收到提醒后自己动手"。会话一旦处于**无人值守**（执行工单的会话 / 所有者不在场），没人写档 ⇒ 主线断。所以需要一个定时动作补上这一段。

**候选面（阻断级修正）**：本会话 id 命中**任一工单的执行会话**（= 工单执行会话）**且** `running=true`。在此之上再二选一：

- ① 越过**硬限**；或
- ② 越过软限后 **45 分钟**仍未产出交接档。

> **这条 `running=true` 前置救过一次事故**：原条件②**没有**它。现场实测发现：越线会话里**绝大多数早已收手**（`running=false`）⇒ 照旧口径一放行，就会对**早已收工的历史会话**开工——每个候选一次付费调用 + 一个僵尸会话。**"替已收手的会话开会话"必须明确列为非目标。**

另外两处"载体不成立"的修正也值得记：判"是否已产出交接档"必须**换权威源**（读审计 + **排除自动生成的档**，否则失败降级自己写的那份档会让该会话**下一轮就判"已产出"、此后永久静默停摆**）；判"近 45 分钟无新回合"必须**换字段**（原字段路径错了一层、且**一个时间字段都没有** ⇒ 照字面实现只能恒真或恒假）。

### 8.2 出机器之前的那道筛（唯一能拦住外发的点）

无人值守的动作顺序是：**读会话尾部 → 交模型 → 写档**。而"档内容禁止清单"的两处检查都在**写盘前** ⇒ **会话尾部原文在过滤之前就已经出了机器**（进了第三方请求体，**不可撤回**）。

⇒ 写死：**同一套正则、作用点前移到"组装请求之前"**；命中即**不调模型**、直接走"只写事实档"的降级；审计只记**命中条数**、不记命中内容。

**位置语义**：它不是"写档前的检查"，而是"**要不要把这段原文发出去**"的**前置闸**——**整套机制里唯一能拦住外发的点**。

### 8.3 比主路径更严的闸门

- **单日 ≤1 次（全局）**，且**计数必须原子预占**（同一临界区内自增；**预占不归还**——成败都算一次"开局"，避免"失败归还"被并发穿透）；跨日必须按日期键复位（否则额度会被**事实上永久用掉**，定时通道再也不触发）；
- 七段结构校验 + 同一源会话只许一次 + 单飞锁；
- **多候选只处理 1 个时其余必须留痕**（候选清单进日报），不许"剩下几十个静默躺着";
- **失败即降级**：模型不可用 / 草稿不合格 / 日志打不开 ⇒ **只写事实档 + 桌面提示 + 日报段，绝不开新会话**；
- 权限闸命中同样走降级路径，**且不计入失败闸**（否则每轮重评同一个低权限会话会连计失败，把"需要人工放行"误判成技术故障，**静默停摆整条主线**）。

### 8.4 三项预算各管一段，不许互相顶替

| 预算 | 管什么 | 量级 |
|---|---|---|
| 解帧 I/O | **读文件** | ≤2 MB / ≤3 秒 |
| 输入侧上限 | **送模型** | ≤1.5 MB（超限截尾：保留首尾、中段省略并标注） |
| 输出封顶 | **模型输出** | 6000 tokens（独立参数 + 独立环境变量，**不复用**其它链的 maxTokens） |

> 这一条是补出来的：原设计只有"I/O 上限"，**那不是 prompt 上限**——叠加"成本闸默认 0 = 不生效"，单次输入的规模与花费**根本没有上界**。

---

## 9. 观测：怎么知道它在工作

### 9.1 三条最容易漏的可见性

一次真实修正是这样发生的：日报段报了"今日 0 次"，但**到底是没跑、还是没读到**分不清。⇒ 三条硬要求：

1. **读数必须带时刻与覆盖窗口**（`asOf` + "今日 00:00 ~ now"）；
2. **"没投出去"与"投了没人动"必须可区分**（分别计数）；
3. **每一条静默失败都要有自己的指标**——候选面长期为 0（🟡 告警线：连续 ≥7 天为 0）、降级次数与原因、采集超预算次数、**孤儿 / 宽限期遗留空会话清单**。

### 9.2 审计事件与它的证据强度（如实声明）

审计是**任意本地进程都能追加的普通文件**（写入方不验身份、无签名），但**有哈希链、可校验**（每条带前一条的摘要）⇒ 准确口径是：**它是"协作审计"，只防损坏、不防有权限篡改**，可以用来排障 / 追责 / 统计，**不能**当对抗性证据。

配套两条硬约束：**新增事件一律走既有追加实现**（裸写一行 JSON 会断链，而这条流水正是"是否已发生"的权威源 ⇒ **权威源被自己写坏**）；**并发写入必须先实测**（并发度提上去之前要验证"两条并发追加后校验仍通过"，未验不许提并发）。

五类主事件：`session-relay`（终态：结果 / 模式 / 基线来源 / 主线名 / 新会话 id / 半完成态形状）、`session-handoff-mark`（补标，含幂等跳过）、`session-relay-denied`（拒绝，带原因）、`rerouted`（通知改投）、`skipped-handoff-window`（改投找不到接手方）；另有一组清理 / 跳过类事件（残留锁、陈旧标记、抢锁失败、单飞被挡、额度被拒、宽限等待、归属依据等）。

### 9.3 两个速率指标（为什么拆两条）

| 指标 | 定义 | 目标 |
|---|---|---|
| **单步成功率** | 创建 + 改名 + 投递三步全成功 | ≥95% |
| **链完成率** | 以链标识聚合，到"接手方补标 + 在途工单改绑完成"才算闭环 | **周均 ≥90%**；连续 3 天 0 完成 ⇒ 日报 🟡 |

**必须给阈值，不能只给口径**——"日报永远 0 次"这种退化，正是因为没有越界判据才没人发现。人为中断与所有者喊停**单列、不计入分母**。

### 9.4 一个"零调用点"的教训

有一次实测发现：某类事件**代码里写了、字段也定了，但生产链上零调用点** ⇒ 依赖它的指标**恒为 0%**。这类缺陷不会报错——**表格完整、字段齐全、就是永远不动**。⇒ 回归项里要有一条"**指标读数必须能对应到具体执行点**"，并且**补标失败 / 未补标时零写入是有意为之**（此时由 CLI 的 stderr 与退出码承担可见性）。

---

## 10. 回滚、退出码与演练

### 10.1 一键停与三种半完成态

**一键停**：关两个开关（agent 侧 / 定时侧各一个），外加"删掉卡死的那把锁"（锁按源会话分，所以只需删那一把）。

**三种半完成态**（都会留下无标题空会话，必须能区分、且**只报不收拾**）：

| 形态 | 判据入口 | 成因 |
|---|---|---|
| ① 建了会话、指令没投出去 | 终态结果 = "部分完成" **且**成因字段不属于"收尾失败"那两类 **且** 形状 = ① | 该行上的原生成因 |
| ② **宽限窗口内被杀**（新增形态） | 该会话**有**"归属依据"行 **或** 有宽限等待行的进入态 | `grace-killed` |
| ③ 宽限已放行、终态行未写就被杀 | 同上，且**无任何终态行** | `grace-killed` |

**为什么"只报不收拾"**：归档是**不可逆写动作**、又挂在启动链上 ⇒ 属"必须先问所有者"的档位。**若日后要改成自动收拾，必须四件齐**：时间窗 + 幂等标记 + 归档前复核"仍是空会话" + 失败只告警。

**一处极易误判的细节**：一个"部分完成"的枚举值**一值两义**（"收尾回写失败"与"建了会话没投指令"共用）⇒ 不加**成因字段**区分，就会把**正常交接出来的新会话误判成孤儿并归档**。所以判据里必须带成因，并配反例单测。

**读取范围也要写死**：判据要读**当月 + 上月**两个审计月文件——只读当月，上月产生的记录**永远读不到**，对应会话会**永久漏报**。

### 10.2 退出码

| 码 | 含义 | 何时出现 |
|---|---|---|
| 0 | 成功 | 链跑完（预览成功也算 0） |
| 2 | 参数错 / 越权拒绝 | 缺参、发起者属禁止类、人工通道参数组合不合法 |
| 5 | **需人工**（fail-closed） | 权限闸命中 / 读不到 · 无"下一步"段 · 主线名取不到或超长 · 配置不可读 · 双载体基线不一致 · 速率 / 失败 / 成本 / 去重闸命中 · 状态文件版本过高 |
| 3 | 部分完成 | 建了会话但投递失败等半完成态；含运行时段基线不一致（归档 + 不投递）；**归档接口不可用 ⇒ 只报警 + 停链交人工** |
| 1 | 其它失败 | 网络 / 服务不可用 / 未预期异常 |

**人工兜底通道**（只写档、不开会话）的边界也写死：**跑**去重键与档结构校验、**跳过**权限闸（它不开会话、不涉及放宽）、**记审计**（结果取值 = "只写档"）、**落去重签名**（防"人工写档后又自动交接一次"）、**且不受总开关约束**——它是兜底手段。

### 10.3 上生产前必须做完的四件事

1. **隔离环境真跑一条链**（含写侧；**还要含失败注入**：给不存在的目标、在投递前杀进程，验半完成态与审计形状）；
2. **沙箱演练**（预览全绿）；
3. **仿真一次权限闸命中**（用只读会话验退出码 5 + 降级可见）；
4. **过审**（独立复核合格）。

> **一条反复被验证的经验**：`--dry-run` **不构成真链验收**——它走不到创建 / 改名 / 投递，**验不出前缀叠加与注入时序**。真链验收只能在隔离实例或生产放行后人工跑一次。**"预览通过"与"跑通"是两件事。**

---

## 11. 一次真实的生产实测（2026-09-22）

放行之后，同一条命令跑完整链，全程 **2.7 秒**：

```
[闸门全过] → 建锁成功 → 创建新会话 → 权限回读通过（一次命中）
→ 改名【续】<主线名>（title 事件 seq=3）→ 投递开工指令（1856 B）
→ 回写档头（链标识 / 接力时刻）→ 锁已释放 → exit=0
```

**接手会话自主跑完两步、全程 10 秒**（补标 + 进入自检），末句回"两步已完成，未越界"，**没有去改绑任何工单**：

- **源会话标题 = 【已交接】<主线名>**（**替换、非叠加**：没有出现 `【完成】【已交接】…`）；接手会话自身 = `【完成】<主线名>`（插件按回合边界把【续】切成【完成】）——**两条标题互不干扰**；
- **标记文件生命周期三段全中**：写出（文件名 = 源会话 id，216 B）→ 被读取消费（自检报"消费 1"）→ 删除 + 台账窗口期状态清空；
- **时序自洽**：交接时刻 ≤ 标记文件时间 ⇒ 命中"同一次交接"判据，故被正确消费而非当"孤儿标记"删掉。

**审计新增 3 类事件**：`归属依据` / `终态`（`result: ok`、`mode: manual`、`baseline: config`、无半完成态形状）/ `动作登记`（L1、已执行、非预览、签名键 `relay|<源会话id>`）。

**权限闸两段都放行**：静态段（配置键 vs 启动链 **4 处**源码字面常量，全部一致）+ 运行时段（回读新会话投影，**一次命中、无宽限等待**）；另有独立断言"新会话权限 == 源会话权限 = true"。

**工单绑定表跑前跑后逐行一致**（唯一差异是相对时间戳的自然流逝）⇒ 本次链未触碰绑定表。

**日报段（摘要）**：

```
## 🔁 会话接力
- asOf：… · 覆盖时间窗：今日 00:00 ~ now · 数据源：审计当月 + 上月
- 今日接力次数：1（成功 1 / 失败 0 / 部分 0 / 人工只写档 0）
- 闸门用量：速率 0 · 失败停招 0 · 成本 0 · 去重 0 · 权限 0 · 单日额度 0 · 同源并发 0
- 闸门拒绝原因 Top：（历史行 1 条）
- 链完成率（当月）：0%（闭环 0/1 · 目标周均 ≥90%）
- 上下文看门额度：今日投递 8 / 因额度未投 0
- 阶段 2（无人值守）：开关未放行 · 今日自动接力 0 次 / 降级 0 次
- 孤儿 / 宽限期遗留空会话清单：0 条
```

> **一处如实标注**：`链完成率 0%` 并非故障 —— 当时依赖的补标事件还**没有执行点**（§9.4 那条教训）。**刚上线时指标为 0，先分清"没数据"与"没工作"。**

**同一次窗口还发现并处理了一个前提偏差**：定时通道**只重启 dsh 是不够的**——调度器是**独立常驻进程**，新增的定时任务必须重启**调度器**才会被加载（生效判据 = 状态里出现该条目）。

---

## 12. 明确不做哪些

- ❌ **不做跨机接力**（换机交接是另一条链；本机制的台账 / 锁 / 窗口期状态都是**单机**的，换机后自动过滤会退化为兜底路径——**这一退化要写进文档，不许只写"不支持"**）；
- ❌ 不自动收拾孤儿会话（**只报**）；
- ❌ 不自动改记忆 / 技能 / 生产代码；
- ❌ 不替"已经收手的会话"开会话；
- ❌ 不做无上限接力——但上限是**保险丝**（防死循环）与**额度**（防静默失效），**不是**"压次数"；
- ❌ 不把"读不到"实现为"放行 / 继续"。

---

## 13. 一张开工前的检查清单

**载体**
- [ ] 交接档模板七段齐全，且第 5 段判据是**可执行**的（含三条反例单测）
- [ ] 档内容四类禁写清单落在模板里，且**外发前的那道筛**在组装请求之前
- [ ] "这次交接发生过没有"的权威源**唯一 = 审计流水**（台账只作可重建缓存）

**闸门**
- [ ] 十二道闸门逐条有：判据 + 命中动作 + 审计行 + 退出码 + 可见位置
- [ ] 计数类闸门**按主线**计，且主线名比较走**同一处归一化**
- [ ] 去重键按**源会话**；单飞锁也按**源会话**（不是链标识）
- [ ] 闸门 → 建锁 → 创建会话的**顺序**写死；全局额度用**原子预占**

**权限**
- [ ] 档位比较走**序表**，不按字符串
- [ ] 静态段（源码常量 vs 配置）+ 运行时段（回读实际继承值）**两段都有**，且"取不到 = 不一致"
- [ ] 宽限重试参数与"宽限内不投不归档"写死；超宽限的归档动作带被归档 id

**标题**
- [ ] 两处词表**同扩**，且"剥前缀"是**分层**的（状态轴重写、接力轴保留）
- [ ] 基名上限取**最紧的一档**（≤59B），动态计算只用于判定
- [ ] **配套判据同步升级**（"以…开头" ⇒ "前缀段包含…"）

**可见性**
- [ ] 日报段带 `asOf` + 覆盖窗口；"没投出去 / 投了没人动"可区分
- [ ] 每条静默失败都有指标（候选面为 0、降级、超预算、孤儿清单）
- [ ] 两个速率指标都有**阈值**，不是只有口径

**放行与回滚**
- [ ] 两个开关**初值 = 关**；首次放行属"必须先问所有者"的档位、关回属可直接执行的档位
- [ ] 一键停 = 关两个开关 + 删那一把锁；回滚范围写清（会话可归档、无删除接口）
- [ ] 上生产前四件事做完（隔离真跑含失败注入 / 沙箱演练 / 权限闸仿真 / 过审）

---

## 14. 复跑与限度声明

- **可复跑的部分**：本文的闸门清单、退出码表、检查清单都可直接比对实现；标题形态与字节预算可按 `§4.4` 的判据在隔离实例上复验（用固定长度的主线名跑一轮，读两条标题的最终形态）。
- **未复跑 / 未实测的部分，如实列**：
  1. **"创建 → 可见"的投影延迟**未单独实测——宽限参数（30 秒 / 10 分钟）是按保守量级定的，**不是实测出来的**；
  2. 对**已归档会话再归档一次**的幂等性**未验**（所以那一支写死为"跳过归档、只报警"）；
  3. 审计追加的**并发安全性**未验（要求施工期先做一次并发实测）；
  4. **跨机**情形下三载体的退化路径只在设计上写明，**未在两台机器上实跑**；
  5. 本文全部阈值都是**特定环境的观测值**——换环境必须重新定门槛，**照抄等于把一次观测当设计参数**。
- **一处口径反转如实记录**：原设计"单日 3 次 / 深度 3"已被**删除**（§6.2），本文不含该口径；若在别处读到旧版说法，以本文为准。
