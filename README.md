# 开源支线 · 文档侧

> **本目录是一份"可发布成品"，但尚未发布到任何公网位置。** 发不发、发到哪里，由项目所有者决定。

## 这是什么

把两套已经跑在生产环境的**运维体系设计稿**脱敏后公开——它们记录的不是"我们的系统长什么样"，而是"做这类系统时踩过哪些坑、怎么判断、怎么兜底"。

**背景一句话**：这是给"长期在一台（或两台）工作站上跑编码 agent、并且越跑越重"的场景设计的运维体系——会话会坏、任务会堆、插件会崩、备份会互相覆盖、换机时会丢状态，而这些问题的解法和"网页服务运维"差别很大。文中所有结论都来自真实事故与实测数据。

| 文件 | 内容 | 原稿规模 |
|---|---|---|
| [`01-多机交接与云中继设计.md`](01-多机交接与云中继设计.md) | 多台工作机之间如何**无缝交接**（离开时交出去、到达时接过来并落到生产路径），以及一台云服务器如何同时承担「固定入口 / 活跃机登记 / 交接中继」三角色 | 700 行设计稿 + 四轮审核 67 条意见登记 |
| [`02-自治式运维管家设计.md`](02-自治式运维管家设计.md) | 给单机 agent 运行环境配一个**自治程度可控**的管家体系：对象分类学与自治光谱、写入守门人、重启闸门、主动告警、坑库自动化、外部信号感知、跨载体对账 | 1120 行设计稿 + 9 个能力域 + 15 项待验证假设 |

配套文档：

| 文件 | 用途 |
|---|---|
| [`发布说明.md`](发布说明.md) | 发布渠道、更新策略、版本号规则、与代码侧项目的互链 |
| [`LICENSE-CC-BY-NC-SA-4.0.txt`](LICENSE-CC-BY-NC-SA-4.0.txt) | 文档许可（**推荐**，理由见发布说明 §2） |
| [`LICENSE-MIT.txt`](LICENSE-MIT.txt) | 代码/脚本许可（与代码侧项目保持一致；若只想用一个，用 CC 那份） |
| `脱敏清单与删改对照表.md` | **不随公开版发布**：生产侧留档，记录哪些内容被删 / 被替换及理由 |
| `复核报告-20260911.md` | **不随公开版发布**：脱敏复核的 grep 结果与人工判定 |

## 怎么读

- **只想知道"多机怎么交接"** → 直接读 `01`。
- **想设计自己的运维管家** → 先读 `02` 的 §4（宪法：对象三型 + 自治光谱）与 §5（安全 / 并发 / 可观测），再看 §6 各能力域。
- **想知道"这两份文档有多可信"** → 每份末尾都有「审核意见落实记录」与「假设与验证时机」两节：前者是**多少个视角挑出多少条问题、落在哪**，后者是**哪些结论还没被验证、验证失败怎么退**。**没有这两节的设计稿，一律按"作者的自我感觉"打折看。**

## 三条阅读提示

1. **占位符**：文中 `<工作区根>`、`<DSH 主目录>`、`A 机 / B 机`、`steward-*` 等均为占位名；模型名与第三方服务名仅示意，替换为读者自有渠道即可。
2. **数字**：文中所有实测数字（文件数、体积、耗时、条数）都来自一次或多次真实观测，**它们是"当时的事实"，不是设计参数**——参考其量级与判断方式，不要照抄阈值。
3. **语气**：原稿是内部设计稿，保留了"这里原来写错了、后来改成这样"的修订痕迹与反驳意见。这是刻意保留的——**设计稿的价值往往在于"为什么否决了另一个方案"**。

## 与代码侧项目的关系

本目录属"开源支线"的**文档侧**；同一个支线的**代码侧**是把某个自建插件从项目中抽出来做通用化（去硬编码、配置化、README、许可证、发布流程），它是**独立的另一个项目**，两半互链、不重复：

- 文档侧（本目录）：先公开**方法论**——成本低于代码通用化，且能先建立外部反馈。
- 代码侧（另一项目）：随后公开**可复用的插件代码**；其 README 应链接回本目录的两份设计稿，说明"该插件的设计依据来自哪一节"。

> ⚠️ **发布前需替换**：代码侧项目的公开仓库地址目前尚未确定，本目录所有指向它的链接都以 `（代码侧项目地址待定：插件通用化项目将随后开源，届时此处补链接）` 标注。发布时替换为真实地址即可。

## 许可

- **文档**：CC BY-NC-SA 4.0（署名 + 非商业 + 相同方式共享）——**推荐**，理由见 `发布说明.md` §2。
- **代码/脚本**（若随本文档一起分发的示例脚本）：MIT。

---

*本目录内容由生产设计稿派生，为独立副本；生产稿仍是唯一权威源，本目录的任何修改都不会回流覆盖生产稿。*
