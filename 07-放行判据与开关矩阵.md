# 放行判据与开关矩阵

> **这篇文档讲什么**：给"想让自动化真的动手"的长期个人运维体系，一套**从建好到敢开**的流程与证据链。它回答三件事：**门槛是怎么算出来的**（样本池怎么并、分级门槛凭什么定这个数、缺数据为什么必须不放行）、**一个动作要放行需要满足什么**（动作 → 风险级 → 开关 → 护栏 → 放行判据这张矩阵怎么读、四条写死的规则是什么）、**怎么防止观测到一半尺子被改**（冻结面、分段重置、编号与命名的避让）。
> **版本**：v1.9（随本仓 `v1.9` 发布；本稿自 `v1.9` tag 起即公开同步。本仓最近一版 `v1.8` 已含 06 号稿）
> **素材基准日**：两次口径定稿（2026-09-11 的分级放行口径、2026-09-13 的开关矩阵与四条规则），以及其后两次口径补正（一处统计归类修正、一处"放行后是否被观测过"的对照窗口列）。
> **占位符说明**：`<DSH 主目录>` = agent 的数据主目录；`<工作区根>` = 工作目录；`steward-*` = 本仓 `02` 号文档既定的管家组件族占位名；`A 机 / B 机` = 两台长期运行的工作站（A = 日常开发机，B = 常驻稳态机）。文中所有开关键名一律用**角色泛称**（"动作族总开关""低水位重启总开关""写后回滚总开关""自修复总开关"），组件一律用职责名（"管家内核""调度器""守护进程""只读面板""动作登记表""模块登记表"）。
> **数字口径（重要）**：本文全部实测数字来自**特定机器上的特定观测**——**环境相关，勿照抄**。它们证明的是"这门尺子长什么样、该怎么被质疑"，不是阈值。照着本文的 25 / 50 条去定你自己的门槛，等于把一次观测当设计参数。

---

## 0. 先看结论

1. **"能力建好了不敢开"是一个独立的问题，它有独立的方法**：暗发布（能力先建好、开关停在最保守档）+ 渐进放行（数据够了再**逐项**上调），而不是"等观测完才开始开发"。放行当天应该只做**改表 + 留痕**，不该出现"临时开发"。
2. **门槛不能按单台机器算**：观测样本是**跨机并集**（同一套判定规则在不同环境下的两次采样），而**放行按机**（先达标先开、已开的退出观测池）。用单机口径配一台告警稀疏的稳态机，等于**死锁**——告警少本来是好消息，却成了放不了行的理由。
3. **"条数"会被复读灌水**：实测池级 18 条告警里只有 **12 个独立状况**（刷屏比 33.3%，同一事件当日重复 5 条）。所以门槛是**两个数**：条数 + 独立状况数，另报刷屏比。
4. **分级门槛的两个数不是拍脑袋**：零真误报时"有 95% 把握断言误报率低于"= `1 − 0.05^(1/n)`。n=25 → 11.3%，n=50 → **5.8%** ⇒ **原 50 条的语义就是"零误报时刚好能说 <6%"**，它贴着 5% 门槛算出来，本身不宽松；所以只有**做错毫发无损**的那一档才配放宽到 25 条，**动数据的那一档保持 50 条不动**。
5. **缺数据必须不放行**（fail-safe）：**绝不把"未知"当"无误报"**。任一台机器的判定明细缺失 ⇒ 池级一律不放行，且该机自报的数字必须显式列出来、不许被静默吞掉。
6. **放行 = 改表，不是改代码**：放行一个动作只做三件事——按报告核对它的放行判据、改开关并留痕（谁 / 何时 / 依据哪份报告）、放行后 **48 小时**内对照一次"真做的"与"预览预测的"是否一致。不一致 → 立即关回。
7. **开关是"活"的**：它不只是放行闸门，也是运行时关停开关（四级：全局降级 ⊂ 总开关 ⊂ 模块级 ⊂ 动作级）。而且**"关"的语义是 drain（不再发起新动作），不是掐断**——禁止"一关就杀进程"式关停，那只会制造半完成状态。
8. **观测期最大的敌人是自己的手**：判定口径一旦改动，前后数据就不可比 ⇒ 需要**冻结面**（哪些能改、哪些改了就得分段并让两台机器一起重置计时），以及一组"尺子变了必须写进报告"的纪律。

---

## 1. 问题：能力建好了，为什么不敢开

### 1.1 两种失败模式

一个自建自动化体系在"动手"这件事上会撞两种坑，方向相反：

| 失败模式 | 表现 | 根因 |
|---|---|---|
| **不敢开** | 动作全都写好了，开关永远停在关 | 没有"放行需要什么证据"的定义 ⇒ 只能凭感觉，而感觉永远倾向于"再等等" |
| **开得太早 / 开得没数** | 开了，但没有人能说清它今天做了几件、做对没有 | 开关能开，但**动作与证据没有登记**，放行变成一次性拍脑袋 |

本文针对的是第一种——以及它派生出的那个更隐蔽的变体：**一堆没人知道的暗代码**（能力存在、开关关着、没有任何地方能看见"如果开了会做什么"）。

### 1.2 做法：暗发布 + 渐进放行

- **暗发布（dark launch）**：能力先开发完、代码进生产，但开关停在最保守档；开发进度**不被观测期卡住**。
- **渐进放行（progressive delivery）**：用观测数据决定**逐项**上调档位——一个动作一个动作地开，不是"整族一起开"。
- **收益**：放行时只需要**改开关 + 改表 + 留痕**，不需要临时开发；观测期与开发期可以并行。

### 1.3 三个必须同时成立的前置

只做暗发布、不做下面三件事，得到的就是"暗代码"。三者缺一，放行都会退化成"放行当天第一次真跑"：

| # | 前置 | 它解决什么 | 缺了会怎样 |
|---|---|---|---|
| ① | **开关矩阵**（本文 §3） | 每个动作都知道：归哪把总开关、干什么风险级、默认什么档、护栏是什么、放行判据是什么 | 放行时得现场推断"这动作受谁管" ⇒ 只能拍脑袋 |
| ② | **预览视图**（dry-run 可见） | 默认关的动作能被看见"**如果开了会执行哪几项**" | 放行当天 = 第一次真跑，风险只是被推迟、没被消化 |
| ③ | **冻结面规则**（本文 §4） | 观测期里"尺子"一变，数据就不可比（放行判据随之失效） | 攒了一个月的数据，在最后一次改口径时全部作废，而且没人知道 |

> **一条经验**：预览视图这条路最容易"看着有、其实没有"。判据不是"有 dry-run 开关"，而是**日报与面板里真的能看见预览行**——否则它只是一段没人调用的代码。

### 1.4 这套东西不解决什么（提前说清）

- **不解决"判定准不准"**：矩阵管的是"敢不敢开"，判定质量由统计口径与误报账管（§2）。
- **不解决"开了之后一直没问题"**：放行只是起点，后面靠对照窗口（§3.8）与关停层级（§3.3 R4）维持。
- **不替代人**：真正的开闸动作永远是**人**做的（见 §5 的第一条）。

---

## 2. 统计依据：门槛是怎么算出来的

### 2.1 样本池：为什么按池算、按机放行

先说清一个容易被混在一起的东西：**"样本怎么算"与"谁放行"是两件事**。

- 判定规则是**同一份代码**（跨机同步），两台机器上的告警是**同一把尺子在两种环境下的两次采样** ⇒ 评估"尺子质量"应当用**池级并集**。
- 但开关状态、环境、负载都是**按机**的 ⇒ 放行必须**按机**，先达标先开。
- **已开自动的那台要退出观测池**：它已经会自动动手了，它的样本不再纯净（自动动作会改变现场）——继续计入会污染"规则质量"这本账。

**为什么不能只按单机算（一次实测）**：

| 设备 | 累计告警 | 已标误报 | 样本速率 | 按单机口径凑够 50 条 |
|---|---|---|---|---|
| B 机（稳态机） | 4 | 0 | ≈2 条/天 | **约 10 月初** |
| A 机（开发机） | 14 | 2 | ≈7 条/天 | 约 09-17 |
| **池级（并集）** | **18** | — | **≈9 条/天** | 约 09-15 |

稳态机那条是**结构性死锁**：它告警少（这是好事），却因此永远凑不齐样本、永远放不了行——**问题出在尺子，不在机器**。

### 2.2 「条数」会被复读灌水：独立状况与刷屏比

同一件事被反复报，会让"条数"这个指标虚高。实测池级 18 条里只有 **12 个独立状况**。所以口径是：

- **独立状况** = 同机 + 同日 + 同事件类型，去重后计 1；
- 另报一个**刷屏比** = `1 − 独立状况 / 条数`（实测 33.3%，其中一个事件当日 5 条同事件）；
- 门槛看**两个数**：条数达标只是必要条件，独立状况数不达标同样不放行。

### 2.3 分级门槛的统计依据

门槛要"看得懂它凭什么"，否则迟早被当成可以商量的数字。这一步用的是一个极简的统计事实：**零真误报时**，"有 95% 把握断言误报率低于"的上界 = `1 − 0.05^(1/n)`。

| 样本数 n | 25 | 30 | 35 | 50 |
|---|---|---|---|---|
| 误报率上界（95% 把握） | 11.3% | 9.5% | 8.2% | **5.8%** |

由此得到两条结论：

1. **50 条的语义 = "零误报时刚好能说低于 6%"**——它是贴着 5% 门槛算出来的，不是随手定的宽松值；
2. 所以放宽只能发生在**做错毫发无损**的那一档：**L1（无害可逆动作全自动）样本 ≥25 + 独立状况 ≥15**；**L2（有损可回滚）保持 样本 ≥50 + 独立状况 ≥30**。

> **这才是"适当放宽"的正确姿势**：放宽的是**风险等级匹配的档位**，不是"凑不齐就把数字改小"。数字与风险级绑定，写进矩阵，不随心情变化。

### 2.4 缺数据为什么必须不放行（fail-safe）

这是全套口径里最值钱的一条硬约束：

> **任一台机器的判定明细缺失 ⇒ 池级一律不放行。**绝不把"未知"当"无误报"。

- 落地形态：池级判定必须带一个"明细完整性"结论，不完整就停在"不可判"；同时**该机自报的数字必须显式列出**（例如"本机自报：误报 2 条 / 未确认 2 条"），不许因为"取不到"就当天不存在。
- **代价要如实说**：这一条会让"明明没问题"的放行被卡住（实测中池级长期停在"明细不全"）。这是**故意付出的代价**——放行晚了只是慢，放行错了是伤害。
- **另一个常被忽略的对称面**：放宽样本门槛**并不会让放行立刻发生**。实测中真正卡住的不是样本量，而是**误报率**（池级 11.1% / 单机 14.3% > 5%）。样本口径解决的是"别饿死、别假死锁"，误报的账要另外做。

### 2.5 两个真实的口径死锁（比门槛数字更值钱）

门槛数字可以照抄，死锁的形态更值得抄。两次都是"尺子把门锁死"，而不是"系统真的不行"：

**死锁一 · 单机口径 + 稳态机**（§2.1）。修法：样本池改为跨机并集、放行按机。**判据**：如果一台机器的样本速率天生低一个量级（因为它是稳态机、不是开发机），那就不能让它单独凑门槛。

**死锁二 · 疑似误报的定义不完整**。旧口径把"未标误报 + 未确认 + 超时"一律算作疑似，而实际数据里 **44 条中有 33 条是不需要确认的"仅记录"类** ⇒ 疑似占比结构性固定在 ≈80% ⇒ **门槛③（疑似 ≤10%）永远过不去**，与判定准不准无关。

修法是在定义里补一句限定："**只有真正推送过的记录才需要确认**"（只记录、没推送过的不在此列）。同时分母与计数范围写清楚：分母不变，但计数只算**本段起点之后**的条目。

**关键在于要不要重置计时**——这里给出一条可复用的判据：

| 改动性质 | 判据 | 是否重置计时 |
|---|---|---|
| **统计归类修正** | 回归**既有权威定义**，告警判定与台账一字未动、历史样本可按新口径**全量重算** | **不重置**（两台机器原计时继续有效） |
| **真改判定规则** | 改了事件判定 / 推送策略 / 扫描面（例如把扫描范围扩到归档区） | **必须重置**（两台机器都重置，并封段） |

> 一句话记法：**改的是"怎么数"，不重置；改的是"什么算"，必须重置。**

### 2.6 放行的三级条件与一次真实判定

**条件（三条同时满足才放行该机）**：

1. **池级门槛达标**（按风险级：L1 ≥25 条 + ≥15 独立状况；L2 ≥50 + ≥30）；
2. **该机观测天数达标**（`minDays` 满）；
3. **该机自身误报率 ≤10%**。

再加上 §2.4 的 fail-safe 与 §3.3 R1 的冻结面约束。

一次真实判定的样子（可作为报告模板）：

```text
池级：样本 18 · 独立状况 12 · 刷屏比 33.3% · 稳态部分 11 / 开发窗 7
     真误报 0（另有 14 条明细未知）· 稳态真误报 0 · 明细**不完整**
     ※ 明细不可得的机器自报：误报 2 条 / 未确认 2 条
L1：🔴 未达标（样本 ✗ · 独立状况 ✗ · 零真误报 ✗（明细不全））
L2：🔴 未达标（同上）
结论：均未达标 → 无任何机器可放行
  · B 机：观测天数未满；池级未过 L1
  · A 机：观测天数未满；本机误报率 14.3% > 10%；池级未过 L1
```

> 注意这份报告的写法：**每一个"✗"后面都跟着原因**，且"明细不全"被单独标出来——它既是否决理由，也是下一步的待办（补齐采集面）。

另外，门槛之外还有两类**三类单列**，不进稳态误报的分子：**开发窗样本**（观测期里正在改文件的机器）、**未确认样本**（ack 缺位）、**环境特有样本**（企业终端防护软件一类的外部因素）。分类账写清楚，才敢说"零真误报"。

---

## 3. 执行矩阵：动作 → 风险级 → 开关 → 护栏 → 放行判据

### 3.1 九列怎么读

矩阵是一张**动作真相表**，一个动作一行、九列。列固定，缺列的**不许放行**：

| 列 | 它回答什么 | 口径要点 |
|---|---|---|
| **动作 ID** | 这是哪个动作 | 必须与动作登记表里的 kind 同名——**同名才能对齐开关** |
| **干什么** | 一句话说清它动什么、不动什么 | 语义边界要写死（"只发请求，不直接重启"这类句子必须出现） |
| **归属哪把总开关** | 放行它要先开哪几把钥匙 | ⚠️ 有的动作要**两把甚至三把键同时开**；"哪把键说了算"必须在矩阵里写明，否则现场必然靠猜 |
| **风险级** | 错了会怎样 | L1 无害可逆 / L2 有损可回滚 / L3 永不自动（见 §3.2） |
| **目标档** | 期望的自治档位 | S0–S5 光谱（见 §3.2），默认最保守，放行 = 逐项上调 |
| **干系哪个模块开关** | 出问题时要关谁 | 动作 ID 与模块 id 不同名时必须显式写对应关系 |
| **护栏** | 它不能越过什么 | 去重 / 单日上限 / 预算 / 白名单 / 并发上限 / "只发请求不执行"……护栏是**放行的前置**，不是事后补的 |
| **放行判据** | 凭什么可以开 | 引用 §2 的门槛 + 本动作特有的前置（例如"必须先有审计链校验单测"） |
| **当前状态** | 登记 / 实现 / 放行 三件事各自如何 | 三者**可以不同步**（已登记未实现、已实现未放行都是常态），所以分三样写 |

> **一个容易被忽略的列义**："放行状态"与"有没有被观测过"是两件事。前者是闸门、后者是对照结论，分两张表更清楚（见 §3.8）。

### 3.2 两条固定的列：风险级与目标档

**风险级**（决定用哪一档门槛、要不要演练到哪一级）：

| 级 | 含义 | 门槛档 | 额外要求 |
|---|---|---|---|
| **L1** | 做错毫发无损、随时可还原（只读、只提醒、可在下一步撤销） | 池级 ≥25 + 独立 ≥15 | 至少过最底线的那一级演练 |
| **L2** | 有损可回滚（动数据、动文件、重启） | 池级 ≥50 + 独立 ≥30 | 恢复路径要过演练，碰启动链的更高档 |
| **L3** | **永不自动**（主程序升级、不可逆操作等） | 无（没有"放行"这回事） | 只通知、只出建议，人工执行 |

**自治光谱 S0–S5**：S0 陈列 / S1 告警 / S2 无害全自动 / S3 监控下自动 / S4 每动作请示 / S5 仅人。**默认档 = S0/S1**，放行是**逐项上调**。

### 3.3 四条写死的规则

#### R1 · 可开发面 vs 冻结面

详细见 §4。一句话：**新增动作、护栏、界面、单测、文档、回滚点**可以自由开发；**判定口径与门槛定义**属于冻结面，改了必须分段 + 两台机器一起重置，并写进报告。

#### R2 · 每个动作都必须"锁后交付"

新增任何自动化动作，交付时**一律**同时满足六条：

1. 代码进生产，但**开关默认关**；
2. **单测 + 夹具**，并且有一类强断言：**开关关着时任何执行调用都被拦、且不留副作用**（沙箱零写入 / 零子进程）；
3. `dryRun` 路径可跑，产出"将要做什么"；
4. **回滚点**（改前 / 改后 + 一键恢复脚本 + 桌面入口）**且过演练**；
5. 登记进矩阵表 + 动作登记表（**未登记的动作一律按 L3 只通知**）；
6. 记一行**变更流水**。

> 第 5 条的另一种写法是硬规则：**未实现的动作不许先登记**。原因：动作登记表是分级映射的单一数据源，通知 / 预览 / 台账 / 预览视图都按它走——先登记后实现会造出"外部可调用、内部啥也不干"的暗代码，而且真到接实现时没人记得它已经"半存在"。落地顺序固定为：**实现（含单测、夹具、dry-run、零动作强断言）→ 登记 kind → 补矩阵行 → 补预览映射 → 记流水**。

#### R3 · 放行 = 改表，不是改代码

放行某动作时**只做三件事**：① 按报告核对它的**放行判据**；② 改开关 + **留痕**（谁、何时、依据哪份报告）；③ 放行后 **48 小时内对照一次** dry-run，确认"真做的"与"预览预测的"一致；**不一致 → 立即关回**。

> 这条规则的价值在于它把"放行"从"一次高风险的开发活动"降级成"一次可留痕的配置变更"。R2 保证"随时能开"，R3 保证"开的时候不用现场施工"。

#### R4 · 开关是活的（kill switch 语义）

开关不只是"放行闸门"，更是**运行时关停开关**：**哪个模块坏了，直接关掉、再去修**。四级关停，越往上越粗，允许组合：

| 级 | 名字 | 载体 | 关掉后 |
|---|---|---|---|
| **L0** | 全局降级 | 一份"自治状态"文件的档位字段 | **全生态**自主动作暂停（含看板定时作业、自动归档等其他自治成员） |
| **L1** | 总开关 | 自治配置文件的四把键 | 对应动作族整族停 |
| **L2** | 模块级 | 自治配置文件的 `模块.<id>.enabled` | 单模块停，其余照常 |
| **L3** | 动作级 | 动作登记表 + 上限配置 | 单动作停 |

两条配套纪律：

- **关闭必须可观测（四条）**：① 每次开关变更写变更历史（谁 / 为什么 / 何时）；② **日报显式列"当前已关闭的模块"**（防"关了但没人知道"）；③ 面板一键关 + 状态色 + 二次确认；④ 关闭后 48 小时内跑一次 dry-run 对照，确认"真的没再做"。
- **开关债规则**：临时关停关掉的模块，**修复后必须在同一张卡里恢复**；长期不恢复的，要么转成"默认关的正式档位"写进矩阵，要么从矩阵删掉——否则 2ⁿ 个组合状态会把系统变成"一堆没人敢开的暗代码"。

> **降级的 fail-safe 方向**：全局降级档位**读不到或文件损坏时按"已降级"处理**。理由：一次文件损坏不应该变成"全生态放开"。
> 但反过来有一条例外要写清：**基础设施类模块**（守护进程、调度器、保护清单）**只受 L2 显式拨闸约束，不吃 L0 降级**——降级本身可能是"配置损坏 ⇒ fail-safe"的机器结论，让机器结论去停掉守护与调度，等于一次损坏就让生态毫无防护。

### 3.4 一条容易踩的坑：编号同名不同义

上表用 L0–L3 表示"关停层级"。但同一个体系里很可能**同时存在好几套 L 编号**——例如：① 关停层级、② 自愈分层、③ 恢复点演练档位、④ **放行分级**（本文 §2 的 L1/L2）。它们**编号彼此撞车、语义互不相干**。

- 最危险的一句话是"**L2 关着**"：它可能指"模块级关停关着"，也可能被读成"L2 档放行（动数据那档）"或"L2 真实演练"。三件事互不相干。
- **处置**：把对照表写进文档（哪套编号、用在什么语境、判据是什么），并规定"**看到 L1 先问是哪一套**"。新体系更省事的做法是：**编号带域前缀**（如 `放行-L1` / `演练-L2` / `关停-L2`），从源头避免撞车。

### 3.5 「热生效」取决于谁在什么时候调用它

一个反复被问的问题：**改了这个开关，要不要重启？** 判据不在开关上，在调用方：

> **开关的读取函数通常是无缓存、每次读盘的** ⇒"热不热"**不取决于开关，只取决于"谁在什么时候调用它"**。

| 调用方形态 | 热生效 | 典型场景 |
|---|---|---|
| **每次新进程**（每次跑都重新读盘） | ✅ 是（下次触发即生效） | 命令行工具、短命定时作业 |
| **每请求 / 每事件重读** | ✅ 是（毫秒级～秒级） | 只读接口、写入拦截层的事件钩子 |
| **进程启动时读一次，之后固化进内存 / 模块常量** | ❌ 否（不重启永不生效） | 常驻调度器的作业注册表、守护进程的启动参数、插件在加载时合并的配置 |

**这条判据的用法**：设计开关时就按"能热生效的形态"做（读文件而非读内存、每请求现算而非启动固化）；已经在内存里固化的，就必须在文档里**写明"要停/起该进程"**，并把它作为已知缺口列出来（"要停只能重启"本身就是一条该被修掉的设计缺陷）。

**一条现成的好范式**：把"暂停某道防线"做成读一个开关文件、且带 3 秒级缓存的形态——面板按钮毫秒级生效、外部改文件 3 秒内生效。**新开关照这个形态做即可。**

### 3.6 「关」的语义是 drain，不是掐断

**关闭只表示"不再发起新动作"；已经在跑的动作默认跑完。**需要立即停时，走显式的回滚，而不是杀进程。

> **禁止"一关就杀进程"**——那会制造半完成状态，而这正是所有难查故障的来源。

### 3.7 重启与半完成态：两条判据说的不是一回事

这套体系里"关 = drain"和"重启"很容易被混为一谈，但它们判的不是同一件事：

- **"关 = drain"** 管的是**开关语义**：关掉某模块 ⇒ 不再发起新动作、已跑的不掐断。
- **重启闸门** 判的是**活跃会话数**（会话文件尾部回合 ∪ 应用进程自己的"运行中"集合 ∪ 任务看板的运行中卡片，三源**并集**）⇒ **不含任何"正在执行的动作"登记**：一次重启**不会等**动作落地，也不会因它排队。

**"正在执行的动作"要分两类看**：

| 承载 | 重启是否受影响 | 在跑状态在哪看 |
|---|---|---|
| **常驻进程**（调度器 / 守护进程）里的作业 | **不受影响**（不随应用重启被杀） | 调度器状态文件的 `作业.<id>.running` 字段 |
| **应用进程内**（会话回合 / 面板发起的动作 / 插件任务） | **会被打断**——这是"半完成态"的唯一来源 | 会话尾部回合 + 审计流水 |

**结论：重启前"不等"正在执行的动作**，三条理由（都不改任何行为）：

1. 会话内动作与面板动作**没有统一登记**，闸门无从枚举"要等谁"；
2. 等待与"低水位 = 活跃会话数为 0"**同义反复**，且活跃判定已明确"不给会话发消息"（发消息会让它重新变活跃 ⇒ 永远等不完）；
3. 强行等待会把重启从"≤1 分钟"拖成**不确定时长**，而重启本身是高频动作。

**兜底不在"等"，在别处**（逐条可核对）：

| 风险 | 兜底机制 |
|---|---|
| 状态文件写坏 | **原子写**：临时文件 → `fsync` → 同卷 rename（占用则重试）⇒ 最坏丢"本次未 rename 的改动"，**不会留半截目标文件** |
| 锁残留 | 锁登记表的 **TTL** + 辅助判据（调度器心跳 / 某作业的 running 位）⇒ 重启后旧锁按陈旧回收 |
| 动作做了一半 | **幂等重放**（下一轮或下次触发重来）+ 审计留痕（append-only **哈希链**，可校验） |
| 会话回合被截断 | 重启协调器的**打断清单** ⇒ 被打断的会话在标题上残留"运行中"标记 = **客观证据**，再由通知与日报治理 |
| 定时作业卡在 running | 状态文件的 running 字段 + 日报的**调度健康判据**（连续失败 ≥2 且最近一次非正常 ⇒ 🟡；≥5 ⇒ 🔴） |

> 还有一条"可见化"经验：把这条口径**打印在动作发生的地方**（发起重启的命令行输出里），而不是只活在这份文档里——文档没人回头翻，动作现场的那行字才会被看到。

### 3.8 放行之后：48 小时预览对照

放行不等于结束。每个动作放行后要跑一次 **48 小时对照窗口**，比"实际做的"与"预览预测的"，四种结论各有固定处置：

| 四态 | 语义 | 处置 |
|---|---|---|
| **一致** | 实际动作 ⊆ 预测，且无预测外的写操作 | **转常态**（回写矩阵该列） |
| **多做** | 出现预测里从未出现过的目标 / 写操作 | **立即关回** + 审计 + 日报红项 |
| **少做** | 预测的候选在窗口内没被执行 | **有解释则中性**（去重窗口 / 单日上限 / 预算门 / 闸门）；**无解释按未观测** |
| **未观测** | 预测集为空、或实际集为空且无任何抑制证据；或判定不可用（基线档缺失 / 损坏、审计不可读） | 动作标「**未验证**」+ **上升给人**；**不许转常态** |

**两条不许**：

1. **零基线不得折叠成"一致"**——"实际 ⊆ 预测"在预测为空时**恒真**，这是最容易骗过自己的形态（实测中就出现过：基线档缺失 ⇒ 预测集 0 条 ⇒ 判定不可用，而不是"一致"）。
2. **"未观测"不是拒绝放行**——它的意思是"这次没观察到，由人决定"（补采 / 维持观察 / 关回 三选一）。

> 实现上有一条硬要求：**对照采集器必须只读、且不得享有限流配额**（否则"为了让对照有数据"而挤占真实动作的窗口，两头都失真）。另外，**比对粒度要先如实写清上限**：如果审计行里没有目标字段、dry-run 行的签名恒为空，那么"目标级/内容级比对"在当前数据上根本做不出来——写进报告的"局限"节，别写成能力。

---

## 4. 冻结面：怎么防止"尺子被改"

### 4.1 可自由开发面 vs 冻结面

| 面 | 内容 | 纪律 |
|---|---|---|
| **可自由开发**（开关仍关即可推进） | 新增自动化动作、护栏（预算 / 上限 / 白名单）、界面与面板、单测、文档、回滚点与演练、dry-run 视图 | 照 R2 交付即可，不影响观测 |
| **冻结面**（改了必须分段 + 两机重置，并写进报告） | ① 告警判定口径（事件判定库 / 推送策略库 / 事件注册与阈值）② **放行门槛与指标分母定义**（误报率分母、未确认口径、两档条数与独立状况门槛）③ **样本池的段语义与台账格式** | 改了就**封段**：旧数据留在旧段、新计时从改动日重新起算，两台机器一起重置 |

**为什么必须冻结**：放行判据建立在"这一段观测的尺子没变"这个前提上。尺子一改，之前攒的样本与新样本**不可比**；继续混算，得到的结论既不是旧尺子的、也不是新尺子的。

**分段要留痕**：段起点时间、封段原因、是否重置计时、哪两台机器重置了——这些都写进报告与状态文件（例如状态里显式写一个 `segmentReset: true/false`），别只留在聊天记录里。

### 4.2 确定性缺陷修复的处置

冻结面不是"绝对不能动"。判定口径里的**确定性缺陷修复**（不是放宽，而是修错）也不受冻结影响，但必须同时满足三条：

1. **说明这是缺陷、不是放宽**（在报告里写清"旧口径错在哪、新口径回归了什么既有定义"）；
2. **留回滚点 + 过演练**；
3. **封段 + 两台机器重置**。

> 与 §2.5 的判据配合使用：**统计归类修正不重置，判定规则变更必须重置。**归类修正之所以不重置，是因为它回归的是**既有权威定义**、告警判定与台账一字未动、历史样本可按新口径全量重算——这两条都成立才叫"归类修正"，缺一条就按"改规则"处理。

### 4.3 两处互为镜像、必须同步改

冻结面规则通常会在**两个地方**各写一份（例如方案里的"冻结面规则"与矩阵文档里的 R1）。这类互指关系必须写清一句：**两处互为镜像，改一处必须同步改另一处**——否则会出现"两套口径各说各话"，而这本身就是一次静默的尺子变更。

### 4.4 文档与实况会漂移：订正注纪律

放行状态是**活**的（今天关、明天开），而文档是**死**的。一份写着"未放行"的矩阵在放行之后就成了错误的引用源。两条纪律：

- **就地标注、不改原文**：在过时的表述旁加一条**订正注**（日期 · 只标注不改原文 · 真源=权威状态文件或命令），写清"这句话对各自交付当刻成立，现状如下"。保留历史痕迹比"把文档改得看起来一直对"更有价值。
- **权威源唯一**：所有"当前是开还是关"的引用，一律指向**状态文件 / 一条只读命令**，不许指向文档——文档只记口径，不记现值。

---

## 5. 明确不做哪些

把"不做什么"写清，比多写十条"要做什么"更能防止体系腐化：

1. **不追求无人放行**：开关的开闸动作**永远由人拍板**。自动化负责攒证据、出报告、给建议；人负责决定。
2. **不把 L3 纳入自动**：永不自动那一档不设"放行"概念，只出通知与建议。
3. **不为了凑门槛而放宽门槛**：样本不够就继续攒，或者等**口径被证明有问题**时修口径（§2.5）——不允许"改小数字"。
4. **不在观测期临时改判定口径**：要改就走 §4.1 的分段与重置流程。
5. **不做"一关就杀进程"**：关 = drain（§3.6）。
6. **不为放行单独开发**：放行 = 改表 + 留痕（R3）；如果放行时需要写代码，说明 R2 没做到。
7. **不先登记未实现的动作**：动作登记表是单一数据源，未实现先登记 = 造暗代码（R2 第 5 条）。
8. **不把"两个不同的数字"拿来比同一件事**：例如"源码里出现多少处环境相关字样"与"耦合行数"是两回事，决定改造量与风险的是**性质**（有没有把某个环境的策略写进逻辑），不是命中数字。（这条是矩阵之外、但同源的记账纪律。）
9. **不在缺数据时放行**（§2.4）。
10. **不把零基线判成"一致"**（§3.8）。

---

## 6. 一张放行前的检查清单

放行任何动作之前，逐条打勾；任何一条打不上勾都意味着**继续等**：

- [ ] **矩阵有它这一行**，且九列齐全（含"归属哪把总开关"与"护栏"）。
- [ ] **风险级与目标档明确**，门槛档与风险级匹配（L1 用 25 / L2 用 50）。
- [ ] **池级门槛达标**：条数 + 独立状况，两个数都够。
- [ ] **该机条件达标**：观测天数满 + 该机误报率 ≤10%。
- [ ] **明细完整性通过**：没有"未知"被当成"无误报"；明细不可得的机器自报数字已显式列出。
- [ ] **护栏齐备且可验证**：去重、单日上限、并发上限、预算门、白名单——每一项都有对应单测。
- [ ] **回滚点过演练**：恢复到哪一档演练、判据是什么、谁验的，都写清（未过对应档演练的恢复点不许一键执行）。
- [ ] **预览视图看得见它**：日报或面板里真的能看见"如果开了会执行哪几项"。
- [ ] **开关形态确认**：这套开关是"热生效"还是"要停/起进程"；若是后者，把停机窗口与影响面写进放行说明。
- [ ] **放行后对照已安排**：48 小时窗口的采集器只读、不占限流配额；四态处置已定义。
- [ ] **留痕就绪**：谁、何时、依据哪份报告；变更流水一行；已关闭模块在日报里可见。
- [ ] **没有触碰冻结面**：本次改动若涉及判定口径 / 门槛定义 / 台账格式，必须先分段 + 两台机器重置。

---

## 7. 复跑与限度声明

**复跑**：本文的口径都可以用只读命令复核——读自治配置文件的四把键与模块开关、读状态文件的降级档位、跑一次矩阵表的"动作 → 模块"零差集校验、跑一次池级判定（输出条数 / 独立状况 / 刷屏比 / 明细完整性）。**判定脚本与矩阵表必须同源**（同一份登记表驱动），文档里出现的数字一律以只读命令的输出为准。

**限度（本文没做到的）**：

1. **未覆盖"放行之后长期是否仍然正确"**：本文只到 48 小时对照；更长期的有效性要看判定质量与误报账的持续观察。
2. **门槛数字是本环境的观测产物**：25 / 50 是**贴着本环境的风险定义**算出来的，换一套风险定义就该重算（公式给了，结论不必照抄）。
3. **编号避让是"事后发现"**：四套 L 编号撞车是使用中暴露的，不是设计出来的；文中给的"带域前缀"是补救建议，不是既有实况。
4. **两处镜像同步靠纪律**：文档互指关系目前靠"改一处必须同步另一处"的人工纪律维持，**没有机械校验**——这是一条已知缺口。
5. **对照粒度有上限**：审计记录缺少目标字段、dry-run 记录签名恒空时，"目标级 / 内容级"比对做不出来（已在 §3.8 如实标注）。

---

> **一句话收尾**：这套东西真正在防的不是"自动化出错"，而是**"没人能说清它昨天做了什么、凭什么今天可以多做一点"**。
