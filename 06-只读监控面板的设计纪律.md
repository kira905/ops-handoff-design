# 只读监控面板的设计纪律

> **这篇文档讲什么**：给"长期在自有机器上跑编码 agent"的那块**只读面板**，设计上踩过的坑与定下来的纪律。它回答三件事：**面板该回答什么问题**（不是"服务在不在"，而是"今天怎么样、哪些东西静默坏了没人知道"）、**一次打开要付多少代价**（实测数据 + 分片取数工程）、**哪些线绝不能越**（读不到就说不读到、只渲染不复算、面板不许把自己读到的东西写回去）。
> **版本**：v1.8（随本仓下一版发布；本稿为本地稿，尚未打 tag）
> **素材基准日**：两轮实测——一次"打开面板要付多少代价"的全量实测与首屏重构（2026-09-14），以及一次同类只读体检面板在通用化过程中暴露的既有缺陷复核（2026-09-21）。
> **占位符说明**：`<工作区根>` = agent 的工作目录；`<DSH 主目录>` = agent 的数据主目录；`steward-*` = 本仓 `02` 号文档既定的管家组件族占位名。文中所有面板与插件一律用角色泛称（"管家面板插件""生态体检面板""宿主"）。
> **数字口径（重要）**：本文全部实测数字来自**特定机器上的特定观测**——**环境相关，勿照抄**。它们证明的是"代价量级"与"判断方式"，不是阈值。

---

## 0. 先看结论

1. 面板第一屏要回答的不是"服务在不在"，而是**"今天有没有事要我处理、有哪些东西静默坏了"**。前者是可用性监控，后者才是长跑 agent 的真需求——**它最危险的失效方式不是崩掉，是静默地跑偏**。
2. 只读面板最值钱的一条纪律是：**读不到就显示「不可探测 + 原因」，绝不显示 0、绝不显示假绿**。这条纪律有三次真实代价作证（§2.2）——三次都不是"面板坏了"，而是**面板把一个未知显示成了正常**。
3. **面板只渲染，不复算**（§4）：同一个事实只许一处权威源。面板一旦复制了判定逻辑，就给自己埋了一条"迟早与真源分叉"的路。
4. 一次打开要付的代价可以被量出来，而且通常**付在没人看的地方**：实测 134 KB 的响应里 **56% 是一个首屏完全用不到的字段**；冷启动 4.65 s 里大部分时间在等最慢的那个采集器（§3）。
5. 取数工程的四件套是 **分片 + 进程内 TTL 缓存 + 单飞 + 超时预算**，外加一条：**任何一段失败只影响它自己那一块**（局部降级）。片要按**耗时**切，不按字段切。
6. 一个只读面板"通用化"的天花板，是**它读的状态由谁产生**（§5）：宿主自带的数据可以通用；依赖同款插件形态的只能"声明依赖 + 降级"；而由你自建体系产出的状态（判定表、清单、决策结论）——**想让面板通用，等于把整套体系一起开源**。这一条最不讨喜，也最省时间。

---

## 1. 面板要回答的问题

### 1.1 两类监控的差别

| | 服务可用性监控 | 长跑 agent 的面板 |
|---|---|---|
| 问的问题 | 服务在不在、响应快不快 | **今天怎么样？有没有事要人处理？有哪些东西静默坏了没人知道？** |
| 失效形态 | 挂掉、超时、5xx | **假绿**：看着正常，其实判定早就失准 |
| 观测对象 | 端口、进程、吞吐、延迟 | 会话、任务账本、自动动作流水、资产与配置漂移 |
| "正常"能不能自动判定 | 能（阈值清楚） | **常常不能**——样本量天生不足 |

最后一行是关键：这类系统的使用者可能只有一个人，**永远攒不够样本量去相信自己的规则**。所以面板不能只做"绿 / 红"，它还得回答**"我这把尺子本身准不准"**。

### 1.2 静默失败的常见形态（面板要能把它们分开）

面板的价值在于把"静默"变成"可见"。设计时是按下面这几类反推指标的：

1. **假绿**：判据读不到数据，被兜底逻辑判成了"正常"。
2. **静默失效**：某个能力实际没生效（配置写错了形态被当成"未设置"），但没有任何地方报错。
3. **被误当成功**：任务"完成"了，交付物却不在——状态结算与真实结果脱节。
4. **静默停摆**：某段数据、某个定时作业停了，页面照常渲染旧值。
5. **量纲错**：指标读得到，却在错误的时区或错误的单位下被解读。

### 1.3 首屏只答一件事

首屏的目标不是"把系统全貌塞进去"，而是让使用者在 **1.5 秒**内知道**有没有事要自己处理**。据此定下来的首屏内容 = **三块 + 一排指标**：

- 一块**一句话判断**（"今天 N 件要你处理 / M 件已自动办好"）；
- 一块**待处理清单**（每条可点开证据）；
- 一块**健康底栏**（各部件心跳 + 数据新鲜度 + 尺子达标没）；
- 一排**治理指标**（不是"内容量"）。

### 1.4 指标从「内容量」改成「治理状态」

改造前的指标是内容量：被中断的会话数、进行中的会话数、待办数、条目总数……**全都算得出来，却没有一个能回答"我现在要不要动手"**。

改成六项之后，每一项都写明**权威源**与**点击去向**：

| 指标 | 它回答什么 | 关键纪律 |
|---|---|---|
| 待我处理 | 有几件事卡在人这一侧 | **分项相加、不去重**；每一项都必须来自权威源，禁止面板自算 |
| 自动能力状态 | 有没有自动化能力被关掉、关了几项 | 必须区分**预期关闭**（按计划关的）与**异常关闭**（自己坏的）；计数来自权威源 |
| 今日自动办好 | 无人值守干了什么 | 无当日记录时显示「今日尚无自动动作」，**不显示 0** |
| 资产异常 | 升级 / 镜像 / 补丁 / 依赖面有没有漂 | 只读缓存，**不为刷新去打外部包源** |
| 在途任务 | 队列里压着多少 | 保留"内容量"口径，用于连续观察 |
| 判据自身质量 | 判据最近误报多不多、样本够不够 | 必须带限定词（按机 / 按池）；**"达标"不许裸写** |

> **顺带一条命名纪律**：如果体系里已经有一个词（比如某个指标缩写）被用在别处指"放行判据"，面板这一排就**别叫同一个名字**。两套尺子同名，迟早有人拿 A 的标准来质疑 B 的数字。本次的做法是另起一个名字，并把"为什么避让"写进方案——**命名冲突要在方案里解决，别留给读者去猜**。

---

## 2. 显示纪律：不可探测 + 原因

### 2.1 三档取值，而不是两档

只读面板的每个格子只有三种合法形态：

| 形态 | 何时用 | 反例（禁止） |
|---|---|---|
| **真值**（含新鲜度） | 探测成功 | 拿旧值当现值、不标数据自身的时间 |
| **不可探测 + 原因** | 探测失败 / 未配置 / 依赖不在 | 显示 `0`、显示"正常"、显示空白 |
| **显式空态** | 探测成功，但确实还没有数据 | 把"还没有"和"读不到"混成一回事 |

三档里最贵的是第三档：**「今日尚无自动动作」和「自动动作读取失败」必须长得不一样**。前者是事实，后者是故障——显示成同一个 `0`，等于把故障伪装成事实。

同时，每条数据都要带**逐条数据自身的 `asOf`**（它的新鲜度），并与"本次生成时刻"分开显示。理由很直接：**防止"看着像实时，其实是十分钟前"。**

### 2.2 三次真实代价（设计教训）

下面三条都是"**面板把一个未知显示成了正常**"。它们来源不同（同一宿主的不同版本、不同配置），**都不是显示逻辑写错了**，而是：

**教训一 ·「读不到」被当成了「没加载」。**
有一类判定要读宿主首页内联的启动清单，用来判断某个插件是不是真的加载了。当宿主没响应、地址配错、或首页投影的形态变了，读取就会失败——而原逻辑只判"等于 false / 等于 true"两种情况，于是**读取失败落进了"没命中 false"的那一侧**，声明了前端能力的插件照样显示 🟢。
> **纪律**：三值逻辑（真 / 假 / **未知**）必须显式建模。"读不到"是第三种值，不是"假"。
> **修法**：读不到且该项有依赖 ⇒ 判黄 + 注明「启动清单不可用 ⇒ 加载状态未知」。

**教训二 · 路径不随配置层解析。**
有一项判定要调一个外部只读脚本去拿结论，而脚本路径取的是**进程默认配置**里的值。当使用者用配置覆盖（隔离实例、脚手架夹具、自定义布局）时，路径就静默退回默认值 ⇒ 那一项莫名变成"不可用"，**日志里什么都不报**。
> **纪律**：凡"路径 / 端点 / 工具位置"这类输入，**取用点必须从当前生效的配置里取**，不能在模块加载期固化成常量。模块级常量 + 事后覆盖 = 静默退回。

**教训三 · 只认一种配置形态。**
有一个"停用某个类目"的开关，代码只认字符串形态；而这个开关的自然写法是数组。传数组时被当成"未设置" ⇒ **停用静默失效，该类目照常参与汇总**。
> **纪律**：布尔 / 列表型的开关**必须把所有自然形态都认下来**（数组 / 逗号串 / 未设置），并在"配置来源自证"里写明它来自哪一层。

> **共同点**：三条都是**"配置化过程中暴露出来的既有缺陷"**——不是通用化引入的，而是把参数从写死改成可配时，**原来靠"恰好写对了"掩盖的问题全部露了出来**。这也说明一件事：**通用化本身就是一次高强度的自审**。

### 2.3 缺数据态必须写成断言

"探测失败就显示不可探测"如果只写在文档里，它迟早会退化。做法是把它变成**可复跑的断言**：用一份**没有数据源**的夹具（只造目录骨架，故意不造外部工具与清单），逐项断言：

- 未配置清单的那一类 ⇒ 给出「未配置」提示项，**不是"0 条"**；
- 缺失外部脚本的那一类 ⇒ 降级 + 说明原因，**不是绿**；
- 汇总类目 ⇒ 状态是 `unavailable`，**不是全零全绿**；
- 依赖外部判定的每一行 ⇒ 状态 `unknown`，**不渲染颜色**；
- 工具目录 ⇒ 如实为 `null`（**不编造一个可能猜错的路径**）。

> 这套夹具在一次真实交付里跑出 **47 条断言全过 / 0 失败**，其中"缺数据态"是独立成段的一组。**没有这一组断言，"不显示假绿"就只是一句口号。**

---

## 3. 取数工程：一次打开要付多少代价

### 3.1 先量，再改

改造前的实测（全部只读）：

| 项 | 实测 | 说明 |
|---|---|---|
| 单个状态接口的响应体 | **134 KB**（另一次 209 KB） | 随流水条目增长 |
| 单次响应耗时 | **冷 4.65 s / 热 2.5–3.4 s** | 连打三次：3.4 / 2.5 / 2.5 s |
| 服务端缓存 | **无**（响应头 `no-store`） | 每次打开都全量重算 |
| 前端 | 一次请求后把 **15+ 个区块**一口气渲染进一页 | 页面长度约 3 个视口高 |

**载荷逐字段量尺**——这是最有用的一步，因为它直接把"该砍谁"算了出来：

| 字段 | 字节 | 占全量 | 首屏是否需要 |
|---|---:|---:|---|
| 运行时的"长期约定全文" | 75,599 | **56%** | ❌ 完全不需要 |
| 变更流水（19 条） | 38,086 | 28% | ⚠️ 只要"今日"一行 |
| 健康（6 个子块） | 6,751 | 5% | ✅ |
| 任务 | 5,478 | 4% | ✅ |
| 审计（只要计数） | 2,830 | 2% | ⚠️ |
| 报告 | 2,069 | 1.5% | ❌ |
| 外部信号（只要异常项） | 1,362 | 1% | ⚠️ |
| 会话（中断 3 条） | 1,244 | 1% | ✅ |
| 记忆 + 摘要 + 元信息 | 884 | 0.6% | ✅（摘要） |

> **首屏真正需要的 ≈ 16 KB = 全量的 12%。** 而占 56% 的那个字段，是把"运行时长期约定全文"接进状态接口的产物——当初的意图是"随手可查"，到了首屏语境里，它就是**最大的死重**。

**耗时分解**（分别计时）：

| 段 | 冷 | 热 |
|---|---:|---:|
| 扫全部会话并解压尾部（用于中断判定） | 428 ms | 36 ms |
| 健康采集（6 子块，含一次跨进程 HTTP） | 585 ms | 564 ms |
| 其余聚合 | ~497 ms | — |
| 审计计数 | 9 ms | — |

由此得到三个**互相独立**的病因：

1. **载荷巨石**：134 KB 一次给，其中 56% 首屏用不到。
2. **采集串行**：一次请求串行跑完 8 个以上采集器 + 2 次跨进程 HTTP 往返 ⇒ **整页等最慢那一步**。其中一次跨插件 HTTP 单项约 **2.4 s**，是固定价。
3. **渲染巨石**：打开即渲染 15+ 个区块，DOM 一次性铺满。

### 3.2 处置：分片 + TTL + 单飞 + 超时预算

- **分片**：接口支持按片取（形如 `?parts=core,metrics,feed`）。**无参调用保持逐字节不变的旧行为**（兼容既有消费者）。
- **进程内 TTL 缓存 + 单飞**：面板是宿主进程内的插件，**不需要落盘快照**——一个进程内缓存就一次性消灭了"快照与实时不一致""半写快照""快照回滚顺序"这一整类问题。TTL 取 **300 s**（复用体系里既有的五分钟节奏，**不新增定时作业**）；并发请求靠单飞合并成一次重算。
- **超时预算**：冷启动的单次重算有总预算（**约 1.2 s 兜底**）；超时就返回**部分数据 + 显式「不可探测 + 原因」**，而不是让整页转圈。
- **逐片 TTL**：不同片的新鲜度要求不同（首屏片秒级，全文类片分钟级）。

**被否掉的方案（值得记）**：

- **落盘快照**：最初选的是"定时落快照、首屏先读快照"。它一次引入了六个新问题（落盘产出、边界、TTL 与快照不自洽、作业要素与锁、半写快照、回滚顺序）——**在"面板本身就是进程内插件"这个前提下，它是用外部状态去换本来免费的收益**。降级为将来备选（只在需要跨重启复用或跨实例共享时才启用）。
- **HTTP 缓存协商（ETag / 304）**：前端用的是"解析 JSON + 非成功即抛错"的取数写法，**304 会被当作失败抛错**；而且响应本来就带 `no-store`，不参与缓存校验。⇒ 在这个前端形态下加它**只增加故障面**。缓存只走 TTL + 单飞。

### 3.3 片要按耗时切，不按字段切

> 这是分片最容易做错的地方：按"业务字段"切出来的片，往往**每片的代价都很像**，等于把一次慢请求拆成三次慢请求。

按实测耗时切出来的片长这样：

| 片 | 内容 | 为什么这样切 |
|---|---|---|
| `core` | 元信息 + 摘要 + 会话（含中断）+ 任务 + 未确认事件（只 id / 级别 / 标题 / 时间）+ 待验收计数 + 审计计数 | 首屏全部要用的东西，预算 ≤ 30 KB |
| `health` | 健康 6 个子块 | **单独成片**：它内部有一次约 2.4 s 的跨进程 HTTP，是**固定价** ⇒ 首屏先出 core，健康异步后填 |
| `feed` | 今日段：**最近 8 条 + 总条数** | 实测"今日段"有 71,796 B，**必须截断**，且不再返回清单尾部 |
| `assets` | 每注册组一行（组名 + 计数 + 最近异常数 + 数据时间） | 移出首屏，点开对应页才拉 |
| `metrics` | 六项指标的计算结果 | 由唯一入口产出，< 2 KB |
| `contract` | 运行时长期约定全文 | 默认不返回，点开对应页才拉 |

**兼容三条**（都要写成断言）：

- 无参响应的字段集 **⊇** 改造前的全量（否则别的消费者会**静默少字段**）；
- `?parts=core` 走**子字段级**白名单（不是只校验片名）；
- **未知片值忽略并回填到响应的元信息里，不返回 4xx**——防前端旧版写错参数时整页崩。

> **一条实测提醒**：该路由是**精确匹配、忽略查询串**的。所以"加参数不会 404、旧调用方零影响"成立；但也因此**参数必须显式实现——光加个参数不会自动分片**（实测加 `?parts=core` 时接口仍然返回全量，这一点是量出来的，不是猜的）。

### 3.4 局部降级：缺一段不影响整屏

前端契约只有几句话，但每句都是纪律：

1. 打开 = **一个首屏请求**；长尾区块**一个 DOM 都不建**。
2. 进对应页才拉它那一片。
3. **每块独立空态 / 降级**：某片失败 ⇒ 该块显示「不可探测 + 原因」+ 重试按钮，**不影响其它块**。
4. 骨架优先：有缓存先画（标注数据时间），没有则显示「采集中（首次需 N 秒）」。
5. 手动刷新的语义 = **强制实时重算**，并显示本次耗时（让使用者自己看达标没）。
6. 长度控制：首屏合计 ≤ **1.5 个视口高**，超出的**一律进二级页**。

**首屏预算（硬断言）**：`core` ≤ 30 KB、首屏实时路径 ≤ 40 KB、**单次打开 30 秒窗口内的请求总字节 ≤ 60 KB**，且**缓存命中与冷启两种夹具各跑一次**。实测落地值：首屏单请求 10.3–17.9 KB、缓存命中 14 ms。

---

## 4. 面板与真源的关系：只渲染，不复算

### 4.1 单一数据源铁律

> **同一个事实只许一处权威源，面板只读、不复制判定逻辑。**

这条纪律的推论很硬：某个数"怎么算"的逻辑**只能在产生它的那一侧存在一次**。面板要显示它，就去读**它的输出**；面板若要"自己再算一遍"，那就是两套实现。

**违反的后果不是"算错"，而是"分叉"**：真源改了口径（阈值调整、字段含义修正），面板那一份不会跟着改；两边在**很长一段时间里都"看起来正常"**，直到某个值离谱到被人发现。

本次落地的四条 + 对应断言：

| 面 | 处置 | 回归断言 |
|---|---|---|
| 误报率 | 只读权威函数的输出 | 面板值与当日报告里的同一项**逐值比对** |
| 心跳新鲜度 | 只读健康判定函数给出的**等级**，面板**不自算时间差** | 同上 |
| 自动能力状态 | 只读状态读取函数的返回值 | 同上 |
| 已关项计数 | 只读模块描述函数的返回条数 | 与报告摘要条数一致 |

> **反例（一个具体的坑）**：某类状态文件里的时间戳是 **UTC**，而页面渲染按本地时间做。如果面板"自己算年龄"，就会把 8 小时的时差算进去，产出**8 小时级的假告警**。所以"这个数换算成几分钟"这件事，**也属于判定逻辑**——它得待在真源里，不在渲染层里。

### 4.2 同源断言：怎么证明"两条路径真的是同一份实现"

面板有两条取数路径：**缓存路径**与**实时重算路径**。它们必须产出**字节级相同**的指标对象（唯一被归一化的字段是计算时刻）。

做法：把两条路径的结果都序列化，比较哈希；不相等即回归。这条断言的价值在于——它是**唯一能在改完代码后"立刻"发现分叉**的手段（其它手段都要等使用者看到数字不对）。

### 4.3 只读边界的两种证法

"只读"是承诺，不是形容词。两条可复跑的证法：

1. **写口唯一性**：某个状态文件的修改时间，**只允许**在它自己的定时作业或显式命令行动作之后变化——面板的读路径**不得**改它。做法：对面板的全部读路径取样窗口，用定时作业的"上次运行时刻"作为写者证据。
2. **退化路径不写盘**：面板做一次实时重算前后，数据主目录的**文件清单与关键文件哈希不变**。做法：清单含体积与修改时间，关键若干份逐字节哈希。

> 再补一条更省事的：**面板的任何路径都不得调用有副作用的接口**（例如"生成建议""调用模型"）。本次直接把这条写成红线：**面板不许出现任何模型调用**——一句话判断由规则拼装，建议读现成产物。

---

## 5. 数据来源三分类与三种处置（"通用化"的天花板在哪）

### 5.1 三分类

一个已经跑在自己机器上的只读面板，在决定"能不能给别人用"之前，先把**它读的每一项数据**按来源分类：

| 类 | 判据 | 典型项 |
|---|---|---|
| **通用** | 数据来自宿主 / 主程序自带的机制 | 插件清单与其声明、插件实体目录里的元信息、宿主首页内联的启动清单、技能目录的元信息、宿主提供的解析库、依赖清单里的实体版本与包源上的最新版 |
| **半通用** | 数据来自**同款但可能不同版本**的宿主或插件形态 | 首页投影的结构、"某个符号落在哪条解析链上"、补丁层的插入段、某个插件注册的服务名 |
| **本机专有** | 数据由**自建体系**产出 | 补丁特征清单、升级规则表（禁升 / 冻结）、断裂符号预检结论、档位判定脚本、依赖面体检脚本、跨表校验器、包名简介字典、工具目录路径 |

> 三类的边界不是"数据重要不重要"，而是**"换一台机器还存不存在"**。

### 5.2 三种处置

| 处置 | 用在哪 | 代价 |
|---|---|---|
| **配置化** | 本机专有项 | 外置成配置（环境变量 / 配置文件 / 插件配置），**默认为空**；使用者得自己写一份 |
| **降级** | 半通用的读取失败、未配置的专有项 | 显示「不可探测 + 配置指引」，**绝不显示 0、绝不猜颜色** |
| **剔除** | 只对某台机器有意义的生成物、纯个人化的文案 | 不进发布集；需要的能力**自带一个精简替代**（见 §5.4） |

配置项的形态本身也要设计，原则两条：

1. **数据根 / 环境名 / 主程序路径**必须可配（这是别人的环境里第一个不一样的东西）；
2. 像**工具目录这类"猜不准"的路径，不自动探测**——探不到就如实为 `null` 并降级。**"猜一个"比"说没有"危险得多。**

### 5.3 反面：为什么"把面板整体通用化"常常等于"把整套自治体系一起开源"

这是本文最不讨喜的结论，但它正是**决定一件面板类组件能不能开源的判据**。

推理链：

1. 面板的输入 = 状态。**面板能显示什么，取决于这些状态由谁产生。**
2. 由自建体系产生的状态（判定表、清单、决策结论、体检脚本的输出），**本身就是那套体系**——它们不是"数据"，而是"判断"。
3. 于是想让面板"到处都能用"，只剩两条路，且都有明确代价：
   - **把判定一起搬过去** ⇒ 你开源的不再是一个面板，而是**整套自治体系**；而且它会立刻带上体系的全部前提（状态文件布局、作业节奏、锁、告警语义）。
   - **只搬"壳"** ⇒ 面板剩下的就是宿主自带的那几项（插件清单、版本、技能元信息），**通用但薄**；专有部分一律走 §5.2 的"降级"。
4. 所以正确的做法不是"把面板通用化"，而是**把面板按数据来源切成三层**：通用的直接给；半通用的**声明依赖**并降级；专有的**配置化（默认空）或剔除**。

> **判据（可直接拿去用）**：把面板要显示的每一项列出来，逐项问一句 **"这一项的数据，是不是只有装了整套体系才会有？"** 是 ⇒ 它不能进通用版。这一列占比越高，"开源这个面板"就越等价于"开源整套体系"。

**实测旁证**：在一次面板类组件的通用化里，真正需要外置 / 降级 / 剔除的项**集中在"判断类"**（规则表、清单、体检脚本），而**"读取类"**（清单、版本、目录元信息）几乎都是通用的。也就是说——**卡住开源的从来不是"面板读了什么文件"，而是"面板凭什么说它坏"**。

### 5.4 顺带一条：别让"发布集不带依赖"变成"开箱不可用"

有一个具体形态值得单列：某插件静态引用了一个**生成的适配层文件**（约 60 KB，实际只用到其中一个服务名）。发布时若"不带它、让使用者自备"，等于**开箱即不可用**（静态引用指向不存在的东西 = 加载就失败）。

正确处置是**剔除生成物 + 自带一个精简替代**（把用到的那一个符号与接口前缀写成几十行的协议常量文件），并让服务名**可被环境变量覆盖**——宿主改名时**不用改代码**；同时在文件头写明"如果这个服务名被移除会有什么症状"（某个入口不出现，而不是静默出错）。

---

## 6. 诚实清单：什么该砍

这一节是"哪些东西其实没人看"的自述。三条判据：

1. **它回答的问题，是否属于"今天有没有事要处理"？** 不属于 ⇒ 首屏不留，进二级页。
2. **它有没有权威源？** 没有 ⇒ 要么先补一个，要么别显示。
3. **它是不是"读者以为自己需要"？** 这类的典型形态是"能看全貌"。

按这三条，本次实际动过刀的：

| 项 | 原本 | 处置 | 理由 |
|---|---|---|---|
| 运行时约定全文 | 塞在状态接口里，占全量 **56%** | 移出默认响应，点开才拉 | 首屏零用处；"随手可查"是设计者的需求，不是面板的需求 |
| 长尾区块（15+ 块） | 全铺在首屏 | 按去向表逐块搬进二级页 | 首屏从 3 个视口高压到 1.5 个 |
| 变更流水 | 19 条全量 | 只取"今日 + 最近 8 条" | 今日段实测 71 KB |
| 资产总览 / 今日动态 | 首屏块 | 移出首屏 | 首屏只答"有没有事要我处理"，明细去对应页 |
| 实体"注册表 + 依赖图" | 设想它存在 | **明确不做** | 实测那个"注册表"根本不存在；**不为了填一个格子去造一个假数据源** |
| 无当日数据 | 显示 0 | 显示「今日尚无自动动作」 | 见 §2.1 |
| 页面分区数量 | 从 6 涨到 8 | 记录在案，**不再继续涨** | 每多一个分区，就多一份"要维持它有人看"的债 |

**明确不做的事（非目标，同样重要）**：

- 不改任何**写动作**的语义（只读面板只重排"看"，不动"做"）；
- 不引入前端框架（保持零依赖，与既有技术形态一致）；
- 不做增量推送（另立方案）；
- **不改账本**（仍然只通过宿主接口读）；
- 不删任何数据源，只改**取用时机**。

> **这一节为什么价值最高**：只读面板最大的失败方式不是"显示得不准"，而是**越长越大、越做越像控制台**。把"什么该砍"写下来，比多做一个图更有用。

---

## 7. 设计教训汇总（可直接复用）

1. **三值逻辑**：真 / 假 / **未知**。读不到就是未知，不许落进任何一侧。
2. **「还没有」≠「读不到」**。两者必须显示成不一样的东西。
3. **凡路径类输入，取用点从当前生效的配置里取**，不要在模块加载期固化成常量。
4. **开关要把所有自然形态都认下来**，并交代"这个值来自哪一层配置"。
5. **缺数据态要写成夹具断言**，否则纪律会退化。
6. **先量尺、再改**：先做一次"逐字段量尺 + 逐段计时"，再决定砍谁——不量就改，通常是凭感觉改。
7. **片按耗时切**，不按字段切；把最慢的那一段单独成片、异步后填。
8. **进程内缓存优先**：当展示层与数据层在同一个进程里时，落盘快照是用外部状态换本来免费的收益。
9. **缓存只走 TTL + 单飞**；在"304 会被当成失败"的取数写法下，加缓存协商只增加故障面。
10. **未知参数不返回 4xx**，而是回填到响应的元信息里——别让前端的一个笔误把整页打崩。
11. **面板只渲染不复算**；"一个数怎么算"与"它换算成多少分钟"，都属判定逻辑。
12. **同源要可证**：缓存路径与实时路径的结果做哈希比对，这是唯一能立刻发现分叉的手段。
13. **只读要可证**：写口唯一性 + 退化重算前后文件清单不变。
14. **面板不许有副作用**：不调模型、不生成、不写状态。
15. **每个红黄项必须能点开看证据**，不接受"只有结论"。
16. **写动作先出 dry-run 预览**（影响项 + 备份位置 + 回滚方式），危险动作视觉隔离。
17. **先给"数据来源"分类**，再决定配置化 / 降级 / 剔除——**卡住通用化的从来不是读文件，而是凭什么下判断**。
18. **猜路径比说"没有"危险**：猜不到的路径就如实为 `null` 并降级。
19. **首屏预算写成硬断言**（字节数 + 请求窗口），否则它一定会涨回去。
20. **把"什么该砍"写进文档**，不然面板会长成控制台。

---

## 8. 复跑方式与限度

**复跑**（都是只读或本地夹具）：

- **首屏渲染断言**：用真实页面 DOM 断言"首屏只含指定区块 + 长尾容器不存在"；
- **载荷预算断言**：缓存命中 / 冷启两种夹具各跑一次；
- **同源断言**：缓存路径与实时路径的指标对象哈希相等；
- **只读断言**：实时重算前后的数据目录清单与关键文件哈希不变；
- **缺数据断言**：用"只有骨架、没有外部工具"的夹具跑一遍，逐项检查「不可探测 + 原因」。

**限度（没做到的）**：

1. 本文的实测数字全部来自**一次或两次观测**，与环境强相关；**量级与判断方式可复用，阈值不可照抄**。
2. "静默不报"那三条是从**配置化过程的副作用**里发现的，不是系统排查的结果——**同类缺陷还可能存在于没有被配置化触及的路径上**。
3. 本文的"诚实清单"只覆盖了首屏与长尾的取舍，**没有覆盖"使用者实际看了哪些块"**（没有埋点）；所以"哪些没人用"是**设计判断，不是统计结论**。
4. **同源断言只对"有两条取数路径"的指标成立**；其余指标只有单一来源，分叉风险靠"不许复制逻辑"的纪律兜底，而不是靠断言。

---

*本文由内部方案与交付记录派生，为独立副本；内部原稿含完整环境标识，仍是唯一权威源。*
