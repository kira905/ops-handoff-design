# 文档体系口径一致性审核

> **这篇文档讲什么**：一套长期演进的文档体系（几十份设计稿 + 一份总纲 + 一个真在跑的机器），**数字会漂、结论会被后来的事推翻、规矩会被另一些机制悄悄架空**。本文记录的是这类审核怎么做：先现场取证、再跨文档横切对照、把问题按四档分级，最后给出回写纪律。它要留下的是**查法与判据**，不是那一轮查出的具体条目。
> **版本**：v1.9（随本仓 `v1.9` 发布；本稿自 `v1.9` tag 起即公开同步）
> **素材基准日**：2026-09-17（当轮全部实况值均为现场只读取证）
> **占位符说明**：`<工作区根>` = agent 的工作目录；`<DSH 主目录>` = agent 的数据主目录；`A 机 / B 机 / C 机` = 长期运行的工作站；`steward-*` = 本仓 `02` 号文档既定的管家组件族占位名。文中组件一律用**角色泛称**（"管家内核""常驻调度器""巡检作业""叫醒通道""上下文看门""任务账本"）。
> **数字口径（重要）**：本文出现的条数、比例与时刻都是**一次审核的快照**，与环境强相关；可借的是**分类方式与判据**，不是数值本身。
> **与 `03` 号文档的分工**：`03` 讲**体系长什么样、家规是什么**；本文讲**怎么发现家规与现实脱节**——前者是规矩，本文是审计。

---

## 0. 先看结论

1. **造出来的东西是好的，烂的是"说明书"**：当轮查出 **24 处**口径不一致，其中**绝大多数不是设计错了**——而是"同一天里先写了一句、几小时后事变了、没人回写"。
2. **最扎眼的一类，是唯一总纲与实况相反**：总纲仍写着"四把总开关全关、所有自动动作零动作"，而机器上**总开关已有一把是开的、两个动作已真跑并留下生产痕迹**。任何新会话照总纲做判断，**第一步就踩空**。
3. **另有 2 处是真设计冲突**（不是笔误）：① 一条家规写"默认关、等拍板才开"，而实现是"**配置缺键 = 默认开**"——两者对同一件事的默认值假设相反；② 多个动作**共用同一份单日额度池**，一个动作吃满之后其余动作**静默降级且完全不可见**。
4. **还有一处既不是写错、也不是冲突，而是"没人定义"**：一个阶段标志位由谁改、依据哪条判据改、改了要不要留痕，**全体系零口径**——而三条写操作线的解锁条件恰好指着它。它是一道**永远关着的门**。
5. **这三类问题，单文档审核 100% 够不到**：它们分别长在"文档之间""文档与机器之间""跨机与家规之间"的缝里（§1）。
6. **审核产出必须自带分级 + 处置 + 复跑方式**：否则它只是又一份**会过期的文档**——本次 24 处里有相当一部分，正是上一轮同类回写留下的尾巴。

---

## 1. 为什么单文档审核够不到这些

此前每一轮审核都是**单文档、单方案**的（问"这份方案照字面能不能实现"）。本轮换的是**横切视角**（问"同一件事在不同文档、以及文档与机器上，是不是同一个说法"）。差别不在"审得更细"，而在**角度**——每一轮都在同一视角里加深，看不到别的缝。

| 视角 | 问的问题 | 能查出什么 | 天生够不到什么 |
|---|---|---|---|
| **单文档** | 这份方案照字面能不能实现？ | 方案内部矛盾、条款不可实现、边界缺失 | 同一事实在**别的文档**里的另一种说法 |
| **跨文档横切** | 同一件事，两份文档是不是同一个口径？ | 数字不一致、权威源不唯一、编号冲突 | 文档与**机器实况**的偏差 |
| **文档 × 实况** | 文档里的现状结论，现在还对吗？ | 快照过期、报告未回写、实现与文档分叉 | 一条规矩在**跨机**场景下还成不成立 |
| **跨机 × 家规** | 换一台机器，这条规矩还成立吗？ | "配置不跨机"这类结构性缺口 | 需要人拍板的**口径级**冲突 |

**判断方法（本轮最有用的一条）**：当一个问题**在任何一份文档内部都自洽**时，就不用再回头审那份文档了——它一定是两处口径之间的缝。逐条问"这条问题横跨哪两个载体"，缝的位置就是问题的位置。

> 本轮 24 处里，**只有 3 处**能在单份文档内部看出来；其余 21 处**必须跨载体对照**才能显现。

---

## 2. 取证底座：先取实况，再读文档

### 2.1 顺序纪律

**先跑只读命令取实况，再读文档。** 反过来（先读文档、带着文档的结论去核对实况）会**先入为主**：你会不自觉地只找"文档说得对"的证据，而本轮最值钱的几条恰恰是"总纲说得不对"。

第二条纪律：**全部只读**——只跑状态查询、只读状态文件、只打只读接口、只读日报与流水；**不改任何开关、不改任何文档、不动任何生产文件**。审核本身若产生副作用，它就不再是审核。

第三条：**每条原值都带取证时刻**（as-of）。没有时刻的现状值，第二天就可能变成错误前提。

### 2.2 取证清单（可复跑）

| # | 取什么 | 从哪取 | 当轮原值（摘要） |
|---|---|---|---|
| P1 | 总开关 / 模块 / 作业 三项状态 | 状态查询命令（只读） | 总开关 **3/4 关**，但**存在已放行项**；已登记模块 16 个（开 10 / 关 6）；作业 24 个（关 5） |
| P2 | 阶段标志位 | 自治配置文件 | 仍为**试运行期**（起 09-10、最少 3 天）——而试运行的退出条件当轮已全部满足 |
| P3 | 执行限额（单日 / 单轮） | 限额配置文件 | **单日 20 / 单轮 4**（当日 08:2x 才新建该文件；此前的默认值是 5 / 2，文档未跟改） |
| P4 | 门槛判定状态 | 门槛状态文件 | 门槛字段**仍是旧口径**（单机样本数 50）**且** `pass=true`——与放行报告用的"池级分级"**不是同一把尺子** |
| P5 | 放行报告 | 报告档（管家报告目录） | 池级样本 **188** / 独立状况 **34** / 稳态 **159** / 真误报 **0** |
| P6 | 任务账本现状 | 只读面板接口 | 总卡 **262**（已完成 236 / 待办 17）· 在途 **26** |
| P7 | 关闭项清单 | 日报当日段 | 已关闭模块 **6** 项；影子观测当日 1 条 |
| P8 | 设计符号是否存在 | 全仓符号检索（非测试目录） | **两个设计里写着的护栏符号：零命中** ⇒ 设计条款未落地 |
| P9 | 编号体系上限 | 总纲与各方案对照 | **同一编号体系两处上限不同**（一处写到 4、另一处已用到 5） |

> 这张表就是整份审核的**底座**：下面每一条不一致，都要能在表里指出它对照的是 P 几。

---

## 3. 三类问题的判据：过期 / 冲突 / 无人定义

这是本篇的核心。**绝大多数"不一致"并不需要拍板，也不该当成 bug**——把它们混成一锅，会让审核报告失去可用性。三类的判据完全可以用三个问句分开：

| 问句 | 类型 | 是否有一方是对的 | 处置手段 | 复发形态 | 谁拍板 |
|---|---|---|---|---|---|
| 同一件事两处说法不同，**哪一处是权威源？** | **① 口径过期（漂移）** | **有**——实况或指定权威源为准 | 回写 / 追加 as-of / 改指针 | **会反复复发**（数字永远在漂） | 不需要，执行即可 |
| 两处说法**各自都合理**，施行起来会互相破坏吗？ | **② 真设计冲突** | **没有**——两边都对，但不能同时成立 | **必须改口径**（不能靠回写解决） | 不复发，但会在**特定场景下静默生效** | **必须人拍板** |
| 有判据指着它，但**没有任何一处说谁负责产出它**？ | **③ 无人定义（空白）** | 无所谓对错——**它根本不存在** | 补一节：判据 + 执行动作 + 留痕 + 回退 | 一直悬着，直到有人踩上去 | 先补定义，再谈归谁 |

> **判据的用法**：逐条先问第一个问句。若"两处都是对的"，才进第二句。若"哪一处都找不到"，它不是不一致，而是**缺口**，别和上两类写在一起。

### 3.1 ① 口径过期的三种子型（判别要点）

| 子型 | 特征 | 判别问句 | 本轮实例（抽象） |
|---|---|---|---|
| **快照型** | 总纲式的"现状快照"被当成不变量引用 | "这个数字是**一次取证**的结果，还是**设计参数**？" | 总纲 §现状段写的开关状态、卡数、样本数，全部是某一时点的快照，却常被当作现状引用 |
| **部分更正型** | 权威源已宣布口径更替，**引用处与实现未跟** | "口径改过之后，还有谁在引用旧口径？" | 门槛口径已由"单机样本数"改为"池级分级"，而状态文件里的旧字段仍在、`pass=true` 照旧可被读 |
| **时序错位型** | 文档写于事件之前，**报告结论在几小时后被推翻** | "这份报告写完之后，同一件事有没有再变过？" | 一份放行报告写"净增量 0"；**8 分钟后**同一夜里的另一次拍板把它推翻了，报告未回写 |

**为什么时序错位型最难查**：它不是"谁写错了"，而是**两个正确的动作发生在错误的时间顺序里**。查法只有一个：**把报告撰写时刻与拍板/变更时刻并排**（见 §4.2 的表 2）。

### 3.2 ② 真设计冲突的判定要点

两个特征同时出现，才算真冲突：

1. **两边各自自洽**——单看任何一边，都找不到毛病；
2. **不能同时成立**——施行起来会互相破坏，且**后果是静默的**（不报错、不告警，只是"某件事没发生"）。

本轮两处（抽象后）：

**冲突一 · "默认关"的家规 vs "缺键默认开"的机制。**

- 家规原文：**代码进生产，但开关默认关；要开必须显式拍板**。
- 实现实况：**配置缺键 ⇒ 默认开**。
- 致命的一层：**代码随备份仓过河，配置不随仓过河**（配置被标为"不同步"）⇒ 新动作在**另一台机器上一上来就是开的**，没有经过任何拍板、也没有影子对照。
- 判据：把"默认值写在哪一侧"问清楚——**默认值必须在配置侧表达，不能靠代码侧缺省**。否则"默认关"这条家规在跨机场景下**不成立**。
- 后果分级：这次是"接机账本并回"（写操作）；**换成一个会动数据的动作，后果更重**。

**冲突二 · 多层"全局共享上限" vs 不断增加的动作数。**

- 实现实况：动作层有"单日动作总额度"、推送层有"单日推送上限"、服务面有"每日条数预算"——**三层各自都是全局共享**。
- 而消费这些上限的动作**还在不断增加**（当轮一天就新增两个作业）。
- 后果：某个动作把额度吃满后，**其余动作全部降级为"只排队、不投递"**，而该状态**只写台账一行，日报/面板/通知都不显示** ⇒ 使用者侧表现为"**该响的没响**"，且无任何征兆。
- 它为什么落在规矩的缝里：家规明文要求"**关闭必须可观测**"与"失败必须可见"，而"**因额度耗尽而降级**"既不算关闭、也不算失败，**两条规矩都管不到它**。
- 处置方向：① 额度按动作类别分池或按优先级排序（至少让"人能感知的通道"优先）；② 降级必须进日报/面板（"被额度挡住 N 条"），并入"关闭必须可观测"清单；③ 明确服务面推送是否占用生态动作的额度池——**两套判据共用一把闸门是设计层面的错配**。

> **共同点**：两处冲突都不是"某人写错了"，而是**两个各自正确的决定在同一个资源上相遇**。所以修法从来不是"改文档"，而是**改口径 + 人拍板**。

### 3.3 ③ 无人定义类的处置

**形态**：某个字段/开关被别处的判据指着（"必须等 X 结束才放行"），但**没有任何一处写清 X 由谁结束、依据什么判据、要不要留痕**。

本轮实例（抽象）：试运行期的退出条件**已经全部满足**（样本与独立状况达标、零真误报），但阶段标志位**没有任何人或机制去改**；全仓找不到转移逻辑——**只有"可写字段白名单"里有它**。后果是双向的：

- 朝前看：写操作线的解锁判据**指向一个永远不会自己变的字段** ⇒ 那扇门永远关着；
- 朝后看：若有人手工把它改掉，**这个动作本身反而没有任何判据与留痕要求**（谁改、依据哪份报告、要不要做对照，全无）。

**处置模板（补定义的最小四件）**：

1. **判据**：用哪一档标准（只要求低档达标，还是高低两档都要）；
2. **执行动作**：一条命令，或一个受控入口；
3. **留痕**：写变更历史 + 变更流水 + 面板显示；
4. **回退**：反方向一条命令能转回去。

> 补完这四件，"那道永远关着的门"才变成一件**可执行、可追责、可回退**的事。

---

## 4. 跨表对照：四张表怎么建

跨文档横切怎么落地？靠**四张固定结构的表**。每张表回答一个不同的问题，填表素材**只来自 §2 的取证底座**（不靠回忆、不靠文档自述）。

### 4.1 表 1 · 载体归属（同一事实只许一处权威源）

| 事实 | 载体 A | 载体 B | 权威源（建议） | 冲突裁决规则 |
|---|---|---|---|---|
| 当前开关 / 模块 / 作业状态 | 总纲现状段 | 开关矩阵表 | **状态查询命令** | 文档一律让位实况 |
| 执行限额（单日 / 单轮） | 动作档参数表 | 观测档记录表 | **限额配置文件** | 文档标 as-of |
| 放行门槛 | 口径档 | 门槛状态文件 | **口径档**（状态文件字段降级为内部观测） | 实现侧待跟改 |
| 定时作业表 | 总纲 / 复盘 / 方案 / 审核册 | 调度器源码 | **调度器 `--status`** | 多口径 → 统一 |
| 编号对照表 | 总纲 | 矩阵 | **二选一 + 另一处留指针** | 自述"同源"≠有仲裁规则 |
| 服务面判据 | 总纲 | 服务面方案 | **总纲（表）+ 方案（细则）** | 审核已判"权威源空白" |

**填表时最常发现的两件事**：① 同一事实被 **3 份以上**文档各写一遍，且**没有任何一处被指定为权威源**；② 两处都自称"与 X 同源"，却**没有仲裁规则**——这时"同源"只是愿望。

### 4.2 表 2 · 时序预算（同一夜里的错位）

| 事件 | 文档记录时刻 | 实际发生 | 后果 |
|---|---|---|---|
| 某动作开闸 | 报告写于 **00:5x**："已回退" | **01:28** 拍板开、01:40 起真投递 | 报告结论 **8 分钟后就翻了** |
| 48 小时对照的起点 | 观测档起点记 **09:38** | **17:17** 才实际放行 | 对照窗口端点差 **8 小时** |
| 限额变更 | 动作档写 **5 / 2** | **08:2x** 改为 **20 / 4** | 验收与额度列全部读错 |

**这张表的价值**：它把"写错"和"**写完之后事变了**"分开。填法就一句话——**把每一份"结论型文档"的撰写时刻，与该主题下后来的拍板/变更时刻并排**；两者相隔越近，结论翻车的概率越高。

### 4.3 表 3 · 输出质量（文档作为判断依据的可靠性）

| 输出物 | 依据可追溯 | 错了的代价 | **逃生舱** |
|---|---|---|---|
| 总纲现状段 | 有取证命令，但**无过期提醒** | 新会话判断踩空 | 每次开工重跑取证命令 |
| 放行报告 | 有统计底座 | **放行决策依据错** | 复核当前开关状态 |
| 服务面现状段 | 有方案与报告 | 把"已打通"当成"还卡在基础设施" | 读最近两份新档 |

**"逃生舱"这一列最值钱**：它回答"**如果这份文档是错的，读者怎么自救**"。没有逃生舱的结论型文档，等于要求读者无条件相信一个可能过期的快照。

### 4.4 表 4 · 影响面 / 幂等

| 动作 | 作用对象 | 半完成态 | 重复执行后果 | 幂等键类型 |
|---|---|---|---|---|
| 模块开 / 关 | 全机动作族 | 无（热生效 ≤1 轮） | 幂等 | 运行键 |
| 作业开 / 关 | 单个作业 | 无 | 幂等 | 运行键 |
| 新作业随备份仓"过河" | **对端全机** | **对端自动开跑**（无放行记录） | 对端重复跑同样动作 | **去重键（配置缺省）≠ 幂等键** |
| 额度耗尽降级 | 全部动作通道 | **静默排队** | 后续动作不再执行 | 运行键（日计数） |

> **本表最实用的一条产出**：**"去重键 ≠ 幂等键"**。一个动作"不会重复执行"不等于"重复执行无害"；把这两件事混为一谈的机制，会在跨机场景里**把一次未放行的默认值变成一次真实动作**。

---

## 5. 处置与回写纪律

### 5.1 四类处置动作

| 处置 | 用在什么情况 | 形态 | 为什么这样定 |
|---|---|---|---|
| **订正注** | 原文不能动（已归档 / 已外发 / 有历史价值） | 在文末或段后加一行：订正注（日期 · 只标注不改原文；真源 = X）：<原口径> ⇒ <新口径> | **保留历史**，让后来者看见"曾经写的是什么" |
| **追加 as-of 块** | 现状类章节整段过期 | 追加"截至某日增量"块，**不抹掉历史** | 抹掉历史 = 下次没人知道这里翻过车 |
| **指针替代复制** | 同一事实两处维护 | 一处留正文，另一处改为指向权威源的指针 | 复制 = 制造下一个漂移点 |
| **指定权威源** | 同一事实多处各写一遍 | 在载体归属表里登记"权威源 = 命令 / 文件 / 文档" | 没有指定，就没有"谁对"的判据 |

### 5.2 回写的三条边界

1. **回写 ≠ 改代码。** 绝大多数不一致是**文档问题**，回写不动实现；凡是需要改实现的，另立施工动作，别混在审核里。
2. **拍板级问题不做回写。** 真设计冲突（§3.2）与口径选择（比如"服务面是否共用额度池"）**必须人拍板**——审核报告只负责把选项与代价列清楚。
3. **审核档自身不构成合格证、不放行任何开关。** 一份审核报告既是"现状结论"，也会**很快过期**；它必须在头部写明这句话，否则会被当成"已经安全了"的证明。

### 5.3 回写清单模板

| 载体 | 处 | 原口径 | 新口径 | 处置类型 | 理由 |
|---|---|---|---|---|---|
| 总纲 | 现状段 | 总开关 4/4 关 · 零动作 | 标注为 as-of 快照 + 现值指针 | 追加 as-of | 与实况相反，误导新会话 |
| 放行报告 | 结论段 | 净增量 0 | 补一节"后续订正（已开闸）" | 订正注 | 报告是放行依据，不能被当成现值 |
| 动作档 | 参数表 | 单日 5 / 单轮 2 | "当时口径；现值以配置文件为准" | 指针 | 数值已由 5/2 改 20/4 |
| 矩阵 | 动作表某行 | 未放行 | 已放行（模块开 + 作业关 + 单一注入者） | 就地更正 | 开关状态已变 |
| 方案档 | 开关归属段 | 受某族总开关主控 | 独立开关（拍板结论） | 就地更正 | 代码已按新口径实现、文档未回改 |

> **一处必写的元信息**：回写清单里要标**改前版本可回退**（哪份备份 / 哪个提交），否则"回写"本身变成一次不可逆动作。

---

## 6. 复发预防：这类问题为什么会反复长出来

1. **时点数字：从"写数字"改成"写命令 + as-of"。** 只要文档里写的是"现在有 19 个作业""总卡 203 张"，它就**出生即过期**。改成"以某命令输出为准（本次 N）"，过期性从内容变成了引用方式。（当轮 24 处里有 6 处属于这一类。）
2. **新增即回写。** 新增一个作业、一条家规、一个开关时，**同步三处：文档 + 状态文件 + 实现**。缺任何一处，都是下次误读的种子——本轮就有"文档订正了、实现没跟"的实例。
3. **无人定义项要有承接人。** 一个字段被别处判据指着却没人负责改，它不是"待办"，而是**结构性缺口**：要么补定义（§3.3 四件），要么把指着它的判据改掉。
4. **权威源唯一化。** 每条事实只留一处正文，其余改指针；宁可多写一行"详见 X"，也不要两处各写一遍。
5. **复跑节奏。** 改动密集期按周复跑（重跑 §2 的取证清单即可，成本很低）；稳态按月。**复跑的成本远低于"照过期快照做决策"的成本。**

---

## 7. 一轮审核的产出清单（可直接复用）

### 7.1 报告结构模板

1. **头部**：这份文档是什么 / 审核范围 / 审核视角（与前几轮的差别）/ as-of 与取证方式 / **"本档不构成合格证"**；
2. **§0 一句话结论**：把最扎眼的一条放第一条，**不要按文档顺序写**；
3. **§1 取证底座**：表格（取什么 / 从哪取 / 原值），带时刻；
4. **§2 阻断**：会让后续判断建立在错误前提上的；
5. **§3 重要**：会让实现/验收/引用出错，但不直接造成事故的；
6. **§4 建议**：结构性优化，不修不会立刻出事；
7. **§5 决策级**：要人重拍口径的（每条写**要拍什么 / 代价 / 收益**）；
8. **§6 横切四表**（§4 的四张表）；
9. **§7 未查证**：哪些没核、为什么、不下结论；
10. **§8 后续动作**：建议的处置清单（**审核意见不升格成承诺**——未经拍板不建执行项）。

### 7.2 分级口径

| 档 | 判定特征 | 当轮数量 |
|---|---|---|
| **阻断** | 会让后续判断建立在**错误前提**上（总纲与实况相反、家规被架空、静默降级） | 3 |
| **重要** | 会让实现 / 验收 / 引用出错，但不直接造成事故 | 15 |
| **建议** | 结构性优化（权威源重复、"已上生产"缺机器判据） | 6 |
| **决策级** | 不是 bug，**要人重拍口径**（每条附代价与收益） | 4 |

### 7.3 末段必写"未查证"

审核报告的**可信度**取决于它敢不敢写"我没核到"。当轮如实写了 5 条：三组文档未逐条读、对端机器**全部未取证**（凡涉及它的说法均引自文档）、某字段是否被面板实际消费未查证、"自评 100% 落地"类说法未逐条复核。**不写这一段的审核报告，读起来更漂亮，实际上更危险。**

---

## 8. 复跑方式与限度

**复跑（全只读）**：

- 重跑 §2 的取证清单（状态查询命令 / 读状态文件 / 只读接口 / 读日报与当日流水）；
- 把新原值填回 §4 的四张表，看哪几行的"权威源"变了；
- 对每条历史不一致，核对回写清单里的处置是否**真的落地**（而不是"记在报告里"）。

**限度（本文没做到的）**：

1. 当轮**未逐条读**若干组文档（只覆盖了子代理摘录的几组），所以"共 24 处"是**下界**，不是全量；
2. **对端机器全部未取证**——凡涉及另一台机器的说法均引自文档，未实证；
3. 只核了**文档规则**，没有实测某些判据在运行中的实际一致性；
4. "各文档自评审核意见已 100% 落地"这类说法**未逐条复核**——本方法不承担该工作；
5. 本文引用的条数、比例与时刻是**一次审核的快照**，会随系统演进而失效。

---

## 9. 方法小结（可直接复用）

1. **审出"缝"比审出"错"更有价值**：单份文档内部自洽的问题，一定长在两个载体之间——先定位它是**哪两处之间的缝**，再动手。
2. **先取实况，再读文档**：顺序反了，你会只找"文档说得对"的证据。
3. **三个问句分开三类问题**：谁是权威源（过期）/ 两边都对但互相破坏吗（冲突）/ 有没有人负责产出它（空白）。**混成一锅的报告没人能用。**
4. **真冲突不靠回写解决**：两边各自正确的决定在同一个资源上相遇时，只能改口径 + 人拍板。
5. **"无人定义"要按四件补齐**：判据 + 执行动作 + 留痕 + 回退——否则它是一道永远关着的门。
6. **快照型数字改成"命令 + as-of"**：这是根治"数字永远在漂"的唯一写法。
7. **同一事实只许一处权威源**，其余一律指针；"两处同源"没有仲裁规则时等于没有权威源。
8. **审核报告自己也会过期**：所以它必须自带分级、处置、复跑方式与"未查证"段，并且在头部写明**它不构成任何合格证**。

---

*本文由内部审核档派生，为独立副本；内部原稿含完整环境标识，仍是唯一权威源。*
