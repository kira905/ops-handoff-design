# 多机交接与云中继设计

> **一份可复用的工程方案**：在"同一时刻只有一台机器在写"的前提下，把两台（或多台）工作机之间的状态传递，从"备份仓 + 手工搬运"升级为**可自动执行、可失败分档、可人工裁决的交接协议**；并给出一台云服务器同时承担"固定入口 / 活跃机登记 / 交接中继"三种角色的设计。
> **版本**：v3.1（定稿）· **性质**：设计稿 + 审核记录，四轮审核意见逐条登记
> **写作视角**：单机 agent 运行环境（本地宿主进程 + 插件 + 计划任务 + 加密备份仓）的运维方
> **占位符说明**：文中 `<工作区根>`、`<DSH 主目录>`、`<仓外工作副本根>`、`A 机 / B 机`、`steward-*`（管家组件族）均为占位名；模型名仅示意，替换为读者自有渠道即可。

---

## 1. 为什么要重做

### 1.1 三次真实事故（都是实测，不是担心）

| # | 事故 | 直接原因 | 恢复代价 |
|---|---|---|---|
| 1 | B 机执行了一次同步，把仓里属于 A 机的 **68 个会话 purge 掉**，并把这次删除推送到远端（A 机此前推的 71 个会话全没了） | 采集脚本使用镜像语义（`robocopy /MIR` = 镜像 + 删除），且**没有 pull / 没有合并** | 从 git 历史逐个 checkout 解 smudge 补回 68/68；采集脚本全面改造 |
| 2 | 仓3 的工作副本被另一侧的同步覆盖，**对方独有的 8 份文档消失** | 采集是覆盖式（`/E` 不 purge，但也不合并不去重） | 从历史提交导出基线，逐条并回 |
| 3 | 看板账本双机分叉（本机 34 张 vs 仓里 40 张，**谁都不是超集**）；审计流水**从未进过仓** | 账本被 git-crypt 加密成二进制 blob，git 无法内容合并；采集白名单漏登记该目录 | 走 Host API 人肉并回 10 张卡 + 修白名单 |

三次事故的共同点：**都不是"传输失败"，而是"传输语义用错"**。

### 1.2 现状实测（数字）

| 项 | A 机 | 仓1 工作副本 | 结论 |
|---|---|---|---|
| 账本 | revision=351 / 47 张 | revision=349 / 47 张 | 卡 id 零差异，**只差 2 张卡状态** |
| 审计流水 | 单文件 52KB | 只有 A 机那份（43.7KB） | 全仓搜另一台主机的名字**零命中** → 对方那份从未被采集 |
| 记忆库 | 250 条（maxId 468） | 246 条（maxId 464） | 单文件加密 blob，**只能整份覆盖** |
| 坑库 / 候选池 | 231776B / 66259B | 231384B / 66267B | 同上；两台都在产，会互相盖 |
| 三仓 | 仓1 ahead=2 未推 / 仓2 干净 / 仓3 干净 | — | 最新改动仍停在本地 |

补充事实：仓1 同步面 = 264 个 tracked 文件；本机 `<DSH 主目录>` 共 3118 个文件（其余为插件实体 / 依赖闭包 / 附件 / 日志，**本就不该进仓**）。近 40 次提交两台机器各有提交 → **通道是通的，缺的是"按节奏交接"和"落地"**。

### 1.3 根因（一句话）

> 我们拿"备份仓"当"同步盘"使，又默认"两台都在写"（两个写者），而流程里**只有覆盖、没有合并**，还缺"拉下来之后把它落到生产路径"的那一层。

拆开是三条：

1. **定位错位**：加密备份仓是"快照 + 换机搬运"，不提供自动同步与冲突合并；而商业同步盘是"后写覆盖先写"且**静默**（实测把一台机器的 SQLite 库同步损坏，FTS 报 malformed，外加 junction 跟随 / EBUSY 锁）。
2. **数据形态**：加密 → 二进制 blob（git 合不了）；append-only + 哈希链（审计禁合并）；时间窗口 / 锁类状态（本就不该同步）。
3. **工程失误**：镜像语义、白名单漏登记、缺落地层、白名单靠文档步骤而非机器可读清单、脚本版本不同步（一台用旧脚本拉新数据）。

---

## 2. 设计前提与总原则

### 2.1 关键前提

> **不会同时开多台机器上的 agent 宿主。**

这是硬前提，也是本方案能大幅简化的唯一原因。

### 2.2 总原则

> **任何时刻只有一个写者 → 把"同步"改成"交接"。**

单写者前提下，§1.3 的第 2、3 条**大部分自动消失**：不存在并发写入，就不需要双向合并；不需要合并，加密 blob 合不了就不是问题。需要做的只有两件事：

- **离开时**：把本机状态完整交出去（Depart）
- **到达时**：把对方状态完整接过来，并**落到生产路径**（Arrive）

### 2.3 Non-goals（明确不做）

- 不做实时双向文件同步（那是同步盘的活，不是本方案的活）
- **不使用镜像语义**（`/MIR`、`--delete`、`rsync --delete`）——**任何形式都不允许**
- 不做"多台同时对等写同一批单文件"（单写者前提是硬约束；若将来违反，交接器必须 fail-closed）
- 不把本地宿主改成监听 `0.0.0.0`（云中继只做入口代理，见 §6.5）

---

## 3. 数据分型（决定各自怎么过河）

### 3.1 三类

| 类别 | 内容 | 过河方式 |
|---|---|---|
| **A 类 · 必须跨机（不可重建）** | 会话、记忆库、长期约定与技能与文档、审计流水、任务账本、坑库 + 候选池 | 加密备份仓 + **解密后语义合并**（§4.3）兜底 |
| **B 类 · 单机状态（不该同步）** | 冷却 / 去重 / 锁 / 心跳 / 降档态 / 观测期计时 / 裸跑台账 | **按主机名前缀各存一份**，永不合并 |
| **C 类 · 本机层（同步不了）** | 插件实体、计划任务、桌面入口、全局 npm 补丁、本地推理栈与代理配置、加密 **key** | **一条命令的 bootstrap**（§4.2 第 5 步）；**key 例外，见下** |

**钥匙例外**：加密 key 是 C 类中**唯一不可能"一条命令 bootstrap"**的项——它是解密仓的前提，本身不可能放在仓里（否则加密无意义），属**自举悖论**。故：

1. **传递方式 = 手工**：冷备在离线介质，换机/重装由人手动解压到约定位置后再 `git-crypt unlock`。换机 runbook 必须明写"key 不在任何仓里，新机第一步是手工取钥匙"。
2. **无钥匙 = 硬闸**：到达流程在第一步之后加"**可解密自检**"——若抽样文件读出来是密文（或 `git-crypt status` 报未解锁）→ **立即停止 Arrive，禁止 pull 后的合并与分发**，并 🔴 告警提示"本机缺 key"。**不允许降级继续**：没钥匙时工作副本里就是密文，语义合并会把密文当 JSON/SQLite 解析（轻则报错、重则产出垃圾并写回）。

**重要认识**：今天"到了另一台发现不对"的例子（面板插件启动崩、长期约定文件回退成旧版、计划任务只有 1/8），**全部是 C 类落地问题**，不是传输问题。

### 3.2 A 类逐项现状

| 项 | 传输通道 | 现状 | 待办 |
|---|---|---|---|
| 会话 | 仓2（采集 `/E` + pull 只补缺） | ✅ 通。**口径说明**：按**会话数**实测 233 条；按**文件数**实测 253 个 `.jsonl.zstd` / 281.7 MB（含子代理的裸 uuid 目录）——两个数字都对，口径不同 | 保归档收益只能 `-Push` / `-Sync -BackupOnly`（`-Sync` 会回流归档会话） |
| 长期约定 / 技能 / 文档 | 仓1 + 仓3 | ✅ 通 | 落地层靠人（曾漏掉一整个工具目录的 83 个文件） |
| 任务账本 | 仓1（加密单文件） | ⚠️ 覆盖式，靠人肉并回 | 走语义合并（§4.3） |
| 记忆库 | 仓1（加密单文件） | ⚠️ 覆盖式 | 走语义合并（§4.3，已有 200+27→227 实证） |
| 审计流水 | 仓1（按主机名前缀采集） | ❌ **另一台那份从未进仓**（对方脚本还是旧版，没有该采集分支） | 交接器统一版本门 + 首跑补采 |
| 坑库 / 候选池 | 仓1（加密单文件） | ⚠️ 覆盖式 | 走语义合并（按签名去重） |
| 裸跑台账 | 仓1（`<host>-*`） | ✅ 通（A 机已采、B 机待采） | 保持"按机存、不合并" |

### 3.3 语义合并可行性（已实证两次）

- **账本**：走 Host HTTP API 并回对方独有 10 张卡（7 新建 + 3 同标题跳过），本机 34→41 张，**零冲突、可幂等**。
- **记忆库**：SQL 只补缺，200→227 条（补对方独有 27 条），FTS 触发器自动同步，**零丢失、零冲突**。

结论：**加密 blob 不能合并 ≠ 数据不能合并**。解密后它们都是结构化数据（JSON / SQLite），可以按语义合并再重新加密。

---

## 4. 架构四层

```
① 传输层   加密备份仓（保持，不换介质）
              ↓
② 落地层   ← 今天的坑 80% 在这：拉了 ≠ 生效
              （生产路径分发 / 插件实体 / 计划任务 / 桌面入口）
              ↓
③ 语义合并层（兜底）
              （解密 → 按语义合并 → 重新加密写回）
              ↓
④ 观测层   统一观测（日报 / 告警 / 审计 / 坑库）
```

### 4.1 传输层（保持）

三仓职责不变：仓1 = 配置与长期资产稳态、仓2 = 会话、仓3 = 工具链。
**唯一改动**：所有采集脚本禁止镜像语义；白名单由 `state-files` 单一数据源生成并双向校验。

### 4.2 落地层（本方案重点，5 个动作）

1. **拉取**：`git pull --ff-only`（只接受快进；分叉 → 停手告警，不动任何生产文件）
2. **合并**：跑 A 类单文件的语义合并（§4.3）
3. **分发**：按**清单文件**（JSON，机器可读）把仓里的文件投放到生产路径；**分发前先备份**目标文件（`_archive/handoff-apply-<ts>/`）。校验用**规范化哈希**：仓库 `core.autocrlf=true`，检出即 CRLF，文本文件直接 sha256 必然误报 → 文本存"规范化 LF 后哈希"、二进制与加密文件才用字节 sha256；失败分「内容不同（真失败）」与「仅换行差异（warn）」两档
4. **部署 C 类**：插件实体（两处副本）+ 计划任务重建/校验 + 桌面入口 + 全局补丁
5. **自检**：交接后自检，结果写入交接单

> 落地层必须**幂等**（重复跑结果一致），并且**失败可定位到具体项**（不接受"回滚整包"之外的模糊状态）。

### 4.3 语义合并层

| 对象 | 合并规则 | 幂等键 |
|---|---|---|
| 任务账本 | 卡 **id** 并集；同 id 取 `updatedAt` 较新者，**但先过时钟门**：合并前计算两侧 `updatedAt` 相对本机时钟的偏差，任一侧超阈值（默认 120s）→ **拒绝该次账本合并** + 🔴 告警要求对时，**不做"谁新谁赢"**；差值 < 5min 的胜负判定视为**时钟不可信区**，不自动决胜、记入 `alerts[]` 转人工；平局取 **host 字典序较小者**（确定性，保证幂等）；**同标题不同 id 一律保留**；**删除用 tombstone**（否则对方删掉的卡会被并集复活），**tombstone GC**：仅当**两台**的 `lastSuccessfulHandoffAt` 均晚于该 tombstone 创建时间才可清理（确保双方都见过这条删除）；`revision` / `recentRequests` **不进合并**（以本机为准，合并后 revision = max+1 并追加一条 merge 记录）；**快照来源标记**：热采推入的账本标 `authority: snapshot`，合并时**只作"补新增卡"的弱来源**，不参与 `updatedAt` 胜负 | 卡 id |
| 记忆库 | 以**内容哈希为主键**去重后只补缺；**id 不重分配**：改用**主机分段发号**——A 机段 `1..999999`、B 机段 `1000000+`，两侧新写入天然不撞号（**理由**：id 被外部文档以固定编号引用，重分配会让引用静默指错）；历史遗留 id 冲突 → **拒绝自动合并该项 + 告警**；`pinned` / `tags` 取并集（pinned 任一为真即真）；FTS 由触发器同步 | 内容哈希 |
| 坑库 / 候选池 | 按坑签名去重；**双口径合并** —— `totalCount = Σ count_i`（事实次数，直接相加）+ `hosts[]`（跨机广度）+ `rate_i = count_i / activity_i`（各机踩坑密度，`activity` = 同期回合数）；排序分 `score = totalCount × 0.5^(距末次天数/30) × (1 + 0.2×(hosts-1))`；另备**工作量归一化档** `weighted_i = count_i × (activity_ref / activity_i)`（`activity_ref` 取两机较小者、权重封顶 [0.5, 2]、`activity_i < 50` 回合不参与） | 签名 / 候选键 |
| 会话 | 只补缺、绝不覆盖；**归档会话按归档清单落到归档目录，不补回活跃目录**（§5.6） | sessionId |
| **文档类** | 复用既有"条目级合并器"（变更流水按 `- HH:MM` 行、维护清单按小节标题）——**不新造规则** | 条目键 |
| **长期约定 / 技能 / 配置** | 语义单文件，**不做自动合并**：报差异 → 白名单自动分发 → 其余转人工裁决 | 文件路径 + 条目标题 |

**安全**：合并器只处理**已登记**的 A 类对象；每次合并前先备份（`_archive/handoff-merge-<ts>/`）、合并后校验（条目数不减、结构合法、SQLite `integrity ok`）、失败自动还原 + 告警。**合并前置两道门**：① **解密自检通过**（缺 key 一律拒绝合并）；② **时钟门通过**。

**执行时机**：账本与记忆库的合并**只在宿主停止时执行**（Arrive-A）；宿主运行期间的账本写入一律走 Host HTTP API（Arrive-B）。

### 4.4 观测层

交接是**管家体系的动作**，必须被体系自己看见：日报有段、失败有告警、动作有审计、踩坑进坑库。

### 4.5 安装源：自建与魔改产物怎么过河

**结论：不新建代码仓。** 安装源已经在仓3 里——缺的不是"仓"，而是"清单 + 执行器 + 版本对齐"三件。

盘点：

| 类别 | 数量 | 现在在哪 | 过河状态 |
|---|---|---|---|
| 自建插件 | 5 个（各带一键部署脚本） | `<工作区根>/<插件目录>/` | ✅ 已在仓3 |
| 魔改补丁脚本 | 25 个幂等脚本 | 工具目录 | ✅ 已在仓3 |
| 第三方插件 | 17 项 dependencies / 21 个宿主包实体 | profile 的 `node_modules/` | ❌ **不在任何仓**（依赖闭包约 400+ 包，**禁止入仓**） |
| client 挂载 | 挂载配置 6 个 insert | profile 目录 | ❌ 需重建 |

四件事（落地层交付）：

1. **机器可读清单** `ecosystem.manifest.json`：插件名 + **显式版本** + 安装方式（npm / link / 本地 deploy）+ 需跑的补丁脚本 + 验证特征串。数据源取现有登记（维护清单 + 面板清单 + profile `package.json`），**不造第二份真相**。
2. **执行器**：装显式版本 → 跑补丁（幂等 + 依赖顺序）→ 部署自建插件 → 重建计划任务与桌面入口 → 校验特征串。
   - 构建差异：有构建步骤的包标 `needsBuild`；纯 JS 的直接 deploy。
3. **版本漂移治理**：实测两处 profile 声明版本与实体版本不一致（如声明 1.2.2 / 实体 1.4.0）→ 一律**显式版本安装**（`@latest` 会静默不生效），清单以**实体版本**为准。
4. **删除项支持**：已从加载清单摘出但实体仍在 `node_modules` 的包 → 清单需能表达"该删"。

**开源支线是两半**：① **代码侧**（抽某个自建插件做通用化试水）；② **文档侧**（即本文档所属的一份脱敏方法论文档）。原方案口径为「开源"方法论文档"成本低于开源代码」，v3.0 定稿时该句一度丢失，后续单独补回。两半同属一条支线，互链、不重复执行。

**代码侧交付物的兼容性要求**：① **兼容宿主版本区间**：若插件依赖 `settings.section` 类 API，而宿主的某个新版移除了旧符号（实测：某新版移除了两个旧设置 API 符号，改为新的注册接口）→ 必须写明支持哪几个宿主版本；② `peerDependencies` 声明；③ **双分支适配**（新旧两套 API 同时支持，运行时探测）；④ 安装文档必须写明 client 插件要走挂载配置的 **`insert`**（不进加载清单，否则启动即崩）。

---

## 5. 交接协议

> **术语映射**：本方案用"交接"语汇，与既有脚本的对应关系如下，实现时不必来回翻译——
>
> | 本方案 | 既有脚本 | 差异 |
> |---|---|---|
> | `Arrive` | 同步脚本 `-Pull` + 手工分发（runbook） | 交接器合成一步，加版本门 / 心跳检查 / 语义合并 |
> | `Depart` | 同步脚本 `-Push`（内部依次调三个采集脚本） | 交接器改为"先体检后采集" + 每仓参数矩阵 + 超时 |
> | 语义合并 | `-Refresh` 回灌 / 人工并回（走 Host API） | 把"人肉并回"固化成组件 |
> | 交接单 | （无对应物） | 新增 |
> | 热采 / 冷采 | `-Sync`（全量）/ `-BackupOnly`（只采集） | 新增分档定义 |
> | 落地分发 | 手工 robocopy / 部署脚本 | 清单驱动 + 规范化哈希校验 |

### 5.1 Arrive（到达 / 开机）—— 拆两段

**实测依据**：入口启动脚本的顺序是 迁移 → 修工作区归属 → **拉起守护进程（宿主随之启动）** → 日报钩子。照原稿"挂在启动链尾部"会让 Arrive 在宿主运行中执行合并，撞上 Host 的 `ledger.lock` 与 SQLite 活动连接。故拆成**宿主未运行段**与**已启动段**。

**Arrive-A —— 必须在守护进程拉起之前执行（宿主未运行，可安全改文件）**

| 步 | 动作 | 失败处理 |
|---|---|---|
| A1 | **版本门 + 解密自检**：① 读本机与仓内 `handoff-version.json`，`minSupported > self` → 拒绝执行；② **抽样解密自检**：三仓各抽 1 个加密文件读头，非明文（密文 / `git-crypt status` 未解锁）→ 拒绝执行 | **fail-closed**：停止 + 🔴（防旧脚本读新数据＝事故 3 成因；防无 key 把密文当数据） |
| A2 | `git fetch` 三仓（**超时 60s**）+ **心跳检查（双字段）**：读仓内 `<host>.json` 的 `lastHeartbeatAt`（任一成功 push 都更新，含热采）与 `lastSuccessfulHandoffAt`（仅完整 Depart/Arrive 更新）；**对端心跳在本机 Arrive 进行中被更新** → 双开嫌疑；**无记录（新机 / 冷启动）→ 放行** | fetch 超时 → **fail-open**（不挡启动，红条 + 后台重试）；双开嫌疑 → **fail-closed**（宿主照常起，但禁止自动分发 / 自动 Depart + 🔴）；`lastSuccessfulHandoffAt` 超阈值 → 🟡 |
| A3 | 分叉检测（`rev-list --left-right --count`）：本机有未推送提交 → 先跑一次 Depart 或转人工裁决 | **fail-closed**：停止本次 Arrive + 告警（**不挡宿主启动**） |
| A4 | `git pull --ff-only` 三仓（**超时 120s**） | 超时 / 失败 → **fail-open**：跳过本次 Arrive + 红条 + 后台重试；**绝不在弱网下阻塞开机** |
| A5 | **离线语义合并**（§4.3：账本 / 记忆库 / 坑库 / 归档清单），逐项先备份后合并、合并后校验 | 单项失败跳过 + 告警，不阻断其余；**解密自检未过则整段跳过** |
| A6 | 落地分发（清单 + **规范化 sha256** 校验）+ C 类 bootstrap（插件实体 / 计划任务 / 桌面入口） | 逐项列名 |
| A7 | 写交接单（`result: arrived`）+ 写会话交接标记（§5.6） | — |

**Arrive 超时预算与失败分档**

- **总预算 300s**（fetch 60s + pull 120s + 合并/分发 120s）。Arrive-A 挂在守护进程拉起**之前**，故"**不许把开机堵死**"是硬约束——没定预算也没写失败是否挡启动时，弱网 `git fetch` 挂起即为开不了机。
- **fail-open（不挡启动、不落数据）**：**传输类**失败——fetch / pull 超时、远端不可达、无网。处置 = 跳过本次 Arrive + 面板红条 + 后台指数退避重试。
- **fail-closed（宿主照常启动，但禁止写操作）**：**完整性类**失败——版本门不过、解密自检不过、分叉、双开嫌疑。处置 = **不挡启动**（避免"断网开不了机"），但**禁止自动 Depart 与自动分发**（否则陈旧数据 + 自动推送 = 重演事故 1），只允许只读使用 + 🔴 直推 + 人工裁决四步（§5.5）。
- **一刀切 fail-open 是错的**：传输失败可以"下次再来"，完整性失败必须"定了再说"。

**Arrive-B —— 宿主已启动后执行**

| 步 | 动作 | 失败处理 |
|---|---|---|
| B1 | 账本相关操作一律走 **Host HTTP API**（绝不碰运行中的账本文件） | 失败 → 告警 |
| B2 | 上报管家面板（🟡→🟢、日报「🤝 交接」段） | — |
| B3 | 自检（"交接后一切可跑"冒烟） | 失败 → 🔴 |

### 5.2 Depart（离开 / 关机）—— 5 步

把"采集"写在"超集体检"之前是**错的**：采集是就地覆盖工作副本，覆盖后"本机 vs 工作副本"必然相同 → 体检恒通过、挡推形同虚设。实测现网脚本恰恰是**先体检后采集**，方案按此顺序定稿。

| 步 | 动作 | 失败处理 |
|---|---|---|
| 1 | 前置检查：本机已 Arrive 过（交接单 + 无未处理告警） | 提示 + 要求确认（`--force` 记审计） |
| 2 | `git fetch` 三仓 | 失败 → `deferred`，不阻断 |
| 3 | **超集体检**（判据 = **远端 HEAD vs 本机生产文件**，不再是"工作副本 vs 生产"） | 不合格 → **中止本次采集** + 告警（不允许只 warn） |
| 4 | 采集（**禁镜像语义**；每仓参数见下表） | 逐仓记录 |
| 5 | 提交 + push + **推后复核**（远端 == 本机）+ 写交接单 | 失败 → `deferred` + 告警 |

**每仓参数矩阵**

| 仓 | 命令 | 理由 |
|---|---|---|
| 仓1（配置与长期资产） | `-Sync` + push | 采集 + pull 合并；已有挡推（且先体检后采集） |
| 仓2（会话） | **`-Push`**（或 `-Sync -BackupOnly`） | `-Sync` 的 pull 合并会把归档会话**回流**进活跃目录，毁掉归档收益 |
| 仓3（工具链） | `-Sync` + push（挡推补齐中） | 覆盖式采集，必须先体检 |

**超时预算**：**非会话仓合计 120s + 会话仓单独 180s，整轮总预算 300s**（早期写的"整轮上限 120s、会话仓单独 180s"字面自相矛盾——单仓 180s 大于整轮 120s；实测会话仓 253 文件 / 280MB，180s 是它的下限）。超时 → 标 `deferred` + 记录未推项，**不阻断关机**。

**robocopy 重试参数是预算的前提**：`robocopy` 默认 **`/R:1000000 /W:30`**——撞上被独占的文件（实测账本锁场景）不是"快速返回 rc≥8"，而是**挂起几十分钟**，上面任何预算都会失效（外层超时也杀不掉挂着等锁的子进程）。故采集一律显式传 **`/R:2 /W:2`**（最多重试 2 次、每次等 2s）**并且**加进程级超时兜底，两者缺一不可；撞锁即记 `skipped(locked)`，不进"失败率"分子。

**关机链插入点**：停机脚本当前**零同步调用**；Depart 应插在**杀宿主之后、清理其余 node 进程之前**——此时数据库句柄已释放（冷采集，避开热采撞锁），而守护进程已停（不会被重新拉起）。

### 5.3 技术强制（不靠人记）

1. **版本门**：数据源 = 本机 `handoff-version.json`（权威：交接器版本 + 三仓采集脚本版本 + `minSupported`）+ 仓内 `handoff-version.json`（发布方写，随仓推送）；旧版读到 `minSupported > self` → 拒绝执行。
   - ⚠️ **自举顺序**：必须先用**手工**分发把新版脚本送到对端并验证，再开门禁——旧版脚本里根本没有读门禁的代码，门禁无法自举。
   - ⚠️ 现有的组件版本登记文件只登记了 9 个管家组件、**不含交接器与任何脚本**（实测），必须扩展它或另建 `handoff-version.json`。
   - ⚠️ **宿主主程序版本也进门禁**：原版本门只覆盖交接器与采集脚本，但记忆库 / 账本 / 插件实体三者的 schema 都跟**宿主与插件版本**绑——多机版本差会让合并器"按旧 schema 解新数据"。加两条：① 交接单增记双方宿主版本 + 关键插件版本；② Arrive 比对不一致 → 🟡 告警 + **合并前要求人工确认**（不自动拒绝：正常灰度升级不该被卡死，但必须让人看见）。
2. **ff-only**：只接受快进，禁止自动 merge/rebase。**并补破口检测**——ff-only 检测不到"对方本地有未推提交（崩溃/断电没 Depart）"，必须靠 §5.1 A2 的**仓内心跳文件**兜底。
3. **超集体检**：判据改为**远端 HEAD vs 本机生产文件**（原"工作副本 vs 生产"会被采集架空）；不合格**中止采集**，不允许只 warn。
4. **采集口径对账**（取代原"未登记拒绝采集"）：原条款照做会**立刻拒绝 18 项**（实测：白名单有 / 登记表没有 = 18 项，含凭证、设置、技能、记忆、账本、状态目录等核心）。改为**两张表 + 双向对账**：
   - 「状态文件登记表」：管 schemaVersion / 持久性 / 忽略规则段 —— 只覆盖管家自己的状态文件；
   - 「采集清单」（新增）：管"路径 + 采集器（直采 / 主机名前缀 / 专用函数）+ 缺文件容忍度" —— 覆盖三仓全部采集对象；
   - 二者做显式映射；现有校验器**只校验忽略规则段、不校验采集白名单**，需扩展；
   - 对账差异**报告不拒绝**：首跑出一次差异清单，人工确认后再开门禁。
5. **禁镜像语义**：脚本层禁止 `robocopy /MIR`、`git clean -fdx`、`rsync --delete`；**落成静态检查脚本**（grep + 白名单）纳入交接自检（否则只是口号）。
6. **单写者校验（一个字段扛两种语义正是原稿冲突的根因）**：不等云中继——第一版就用**仓内心跳文件**实现，且**必须拆成两个字段**：
   - `lastHeartbeatAt`：**任一成功 push 都更新**（含 30 分钟热采）。语义 = "对端还活着"。**新鲜 = 双开嫌疑**（§5.1 A2 用"本机 Arrive 进行中被更新"判定，避免正常 Depart→Arrive 流程每次都误报）。
   - `lastSuccessfulHandoffAt`：**仅完整 Depart / Arrive 结束才更新**。语义 = "对端把状态放好了"。**陈旧 = 对端崩在半路没交接完**。
   - ⚠️ 原稿两处条款（"心跳过期→🔴" vs "见对方心跳新鲜即告警"）字面互斥且语义未定义 → 正常交接必然误报、而崩溃落在两次热采之间的盲窗（心跳 ≤30min"不过期"）又必然漏报。拆字段 + 写死"谁更新 / 新鲜算什么 / 陈旧算什么"后，两处统一为本节口径。**对端无记录（新机 / 冷启动）= 放行**，不告警。
   - **触发源互斥**：同机四类触发（开机钩 Arrive / 关机钩 Depart / 热采 tick / 远程"收工"）**共用调度器单例互斥**（不新造锁），并在同一把锁上定义 **drain 语义**：关机钩撞上正在跑的热采 tick → 等其结束（上限 = 剩余预算），**不许并发**；Arrive 进行中发现远程 Depart 请求 → 直接回 `busy`，不做队列。`_archive/handoff-*` 备份目录同步定 GC 策略（保留最近 20 次 + 超 30 天清理）。
   - 云中继上线后再叠加租约（TTL），与心跳字段**并存不替换**。

### 5.4 交接单（handoff record）

- 位置：本机 `<DSH 主目录>/handoff.json`（不同步）+ 仓内 `dsh/<handoff 目录>/<host>.json`（同步，按主机名前缀）。
- **落盘机制**：复用管家状态存储（原子写 + `schemaVersion` + 读改写锁），禁止自行 `writeFileSync`。
- 字段：`schemaVersion` / `host` / `role(arrive|depart)` / `startedAt` / `finishedAt` / `result(ok|deferred|failed)` / `repos[{name,localHead,remoteHead,behind,ahead,pushed,sizeBytes}]` / `volumes[{repo,sizeBytes,files,thresholdBytes,overThreshold}]` / `hostVersion` + `pluginVersions{}` / `merged[{file,added,changed,skipped}]` / `applied[{key,repoPath,sha256,ok}]` / `bootstrap[{item,ok}]` / `handedOffSessions[]` / `deferred[]` / `alerts[]` / `lastHeartbeatAt` + `lastSuccessfulHandoffAt`。
- **`applied[]` 路径口径**：`key` = 交接清单条目键（机器可读、跨机同义），`repoPath` = 仓内相对路径；**生产绝对路径只在本机内存里做映射，绝不写入交接单**——写成绝对家目录路径既违反字段白名单纪律，对方读它也毫无意义。
- **字段白名单要机器可校验**：白名单落实为 `handoff-record.schema.json`，写交接单前按 schema 校验（多余字段直接拒绝写入），否则"字段白名单"只是口号。
- **体积治理**：每次交接记录三仓体积与文件数，超阈值（仓1 200MB / 仓2 1.5GB / 仓3 400MB，可配）→ `alerts[]` + 面板红条 + 告警事件。只治"搬多勤"治不了"账本 / 坑库每次变更整份入库 + 会话只增不减"的累积（实测 281.7MB），故另立治理规程：静默期跑 `git gc --aggressive`，触阈值走"history 压缩 / 换新仓"流程（换仓须重配忽略规则与加密 key，属 C 类手工动作）。
- **不进交接单**：密钥、Secret、会话内容（只记 id 与条数）。
- **不加密**：交接单只含路径 / 主机 / 时间 / id / 条数与告警摘要，**无密钥、无会话内容**；不加密换来的是**云中继能直接读它**（若加密，云端就得持 key，等于把密钥多放一处、反而扩大暴露面）。因此忽略规则**不加**该目录，但守两条纪律：① **字段白名单**（禁写 Secret / 会话内容 / 浏览器 URL / 完整家目录路径）；② 告警文本截断并脱敏（路径统一写成仓内相对路径）。状态文件登记表与采集脚本仍须同步登记（否则重演"登记了不采"）。

### 5.5 失败与降级

- 传输失败（无网 / 远端不可达）→ `deferred`，不阻断使用，下次 tick 重试。
- **可见性铁律**（"无感 vs 失败可见"的矛盾）：Arrive 结束时**无条件**播报一行「交接状态 + 数据新鲜度截止时间 + 未推项数」（面板 + 桌面宠物 + 日报），🔴 走直推通道——🟡 只进日报不够，使用者会误以为数据是新的；`lastSuccessfulHandoffAt` 超阈值（如 24h）→ 面板红条。
- 分叉 / 版本门失败 / **解密自检失败** / **双开嫌疑** → 🔴 直推 + **禁止自动处置**，走**人工裁决四步**：① 判谁为准（比对两侧生产文件的"真内容独有行"）；② 把落后一方独有内容**回灌**本机（分发脚本，不是 git merge）；③ 重跑一次完整 Depart；④ 复核远端 HEAD == 本机 HEAD，交接单记 `resolvedBy: 人工`。
- 落地失败 → 逐项列名 + 保留仓内副本，允许重复 Arrive（幂等）。
- 交接期间 → 重启闸门感知（不接受自动重启请求）。
- **启动链行为**：Arrive-A 失败**一律不挡宿主启动**（分档见 §5.1）——开机永远先能进 GUI，出错只降级为"只读 + 红条 + 禁止自动写"。"停止 + 告警"必须写清是停止 Arrive 还是停止开机，否则弱网下即等于开不了机。
- **应急通道**："不保留两套逻辑"自第一版起执行，但**旧手工 SOP 降级为文档化应急通道保留**，直到观察期满（30 天 / 1 周）且无 🔴 事故，才允许物理下架。**"撤旧"与"观察期未满"不能同时成立**。
- **审计自指闭环**：交接器分发文件 → 进写入审计 → 审计流水又要被采集 → 若审计把交接器的写入判成"非法改动"，就会**回滚刚分发的内容**。三层都要写死：① 登记交接分发目标为"管家自身写入"豁免；② 交接进行中暂停磁盘层审计（只告警不回滚）；③ **交接器自身的写入动作不算"未审计改动"**（自指豁免，与写坑库同款）。

### 5.6 会话交接标记（防对方误报"被打断"）

- Depart 时把"回合未闭合"的会话 id 写入交接单 `handedOffSessions[]`。
- Arrive-A 落盘后写标记文件（会话 id + 来源主机 + 导入时间）。
- 告警体系判定"被打断会话"时，对这批会话在 **N 小时（建议 24h）内排除或降级**，避免每次交接都在对方机器刷出一片"被打断"噪声。
- 归档会话另按"归档清单**按主机名前缀随仓同步**，Arrive 时把对方已归档的会话直接落到本机归档目录，**不要**补回活跃目录"处理（否则归档收益回退）。

### 5.7 时间语义与预算表

> 全文有 8 处以上条款依赖"心跳 / 阈值 / 预算"，散落在多节 —— 两处字面冲突正是这么来的。本节是**唯一权威口径**，各节只引用、不再各自重述。

| 项 | 时钟 / 更新者 | 值 | 触发后果 |
|---|---|---|---|
| `lastHeartbeatAt` | 本机墙钟；**任一成功 push**（含热采）更新 | 陈旧阈值 90min（热采 30min × 3） | 陈旧 → 🟡；**在本机 Arrive 进行中被更新 → 双开嫌疑 🔴（fail-closed）** |
| `lastSuccessfulHandoffAt` | 本机墙钟；**仅完整 Depart / Arrive 结束**更新 | 24h（面板红条）/ 72h（🔴 直推） | 超阈 → 红条 + 告警事件；接 §5.5 可见性铁律 |
| 会话 RPO（`handedOffSessions[]` 覆盖到哪） | — | **"下个闭合周期"或 Depart，取先到者** | 见 §8 冷档 |
| 合并时钟门 | 本机墙钟 vs 对端 `updatedAt` / 远端 commit 时间 | 偏差 > **120s** 拒绝合并；胜负差 < **5min** 转人工 | 拒绝 → 🔴 要求对时；转人工 → 🟡 |
| Arrive 预算 | **单调时钟** | fetch 60s / pull 120s / 合并分发 120s，**总 300s** | 超时 → fail-open（§5.1） |
| Depart 预算 | **单调时钟** | 非会话仓 120s + 会话仓 180s，**总 300s** | 超时 → `deferred`，不阻断关机 |
| 交接会话告警抑制窗口 | 本机墙钟 | 24h | 窗口内降级，不刷"被打断" |
| 仓库体积阈值 | — | 仓1 200MB / 仓2 1.5GB / 仓3 400MB | 超阈 → 🟡 + 面板红条 |
| `_archive/handoff-*` GC | — | 保留最近 20 次或 30 天（先到者） | 超期静默清理（属备份，不告警） |

**两条纪律**：

1. **必须写明用的是哪个时钟**：墙钟只用于**跨机比较**（心跳、`updatedAt`）；**一切"超时"一律用单调时钟**——NTP 校时会让墙钟前后跳变，用墙钟计超时会算出负值或瞬时超时。
2. **对时校验**：合并前比对"本机墙钟 vs 远端最新提交时间"，偏差 > 120s → 账本与记忆库**拒绝合并** + 🔴 要求人工对时。理由：久置不开机的机器时钟漂移会让旧版本静默"较新"胜出，是**无告警的数据丢失**。

---

## 6. 云中继（云服务器 + 手机端）

### 6.1 现状与目标

- 现状：手机访问靠临时隧道（**随机地址、每次变、首次打开卡 3-5 分钟**，已实测）；局域网代理仅应急。企业自建应用已建、发消息脚本已就绪，但发消息被 **errcode 60020（IP 不可信）** 卡住。**澄清**：真正要配置的是**企业可信 IP**；官方要求"配可信 IP **之前**先设置可信域名或接收消息服务器 URL"，所以正确顺序是 **① 回调 URL → ② 企业可信 IP（填云服务器固定出口）→ ③ 应用消息才能发出**。
- 目标：**固定公网入口**（手机一个地址永远可用）+ 企业 IM 双向通道 + **多机活跃机登记 / 交接中继**。
- 既有的"云服务器固定入口"任务卡由本方案接管：沿用其步骤（采购 → 云端加固 → frp → HTTPS → frpc 自启 → 认证 → 企业 IM → 文档回滚），**新增第 8~10 步（多机部分）**。

### 6.2 云服务器的三重角色

| 角色 | 解决什么 | 组件 |
|---|---|---|
| ① **手机固定入口** | 手机连同一个地址，不管哪台在跑 | frps + Caddy（HTTPS）+ 入口认证 |
| ② **活跃机登记与租约** | 记录"现在是哪台在跑、心跳、最后交接"；将来万一双开能立刻发现 | 轻量 HTTP 服务 + 心跳文件 + 租约（TTL） |
| ③ **交接中继与仲裁** | 记录"最后交接时间 / 由哪台推的 / 是否成功"；提供"push 前强制检查"的检查点 | 读取仓内 `<host>.json` + 暴露只读 API |

三者的关系：**②③ 是多机方案的一部分，① 是最初的移动端需求**——同一台机器、同一套证书、同一份访问控制，一次部署三用。

> **读取凭据**：交接单**不加密** → 云端**无需解密 key**，只凭代码托管平台的只读权限（deploy key / 只读 token）读 `<host>.json`；云端对外（手机端）暴露的接口另用一次性 token 鉴权，token 只存云端环境变量，**不进仓、不进文档、不进记忆**。

### 6.3 与推送通道的关系

- 现有三通道：桌面宠物 + 宿主动态会话注入 + 日报。
- 新增第四通道：**企业 IM 应用消息**（云端发送，不依赖本机在线）→ 交接失败/告警能在**人不在场时**收到。
- 通道选择规则：🔴 安全/数据类 → 全通道；🟡 → 日报 +（可选）IM 摘要；夜间免打扰对 IM 同样生效（交接失败除外，穿透）。

### 6.4 手机端要看到什么

- 固定入口打开宿主 GUI（原需求）；
- **交接状态卡**：最近一次交接（哪台 → 哪台 / 时间 / 结果 / 差异条数）；
- **未推提醒**：某台有未推送改动超过 N 小时 → 手机可收到并一键提醒（后续可扩展为远程触发 Depart）。

### 6.5 安全红线

- ❌ 禁止把宿主改成监听 `0.0.0.0`（官方硬编码拒绝，且等于把 RCE 交给公网）；隧道只做"loopback 反代"。
- 云端只放行 SSH 与 80 / 443。**家庭出口 IP 是动态的，不能用"限本机 IP"** → 改为：仅密钥登录（禁密码、禁 root 直登）+ **非标 SSH 端口** + `fail2ban` + 云厂商侧安全组/访问控制；80 / 443 只给入口与回调。
- 入口必须带认证（Basic Auth 起步，后续换更强）；沿用"Host/Origin 改写"思路绕开宿主信任栅栏。
- **Secret / 应用 ID / 密钥一律不进文档、不进长期记忆、不进任务卡**（存本机凭证文件，随加密备份）。
- 云端组件**只读仓/只读 API**，不允许云端直接修改仓内容（避免出现"第三个写者"）。
- 一键回滚脚本必须实测通过；回滚后回到临时隧道可用状态，云端资源保留不删。

### 6.6 企业 IM 双向通道：手机助手

云中继不只是"入口"，还是**双向通道**——可以直接在 IM 里指挥宿主里的 agent。

**官方能力（已查证）**：
- 应用可接收用户消息：配置"接收消息服务器 URL"后，平台把消息 POST 到云服务器（[接收消息](https://developer.work.weixin.qq.com/document/path/96466)）。
- **5 秒硬限制**：5 秒内不响应即断连并重试 3 次；**无法保证 5 秒内答完 → 直接返回 200 空串，改用"主动发消息接口"异步回复**（[回调配置](https://developer.work.weixin.qq.com/document/path/96238)）→ 这是对话机器人的正解。
- 主动消息频率：每应用 ≤ 账号上限 × 200 人次/天（单人使用充裕）（[发送应用消息](https://developer.work.weixin.qq.com/document/path/90236)）。
- ⚠️ 坑：若在管理端开启"始终进入主页"，IM 端**只能收文本且被截断到 20 字节** → 不要开启。

**省钱设计**：手机端单开一个专属会话（"手机助手"），**固定挂低成本模型**，干活才切主力模型（宿主模型为会话级切换，天然支持这种分工）。两个已知边界：小上下文模型装不下长会话会返回 400（需定期重开或限轮数）；某些渠道侧有安全审核，**特定内容类型必被拦**，手机端不做这类用途。

**优先做这五件事**：
1. **告警闭环**：推"交接失败 / 磁盘满 / 任务卡住" → 使用者回指令 → 云端解析 → 触发动作（走重启闸门 + 权限档）
2. **查状态**：发"状态" → 回宿主在线 / 计划任务 / 余额 / 在途卡 / 上次交接结果
3. **远程触发**："收工"（Depart）/ "重启"（走排队闸门）/ "归档"
4. **派活**：自然语言 → 转 prompt 投入指定会话
5. **图片**：IM 图片消息 → 落盘 → 交宿主 agent 看图（视觉模型通道）

**架构（安全第一）**：
- ❌ 禁止"云端反向调用家里宿主的 API"（等于把写接口挂公网）。
- ✅ 采用**出站轮询**：云端只做"信箱 + 鉴权 + 限流"，家里宿主主动出站拉指令、跑完推结果 → **公网零写入暴露**。
- 本通道是**第 4 条通道**（现有：桌面宠物 / 会话 / 日报）；指令解析纳入权限档，**白名单只认本人 userid**；敏感动作（重启、删卡、改配置）二次确认。

---

## 7. 与既有管家能力的复用（不另起体系）

| 已有能力 | 在本方案中的用法 |
|---|---|
| 管家状态存储（原子写 / 读改写锁 / schemaVersion） | 交接单、合并器的落盘与并发保护 |
| 管家调度器（tick 契约 / 单例互斥） | 定时自动交接、重试 |
| **重启统筹**（闸门 / 批次 / 心跳） | 交接期间不接受自动重启；重启前先 Depart |
| **主动告警**（事件分类 / 冷却 / 分级） | 新增事件类：`handoff-failed` / `handoff-overdue` / `repo-diverged` / `dual-active` |
| **坑库学习**（坑库 / 候选） | 交接踩的坑进坑库（自带自指豁免） |
| **写入守门人**（审计 / 哈希链 / 豁免） | 交接改动长期约定/脚本 → 自动进审计；审计流水按主机前缀跨机可见（正好补 §1.1 事故 3 的洞） |
| **磁盘层豁免** | 磁盘层审计会把交接器刚分发的文件当成"非法改动"——一旦自动回滚放开就会**回滚刚分发的内容**。三层都要做：① 把交接分发目标加进"管家自身写入"豁免；② 交接进行中**暂停磁盘层审计**（只告警不回滚）；③ **交接器自身动作的自指豁免**——否则"分发 → 审计 → 回滚 → 再分发"成死循环 |
| **日报管线** | 升级为「🤝 交接」段：上次交接时间/结果/差异/未推项 |
| 观测期台账 | 交接失败率纳入观测 KPI（单机口径，不跨机合并） |
| 生态面板 | 新增"交接器版本 / 交接健康"只读分组 |

**纪律**：不再新增一堆独立的平行同步脚本（这正是过去互相打架的根源）；新增逻辑统一进管家的工具目录，并登记组件版本。

---

## 8. 无感体验设计（必须分热/冷两档）

**核心修正**："每 10 分钟全量 Depart"与实测数据不兼容——会话 253 文件 / 280 MB，全量采集 + 加密 + commit 一天 144 次，仓库会膨胀，且必然撞上运行中数据库的独占（撞锁的主要形态**不是** `rc≥8` 快速失败，而是 robocopy 默认 `/R:1000000 /W:30` 的**长时间挂起**）。改为**热 / 冷两档**。

| 场景 | 期望体验 | 实现 |
|---|---|---|
| 开机 | 打开宿主就是"另一台离开时的样子" | `Arrive-A` 挂在守护进程拉起之前，`Arrive-B` 挂其后；后台跑不挡 GUI |
| 使用中（热） | 无需手动点同步 | **每 30 分钟**热采：推小文件（长期约定 / 技能 / 文档 / 账本 / 坑库 / 审计 / 交接单）**+ 只搬"已闭合"的会话**；**数据库一律跳过**（记忆库跨机只走 Depart 冷采 + Arrive-A 语义合并）；**无变化不提交**（git 检测空变更即跳过）；逐文件快照方式见本节末表 |
| 使用中（冷） | 大件不频繁搬 | 正在跑 / 未闭合的会话等它闭合后随下一轮热采搬 —— **每个闭合周期搬一次**（原"一个会话一生只搬一次"在用户**重开已闭合会话**后即失效，此后增量永不入仓，期间崩溃=丢）；判据 = 该会话 `mtime` / `size` 与**上次搬运快照**不一致即重搬；与"消灭中间版本"的冲突用 **Depart 冷采兜底 + 每会话保留版本数上限**约束；该 RPO 显式登记为假设项 |
| 关机 | 忘了推也不会丢 | `Depart` 挂停机脚本，插入点在**杀宿主之后、清理其余 node 之前**（冷采集，句柄已释放）；预算 = 非会话仓 120s + 会话仓 180s（总 300s），超时标 `deferred` 且不阻断关机 |
| 到达后 | 看得见交接结果 | **无条件**播报：「✅ 已从 B 机接过来：记忆 +27 / 卡 +10 / 会话 +161 / 审计 +1 · 数据新鲜度截止 11:40 · 未推项 0」（面板 + 桌面宠物 + 日报） |
| 出问题 | 人不在场也能知道 | 🔴 直推 → 企业 IM（云中继通道）；`lastSuccessfulHandoffAt` 超 24h → 面板红条 |

**为什么是"只搬闭合会话"而不是"跳过会话"（实测依据）**

实测（253 个会话 / 281.7 MB）：近 1h 变 **5 个文件 / 6.94 MB**、近 3h 变 **19 个 / 29.54 MB**、近 24h 变 **203 个 / 228 MB**；单文件平均 **1.14 MB**、最大 8.5 MB。`robocopy /E` 本就跳过未变文件、git 也只提交变化 → **文件级增量早就在做**；真正的浪费不在"搬多少"，而在"搬多勤"：

- 会话是 **append-only**：追加几 KB 也要整份复制；
- 加密后是二进制 blob，**git 存不下 delta** → 每次变化都留一整份新版本；
- ⇒ 同一会话一天被热采 10 次，仓库就永久留下 10 份整本历史，而其实只需要最后那一份。

改成"**只搬已闭合的会话**"后：① 覆盖 98%+ 的数据（实测：64 个会话中 63 个已正常收尾）；② **每个闭合周期只搬一次**，同一闭合周期内中间版本归零（估算把仓库增长降到 1/4 ~ 1/5）——**重开的会话按"新闭合周期"重搬**，否则增量永不入仓；③ **顺带彻底消掉"半截 zstd 帧"风险**——运行中的文件根本不碰；④ 正在跑的那个会话等闭合后或 Depart 时再搬。

**热采一致性补充**：数据库被宿主持句柄（写入期间可能独占）→ 热采一律跳过，只在 Arrive-A（宿主未起）或走 SQLite 在线备份（`VACUUM INTO` / backup API）。会话闭合判定复用尾部事件读取（`lastEvent` + 未闭合事件类型白名单，带 mtime/size 缓存），每文件只读尾部若干 KB；仍做 **zstd 帧完整性校验**（`28 B5 2F FD` 逐帧解），不完整则丢弃等下轮。

**热采逐文件快照方式表**（账本 / 坑库 / 审计这类"运行中被写"的文件必须各定口径）

| 热采对象 | 快照方式 | 撞锁 / 撕裂的处置 |
|---|---|---|
| 任务账本 | **走 Host API 导出**（服务端一致性导出，带 `revision`），**不复制文件**——宿主运行时持锁并做读改写，文件复制既可能撞锁、也可能推入与最新 revision 不符的滞后快照 | 导出失败（Host 不可达 / 未起）→ 跳过本轮、不算失败；导出结果标 `authority: snapshot` |
| 坑库 / 候选池 | 文件复制（原子写，读到的必是完整版本） | 存在 `.lock` 或原子写临时文件 → **跳过本轮** |
| 审计流水 | 文件复制 + **行边界校验**：末尾若为半行（无换行 / JSON 不完整）→ **只采集完整行前缀**，半行留到下轮 | 撕裂的审计会在对方破坏哈希链 → **宁可少推一行，也不推半行**；连续两轮丢弃 → 🟡 告警 |
| 交接单 | 文件复制（原子写） | 撞锁 → 跳过本轮 |
| 长期约定 / 技能 / 文档 | 文件复制 | 撞锁 → 跳过本轮 |
| 会话（**仅已闭合**） | 文件复制 + zstd 帧完整性校验 | 校验不过 → 丢弃等下轮 |
| 数据库 | **不推** | — |

**统一纪律**：热采中任何一项"存在锁文件 / 校验不过"→ **跳过该项本轮并记 `skipped`**，**绝不允许**中断整仓采集、也绝不允许推送撕裂快照入仓。这条纪律对**全部热采对象**生效。

**边界（要说清楚）**：C 类（插件实体 / 计划任务 / 全局补丁）**不可能无感**——每台机必须装一次，但可以做到"**一条命令 / 开机自检自动补**"。

---

## 9. 状态文件与配置登记

新增登记项（写入 `state-files` 单一数据源，白名单自动生成）：

| 文件 | schemaVersion | persistence | sync | 判据 |
|---|---|---|---|---|
| 本机交接状态 | 1 | persistent | **false** | 时间窗口 |
| 仓内交接单（`dsh/<handoff 目录>/<host>.json`） | 1 | persistent | **true**（按主机名前缀） | 跨机可见的交接单，沿审计流水先例 |
| 活跃机 / 心跳状态（云中继用） | 1 | volatile | false | 时间窗口 |
| 云中继侧 | — | — | **不属于本机** | 云端独立部署，凭据不入仓 |

同时处理遗留的"未接线"三项（已登记但未采集的状态文件），先明确口径为 `sync:false`，避免"登记说同步、实际没采"的假象。

---

## 10. 施工序列

> 本表只保留设计口径；卡的实际状态见任务账本。

**已拆两段**：**H1a** = 独立 CLI 手动交接器（不接告警事件 / 审计豁免 / 面板 / 关机开机自动钩子 → 不碰判定与审计侧，**可提前做**）；**H1b** = 正式版（版本门强校验 + 30 分钟热采 + 自动钩子 + 接管家，等宿主升级完 + 两条决策定案）。

| 卡 | 内容 | 依赖 | 备注 |
|---|---|---|---|
| **H0** | 立规矩 + 清尾巴：仓3 补挡推、修仓1 忽略规则缺的两行、`git rm --cached` 损坏残留、推掉未推提交、关机/开机链插入占位 | — | **基本低风险，但带两条前提**：①"推掉未推提交"必须**走采集脚本 `-Push`**（仓1 自带挡推），**禁止手工 `git push`**——已实测：仓里对方独有内容一推就丢；② 推前逐文件比对本机 vs 远端，发现"仓里更全"先分发回本机再推 |
| **H1** | 交接器：Arrive/Depart 骨架 + 版本门 + ff-only + 分叉拒绝 + 交接单 + **心跳双字段** + **时间语义与预算表落地** | H0 + **条款冻结** | 核心；**开卡前四条阻断级条款必须先定稿**（否则写出来即返工） |
| **H2** | 语义合并器：账本 / 记忆库 / 坑库 + 候选池（含备份 + 校验 + 幂等） | H1 | 根治"覆盖" |
| **H3** | 落地层：机器可读清单 + 生产路径分发 + C 类 bootstrap（含安装源清单 + 执行器） | H1 | 治"到了发现不对" |
| **H4** | 管家接入：告警事件、日报「🤝 交接」段、面板交接卡、审计联动、重启闸门互斥 | H1 | 不另起体系 |
| **H5** | 云中继：云服务器（固定入口 + 活跃机登记 + 交接中继 + 企业 IM 双向通道）+ 手机交接状态卡 | H0–H4 + 采购 | 原方案卡并入，含新增 8~10 步 |
| **H6** | 开源支线（可选）：抽一个自建插件做通用化试水（脱敏 / 配置化 / README / license） | 独立排期 | 非本轮目标 |

**并行建议**：H0 → (H1) → H2/H3 可并行 → H4 跟 H1 走 → H5 等采购到位（内含企业 IM 双向通道）。H5 的"活跃机登记"组件可提前在 H1 里预留接口。H6 与其他卡无依赖，随时可起。

---

## 11. 验收标准（逐条实测，不许只读代码）

1. **单机回归**：单机跑 `Arrive` + `Depart`，三仓差异清零、交接单生成、自检全绿。
2. **真实交接**：A 机 `Depart` → B 机 `Arrive` → B 机上能看到 A 机的记忆 / 卡 / 会话 / 审计（**含 A 机审计流水在仓内可读**）→ B 机 `Depart` → A 机 `Arrive` 反向看到。
3. **分叉防护**：人工构造"两边各有一条提交"→ 交接器**拒绝 push/pull 并告警**，远端未被修改。
4. **版本门**：人工把本机交接器版本改成过期值 → 拒绝执行并告警。
5. **禁止镜像语义**：静态检查（grep `/MIR`、`--delete`）在交接自检中为 0 命中。
6. **语义合并**：人工在两侧各加 1 张卡 / 1 条记忆 / 1 个坑 → 合并后两侧条目数正确、无重复、revision 正常递增。
7. **落地层**：在一台"缺插件实体 + 缺计划任务"的状态下跑 `Arrive` → 自动补齐并自检通过。
8. **云中继**：手机用固定地址打开宿主（会话/发消息/面板正常）；家里重启后隧道 5 分钟内自愈；企业 IM 真实收到一条交接告警；回滚脚本真跑一次。
9. **时钟门**：把某侧时钟人为调偏 +10min → 合并**拒绝**并 🔴 要求对时；调回 → 合并正常；构造 `updatedAt` 差 < 5min 的冲突 → 进 `alerts[]` 转人工，不自动决胜。
10. **Arrive 不挡开机**：断网 + 远端不可达 → Arrive-A 超时后 **宿主仍能正常启动**（红条 + 后台重试）；构造分叉 → 宿主能启动，但**自动 Depart 与分发被禁止**（fail-closed = 只读）。
11. **热采不撕裂**：宿主运行中连打 3 轮热采 → 账本走 Host API 导出、审计**无半行入仓**（每行可解析、哈希链连续）、撞锁项记 `skipped` 且**整仓采集不中断**（验 robocopy 显式 `/R:2 /W:2` 生效）。
12. **体积与路径口径**：交接单含 `volumes[]` 且超阈告警可触发；`applied[]` 与交接单全文**不含任何生产绝对路径**（schema 校验直接拒绝）。

---

## 12. 风险与回滚

| 风险 | 缓解 |
|---|---|
| 交接器自身出 bug 导致数据被覆盖 | 每次合并/分发**先备份**（`_archive/handoff-<ts>/`）+ 一键恢复脚本 + 只处理已登记文件 |
| 自动 Depart 在"双开"时误推 | 单写者校验（§5.3 第 6 条）+ ff-only + 超集体检 + 推送前二次确认（检测到异常即 fail-closed） |
| 云中继暴露面被攻击 | 只反代 loopback、强制认证、安全组最小化、fail2ban、云端只读仓（§6.5） |
| 老人（旧脚本链）与新人（交接器）并存导致混乱 | 交接器上线即**替换**旧的手动按钮语义；**但旧 SOP 降级为文档化应急通道保留到观察期满**（撤了旧路径，新路径出事时无路可退） |
| **Arrive 阻塞开机**（弱网 / fetch 挂起） | Arrive-A 全项定超时（总 300s）+ 分档：传输失败 fail-open、完整性失败 fail-closed 但**不挡启动**（只禁自动写） |
| **多机时钟漂移致旧数据"较新"胜出** | 合并时钟门（§5.7）：偏差 > 120s 拒绝合并 + 🔴 要求人工对时；胜负差 < 5min 一律转人工 |
| **仓库体积累积触顶** | 交接单记录三仓体积 + 阈值告警（§5.4）+ 静默期 `git gc` / 换仓规程（§5.7） |
| 观测污染 | 交接台账按机存、门槛按机判 |

---

## 13. 设计决策记录

| # | 议题 | 结论 | 理由 |
|---|---|---|---|
| 1 | 对等还是主从 | **对等（谁开谁写）** | 靠交接协议保证，不需要一台常驻"主" |
| 2 | 自动化程度 | **全自动**（开机 Arrive + 关机 Depart + 后台热采 30 分钟） | 手工 SOP 已被三次事故证明不可靠 |
| 3 | 语义合并授权 | **账本 / 记忆库 / 坑库三类全部授权** | 带备份 + 校验 + 回滚，风险可控；不合并则只能靠人肉并回 |
| 4 | 云服务器兼做交接中继 | **是（一机三用：手机入口 / 活跃机登记 / 交接中继）** | 同一套证书与访问控制，边际成本≈0 |
| 5 | 顺序 | **交接优先，云中继等采购** | 交接是事故根因，云中继是体验增强 |
| 6 | 企业 IM 双向通道 | 先落设计，随云中继一起实现 | 依赖云端回调 URL 与可信 IP |
| 7 | 开源支线 | **做**：先抽一个自建插件做通用化试水；**方法论文档（本类文档）先行** | 文档脱敏成本低于代码通用化，且能先建立外部反馈 |
| 8 | 热采频率与会话口径 | 放慢到 **30 分钟**；会话由"跳过"改为"**只搬已闭合会话**" | 不丢数据、无中间版本、天然避开半截帧 |
| 9 | 坑库跨机 count 口径 | **双口径 + 跨机加成**（`totalCount` 直接相加 + `hosts[]` + `rate`），另备"工作量归一化"档 | 见 §4.3（数学说明：直接相加已等价于按工作量加权，额外乘权重只会让"次数"失真） |
| 10 | 交接单是否加密 | **不加密**（字段白名单 + 告警脱敏） | 换取云中继可直接读取；加密则云端须持 key、反而扩大暴露面 |

---

## 14. 假设与验证时机

> 规则：任何"待验证"的能力都在此登记，排期时前置一个 spike；验证失败的退路写清楚。

| # | 假设 | 验证时机 | 失败退路 |
|---|---|---|---|
| A1 | 使用者确实不会同时开多机宿主 | 上线后观察 30 天（是否出现 `dual-active`） | 云中继租约强制单写者 |
| A2 | 解密后语义合并可行且无损 | H2 单测（含人工构造冲突样本） | 退回"只能整份覆盖 + 人工裁决" |
| A3 | C 类 bootstrap 能把"换机必补清单"完全自动化 | H3 沙盒：从干净目录一条命令起 | 保留清单 + 人工勾选 |
| A4 | 坑库跨机合并口径可接受 | H3 落地后复核排序质量 | 只用单机口径 |
| A5 | 云服务器可同时承载手机入口 + 中继 + IM 回调 | H5 压测与实测 | 入口与中继拆两台 |
| A6 | 后台自动 Depart 不会与使用者操作打架 | 上线后观察一周 | 自动 Depart 降级为提示 |
| A7 | 单写者假设下仓2 不再需要双向合并逻辑 | H3 验证（回流问题是否消失） | 保留手工合并入口 |
| A8 | 交接失败时"降级为手动"体验可接受 | 上线后观察（失败率纳入观测 KPI） | 见 §5.5 应急通道 |
| A9 | **会话 RPO = "下个闭合周期"或 Depart（先到者）可接受** | 上线后观察：重开的会话是否按 `mtime`/`size` 重搬 | **实测丢过一次增量即收紧为"闭合即搬"** |
| A10 | 三仓体积阈值（200MB / 1.5GB / 400MB）够用 | 落地后按月复核，并验证"超阈前真能提前告警" | 调阈值 / 走换仓流程 |
| A11 | 多机时钟偏差通常 < 120s | 上线后观察时钟门触发次数——频繁触发说明某台长期漂移，需先修时间同步 | 自动校时 / 放宽阈值 + 转人工 |

---

## 15. 审核意见落实记录（四轮）

**口径声明**：多轮审核**视角互不重叠**（第 1 轮架构、第 2 轮实现、第 3 轮运行期并发与时间/容量、第 4 轮机制演练与边界），因此**前几轮不构成后几轮的合格证**——不要用"前 N 条已全部落地"给方案发合格证。

### 第一轮 + 第二轮 + 第三轮（共 52 条 · 逐条登记）

| 编号 | 意见摘要 | 落点 |
|---|---|---|
| B1 | 热采一致性（数据库独占 / zstd 半截帧） | §8（热采跳过数据库；只搬闭合会话 + 帧完整性校验） |
| B2 | Depart 体检顺序反了（会被自己的采集架空） | §5.2（体检早于采集；判据改"远端 HEAD vs 本机生产"） |
| B3 | "未登记拒绝采集"照做会立刻卡死 | §5.3 第 4 条（改"两张表 + 对账，报告不拒绝"） |
| B4 | Arrive 挂载点在宿主启动之后 | §5.1（拆 Arrive-A / Arrive-B） |
| B5 | 版本门无数据源、无法自举 | §5.3 第 1 条（版本文件 + 手工自举顺序） |
| B6 | 单写者破口（对方崩溃没 Depart） | §5.1 A2 + §5.3 第 2 条（仓内心跳） |
| B7 | 合并器三定义缺失（删卡复活 / 记忆 id 撞车 / 同标题跳过） | §4.3（tombstone / 主机分段发号 / 同标题不同 id 保留） |
| B8 | autocrlf 导致 sha256 分发校验误报 | §4.2 第 3 条（规范化哈希 + 两档失败） |
| B9 | 事故 2 的当事对象（文档 / 长期约定 / 技能）无合并规则 | §4.3（文档类复用条目合并器；长期约定只报差异） |
| I1 | tick 载体与容量成本 | §8（30 分钟热采 + 空变更不提交） |
| I2 | 每仓调用参数矩阵（仓2 回流坑） | §5.2 每仓参数矩阵 |
| I3 | 交接过去的未闭合会话污染对方告警 | §5.6 会话交接标记 |
| I4 | 关机链插入点与超时 | §5.2（杀宿主后、清 node 前；预算口径修正为 120s + 180s = 300s） |
| I5 | 对方归档会话被补回活跃目录 | §5.6（归档清单按机同步，落到归档目录） |
| I6 | 交接单登记口径（本机路径 vs 仓内路径） | §5.4 |
| I7 | 磁盘层审计可能回滚交接内容 | §7 新增"磁盘层豁免"行 |
| I8 | 分叉后的人工裁决无规程 | §5.5 人工裁决四步 |
| I9 | "无感"与"失败必须可见"矛盾 | §5.5 可见性铁律 |
| I10 | 代码侧交付缺兼容性论述 | §4.5 兼容性要求（4 条） |
| I11 | 企微 60020 表述 | §6.1（回调 URL → 可信 IP → 发消息的顺序） |
| I12 | 未决项与"已定案"并列 | §13 补充决策 8–10 |
| S1 | 句读 + 分发前备份 | §4.2 第 3 条 |
| S2 | 术语双轨 | §5 章首术语映射表 |
| S3 | 会话口径（会话数 vs 文件数） | §3.2 口径说明 |
| S4 | 交接单落盘机制 | §5.4（复用状态存储） |
| S5 | 云端细节（SSH / 凭据 / 加密） | §6.2 读取凭据注 + §6.5 SSH 策略 |
| R1 | 阻断 | 热采缺"运行中被写文件"的一致性快照口径；"记忆快照"与"跳过数据库"并列自相矛盾 | §8 快照方式表 + 统一纪律；§5.2 `/R:2 /W:2`；§4.3 `authority: snapshot` 弱来源 |
| R2 | 阻断 | 心跳两处条款字面冲突，双开与崩溃均有盲窗 | §5.3 第 6 条**拆双字段** + §5.1 A2 重写 + §5.7 |
| R3 | 重要 | 账本合并 `updatedAt` 胜负依赖多机墙钟，无偏差防护 | §4.3 时钟门 + §5.7 对时校验 |
| R4 | 重要 | 加密 key 属 C 类却不可能经仓 bootstrap；语义合并排在其前 | §3.1"钥匙例外"（手工传递 + 无 key 硬闸）+ §5.1 A1 抽样解密自检 |
| R5 | 重要 | "会话一生只搬一次"对**重开**的会话失效 | §8 冷档改"每个闭合周期搬一次" + 假设 A9 |
| R6 | 重要 | 仓库体积无上限治理（只治频率不治累积） | §5.4 `volumes[]` + 分仓阈值告警 + gc/换仓规程；§5.7 |
| R7 | 重要 | Arrive 无超时预算、失败是否挡启动未定义、撤旧无应急通道 | §5.1"超时预算与失败分档" + §5.5 两条 + §12 风险行 |
| R8 | 建议 | tombstone 无生命周期 / GC | §4.3（两台 `lastSuccessfulHandoffAt` 均晚于 tombstone 方可清理） |
| R9 | 建议 | 记忆 id 重分配可能静默断引用 | §4.3 主机分段发号；遗留冲突拒绝自动合并 + 告警 |
| R10 | 建议 | 宿主主程序版本未进门禁 | §5.3 第 1 条 |
| R11 | 建议 | 超时预算自相矛盾（整轮 120s vs 单仓 180s） | §5.2（120s + 180s = 总 300s）+ §5.7 |
| R12 | 建议 | 四种触发源缺统一互斥 | §5.3 第 6 条（共用一个单例 + drain 语义 + 拒绝远程 Depart + 备份目录 GC） |
| R13 | 建议 | `applied[].path` 与字段白名单冲突 | §5.4（改 `key` + `repoPath`；绝对路径只留内存映射 + schema 校验拒绝） |

**同时补的三条（非审核提出，复核时发现）**：

1. **H0 不是无条件低风险**："推掉未推提交"若手工 `git push` 会丢掉仓里对方独有内容 → §10 H0 加两条前提。
2. **审计自指闭环缺失**：分发 → 审计 → 回滚 → 再分发 成环 → §5.5 三层豁免 + §7 表。
3. **自评口径**：收回"全部落地"的自我评价。

### 第四轮（视角补全审核 · 未落地，待裁决）

**方法差异**：前 3 轮审"条款对不对"，本轮审"**机制跑起来对不对、边界清不清、谁才是唯一写者**"。

**T1 · 合并幂等性在计数类字段上不成立**
- 事实：§4.3 表里的"**幂等键**"列**是去重键，不是运行键**——它回答"两个条目是不是同一个"，不回答"这次合并是否已经应用过"。
- 后果：① 账本"合并后 revision = max+1 并追加一条 merge 记录"→ 重复 Arrive 会再 +1 并再追加一条，而 §5.5 明确写"允许重复 Arrive（幂等）"；② 坑库 `totalCount = Σ count_i` 直接相加 → 同一份对端数据合并两次即**计数翻倍**，而 count 正是权重、上浮、升级候选的输入。
- 建议落点：给每个 A 类对象加**运行键**（记 `{object, remoteCommit, mergedAt}`，同一 `remoteCommit` 已应用则整段跳过），或把计数类合并改成"从上次基线重算 + 覆盖写"；§11 增验收：**同一份远端数据连跑两次 Arrive，条目数 / count / revision 与跑一次完全一致**。

**T2 · "单写者"在单机内部不成立——独立计划任务在宿主停机窗口照跑，前提被架空**
- 实测：重启巡检每 **1 分钟**、告警巡检每 5 分钟、文件系统巡检每 10 分钟、坑库巡检每 30 分钟、外部信号每周一次，另有日报 / 周报 / 观测台账共 8 个——**全部是独立系统计划任务，不依赖宿主运行**。
- 后果：① §5.1 A1 的前提"宿主未运行，可安全改文件"**不等于"没有别的东西在写"**：坑库巡检正在写 A 类对象，撞 A5 / A6；文件系统巡检会把 A6 刚分发的长期约定 / 技能 / 文档判成"未审计改动"回滚；② 重启巡检每分钟都可能把宿主拉起来，与 Arrive-A 撞车；③ §5.3 第 6 条的互斥清单只覆盖四类**新**触发，**既有 tick 未纳入**。
- 建议落点：把调度器单例锁扩到**全部管家 tick**，交接单增记"交接期内被跳过的 tick 清单"；重启巡检在交接窗口内必须回 `busy`、不得拉起宿主。

**T3 · 重复 Arrive 会静默覆盖本机在两次 Arrive 之间的改动（缺 Depart 的对偶门）**
- 事实：落地分发是"工作副本 → 生产路径"，而 `git pull --ff-only` 只保护**工作副本**；生产路径的本机改动不受 git 保护。Depart 侧有前置检查，**Arrive 侧没有对偶检查**。
- 后果：重复 Arrive 会把本机在两次 Arrive 之间的合法改动静默回退成仓内版本——形态与事故 2 同款，只是被覆盖的是本机自己。
- 建议落点：分发之前加"**上一次交接单记录的 `applied[].sha256` vs 目标生产文件当前 hash**"比对；不一致 → 该文件列 `skipped(diverged)` + 告警 + 人工确认；生产独有文件默认不删（禁镜像语义已保证）。

| # | 级别 | 问题 | 建议落点 |
|---|---|---|---|
| T4 | 重要 | **双开检测率不足**：判据只在"本机 Arrive 进行中被更新"这一极窄窗口生效，检测率受 30 分钟热采周期限制 | 判据放宽为「`lastHeartbeatAt` 显著新于 `lastSuccessfulHandoffAt`」；并把"双开嫌疑期间禁止热采（fail-closed）"写成硬条款 |
| T5 | 重要 | **记忆库 RPO 未登记，且是 A 类里最差的**：会话有登记 + 每 30 分钟搬闭合会话，记忆库只在 Depart 冷采；方案自己在 §8 提到 `VACUUM INTO` 可在线备份，却把"不推"当结论 | 二选一：① 热采用 `VACUUM INTO` 导出快照；② 显式登记"记忆 RPO = Depart" + asOf 告警 + 拔电丢数据演练 |
| T6 | 重要 | **时钟门 fail-closed 无降级路径**：单侧长期漂移 → 账本 / 记忆库合并被永久拒绝，功能实际瘫痪（比覆盖事故更静默） | 加"拒绝合并期间的可用性降级"（只读继续 + 面板红条 + 一次自动校时）+ 纳入人工裁决四步 |
| T7 | 重要 | **`authority: snapshot` 弱来源语义未闭合**：只写了"不参与 `updatedAt` 胜负"，未定义 tombstone / `revision` / `recentRequests` 是否随快照进入 | §4.3 补一行白名单：快照来源**只允许贡献"本机不存在的卡 id"**，其余字段一律丢弃 |
| T8 | 重要 | **"交接进行中暂停磁盘层审计"缺超时、缺先声明后分发**：交接中断 → 暂停态永久残留 = 磁盘层审计静默失效 | 交接启动**先写 running 状态**再分发；running 状态加 TTL（如 30 分钟），超时自动恢复审计 + 🟡 |
| T9 | 重要 | **挡推（超集体检）缺误报处置与逃生舱**：只写"不合格 → 中止采集 + 告警"，假阳性会让 Depart 永久失败 | 挡推失败输出"差异清单 + 人工裁决入口 + 逃生舱（记审计）"，判据沿用"真内容独有行" |
| T10 | 建议 | 版本门数据源二选一未定；生态清单与既有登记构成**第四份真相** | 定死单一权威源；清单改为**生成器 + `--check` 对账模式** |
| T11 | 建议 | **被 abort / 被打断的会话算不算"闭合"未定义** → 若不算闭合，这类会话落入永不热采集合，RPO 退化为 Depart，而它们恰恰是最容易丢的 | §8 明确"abort 后尾部事件形态"的闭合判据；登记为新假设 |
| T12 | 建议 | 云中继引入**新的外部依赖**（"push 前强制检查点"），但失败分档没有"云端不可达"一档；手机端展示未要求 asOf / 陈旧标记 | 云中继前置补分档（云端不可达 → 交接照常、只降级中继功能）；手机端交接状态卡强制带 asOf |
| T13 | 建议 | 交接器的进程与停机脚本的"清理其余 node 进程"匹配规则需核对（交接器命令行含工具目录路径，有被当"其余 node"清掉的风险） | 开工前核停机脚本的匹配逻辑，把交接器进程显式排除 |

**决策级（需重新裁决）**

- **D1 · 砍范围**：云中继的角色②③（活跃机登记 / 交接中继）建议降级为只读展示。依据 = A1「不会同时开多机」若成立，角色②③ 的收益≈0，而成本 = 云端常驻 + 家庭↔云端新依赖 + 两类新失败模式；手机固定入口（角色①）才是最初的需求。**建议**：角色① 照做；②③ 先只做"云端只读展示 + 心跳可见"，**不做"push 前强制检查点"**（那是把云端变成写路径上的单点）。
- **D2 · 存量 id 分叉缺收口动作**：主机分段发号只治未来，但两侧历史条目 id 空间已重叠且内容不同，现规则给出的是"拒绝自动合并 + 告警"——即**永久红灯**，A 类"必须跨机"在此落不了地。**建议二选一**：① 一次性收口（历史条目按 host 归属重标到新段，同时全局替换外部引用）；② 改口径为"内容哈希主键、id 仅作显示别名"。

**本轮边界与不承诺**：本轮为**文档级审核 + 计划任务清单实测**，未做实机复现；T1 / T2 / T3 的复现方式与实测归实现阶段前置 spike；本轮**未复核**前三轮已登记项，也**不承诺"全部落地"**。
