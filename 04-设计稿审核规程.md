# 设计稿的多轮审核规程：五轮视角 + 横切四表 + 五环演练

> **版本**：v1.7（本仓新增）· **性质**：方法论 + 可复跑检查清单（不是某一份方案的具体结论）
> **写作视角**：给"写完一份工程方案、准备开工之前想把它审一遍"的人——五轮各换一个视角，末尾再加一道落地后对标闸门。
> **适用**：任何要开工的工程方案（跨组件设计 / 插件设计 / 迁移方案 / SOP / 交接协议…），用在**方案定稿、开工之前**。
> **占位符说明**：文中 `<工作区根>`、`<宿主主目录>`、`A 机 / B 机`、`steward-*` 等均为占位名；文中所有实测数字都是"当时的事实"，不是设计参数。
> **由来（中性转述）**：这套规程来自一次真实复盘——一份方案连续经过多轮审核，之后仍然暴露出结构性问题。归因结论不是"审得不够多"，而是**每一轮都在同一个视角里加深**；于是把散落在各方案修订史里的既有视角收拢成可执行清单，并补上"横切四表 + 五环"与"前提实证"两轮。
> **一句话**：**五轮各审一层，谁也替代不了谁**——任何一轮"全部落地"都不构成合格证。

---

## 0. 总览：一套完整审核 = 5 轮 + 1 道闸门

| 轮次 | 视角来源 | 一句话在干什么 | 检查清单 | 典型产出 | 谁审合适 |
|---|---|---|---|---|---|
| **轮一 · 工程契约** | "专业团队从零做这个系统"框架 | 把工程上迟早要的契约一次性补齐 | §1 | 安全/可观测/并发/状态文件/假设登记/Non-goals 成条 | 作者自审可做 |
| **轮二 · 实现者视角** | "要写代码的那个人" | 每条条款**照字面实现**会立刻撞上什么 | §2 | "照做就出问题"清单（改起来最便宜，价值最高） | 建议换人/换模型 |
| **轮三 · 运行期** | 并发 / 时间 / 容量 / 崩溃 | 跑起来之后它会怎么坏 | §3 | 竞态、时钟、TTL、RPO、体积、GC 类意见 | 外部模型/换视角 |
| **轮四 · 横切四表 + 五环** | 归属 / 时序 / 质量 / 影响面 | 机制**跑起来对不对**、边界清不清、闭环没闭环 | §4 | 阻断级结构性意见 | 作者+交叉审 |
| **轮五 · 前提实证** | "以为有其实没有" | 把方案依赖的外部事实**跑一遍命令验掉** | §5 | 实证表（假设 / 证据 / 结论 / 退路） | 任何人，命令说话 |
| **闸门 · 实现后对标** | —— | 落地时确认"真按方案做了"，并兜住实现期野参数 | §6 | 条款打勾表 + 偏差清单 | 落地者 |

**推荐执行顺序**：轮五（几分钟先排雷）→ 轮二 → 轮三 → 轮四 → 轮一（补齐契约）→ 落地后跑闸门。
理由：先验前提（不然后面全建在沙子上）→ 再看实现 → 再看运行 → 再看横切 → 最后补工程契约。

**选轮矩阵（小方案别硬跑全套）**：

| 方案规模 | 建议轮次 |
|---|---|
| 单文件 / 单脚本改动（≤1 张执行卡） | 轮二 + 轮五 |
| 跨组件、有状态文件、有自动化 | 轮二 + 轮三 + 轮四 + 轮五 |
| 跨机 / 搬数据 / 动权限 / 有定时任务 / 要交接 | **全跑**（轮一~五 + 闸门），且轮四的影响面表必做 |

**总纪律**：轮次之间**互不构成合格证**；每轮登记只写"本轮暴露的新面"，**不许写"全部落地/全覆盖"**。

---

## 1. 轮一 · 工程契约轮（把迟早要的东西补齐）

**问法**：*"这套东西交给半年后的别人维护，它靠什么自我解释、自我约束、自我恢复？"*

来源：一份自治式运维管家方案的第一轮修订（依据"专业团队从零做这个系统"框架审核列出的 7 项）。检查清单八组：

| # | 检查项 | 具体问法 | 缺失的后果 |
|---|---|---|---|
| 1 | **安全与隐私** | 敏感数据清单（密钥 / token / PII 存在哪、能否进日志、展示是否掩码）；审计介质是否可抵赖（**如实写**：哈希链防损坏、不防有权限篡改）；动作主体权限矩阵；fail 策略表；**爆炸半径与回滚验证** | 密钥进日志、自动动作无人可挡 |
| 2 | **可观测性与 SLO** | SLI/SLO 清单（含越界动作）；指标命名（**跟业界 SemConv，不自造**）；`correlationId` 是否贯穿全链路；**数据新鲜度 `asOf`**（"显示旧数据比不显示更危险"）；采集开销预算（内存 / CPU / tick 耗时）；**告警可执行模板**（现象 / 影响 / 建议动作 / 依据，缺一段即不合格） | 出事看不到、看到看不懂、看懂不知道干什么 |
| 3 | **并发与原子写** | 单例 tick 互斥（锁必须**原子创建**，禁 `existsSync` 再写）；原子写（tmp → fsync → rename，**绝不退化为直写**）；状态读写唯一入口 | 双跑、半截文件、丢更新 |
| 4 | **状态文件清单** | 每个状态文件：持久 / 易失、retention、`schemaVersion`、**丢失后果**、**是否允许跨机同步** | 临时目录被清 → 降档态丢失 → fail-safe 反转 |
| 5 | **假设与验证时机** | 每条"待验证"的能力登记：假设 / 风险 / 验证方式 / 时机（哪个执行卡开工前）/ **失败退路** | 开工后才发现路走不通 |
| 6 | **Non-goals 显式排除** | 明写"不做什么"，防后人"以为有" | 缺失被当成漏洞，或重复造 |
| 7 | **工作量口径** | 按"实现 + 实测 + 文档"三段计，高风险件（核心守护、拦截器类）按 ×2~2.5 | 排期崩、半成品上生产 |
| 8 | **回归项** | 每个能力给出自己的单测清单（两边都要测：正例 + 反例） | 只测 happy path |

**实例（第一轮补齐的七项）**：建模地基四件（因果 id / 状态 / 依赖边 / 来源）、安全与隐私、可观测性与 SLO、维护窗口（防重启触发自身告警）、单函数 dry-run + 动作主体校验 + 爆炸半径、误报抑制表 + 可延迟维度 + 确认与升级 + 误报反馈、状态文件清单 + 假设表 + 砍单线 + 工作量口径 + 各能力域回归项。

---

## 2. 轮二 · 实现者视角轮（条款照字面实现会怎样）

**问法**：*"把这条条款交给要写代码的人，他照字面实现，第一步会撞上什么？"* ——**不是问"能不能做"，是问"照做会立刻出什么问题"**。

来源：某方案审核章节的第一轮（工程化自审，含 4 项关键前提实证）与第二轮（独立交叉审核：阻断 9 / 重要 12 / 建议 5）；另一份方案的第二次独立审查（19 条）同理。检查清单六组：

| # | 检查项 | 问法 | 实例（真实命中） |
|---|---|---|---|
| 1 | **执行点与时机** | 这条挂在哪个执行点？**执行时目标资源处于什么状态**？ | "落地"动作挂在启动链尾部 → 那时主程序已在跑，直接撞账本文件的独占锁与数据库的活动连接；体检写在采集之后 → 采集就地覆盖工作副本 → 体检恒通过 |
| 2 | **照字面的即时后果** | 按字面实现，第一次跑会发生什么？会立刻拒绝多少东西？ | "未登记即拒绝采集"照做 → **立刻拒绝 18 项**核心文件（含凭证 / 配置 / 技能目录）→ 改为"两张表 + 对账，报告不拒绝" |
| 3 | **载体与接口存在性** | 要调的 API / 命令 / 字段**真的存在吗**？语义是什么？ | 桌面提示插件**没有"显示任意文本"的端点**（推送方案落不了地）；任务看板的排期接口 patch **只有 `{enabled, cron}`**，没有"单次改期"语义（方案文案不成立）；给会话发消息**不打断**当前回合（结论对、理由写错） |
| 4 | **语义自相矛盾** | 同一件事的两处条款字面是否互斥？数字是否互相打架？ | 心跳两处条款（"过期→🔴" vs "新鲜→告警"）字面互斥；超时预算"整轮 120s"与"单仓 180s"矛盾 |
| 5 | **比较与校验口径** | 哈希 / 行尾 / 编码 / 时区 / 路径，比较时会不会**必然误报**？ | 仓库 `core.autocrlf=true` → 文本直接 sha256 必然误报；审计命名正则不含 `-` → 124/124 全误报 |
| 6 | **边界与空态** | 空库 / 首次运行 / 无记录 / 新机 / 冷启动，走哪条分支？ | 无心跳记录（新机）= 放行、不告警；首次扫描属 bootstrap，不占 tick 预算 |

**产出特征**：这类意见**改起来最便宜**（多半改一句话或换个挂载点），且不改就会在开工第一天爆炸——所以排在第一批审。

---

## 3. 轮三 · 运行期轮（并发 / 时间 / 容量 / 崩溃）

**问法**：*"它上线 30 天后会怎么坏？"* 五个维度（来自一轮外部复审）：

| # | 维度 | 问法 | 实例 |
|---|---|---|---|
| 1 | **并发** | 谁可能同时在写同一份东西？触发源之间互斥吗？撞锁时的语义是等、跳过还是并发？ | 热采集在主程序运行中复制账本 → 撞锁/撕裂；四类触发源共用一把锁 + 排空（drain）语义 |
| 2 | **时间** | 用**哪个时钟**比较（墙钟 vs 单调）？超时预算多少？TTL 多长？RPO 覆盖到哪一刻？ | 账本合并按 `updatedAt` 胜负 → 依赖双机墙钟，漂移会让旧版本静默胜出；"一生只搬一次"对**重开**的会话失效 |
| 3 | **容量** | 体积会怎么长？加密 blob 有没有 delta？GC / retention / 换仓规程在哪？ | 加密后 git 存不下 delta → 同一会话每天留一份整本 |
| 4 | **崩溃与恢复** | 断电 / 崩溃落在流程哪一步？留下什么未闭合状态？怎么辨别与恢复？ | 心跳只有一个字段 → 无法区分"双开"与"崩溃没交接完" |
| 5 | **观测污染与自举** | 测试数据 / 历史数据会不会污染统计？有没有"鸡生蛋"的自举环节？ | e2e 测试拦截污染干预统计；加密密钥不可能放在自己被加密的仓里 → **自举悖论** |

---

## 4. 轮四 · 横切四表 + 五环演练

前几轮审的都是"条款本身对不对"，这一轮审**机制在真实路径上是否成立**：先取证，再横切扫，再逐机制走五环。

### 4.0 审核前三件取证（不做这三件，四表填不出来）

1. **机器实况取证**——凡"文档说 X"，去机器上验一次。手段：`schtasks /query`、`Get-Process`、`Test-Path`、`git log`、HTTP 探测、读生产状态文件、**看真实数据样本**。
   - 实例：某方案假设"跨机落地的第一步可以在主程序未运行时安全改文件"→ 一次调度查询（`schtasks /query`）查出 **8 个计划任务独立于主程序运行**（分钟级巡检 / 十分钟级巡检 / 半小时级学习 / 小时级外部信号各一档）→ 前提被架空。
   - 实例：某循环检测的阈值靠**近 6 小时 40 个真实会话**才定下来（仅 1 个会话重复率 0.624，其余 39 个 0.000）。
2. **参数与数字清单**——抄出所有阈值 / 周期 / 上限 / 预算，逐条问"最坏情况是多少？这个数字面向开销还是面向人的体验？"（一份方案写了 30 分钟 tick 与 <2s 预算，却没人算"撞坑后最坏 30 分钟才可见"）。
3. **载体清单**——列出全部事实载体，问"同一事实在几处存在、谁是权威"（记忆 / 坑库 / 变更流水都能记教训，边界从未定义）。

### 4.1 横切四表

> 速记：**归属（谁是真源）· 时间（多久能知道）· 质量（判断准不准）· 影响（动作干净吗）**

**表 1 · 载体归属表**——*这条事实写在哪几处？谁是权威源？其余是缓存/视图吗？冲突怎么裁？*

| 事实 | 载体 A | 载体 B | 权威源 | 冲突裁决 | 缺陷 |
|---|---|---|---|---|---|
| 例：任务卡状态 | 账本文件 | 面板缓存 | 宿主 API（唯一写口） | 乐观锁重放 | ✅ |
| 例：坑的经验 | 坑库条目 | 记忆条目 | ? | ? | 🔴 无权威源 |

**表 2 · 时序预算表**——*从"事情发生"到"使用者知道并能行动"，最坏多久？卡在谁身上？超界谁告警？*

| 事件 | 上界延迟 | 决定延迟的参数 | 可接受 | 超界告警 |
|---|---|---|---|---|
| 例：踩新坑 | 最坏 30 分钟 | 坑库学习任务的间隔 | 勉强 | 无 → 🔴 |
| 例：交接失败 | 直到回家 | 免打扰 + 本地通道 | 否 | 外部通知通道待建 |

**表 3 · 输出质量表**——*机制产出的判断/建议/清单/阈值，错了会怎样？谁度量它准不准？依据可追溯吗？*

| 输出物 | 准确率度量 | 依据可追溯 | 错了的代价 | 逃生舱 |
|---|---|---|---|---|
| 例：坑升级候选建议 | 无 | 启发式，无错误码映射 | 使用者被误导 | 人工纠正 |

**表 4 · 影响面 / 幂等表**——*这动作动谁？动一半失败留什么？重复执行会累加吗？"幂等键"是去重键还是运行键？*

| 动作 | 作用对象 | 半完成态 | 重复执行后果 | 幂等键类型 | 回滚范围 |
|---|---|---|---|---|---|
| 例：账本合并 | 账本 | 部分卡并入 | revision 再 +1、追加合并记录 | 去重键 ❌ | 整包 |
| 例：坑库合并 | 坑库 | 计数已加 | 总计数翻倍 | 去重键 ❌ | 整包 |

### 4.2 五环演练

对每个"检测 / 判定 / 动作 / 协议"按五环口述一遍，**每环必须有人、有出口**：

| 环 | 问句 | 常见断裂 |
|---|---|---|
| ① 触发 | 什么条件走到这一步？条件可测吗？字段真存在？ | 判据依赖的字段不存在 |
| ② 判定 | 判错会怎样？会静默吗？盲窗在哪？检测率 / 误报率各多少？ | 判据只在极窄窗口生效（双开检测只在落地窗口判） |
| ③ 动作 | **下一步是谁做、多久做完、没有能力做怎么办** | 检测到却无处置通道（循环检测只告警、无强制中断通道） |
| ④ 后果 | 对别人 / 别的数据做了什么？半完成态什么形状？ | 打断他人正在写的活（重启闸门打断正在写数据的会话） |
| ⑤ 失败 | 失败谁看得见？会不会静默失效？fail-open / fail-closed 的**可用性代价**谁承担？ | 暂停态无 TTL → 永久静默失效 |

> **重点**：③、⑤ 两环是文档审核最常整段跳过的两环，也正是"机制写了但没法用 / 用坏了没人知道"的高发区。

---

## 5. 轮五 · 前提实证轮（静态存在性："以为有其实没有"）

**问法**：*"这条假设里，哪一个是我'以为有'的？能不能用一条命令验掉？"*

**方法（四步）**：① 把方案依赖的外部事实列成假设清单 → ② 每个假设写一条**最小复现命令**（查端点列表 / 查源码字段 / 打一次 API / 读一次文件） → ③ 跑，**记录实际输出** → ④ 结论写进实证表，失败的写退路。

| 假设 | 验证命令（示意） | 结论 | 退路 |
|---|---|---|---|
| 桌面提示插件有"显示任意文本"的端点 | 抓插件注册的端点清单 | ❌ 只有状态 / 挂件 / 指令 / 桌面 / 素材等端点，没有该端点 | 先给插件打补丁补端点，否则推送降级为会话+报告 |
| 任务看板支持"单次改期" | 读排期接口的 patch 字段 | ❌ 只有 `{enabled, cron}` | 文案改"改 cron"，不写具体日期 |
| 发消息能打断当前回合 | 读会话接口的 `prompt(content, mode)` 语义 | ⚠️ 排队不打断；**真因是"通知会制造新回合"** | 结论不变（禁止通知活跃会话），理由改正 |
| 能指定任意会话发消息 | 打一次会话消息 API | ✅ 支持 | 多播无需新 API |

**纪律**：实证要写进方案文档（假设 / 证据 / 结论 / 退路），**不能只藏在会话里**——它正是后面几轮的地基；最高风险级的假设（如"使用者不会同时开两台机器"）还要给观察手段与观察期。

---

## 6. 闸门 · 实现后对标（落地时必做，别审完就散伙）

审核止于方案定稿 = 实现期成了无人区。落地时补这一步：

1. **条款打勾表**：把方案条款抄成表（条款 / 实现状态 / 证据 / 偏差），随卡归档，作为"真的按方案做了"的证据。
2. **实现期新增物单独列一栏**——审核从没见过的野参数高发区：新阈值（循环判据 3000 字符 / 重复率 0.3）、新依据（外部信号源清单改由"可用性探测"驱动）、新启发式（坑库候选建议的生成函数）、新间隔（热采集 30 分钟）……**实现时顺手定的东西最容易无人审**。
3. **偏差分两类**：① 实现偏离方案（某外部信号能力：方案写"使用者点名 + 与主程序同源"，实现按可用性重排、还漏掉一个供应商）→ 要么改回、要么改方案，但**必须显式二选一**；② 方案本身错 → 回写方案文档。
4. **结论留痕**：写进方案文档的"实现后对标"小节，或卡验收里写"逐条已对标，偏差 N 处见 …"。

---

## 7. 通用纪律（五轮共用）

### 7.1 输出规范（意见怎么记，比意见本身更影响可用性）

**分级**：**阻断 / 重要 / 建议 / 决策级**。决策级 = 需使用者重新裁决（范围、口径、取舍、代价权衡）——单列、写清"代价 vs 收益"，**不夹带、不代办**。

**每条四要素**（缺一条就容易变成下一轮的重复劳动）：
1. **事实**（含证据：文件章节号 / 实测命令与输出 / 数据样本）
2. **后果**（不改会怎样，具体到场景）
3. **为什么前几轮够不到**（视角差异，而不是"他们没看出来"）
4. **建议落点**（改哪节 / 加什么字段 / 增哪条验收）

**登记位置**：写进方案文档的审核章节。

- **不要给未拍板的意见单独建卡**——那等于把"意见"升格成"承诺"，还制造第二份真相（方案里一份、卡里一份）。
- 正确姿势：意见留方案文档 → 使用者裁决 → **随方案自身的执行卡一并吸收**（进前置条件或验收清单）。

**措辞纪律**：不写"本轮全部落地 / 已全部覆盖"；轮次头写清**视角声明**（这轮换了什么视角），一眼看出是否又在同一层打转。

### 7.2 审查者与授权

- **自审 / 交叉审 / 外部模型分开记**：自审擅长文档矛盾、路径、计数；交叉审（换人换模型）抓机制缺陷；外部模型适合"给它一个视角，让它只从那个视角找"（一轮外部复审就是这样补上了运行期的全新面）。
- **授权松绑**：若授权被限定成"不动已拍板结论"，结构性 / 方向性问题必然全漏。正确写法 = 「**可以提推翻建议，标决策级交使用者裁决**」。（某方案曾连续七轮审核，每轮落款都是"已拍板结论一律未动"——这就是盲区的制度成因。）
- **不许自称全覆盖**：覆盖矩阵（"全链路覆盖、无意外盲区"）回答的是"**有没有机制**"，**不回答"机制好不好"**——一次复盘里的六条问题，全部落在被标 ✅/📋 的格子里。

### 7.3 反模式速查（都是踩过的）

| 反模式 | 症状 | 解药 |
|---|---|---|
| 纸面审 | 意见全是路径 / 计数 / 措辞 | §4.0 三件取证 |
| 覆盖=合格 | "✅ 全覆盖，无意外盲区" | 轮四 |
| 单一 KPI | 观测窗口只盯一个指标（试运行期只盯误报率） | 表 3 输出质量 |
| 摘要互锁 | 每轮都写"全部落地" | §7.1 措辞纪律 |
| 意见建卡 | 未拍板意见进了看板 | §7.1 登记位置 |
| 实现野参数 | 实现期定的阈值无人审 | §6 逐栏 |
| 事实单点使用 | 同一事实只在某一节用（"没有强制中断通道"只写在超时预算处，没迁到处置环节） | 四表横向扫 |
| 预言不自我应用 | 方案写了正确的失败模式，却没拿它扫自己的机制 | 五环逐机制过 |
| 只审一层 | 连续几轮都在同一视角里加深 | §0 选轮矩阵 + 轮次视角声明 |

---

## 8. 实例对照（写新方案时可对照）

- **归因**：一份内部复盘文档的归因章节 —— 六条问题 × 为什么七轮没看出来。
- **应用**：一份跨机交接方案（轮一~三共 52 条 + 轮四 15 条）。
- **最硬的一条示范（轮四）**：不是读文档读出来的，是 `schtasks /query` 查出来的——8 个计划任务独立于主程序运行 → 方案的"单写者"前提在**单机内部**就不成立。
- **第二条示范（轮四）**：纯横切推理——方案在两处硬要求"幂等"，而"幂等键"那一列其实是去重键 → 重复执行使 revision / 计数累加。
- **轮二示范**：排期接口的 patch 只有 `{enabled, cron}`（读源码）→ 方案里"从 09-15 改到 09-20"的文案不成立。
- **轮三示范**：心跳单字段无法同时表达"对端还活着"与"对端把状态放好了" → 拆双字段。

---

## 9. 最小执行版（时间紧就做这五步）

1. 抄**参数清单** + 查一遍**机器实况**（§4.0）；
2. 四表**各填一遍**（哪怕每张只有 3 行）；
3. 挑 3 个最关键的机制**走五环**（优先挑带写操作、带计数、带超时的）；
4. 意见按 §7.1 的**四要素 + 分级**写进方案文档；
5. **决策级单列**交使用者，其余随方案一起建卡。

---

## 附 · 意见条目模板（直接复制）

```markdown
**意见 #N · <一句话结论>**

- 事实：<证据：文件 §x / 实测命令与输出 / 数据样本>
- 后果：<不改会怎样，具体场景>
- 为什么前几轮够不到：<视角差异>
- 建议落点：<改哪节 / 加什么字段 / 增哪条验收>
- 分级：阻断 | 重要 | 建议   （决策级单独一节：代价 vs 收益 + 需重裁的拍板项）
```

## 附 · 一页速查卡

```
轮一 工程契约：安全隐私 / 可观测 SLO / 并发原子写 / 状态文件清单 / 假设登记 / Non-goals / 工作量 / 回归项
轮二 实现者视角：挂在哪个执行点 · 照字面实现会拒绝什么 · 接口字段真存在吗 · 两处条款打架吗 · 比较口径必然误报吗 · 空态走哪支
轮三 运行期：谁同时在写 · 用哪个时钟/超时/TTL/RPO · 体积怎么长怎么 GC · 崩溃留什么 · 测试数据污染吗
轮四 横切四表：归属 / 时间 / 质量 / 影响面；五环：触发→判定→动作(谁做/多久/没能力怎么办)→后果(半完成态)→失败(会不会静默)
轮五 前提实证：哪条是"以为有"？一条命令验掉它
闸门 实现后对标：条款打勾 + 实现期新增阈值/依据单独列栏

三取证：查机器实况 · 抄参数清单 · 列载体清单
三纪律：不自称全覆盖 · 不给未拍板意见建卡 · 不动已拍板结论≠不动结论（决策级单列）
```
