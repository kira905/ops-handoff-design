# 一次错误的性能归因，与更正后的结论

> **这篇文档讲什么**：一次"多会话并行就跑卡"的性能排查——**初版把 CPU 打满归因给缓存全量重写，复测后当场推翻自己**，并在正文里保留了那版错误归因与更正过程。它更想证明的不是结论，而是**结论是怎么被推翻的**。
> **版本**：v1.7（**已于 2026-09-20 随本仓 tag `v1.7` 双端发布**；本行原写"随本仓下一版发布 / 尚未打 tag"，属撰写时点口径，现按发布实况订正） · **实测基准日**：2026-09-17
> **占位符说明**：`<DSH 主目录>` = agent 的数据主目录；`<工作区根>` = 工作目录。本文出现的组件一律用泛称（"上游缓存包""JSON 存储后端""宿主 API 代理层"）。
> **数字口径（重要）**：本文**全部实测数字都来自一台特定机器上的一次观测**——**环境相关，勿照抄**。它们的价值在于"量级与判断方式"，不是阈值。照抄 110% 或 0.8% 去判断你自己的环境，等于把一次观测当设计参数。

---

## 0. 先看结论（第一条就是"我原来写错了"）

1. **初版归因是错的，且被自己的复测推翻**：初版称"主进程 110% CPU 是被缓存的全量重写占满"。复测测得单次全量序列化只有 **9.7 ms**，按当时的写入频率折算**约 0.8% 单核**——**0.8% 解释不了 110%**。
2. **真正的相关变量是"正在运行的会话数"**，与缓存体积无关：同一台机、同一份缓存，只改并行会话数，CPU 就从 12% 走到 110%。
3. **那套改动仍然有价值，但不是原来宣称的那个价值**：磁盘写入从 1.9 MB/s 降到 0（真实、长期）；**对 CPU 的贡献约 0.8%，对卡顿没有实质改善**。
4. **对症的方向是并行度**（独立实例 / 少开并行会话 / 控制单会话长度），不是继续优化缓存写入。
5. **方法论上的收获**：一张"看起来全是收益"的前后对比表，可以把错误归因养得很好看——**因为它缺一个被排除的混淆变量**。

---

## 1. 症状与初版归因（错的那一版）

**症状**：并行开 3 个以上会话开始有体感变卡；会话自己在跑任务时又开子程序，卡得更明显。

**初版的观测与推理**（这些观测本身都是真的）：

| 观测项 | 改造前取值 | 手段 |
|---|---|---|
| agent 主进程 CPU | **稳态 110%**（占满 1 个逻辑核；本机 22 核用不上） | 两次读取进程累计 CPU 后求差值 |
| 主线程 CPU | 一条线程累计 9515 s，状态 Running | 按线程累计 CPU 排序 |
| 线程池 | 4 条线程各烧约 1000 s | 同上（说明有密集的 fs / crypto 工作） |
| 会话投影缓存文件 | **2217 KB / 651 条记录**，而磁盘上真实会话只有 129 个 | 解析缓存文件 + 递归扫描会话目录 |
| 该文件重写频率 | **20 秒重写 17 次**（≈ 0.85 次/秒 × 2.27 MB） | 每 500 ms 轮询文件修改时间 |
| `session.list` 单次耗时 | **3.0 / 5.6 / 7.0 / 13.3 / 14.4 秒**（波动剧烈） | 对宿主 API 计时 |
| `session.list` 期间主进程 CPU | **13.94 s CPU / 14.36 s 墙钟 = 97%**，期间读盘 0 次 | CPU 差值 + 读操作计数 |
| `session.list` 分页参数 | `limit` / `activeOnly` **被直接忽略**，恒返全量 | 带参调用实测 |

初版由此推出：*110% 就是"每 20 秒 17 次、每次 2.27 MB 的全量重写"打出来的*。

**这个推理的漏洞**：它只证明了"写入很频繁"，**没有量过"每次写入有多贵"**——把频率当成了成本。

---

## 2. 归因更正：复测把自己推翻了

复测只做一件事：**测一次全量序列化到底要多久**。

| test | 结果 |
|---|---|
| 2274 KB 缓存单次全量序列化 | **9.7 ms**（不是几百毫秒） |
| 按改造前实测写入频率 0.85 次/秒换算 | **仅 0.8% 单核** |
| 对照：改造前实测稳态 | **110%** |

**0.8% 解释不了 110%。** 归因链条到这里就断了。

> **更正后的表述**：*"缓存全量重写是我 CPU 打满的原因"* 是错的。它确实在制造**无谓的 I/O 与 SSD 磨损**，也确实在拖慢响应（排队效应，见 §10），但**它不是 CPU 打满的来源**。

---

## 3. 真正的相关变量是「正在运行的会话数」

把变量换成"并行度"之后，相关性立刻出来了——**同一台机、同一份缓存，只变正在运行的会话数**：

| 时刻 | 正在运行的会话 | CPU 实测 | `session.list` 实测 |
|---|---|---|---|
| 改造前 | 4 个 | 110% | 3～14 秒 |
| 刚重启（只剩 1 个） | 1 个 | **12%** | 2.8～3.1 秒 |
| 之后（又开起来） | 3 个 | **88～109%** | 3.2～4.2 秒 |

**正确结论：CPU 与延迟随"正在运行的会话数"走，与缓存体积无关。**

机理也不神秘：agent 主进程是**单线程事件循环**，要并发跑 N 个重负载会话。当时那 3 个运行中会话的事件序列长度分别是 **112462 / 178222 / 23774**（seq）——**单会话越长，每轮请求的成本越高**。3 个这样的会话并行，就足以把 1 个核跑满，于是每个会话的交互都变慢。

据此，对症的处置是「**并行度**」方向，而不是继续优化缓存写入：

- 需要真并行时开**独立实例**（进程级隔离，各吃各的核）；
- 或**减少同时运行的会话数**；
- 或**控制单会话长度**（长会话不是免费的，它的每轮成本随时间序列长度线性上涨）。

---

## 4. 另一处测量解读修正

初版还写过一句"`session.list` 期间主线程 CPU 占 97%，所以是这个 API 把主线程占住了"。

**这句解读也不成立**：`Get-Process.CPU` 这类取值量的是**整个进程**的 CPU。那段时间**其他正在运行的会话同时也在烧 CPU**——所以那个 97% 是"进程总共烧了多少"，**不是 `session.list` 独占的证据**。

> 这是同一类错误的第二个样本：**用一个粗粒度指标去证明一个细粒度结论**。同一篇记录里出现两次，说明它不是我偶尔手滑，而是"观测工具的量纲没有和被解释的对象对齐"。

---

## 5. 那么这套改动还剩什么价值

诚实记账（不夸大）：

- ✅ **磁盘写入 1.9 MB/s → 0**：确定的、长期的收益（SSD 寿命 + 消除无谓 I/O）；
- ✅ **缓存陈旧记录不再累积**（651 → 127 条），文件 2217 → 459 KB，**内存与加载开销下降**；
- ✅ **无谓排队消失**：改造前 `session.list` 的 14 秒里，有一部分是"主线程被写入占住后的排队放大"，那部分被消掉了（见 §10 的残留问题）；
- ❌ **对 CPU 的贡献约 0.8%，对卡顿没有实质改善**。

**为什么仍然保留这套改动**：它的收益是真的，只是**和最初宣称的那个收益不是同一个**。清理缓存与调低写入频率本身没有副作用，代价只是一次停机窗口（§8 C）。

---

## 6. 初版给的根因链（哪几环成立、哪一环不成立）

初版列了 5 环，逐环复核如下：

| 环 | 内容 | 复核结论 |
|---|---|---|
| 1 | **单线程天花板**：所有会话、SSE、工具执行、缓存写入共享一个事件循环 | ✅ 成立，**且这才是主因** |
| 2 | **缓存只涨不缩**：上游缓存包文档明说"不提供淘汰或保留接口，记录会随会话持续累积" → 651 条里 524 条是陈旧记录 | ✅ 成立（所以有了 §8 A） |
| 3 | **JSON 后端全量重写**：上游 JSON 存储后端"每个写入原语都通过临时文件 + fsync + 原子 rename **替换重新发布整个文件**"，其文档自己写明"规模问题应换 SQLite 后端" | ✅ 成立，但**单价只有 9.7 ms**（§2） |
| 4 | **节流按会话计**：某类事件是无条件必写点，而节流计数器是**每会话一份** → N 个会话并行 ≈ N 倍写入频率 | ✅ 成立（所以有了 §8 B） |
| 5 | 三者相乘 ⇒ 卡的程度 ≈ 并行会话数 × 会话总数 | ❌ **不成立**：第 2–4 环的单价太低（0.8%），相乘也到不了 110%；真实关系只有"并行会话数"这一项 |

**教训**：一条根因链里，"每一环都是真的"**不等于**"这条链成立"。链条断在**量级**上，而不是断在事实上——所以复核必须有一步是**算单价**。

---

## 7. 落地动作 A：清理陈旧缓存

新增一个只读优先的清理脚本：

- **默认 dry-run**，`--apply` 才写；写前自动备份 + sha256 校验；原子替换（同目录临时文件 → rename）。
- **严格复刻上游的序列化格式**（单元元信息 + 全局区 + 表区，`JSON.stringify(doc, null, 2)` 且末尾带换行）——**格式不还原，后端下次加载就会拒绝整个文件**。
- **判据**：只保留 id 出现在"当前会话目录"或（加 `--keep-archived` 时）"归档会话目录"下的记录。
- 实测：**2217 KB → 459 KB（约 21%），651 → 127 条**。

## 8. 落地动作 B：调低写入节流

走**用户补丁层**（插件配置的用户覆盖层），不动上游包本体：

```yaml
- id: session-projection-cache        # 上游缓存插件
  config:
    writeEveryEvents: 2000           # 原 200
    writeIntervalMs: 60000           # 原 5000
```

**为什么走补丁层**：升级上游包时，补丁层里的用户覆盖会被保留，改上游包体则会在下次更新时被冲掉。

## 9. 落地动作 C：把"停机作业"做成能自己兜底的链路

这一步的难点不是清理，是**清理必须在进程停止时做**：该 JSON 后端的语义是"**内存中的单元状态具有最终决定权**"，运行中清理会被内存状态整份写回。而常规重启闸门的语义是"杀掉即拉起"，**中途没有可操作窗口**（且发起者自己会被打断）——所以不能复用它。

做法：

- 脚本由 **VBS 直接启动 node**（不经由 agent 会话），完全脱离宿主生命周期；
- 脚本内部先 spawn 一个 **detached 看门狗**：150 s 后若服务端口仍无监听，兜底执行"启动守护进程"，**杜绝"服务死了、守护进程也停着"的瘫痪态**；
- 主流程：看门狗 → 停守护进程 → **`taskkill /PID <pid> /F`（不带 `/T`）** → 等端口释放 → 清理脚本 `--apply` → 启动守护进程（带"手动开机"标记，避免哨兵把手动启动当成异常重启）；
- 全过程写日志，事后可按时间点回查。

---

## 10. 上生产前的隔离验证

- 用**上游 JSON 存储后端的真实加载类**去加载清理产物：加载成功、127 条记录、逐条的标识 / 行 / 版本字段 schema 断言全过 ⇒ 排除"介质损坏""版本不匹配"两类风险。
- 全程在**临时目录**里操作，未触碰生产数据目录。

> 这一步是这类改动的准入门槛：**"改状态文件"的脚本，没有经过真实后端加载验证，不上生产。**

---

## 11. 残留问题（未解，也**不建议**继续动）

`session.list` 仍有约 **3 秒**。成因是宿主 API 代理层会对**129 个冷会话逐个**做一次空白探针 + 投影计算（按固定批大小分批），属**上游代码的固有成本**；且该 API **没有分页**（`limit` / `activeOnly` 被忽略）。

改造前那 14 秒 = 上述固有成本 **+ 主线程被缓存写入占住后的排队放大**；后一部分已经消掉了，前一部分**动不了**（改上游代码的收益小于维护成本，且升级即冲掉）。

> **这也是本次归因更正的一个副产品**：把"14 秒"拆成"固有成本 + 排队放大"之后，才知道哪一半是自己的、哪一半不是。

---

## 12. 踩到的坑（4 条，可直接复用）

| # | 坑 | 表现 | 处置 |
|---|---|---|---|
| 1 | **备份目录名尾部带点** | 用 `new Date().toISOString().replace(/[-:T]/g,'').slice(0,15)` 生成时间戳，得到 `20260917090208.`（含尾点）；Windows 建目录时**静默去掉尾点** → 打印出来的回滚路径指向一个**不存在的目录** | 改用本地时间格式化，**不要拿 ISO 串切片当目录名** |
| 2 | **幽灵目录** | 上条产生的尾点目录建出来后，常规 API（`Test-Path` / 列目录）**解析不到它**，只有 `cmd dir` 列得出来 | 清除需用扩展路径前缀：`cmd /c rd /s /q "\\?\<绝对路径>."` |
| 3 | **`taskkill` 中文输出乱码** | 作业日志里 `taskkill` 的 stdout 是 GBK，混进 UTF-8 日志成乱码（**无实害**） | 判据看返回码，不看它打印了什么 |
| 4 | **重启闸门插不进停机窗口** | 闸门语义是"活跃 = 0 立即重启"，**没有可操作的中间窗口**，且发起者自己会被排除 / 打断 | 需要"停机做事"时**不能复用重启闸门**，要单独做停机链路（§9） |

---

## 13. 复发预防

- **陈旧记录会重新累积**（上游无淘汰接口）⇒ 定期**只读预演**一次清理脚本，看"陈旧记录"条数；条数重新上量，再走一次本流程。
- 写入节流已是 2000 事件 / 60000 ms；若仍嫌写入频繁，**只调大这两个值、不动别的**（上游配置 schema 要求这两个值不小于 1）。

---

## 14. 方法小结（本篇真正想留下的东西）

1. **测频率 ≠ 测成本。** 知道"每 20 秒写 17 次"不等于知道"写一次多贵"——**单价必须单独测**。本次归因错误就死在这一步。
2. **前后对比表缺一个被排除的混淆变量，就足以养出一个错误归因。** 本次的"改造前 110% → 重启后 12%"看起来像收益，**实际变量是并行会话数从 4 降到 1**。
3. **量纲要对齐。** 用"整进程 CPU"去证明"某个 API 独占主线程"，量纲不匹配——同一篇记录里犯了两次。
4. **推翻自己的结论要写进正文，不要悄悄改掉。** 保留错误归因 + 更正过程，比只留一个正确结论对读者更有用：**它示范了"发现错了以后怎么办"**。
5. **改对了东西、但没解决原问题，也要如实记账。** "0.8% 的收益 + 真实但不同的价值"必须写清楚，否则下一轮排查会用错误的因果关系继续推理。
6. **对状态文件下手，先验证加载、再谈清理。** 并用真实后端做隔离验证（§10），不在生产目录上做实验。

---

*本文由内部实测记录派生，为独立副本；内部原稿含完整环境标识，仍是唯一权威源。*
