# 发布说明（发布形态 · 渠道 · 版本策略 · 多仓拓扑）

> **状态：已发布（Gitee + GitHub 双端）**
> - Gitee（镜像）：<https://gitee.com/kira905/ops-handoff-design>
> - GitHub（主）：<https://github.com/kira905/ops-handoff-design>
> - 当前 tag：`v1.0`（首次发布）→ `v1.1`（多仓拓扑）→ `v1.2`（地址表）→ `v1.3`（账号名统一）→ `v1.4`（中英双份 README + 翻译政策）→ `v1.5`（新增 03 全景文档 + 02 指向行 + 许可口径同步）→ `v1.6`（全景稿重写 · 服务面与家规 R6–R8 · 三份文档统一改名）→ `v1.7`（新增 04 审核规程 + 05 性能归因实测记录）→ `v1.8`（新增 06 只读监控面板的设计纪律）
> - **`v1.8` 已于 2026-09-21 双端发布**（提交 `8233ef4`，Gitee + GitHub 两端的 `main` 与 `tag v1.8^{}` 均指向它）
> - **下一版 `v1.9`（内容已在本地就绪，尚未打 tag）**：新增 `07-放行判据与开关矩阵.md` 与 `08-文档体系口径一致性审核.md`（均为生产文档的脱敏派生稿）
> - 两端账号名已统一为 `kira905`（原 GitHub 账号 `kira905-cloud` 已改名，旧地址 301 重定向）
> 编制日期：2026-09-11

---

## 1. 发布内容与形态

### 1.1 发布单元

**一次发布 = 一个文档仓**，内容为本目录下的 11 个公开文件（README 中英双份 + 八份文档 + 发布说明）+ 1 份许可文件：

```
<发布仓根>/
├── README.md                          # 索引 + 阅读指引 + 许可说明
├── README.en.md                       # 英文版 README（GitHub 按浏览器语言自动优先展示）
├── 01-多机交接与云中继.md
├── 02-自治式运维管家.md                # 2026-09-11 设计快照（头部指向 03）
├── 03-体系全景与家规.md                # 更新后的全景（一个班底两个面 · 五层架构 · 家规 R1–R8 · 路线 · 缺口账本）
├── 04-设计稿审核规程.md                # 五轮视角 + 横切四表 + 五环演练 + 落地后对标闸门（怎么审一份方案）
├── 05-一次错误的性能归因.md            # 实测记录：初版归因被自己的复测推翻 + 如实记账 + 方法小结（怎么自我更正）
├── 06-只读监控面板的设计纪律.md        # 只读面板的设计纪律：该回答什么 + 显示纪律（不可探测 + 原因）+ 取数工程 + 只渲染不复算 + 什么该砍
├── 07-放行判据与开关矩阵.md            # 从"建好"到"敢开"：门槛算法（跨机并集取样、按机放行、缺数据不放行）+ 动作→风险级→开关→护栏→放行判据矩阵 + 冻结面
├── 08-文档体系口径一致性审核.md        # 一套长期演进的文档体系怎么审：过期 / 冲突 / 无人定义三类判据 + 四张横切对照表 + 处置与回写纪律
├── 发布说明.md
└── LICENSE                            # 文档许可 = CC BY-NC-SA 4.0
```

**不发布**：`脱敏清单与删改对照表.md`、`复核报告-<日期>.md`（生产侧留档，含本机标签）。

### 1.2 形态选择：为什么是"文档仓"而不是"放进代码仓的 docs/"

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **独立文档仓**（推荐） | 与代码侧解耦、可单独打 tag、读者不必 clone 代码 | 需要多维护一个仓 | ✅ **选它** |
| 放进代码侧仓的 `docs/` | 少一个仓 | ① 文档的核心读者（想设计自己体系的人）不需要装插件；② 代码侧的 release 节奏会绑架文档更新；③ 文档许可（CC BY-NC-SA）与代码许可（MIT）混在一个仓里容易误解 | ❌ |
| 只发到博客 / 论坛 | 传播快 | 不可版本化、不可 diff、链接易腐 | ❌（可作为**分发**渠道，不作为**权威源**） |

### 1.3 发布渠道（三档，按投入递增；**由所有者拍板**）

| 档 | 渠道 | 说明 | 前置 |
|---|---|---|---|
| **A · 最小** | 私有→公开的代码托管平台仓（Gitee / GitHub 任一） | 建仓、推送、打 tag；**注意：若沿用现有的私有备份仓，必须新建独立公开仓，绝不能把私有仓直接切公开**（私有仓里含会话/凭证/加密资产） | 建仓 + 许可署名替换 |
| **B · 加一步** | A + 一篇发布帖（社区版块 / 博客） | 发布帖只做"摘要 + 三个最反直觉的结论 + 链接"，不搬运全文 | A 完成 |
| **C · 完整** | B + 把两份文档的"审核意见落实记录"拆成单独文章 | 那两节的复用价值最高（"怎么看一份设计稿是否可信"） | B 完成 |

> ⚠️ **安全前置（必做）**：发布前必须确认——① 新仓**不含**任何私有仓的历史（新建仓而非改可见性）；② 发布用署名不是真实姓名 / 常用 ID 泄露组合（若在意匿名性，用项目名署名）；③ 全文再过一次 `脱敏清单与删改对照表.md` §五 的 grep。

---

## 2. 许可选择（二选一已择定）

**结论：文档 = CC BY-NC-SA 4.0；代码 / 脚本（组件仓）= 各自持自己的许可，当前主许可为 AGPL-3.0 + 商业授权（双许可）。**

| 候选 | 适合什么 | 本项目判断 |
|---|---|---|
| **MIT** | 希望被最大范围复用，包括被商业产品吸收 | ❌ 不用于**文档**：MIT 是软件许可，用在长篇方法论上语义不匹配（它不谈"演绎作品必须同协议共享"），且允许他人把内容打包进付费产品而不回馈 |
| **CC BY-NC-SA 4.0** | 希望**被学习、被引用、被改编**，但不希望被**商业套利**；且改编作品要保持开放 | ✅ **选它**：与方法论公开的意图一致——署名保住出处、非商业避免被拿去卖课 / 卖咨询而不回馈、相同方式共享保证改进回流 |

**两处细节**：

1. **代码侧的许可与本目录无关**——代码侧项目（抽插件通用化）**主许可为 AGPL-3.0**，两者并不冲突：**同一支线的两半可以用不同许可**（文档 NC、代码 copyleft 并另售商业授权，是常见组合：文档防止商业套利，代码在"开放使用"与"保留商业谈判资格"之间取平衡）。发布说明里要写清这一点，避免读者误以为"代码也不能商用"——**商业使用走商业授权**。
2. **若只想用一个许可**：用 **CC BY-NC-SA 4.0**（本仓已附）。它的代价是"示例脚本也不能商用"，需接受。

**取名与署名**：两份许可文件里的 `kira905` 占位符**发布前必须替换**；建议用项目名而非真实姓名署名。

3. **许可变更只约束未来版本（必须如实告知）**：组件仓已从 MIT 变更为 AGPL-3.0（`v0.2.0` 及之前为 MIT，`v0.3.0` 起为 AGPL-3.0）。**已经按 MIT 发布的版本，他人已获得的授权收不回**——许可变更只对**新版本**生效。这不是瑕疵，是开源许可的固有语义，写清楚比藏着好。
4. **许可变更的落点 = 三处齐改**：`LICENSE` 文件 + README 双份的 License 段 + `package.json` 的 `license` 字段。**任何一处漏改，读者就会面对三种互相矛盾的授权声明**——每次发版前按 §5 清单核一遍。

---

## 3. 版本号策略

### 3.1 版本号形态

**`v<主>.<次>`，写在 README 顶部与每份文档头部。** 不跟随代码侧版本号（两者节奏独立）。

| 变更类型 | 版本动作 | 例子 |
|---|---|---|
| 新增一份文档、新增一整节、新增一轮审核记录 | **次版本 +1** | v1.0 → v1.1 |
| 修正错别字、补链接、脱敏补漏（不改结论） | **不升版本**，改一次提交 | — |
| 推翻既有结论 / 重写某章 / 替换占位符体系 | **主版本 +1** | v1.1 → v2.0 |

### 3.2 每份文档头部的元信息（必须保留）

```
> 版本：vX.Y（状态）· 性质：设计稿 + 审核记录
> 写作视角：<一句话说明这是给谁、解决什么问题>
> 占位符说明：<本文用到的占位名一览>
```

### 3.3 更新节奏与触发

**不设固定节奏**，按事件触发：

1. **生产稿有实质更新**（新的一轮审核、推翻旧结论、新增能力域）→ 同步一次本目录（派生、改述、再脱敏）。
2. **外部反馈指出的错误被确认**（读者发现结论有误 / 环境已变）→ 在公开稿上直接修，**并在该文档末尾加一行「修订记录：vX.Y — <改了什么、为什么>」**。
3. **脱敏漏网点被发现**（哪怕是极小一处）→ **立即修 + 升次版本 + 在修订记录里注明"脱敏修订"**（透明化是最低成本的信任维护）。

### 3.4 与生产稿的关系（铁律）

> **公开稿是从生产稿派生的独立副本；任何情况下不得用公开稿反向覆盖生产稿。**

- 生产稿才是唯一权威源（它含完整路径、真实环境、内部治理流水）。
- 派生是**单向**的：生产 → 公开。公开稿上收到的好建议（措辞、结构、补充说明）**可以**手工回灌到生产稿，但必须人工确认、逐条应用，不允许整文件覆盖。
- 本目录内不保留任何指向生产路径的引用（否则等于把内部布局写进公开稿）。

---

## 4. 仓库拓扑与互链（多仓结构 · 一次定死）

> **本节解决一个必须现在定、以后很难改的问题**：后续要把管家体系的各个组件**分别开源**，如果命名与互链约定不先定死，等文档仓的链接散出去再改就会造成大量死链。

### 4.1 拓扑：1 个文档仓 + N 个组件仓（按功能横切）

```
<账号>/                                   ← 用现有账号聚合，暂不建组织（见 §4.4）
├── ops-handoff-design      【文档仓】      ← 本仓：方法论与设计稿（唯一权威叙述）
├── dsh-butler-archive      【组件仓】      ← 会话归档管理（通用痛点、纯 JS 零依赖）· **已发布**
└── ...（按需增加，每个组件一仓）
```

> **为什么不预先公布仓名（2026-09-20 修订）**：本图在 v1.1 曾并列四个**规划**仓名，兑现情况是——**一个已发布、三个至今没有建仓**，且其中两个按 §4.6 的准入四条本来就过不了（"必须与本机环境一起工作"的面板类，属反面清单）。
> **列一个不存在的仓名 = 给读者一个 404 + 给自己一个旧承诺。** 因此本图**只列已发布的仓**；后续组件按 §4.6 逐个体检，**通过一个发布一个**，不预先公布名字。

**为什么不是"一个仓放全部"，也不是 monorepo**：

| 方案 | 判断 |
|---|---|
| **多仓（1 文档 + N 组件）** ✅ | ① **许可证可以不同**（文档 NC / 代码 copyleft + 商业授权，本就是刻意分开的）；② 每个组件**独立打 tag、独立发 release**，一个组件的 bug 不阻塞其他；③ 组件仓的 README 可以精确回链"设计依据在文档仓哪一节"；④ 外部 issue 归属清晰 |
| 单仓多目录（`docs/` + `packages/*`） | 文档读者要 clone 代码、代码读者要下载文档；许可证混放容易被误读；一次 release 绑住所有组件 |
| monorepo + 工作区工具 | 这些组件**彼此独立**（没有共享依赖、没有统一构建），引入工作区工具纯属自找复杂度 |

**唯一真成本**：N 个仓要 N 次维护动作。缓解办法 = **统一命名前缀 + 统一 README 骨架**（下一节给模板），让"新增一个组件仓"变成填空题而不是设计题。

### 4.2 命名与版本约定

| 项 | 约定 |
|---|---|
| 仓库名 | 文档仓 `ops-*`（本仓 `ops-handoff-design`）；组件仓 `dsh-<功能>` |
| 分支 | 统一 `main` |
| 组件 tag | `vX.Y.Z`（组件自己的语义化版本） |
| 文档 tag | `vX.Y`（两段，见 §3.1） |
| **批次 tag（建议）** | 每次"文档 + 若干组件"同步对外时，在文档仓多打一个 `release-YYYY.MM`，把这一批的组件版本写进说明——这样"某年某月这一整套是什么状态"有据可查，不必靠组件各自的 tag 对齐 |
| 组件 README 必含 | ①「设计依据」节（回链文档仓 + 具体章节）②**宿主版本兼容区间**（插件类必写，见 01 文档 §4.5）③ 许可证 ④ 安装方式 |

### 4.3 双向互链（新增组件仓时的固定动作）

**组件仓 README 顶部**固定加这一节（把 `<组件名>` 换掉即可）：

```markdown
## 设计依据

本组件是「<文档仓名>（<文档仓地址>）」所述体系的一个实现，设计依据：

- 它为什么存在、边界在哪 → 见《自治式运维管家设计》§4（宪法：对象三型 + 自治光谱）
- 它与宿主版本的兼容区间 → 见《多机交接与云中继设计》§4.5「兼容性要求」
- 本组件特有的坑与实测 → <本仓 ISSUE / 文档>
```

**文档仓侧**要做两件事：

1. 本文件 §4.5 维护「组件索引表」——**新增组件仓后必须补一行**，否则读者不知道有哪些实现可用。
2. 两份设计稿里凡提到"抽插件 / 通用化 / 兼容性要求"的地方（如 01 的 §4.5、02 的 §7.1），**保持泛指不改**——设计稿讲的是通用方法，不该绑死到具体实现；具体实现的链接统一收在索引表里，这样设计稿不会因为组件改名而失效。

> 这个分工很关键：**设计稿里不写具体仓库地址**（写死即死链），**索引表里才写**（一处维护）。

### 4.4 账号还是组织？

| 方案 | 何时选 |
|---|---|
| **用现有账号**（推荐，当前） | 组件 ≤ 5~6 个、单人维护。省事，不用配组织权限 |
| 建一个组织（Organization） | 组件 > 6 个，或将来接受外部贡献者、需要按仓分工时。迁移成本 = 改一遍 remote 与 README 里的链接前缀 |

**现状（2026-09-11）**：**两端账号名已统一为 `kira905`**（原 GitHub 账号 `kira905-cloud` 已改名；旧地址自动 301 重定向）。新建组件仓时两端路径一致，组件索引表只需一套前缀。

> 阈值与迁移：达到 6 个仓以上、或需要外部贡献者时再迁组织；迁移 = 改一遍 remote + 按索引表批量改链接前缀（**一次改完**，别零散改）。

### 4.5 组件索引表（文档仓的"实现目录"）

> **新增一个组件仓 = 在本表补一行 + 在该仓 README 加「设计依据」节。** 这是唯一需要维护的清单。
> **本表只登记已发布的仓**（规划中的不写进来，理由见 §4.1）；表内《…》指本仓的哪一份文档、`§` 指那一份的章节。

| 组件仓 | 做什么 | 许可证 | 设计依据 | 状态 |
|---|---|---|---|---|
| `dsh-butler-archive` | 会话归档的通用化实现（去硬编码、路径可配、零依赖） | **AGPL-3.0**（+ 商业授权） | 《自治式运维管家》§2 L4 归档、§6 P2 事件源「归档出错」 | ✅ 已发布（Gitee + GitHub 双端）· 2026-09-16 由 `dsh-archive-sessions` 改名，旧名自动重定向 |

### 4.6 什么组件**不该**开源（准入四条）

不是所有组件都适合公开。**四条任一不满足 → 不开源**：

1. **能力独立性**：离开本机环境还能独立运行（去硬编码、路径可配、无私有依赖）。
2. **上游许可清白**：若组件 fork 自上游，**必须同时满足三条**——① **上游许可允许再分发**（MIT / Apache-2.0 等宽松许可；上游为专有许可或**未声明许可**的，一律不开）② **保留上游的许可与版权署名**（含 NOTICE，若上游提供）③ **标明改动清单**（README 或 CHANGELOG 写清"相对上游改了什么"）。**三条齐 = 可开；缺任一条才不开。**
   > 本条原先写作"**不是上游项目的 fork**"，很容易被读成"**fork 一律不许开**"——那是**误读**。fork 是常见做法，判据是"**守不守规矩**"，不是"是不是 fork"。
3. **不含环境耦合**：不把某个特定环境（公司防护策略、私有网关、个人偏好）写进逻辑。
4. **有独立叙述价值**：能为读者解决一个可复述的问题；只是内部流程胶水的组件，开源价值 ≈ 0。

**反面清单（不适合开源的类型，仅列特征不列名字）**：宿主 SDK 依赖过深、必须与本机配置一起工作的面板类组件；写坏了会毁数据的核心部件；以及**把内部监控/审计逻辑嵌死在单个组件里的**——那部分应当在抽通用版本时**整体剔除**，而不是"顺手开源"。

### 4.7 SDK 驱动的组件跟随策略（重要 · 决定"什么时候能开"）

组件 ↔ 宿主 SDK 的版本兼容**不是"升了就完事"**：已实测过一次 **SDK 破坏性变更**（某新版移除了插件用于注册配置项的旧 API 符号，替代为新接口）——这类变更会让老组件**启动即崩**。

因此：

- **组件仓必须写"支持宿主版本区间"**，且必须有**双分支适配**（新旧 API 同时支持、运行时探测）才算合格交付（依据见 01 文档 §4.5 的四条兼容性要求）。
- **宿主大版本升级前**，先跑一次**依赖断裂面审计**（把待升级版本的包解开，做符号级比对，列出被删/改签名的符号 → 逐个对照组件源码），审计通过再升。
- **组件开源 ≠ 固定在某个宿主版本**：开源的组件越多，"宿主升级要同时验证多少个仓"这件事的成本就越高——这也是**不把所有组件一次全开源**的现实理由（先开 1-2 个，把发布与验证流程跑顺）。

### 4.8 语言与翻译政策（先定，后翻译）

**结论：不对称 —— 英文 README + 中文技术正文，翻译按需触发。**

| 内容 | 语言 | 理由 |
|---|---|---|
| README（含本仓 `README.md` / `README.en.md`）、仓库描述、发布说明 | **中英双份** | 国际读者要在投入时间之前就能判断"这跟我有没有关系"；README 篇幅可控 |
| 文档（01 / 02 / 03 / 04 / 05 / 06 / 07 / 08） | **中文为原文**；英文单篇按需翻译 | 千行设计稿的忠实翻译是需要长期维护的第二成品。全量提前翻译的两种结局：两份都烂掉，或因"有译文"而不敢更新原文 |
| 组件仓 README、组件仓内文档 | **英文必备** | 组件 README 是它的用户界面；短，且是陌生人读到的第一份东西 |

**落地约定**：

1. **命名**：英文版 = `XX-<名称>.en.md`（例：`README.en.md`），**新增在中文件旁边，绝不替换原文**。GitHub 会按浏览器语言自动优先展示对应语言版本（`README.en.md` 对英文、`README.md` 对其它）。
2. **权威源**：**中文文件始终是唯一权威源**；英文版是派生，与设计稿—生产稿的关系同理（单向派生，不回灌）。
3. **触发条件**：需要某份设计文档的英文版 → **开 issue 指名**（需求本身是排期信号）；或某文档收到第一条英文提问时，优先翻译那一份。
4. **新增语言的门槛**：只有出现**稳定的**非中文读者来源时才加第三种语言——每多一种语言就多一份要与原文同步的产物。

### 4.9 与第三方的互链

- 引用业界标准 / 论文 / 官方文档时**给原始链接**（已在文档内标注），不要只写"业界认为"。
- 引用商业产品的官方文档（如企业 IM 回调、状态页 API）时，只引用**公开文档链接**，不写任何账户、应用 ID、密钥。

---

## 5. 发布检查清单（打勾才发）

**首次发布（已执行 · 2026-09-11 · `v1.0`）**：

- [x] 新建独立公开仓（**不是**把私有备份仓改可见性）——两个平台都是**全新空仓**，与私有仓零历史关系
- [x] 两处 HEAD 一致（`ff535f0`）、tag `v1.0` 双端就位
- [x] 脱敏 grep 零命中（含 GitHub **API 直读**的内容级复核）
- [x] 留档文件（脱敏清单 / 复核报告）未进仓（`.gitignore` 已排除且实测未出现）
- [x] 发布后自检：匿名可读、正文完整、代码块与中文文件名正常、页面无内部链接

**每次新增组件仓时**：

- [ ] 走 §4.6 准入四条（任一不满足 → 不开源）
- [ ] 仓名符合 §4.2 命名约定；分支 `main`
- [ ] README **英文必备**（§4.8）+「设计依据」节（回链文档仓 + 具体章节）+ 宿主版本兼容区间 + 许可证 + 安装方式
- [ ] **回本仓 §4.5 组件索引表补一行**
- [ ] 组件仓内不含：本机路径 / 用户名 / 主机名 / 凭证 / 会话内容（跑一次与本文档同款的 grep）

**每次文档更新时**：

- [ ] 更新是从生产稿派生（**不是**直接改公开稿再回灌）
- [ ] 跑脱敏 grep，零命中
- [ ] `脱敏清单与删改对照表.md` / `复核报告-*.md` **未**进入发布仓
- [ ] 按 §3.1 决定是否升版本；升了就更新文档头部的版本号
- [ ] **新增文档时**：README 中英双份的目录表各补一行、`§1.1 发布单元` 的文件树同步、发布说明的变更记录补一行
- [ ] **旧文档一律「不改正文、只加指向行」**（与内部文档治理同口径）——要更新内容就**新开一份**，不要把旧稿改写成"最新状态"（否则演进轨迹消失，且两次派生会互相覆盖）
- [ ] **改了两份设计稿时**：对应的英文版（若已存在）同步更新，或在该文件加一行「英文版基于 vX.Y，已过期」
- [ ] **涉及许可变更时**：`LICENSE` + README 双份的 License 段 + `package.json` 的 `license` 字段**三处齐改**，并在 README 变更记录里说明"哪个版本起换了什么许可"
- [ ] 打 tag：文档仓 `vX.Y`；组件仓 `vX.Y.Z`
- [ ] 发布后自检：用**无痕窗口 / 未登录状态**打开公开页，确认正文可读、无登录墙、无内部链接

---

## 6. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-11 | 建档：定发布单元（独立文档仓）、许可（文档 CC BY-NC-SA 4.0 / 代码 MIT）、版本策略（事件触发的语义化两段版本号）、与代码侧项目的双向互链要求、发布检查清单 |
| 2026-09-11 · v1.1 | §4 由「与代码侧项目的两层关系」扩写为**多仓拓扑**：1 文档仓 + N 组件仓、多仓 vs 单仓 vs monorepo 的取舍、命名与批次 tag 约定、组件 README「设计依据」骨架、账号 vs 组织的阈值、**组件索引表（新增组件唯一要维护的清单）**、**不开源的准入四条 + 反面清单**、SDK 破坏性变更的跟随策略（为何不一次全开源、宿主升级前先跑断裂面审计）；§5 检查清单拆成"首次发布 / 新增组件 / 文档更新"三组 |
| 2026-09-11 · v1.2 | 双端发布落地：写入**已发布地址表**（Gitee 镜像 + GitHub 主）、记录 GitHub 账号名与 Gitee 不一致及统一计划（§4.4）、首次发布检查清单打勾留档；发布流程实测结论：GitHub 网页 Import 只建空壳、**内容必须从本地发布仓 push 补齐**（且平台自动建仓的 Import 会丢掉历史与 tag） |
| 2026-09-11 · v1.3 | 账号口径统一：两端均为 `kira905`（GitHub 侧改名完成，旧地址 301）；地址表与 §4.4 同步更新，移除"账号不一致"的临时说明 |
| 2026-09-11 · v1.4 | 新增**语言与翻译政策**（§4.8）与 `README.en.md`：README / 仓库描述 / 发布说明中英双份，设计稿中文为原文、英文按需单篇翻译，组件仓 README 英文必备；README 增「语言与翻译」节；检查清单补英文项（组件仓英文必备 + 设计稿更新时英文版同步或标注过期） |
| 2026-09-15 · v1.5 | 新增 **`03-全家桶愿景与家规设计.md`**（更新后的全景：班底代号体系 / 五层架构 / 家规 R1–R5 / 四套 L 编号对照表 / 演进路线 / 生态复盘压缩版）；`02` 头部加指向行（**本文是 09-11 快照，正文不动**，与内部文档治理同口径）；README 中英双份与 §1.1 发布单元同步为 6 个公开文件 + 两个 README；**许可口径同步**（代码侧主许可改为 AGPL-3.0 + 商业授权，§2 与 §4.5 组件索引表随之更新）；修正 README 中指向旧许可文件名的死链 |
| 2026-09-20 · v1.6 | **全景稿按"一个班底两个面、两套判据"重写**（观测基准日 2026-09-20）：`03` 更新为服务面并入后的全景——新增角色「小秘书（服务面）」与第 ④ 层入口、新增家规 **R6 服务面判据（定稿六条，每条带"阈值 / 权威源 / 取证 / 留痕"五列）**、**R7 账本类事实仲裁（G 号主键 + 三级证据序）**、**R8 跨线契约（单写者让路表）**；新增实测硬约束（认证门与存活判据冲突 / 账本进程独占锁与主目录独占）、容量实测（单实例并发容量与双实例摊销）、两处已定档口径（会话归档分档 / 归档与清账的区别）；结论 3 条 → 5 条、教训 6 条 → 11 条、缺口表补到 G29；**三份文档统一改名**（去掉标题里的"设计"字样）；README 中英双份重写（改为对外作品口吻）；`02` 头部指向行与 `LICENSE` 文件名清单同步；**§4.1 拓扑图与 §4.5 组件索引表对齐实况**——只列已发布的仓、组件仓名更正为 `dsh-butler-archive`（原写 `dsh-archive-sessions`），并明确「**不预先公布仓名、通过一个发布一个**」的口径 |
| 2026-09-20 · v1.6 修订（**不升版本**） | §4.6 准入第 2 条由「不是上游项目的 fork」改写为 **fork 三条件**（**上游许可允许再分发** / **保留上游许可与版权署名** / **标明改动清单**）——原写法会被读成「fork 一律不许开」，属误读；按 §3.1 属"澄清既有结论、修正表述"，故**不升版本**、直接提交 |
| 2026-09-20 · v1.7（**已发布：Gitee + GitHub 双端**） | 新增 **`04-设计稿审核规程.md`**（五轮视角 + 横切四表 + 五环演练 + 一道落地后对标闸门：一份方案在开工之前该怎么审）与 **`05-一次错误的性能归因.md`**（实测记录：一次"并行会话就跑卡"的性能排查——初版把 CPU 打满归因给缓存全量重写，**复测后当场推翻自己**并如实更正；含"改对了东西、但没解决原问题"的记账、4 条可复用坑与 6 条方法小结）；§1.1 发布单元同步为 **8 个公开文件（五份文档）**、文件树补 `04` / `05`；§4.8 语言政策表的文档范围对齐实况；README 中英双份的目录索引、阅读提示与版本表同步（**两卡并行产出、共享同一工作区，各自留复核报告与交付报告**） |
| 2026-09-21 · v1.8（**已发布：Gitee + GitHub 双端**，提交 `8233ef4`） | 新增 **`06-只读监控面板的设计纪律.md`**（一块只读面板该怎么设计：该回答什么问题——不是"服务在不在"而是"今天怎么样、哪些东西静默坏了没人知道"；**显示纪律**「读不到就显示不可探测 + 原因，绝不显示 0、绝不显示假绿」；**取数工程**「分片 + 进程内 TTL 缓存 + 单飞 + 超时预算 + 局部降级」；**只渲染不复算**；数据来源三分类与三种处置，以及"通用化的天花板在哪"；一份**什么该砍**的诚实清单；含三次"静默不报"的设计教训）；§1.1 发布单元同步为 **9 个公开文件（六份文档）**、文件树补 `06`；§4.8 语言政策表的文档范围对齐实况；README 中英双份的目录索引、阅读指引、语言政策与版本行同步（**该版本当时先本地提交、未推双端未打 tag，随后于同日双端发布**） |
| 2026-09-21 · v1.9（**本地稿，尚未打 tag**） | 新增 **`07-放行判据与开关矩阵.md`** 与 **`08-文档体系口径一致性审核.md`**（均为生产文档的脱敏派生稿）：`07` 给"已经建好的自动化能力，按什么条件才敢真的放开让它动手"——跨机并集取样本 / 按机放行 / 缺数据一律不放行（fail-safe）/ **动作 → 风险级 → 开关 → 护栏 → 放行判据**矩阵与四条写死的规则 / 冻结面与分段重置 / 放行后 48 小时对账；`08` 给"一套长期演进的文档体系怎么被审出说明书与机器已经对不上"——**过期 / 冲突 / 无人定义**三类判据、四张横切对照表、问题分四档与回写纪律（一轮实测查出 24 处口径不一致）；§1.1 发布单元同步为 **11 个公开文件（八份文档）**、文件树补 `07` / `08`；§4.8 语言政策表的文档范围对齐实况；README 中英双份的目录索引、阅读指引、语言政策与版本行同步；**头部「当前 tag」段订正**（原写"下一版 `v1.8` 尚未打 tag"，实况是 `v1.8` 已于 2026-09-21 双端发布 `8233ef4`）；**本轮只本地提交：不推双端、不打 tag** |
