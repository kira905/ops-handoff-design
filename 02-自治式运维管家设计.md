# 自治式运维管家设计（从只读仪表盘到真管家）

> **📌 本文是 2026-09-11 的设计快照**——记录"当初怎么想、为什么这么设计"。**更新后的全景（班底与分层 · 家规五条 · 演进路线 · 生态复盘）见 [`03-全家桶愿景与家规设计.md`](03-全家桶愿景与家规设计.md)**。两份文档**互不替换**：**"怎么想的"看本文，"现在长什么样"看 03**。本文正文保留当时的原始口径（含当时未验证的假设与已修订的痕迹），不回写。
> **修订记录（v1.5）**：本次仅做两处**非内容性**改动——① 增加上面这行指向；② 脱敏修订：§0 中一处角色称谓统一为"使用者"（口径与 03 号保持一致）。**技术内容与结论未做任何修改。**

> **一份可复用的设计稿**：给"单机 agent 运行环境"配一个**自治程度可控、失败可回滚、判定可校准**的管家体系——它盯着宿主、会话、任务、插件、备份与外部信号，能主动告警、能自动处置低风险问题、能自我校准，但**在任何时刻都能说清"它为什么这么做、做错了怎么退回去"**。
> **版本**：v2.4 · **状态**：需求收口 + 全能力域补齐到可开工（未动工）
> **占位符说明**：`<DSH 主目录>`、`<工作区根>`、`<插件目录>`、`steward-*`（管家组件族）为占位名；模型名（如"低成本小模型""主力模型""本地推理模型"）仅示意，替换为读者自有渠道即可。

---

## 0. 方案缘起：从"只读"到"可写"的边界判断

第一版管家设计的目标就写在一句原则里：**只读聚合 —— 总览 = 读各载体做聚合展示，不新增事实来源**。它的天花板是设计时自己封的：使用者打开面板才看得到，看不到"当下正在发生什么"。

这一版要回答的是：**加多少自治度、加在哪一步、靠什么兜底**。三条边界：

1. **加法只加在"后果可逆"的台阶上**：无害可逆的自动做，有损但可回滚的自动做（强制留备份），可能丢 / 难撤销 / 涉使用者意图的一律只通知或预览确认。
2. **自治度是设计决策，不是能力上限**：再能干的 agent 也可以被设计成"每个动作都要请示"；反过来，简单的活（清空壳）全自动正合适——**给低风险高频动作加人审批，审查成本高于错误成本**。
3. **一个人用的系统，样本永远不够大**：所以判定规则必须能被逐日校准（误报率是第一 KPI），而不是靠"我觉得它准"。

---

## 1. 能力缺口（六维）

| 维度 | 只读仪表盘 | 真管家该有 |
|---|---|---|
| 感知 | 打开面板才看 | 事件驱动：任务失败、归档异常、会话中断**当下就知道**（⚠️ **能力上限**：外部通道到位前，🔴 告警实际是"人回到机位时触达"——桌面进程与会话通道对不在场的人无效） |
| 判断 | 只报数字 | 排优先级、查冲突（多会话重复干活）、说清"今天该干啥" |
| 行动 | 只有两个写口子（忽略 / 转卡） | 归档 / 清理 / 催办 / 重试 / 预检——**白名单 + 预览 + 确认 + 审计** |
| 记忆 | 靠 agent 自觉写流水 | 自动沉淀进度/结论/教训，会话死了也不丢 |
| 沟通 | 等人来看 | 主动推送（外部 IM 通道依赖云服务器，尚未通） |
| 闭环 | 断在"派活"和"验收" | 发现 → 派活 → 执行 → 验收 → 归档 全链路 |

---

## 2. 会话生命周期覆盖矩阵（全生命周期对照）

> 以"一个 agent 干活从生到死"为轴核对方案覆盖——**✅=已在生产 / 📋=方案细则齐 / ⚠️=部分或待验证 / 🚫=设计决策排除（有意不覆盖）**。

| 阶段 | 环节 | 保护/治理机制 | 载体 | 覆盖 |
|---|---|---|---|---|
| **L0 诞生** | 任务来源 | 人工指令 / 任务卡（领命）/ 定时卡 / 自动建卡 | 任务账本 | ✅ |
| | 开工仪式 | 开工先查账 / 高频坑 Top3 自动带 / 当日首次开机日报 + 建议 | 长期约定 + 日报管线 + 坑库 | ✅ |
| | 环境就绪 | 开工预检 5 项（磁盘余量 / 代理 / 本地推理栈 / 宿主 / 手机通道） | 日报管线改造 | 📋 |
| **L1 执行** | 上下文管理 | 上下文健康阈值（超模型上限 70% 提示开新会话） | 告警学习项③ | 📋 |
| | 走偏检测 | 超长 / 高轮会话 | 告警学习项⑦ | 📋 |
| | 空转 / 死循环 | 事件序号不涨 / 同签名重试检测 | 告警学习项① | 📋 |
| | 成本 | token 按会话 / 按卡归因 + cache 占比 + 超限停 | 归因升级 | 📋 |
| | 翻车 | 错误增量扫描 → 坑库自动写（≥2 次） | 坑库自动化 | 📋 |
| | 实时提醒 | 工具失败命中坑库当场提示 | 待验证宿主 hook | ⚠️ |
| | 回复质量判断 | （不设 LLM judge：单用户样本小 + 成本） | — | 🚫 设计排除 |
| **L2 意外** | 崩溃 | 哨兵计数 → 修复 → FROZEN 等人工 | 守护进程 | 🚫 **有意不接**"崩溃后续干"（自动续干风险高于收益） |
| | 演练 | （原计划的演练卡已回收，方案未接住） | — | 🚫 有意不覆盖 |
| | 卡死 | 空转检测 🔴 主动推 | 告警学习项① | 📋 |
| | 计划内重启 | 重启统筹（低水位 / 排队 / 兜底窗口 / 打断清单） | P0 | 📋 |
| | 会话文件损坏 | zstd 帧 + JSONL 完整性（顺带扫） | 扩面③ + 坑库扫描 | 📋 |
| **L3 收尾** | 完成证据 | 待验收自动预检（`node --check` / 文件存在 / hash） | 派活学习项④ | 📋 |
| | 验收闸门 | 待验收 → 人工确认 → 完成 | 任务账本 | ✅ |
| | 失败处置 | 失败卡重试（token 预估超阈值强制确认 / 更高阈值拒绝）/ 失败 🔴 推 | 写操作 + 告警 | 📋 |
| **L4 沉淀** | 归档 | 自动归档（>48h 每 6h）+ 空壳清理 | 归档插件 | ✅ |
| | 坑库 | 分主题自动写 + 动态权重 + 阶梯升级 | 坑库自动化 | 📋 |
| | 留痕 | 变更流水（谁 / 何时 / 什么） | 日志聚合 | ✅ |
| | 成功方法论 | 靠验收对话 + agent 自觉写记忆（防噪音不自动化） | 长期约定 | 🚫 设计定位 |
| **L5 治理** | 写入守门 | 长期约定 / 技能 A 层拦截 + 文档 B 层回滚 | 写入守门人 | 📋 |
| | 校准 | 干预统计 → 分级校准 | 守门人学习项⑤ | 📋 |
| | 一致性 | 定时卡成功率 <70% | 告警学习项⑥ | 📋 |
| | 体检 | 结构健康（链接 / 未分组 / 版本漂移）+ 计划任务码 + 守护进程存活 | 扩面①④ | 📋 |
| | 进化 | 升级授权三色 + 待验收预检引擎复用 | 升级分级 | 📋 |
| **横切** | 通知 | 桌面宠物 / 会话 / 报告 /（外部 IM 待云端） | 告警通道 | ✅ 本地 |
| | 安全闸 | 动态降档（全局自治开关覆盖全生态） | §4.4 | 📋 |
| | 注册制 | 监控注册表（首批 CI = 生态盘点） | §4.5 | 📋 |
| | 外部信号 | 供应商模型 / 状态页 / 公告 | P6 | 📋 |

**核对结论**：L0–L5 + 横切全覆盖；⚠️ 仅 1 处（实时提醒待验证宿主 hook）；🚫 3 处为有意排除（质量 judge、成功经验自动化、崩溃后续干）——**无意外盲区**。

---

## 3. 设计原则

| | 上一版（只读） | 本版 |
|---|---|---|
| 数据 | 只读 | 读 + **受限写**（动作白名单） |
| 触发 | 打开面板 / 每日日报（当日首次开机自动跑） | **事件驱动**（状态变化才通知）+ 定时兜底 |
| 执行 | 无 | 走任务卡（真 agent 会话）+ 预算闸门 |
| 通知 | 只落文件 | 会话 / 桌面宠物 / 外部 IM（待云服务器） |

**不变的三条铁律**：
1. 不造单点 Agent（管家 = 体系，不是某个会话）
2. 状态全外置（任务账本 / 记忆 / 流水文件）
3. **无 Agent 检测优先**（能用时间戳 / 文件判定的，不叫 LLM）

**新增的三条闸门**：
1. 任何写动作必须先 **dry-run 预览**（返回"将影响 N 项"），确认后才执行
2. 每个动作写 **审计流水**（谁 / 何时 / 什么 / 结果），可回溯可撤销
3. 自动执行按**后果严重度分级**（L1 全自动 / L2 自动 + 备份 / L3 只通知或预览），另加每日次数 / token 上限

---

## 4. 元分类学（宪法）：对象三型 + 自治光谱 + 注册制

> **定位**：各能力域条款是"具体规定"，本节是"宪法"——所有"要不要盯 / 要不要自动 / 要不要人批"的决策沿两根轴落位，不再一事一议。
> **依据（业界成熟体系）**：Knight Institute / arXiv 2506.12469 自治五级（用户角色论）、CSA《Levels of Autonomy for Agentic AI》（借 SAE J3016 六档）、HITL / HOTL / Over-the-loop（Databricks / IBM / EU AI Act 监督方法）、SRE 事故分级 P1–P4、CMDB 配置项注册制（ITIL）、Anthropic《Measuring agent autonomy》（经验用户从逐动作批准转向监控 + 例外介入）。

### 4.1 两个正交轴（先定位，再定档）

| 轴 | 问题 | 答案形态 | 决定什么 |
|---|---|---|---|
| **轴一 · 对象三型** | 这对象"状态错位"的后果长什么样？ | 资源型 / 期望型 / 风险型 | 要不要盯、盯哪个方向 |
| **轴二 · 自治光谱** | 人对这对象的处置介入多深？ | S0–S5 | 怎么处置（自动 / 告警 / 人批） |

> 业界印证（Knight）：**自治度是设计决策，可与能力解耦**——再能干的 agent 也可以被设计成"每动作请示"；反过来说，简单的活（清空壳）全自动正合适，加人反而是负担。

### 4.2 对象三型判据（问三问）

| 问 | 是 → | 否 → |
|---|---|---|
| ① 它有"应当保持"的期望值吗（进程数=1、结果码=0、链接有效）？ | **期望型**：偏离 = 故障 → 偏离才告警 | 继续问 |
| ② 关掉 / 没有它会坏事吗？（按需开的资源） | **资源型**：开≠好、关≠坏 → 只进总览 + 预检，**绝不告警** | 继续问 |
| ③ 它"开着"本身危险吗（公网暴露 / 烧钱 / 越权）？ | **风险型**：反向盯"开"（开太久 / 无请求 / 超限） | 无存在感 → 不注册 |

> 例：本地代理端口 / 本地推理栈端口 / 局域网代理端口 = 资源型；守护进程 / 宿主服务 / 计划任务 = 期望型；公网隧道 = **风险型**（它在线 = 暴露面，从来不该"期望在线"——隧道死了是安全态不告警，"开着没人用"才是事）。

### 4.3 自治光谱 S0–S5（对齐业界词汇）

| 档 | 命名 | 谁决策 | 业界对应 | 行为模板 |
|---|---|---|---|---|
| **S0 陈列** | 只展示，不判断 | 人看 | observer / 无自动化 L0 / SEV-4 | 自动进总览，永不打扰 |
| **S1 哨兵** | 机发现 → 人知晓 | 机发现，人处置 | on-the-loop（HOTL）/ SEV-1..3 通知 | 冷却 + 日上限 + 免打扰 + 只报变化 |
| **S2 自理** | 无害全自动 | 机 | 低风险高频全自动（HITL：别加人） | 审计留痕，无护栏 |
| **S3 领命** | 计划级批准 + 护栏内自主 | 人批计划，机执行批内 | CSA L2 / plan-level approval | 强制备份 / 上限 / token 预算 + 结算进「待验收」 |
| **S4 请旨** | 动作级人批准 | 人 | in-the-loop（HITL）/ approver | dry-run 预览 → 确认 → 执行 + 逃生舱 |
| **S5 亲为** | 人执行 | 人 | operator / 无自动化 | 仅人可（Agent 不能 force 自己） |

> **授权链（自治证书）**：每个 S2–S4 档位 = 该对象的「自治证书」——记录「此档由谁于何时批准」；**评审节奏随档位**：S2/S3 动作由干预统计驱动校准（老被拒绝 → 降档候选），S4 动作从没被拒过 → 升档候选（升前须人批准）。
> **SAE 类比**：S3 = L2 级自动驾驶（人监控、可随时接管），S2 = 封闭园区 L4——一个受控的本地生态就是那个"封闭园区"，护栏（备份 / 上限 / 审计）齐了才能放 S2。

### 4.4 动态降档（异常时自治自动降级）

体系检测到异常（守护进程异动 / 计划任务被拦 / 审计触发 red / 升级失败回滚中 / 安全事件）→ **全局临时降档**：S2→S1、S3→S4（只通知 / 要批准），写审计行；异常确认恢复后回原档。

> 语义：**绝不在体系不稳时擅自自动执行**——"会动手"的前提是"体系是稳的"。这条直接焊进自动执行护栏。
> **升级为全生态安全闸**：降档不能只约束自动执行——生态里其他 S2/S3 自治成员（自动归档定时器、定时任务卡、崩溃修复动作）在体系异常时照样自动动文件 / 烧 token。实现 = **全局自治开关**：降档状态文件（**必须放持久目录**）写成 `{mode:'degraded', until, reason}` / `{mode:'normal'}` → **所有 S2/S3 自治成员**在 tick / 启动前读它，degraded 期间暂停自动、转 S1 通知；恢复后自动回档。
> ⚠️ **反例警示**：早期版本把降档状态放临时目录，与"安全状态必须持久化"的决定直接冲突——按原文实现会让临时目录被清理后**降档态丢失 → 静默放行自动动作（fail-safe 反转）**。

### 4.5 注册制（CMDB 化：新增对象不靠人记）

- **形态**：`<DSH 主目录>/steward-monitor-registry.json`，每个被管对象 = 一个 CI：
  `{id, aliases[], 型, 探针(端口/进程/URL/状态文件), 期望值或风险阈值, S档, owner, state, provenance, edges[], 关联流水, schemaVersion}`
- **建模地基四件**（地基缺失 = 长期返工风险最高处）：
  1. **CI id 规则（稳定身份）**：`<型前缀>-<规范化名>-<设备短码>`，如 `exp-<组件>-<短码>`、`res-<组件>-<短码>`、`risk-<组件>-<短码>`。规范化 = 小写 + 去空格 / 下划线；**id 一经分配永不变更**——改名走 `aliases[]`，路径 / 端口 / 版本变化不换 id；跨设备同一逻辑对象 = **同 name + 不同设备短码**（两台 id 不相等但可归组）。
  2. **state 状态机（对齐 ITIL status accounting）**：`discovered → configured → active → degraded → upgrading → deprecated → retired`；每次迁移写审计（谁 / 何时 / 依据）；`retired` 只标记不删除（供历史查询与"幽灵项"识别）。注册表的「验证审计」比对对象就是 **state + 实况**。
  3. **edges 关系边（比清单值钱）**：`edges: [{to, rel, evidence}]`，`rel ∈ depends-on | patched-by | exposes | owned-by | supersedes`。用途：**升级影响面**（主程序 → 依赖它的插件 → 补丁脚本 → 需重打的验证特征）、**故障根因**（插件崩 → 谁依赖它）、**对账**（"同实体共现"与"真依赖"必须分开，否则对账误报）。**影响面判断优先走 edges 查询，不再用决策表硬编码。**
  4. **provenance 来源血缘**：`official | community | self-built | manually-patched | unknown`，附证据（包名@版本 / 补丁脚本路径 / 建档日期）。**自动发现的新对象默认 `unknown` 并标灰**——"来源不明"本身是风险信号，不静默当正常；**unknown 对象禁止进入自动升级链**。
- **自动发现**：探针扫一遍 → 未注册的新对象**自动进总览（S0 陈列档）**，永不打扰 → 想让某对象告警：面板一键「升期望型 / 调 S 档」（写审计）——判断成本从"逐个想"降到"模板猜 + 一键改"。
- **验证审计**：定期核对注册表 vs 实况——注册了却不存在（幽灵项）/ 存在却没注册（漏网项）→ 提示更新，防注册表烂掉。
- **行为继承**：型 + 档 两维决定 预检 / 总览 / 告警 全套行为——各能力域只写规则不写死对象，新对象零代码入列。

### 4.6 七决策面映射（证明"分类早散在各处，此处统一"）

| 决策面 | 原有分类 | 本质判据 | 光谱落位 |
|---|---|---|---|
| 监控对象（状态） | 资源/期望/风险三型 | 轴一 | 型决定盯法；偏离/越界事件走 S1 |
| 写动作 | L1/L2/L3 | 做错后果（轴二同源） | L1→S2、L2→S3、L3→S4 |
| 事件通知 | 🔴/🟡/⬜ | 不说的代价（对齐 SEV-1..4） | 全部 S1（🔴=SEV-1/2、🟡=SEV-3、⬜=SEV-4 只陈列 S0） |
| 文件审计 | A 写前拦截 / B 写后回滚 | 写坏的后果 | A≈S4（凭证=人审）、B≈S3（自动+可回滚） |
| 坑 | 记录→上浮→升级候选 | 复发频率 × 后果 | 记录/上浮=S2，升级候选=S4（生成建议不自动改） |
| 升级授权 | 🟢/🟡/🔴 | 升坏的后果 | 🟢→S2、🟡→S3、🔴→S4 |
| 任务卡 | 一次性 / 定时 / 交互 | 要不要人盯 | 执行全程=S3 领命（建卡=批计划、待验收=人验收）；**自动动作免建卡直执行，需要 LLM 会话干活的活必须人批建卡——禁止自动建卡** |

### 4.7 授权合成规则（三套授权并存必须仲裁）

**问题**：§4.3 给**对象**定 S 档、§5.3 给 **actor** 定权限（按 L1/L2/L3）、§5.5 给**动作**定影响上限——三者各自成立，但组合起来谁说了算没写 → 实现时必然分歧。

**合成公式（取最保守者胜）**：`允许执行 = min(对象 S 档能力, actor 矩阵能力, 动作 L 级处置)`

| 维度 | 来源 | 给出什么 |
|---|---|---|
| 对象 S 档 | 注册表 `CI.S档` | 该对象被允许的最大自治度 |
| actor 矩阵 | §5.3 | 该调用者的最大权限 |
| 动作 L 级 | 动作注册表 `level` | 该动作的后果分级 |

**判定顺序（短路，先拒后降级）**：
1. actor 不满足权限矩阵 → **拒**（最先判，fail-close）；**自动 actor 额外受"动作注册表"约束——不在注册表内的动作一律拒**
2. 动作 L 级 = L3 且 actor ∉ {面板、主会话} → **拒**
3. 对象 S 档 < 动作所需档位 → **降级为「只通知 / 预览」**（不拒绝——避免因对象档位低而完全不能干）
4. 全部通过 → 执行，并断言 `affectedCount ≤ maxAffected`

**降档态下的矩阵变化**：
- 全局 degraded 时，**所有 actor 的 L2 及以上一律转 S1 通知**（含计划任务的"预登记动作"）
- 仅保留 L1 无害动作可自动执行，且 actor 必须是面板 / 主会话
- 恢复 normal 后自动回到原矩阵

> 一句话：**对象档位管"能不能自动"，actor 管"谁有资格"，L 级管"后果多严重"；三者取交集，降档态只留最保守的一条。**

---

## 5. 安全、隐私、并发与可观测

> 定位：各能力域定义"做什么"，本章定义"**出事了怎么办、谁有权、东西放哪**"。缺这一章，前面所有闸门都只是流程约定而非工程约束。

### 5.1 敏感数据清单与脱敏规则

| 数据 | 位置 | 采集边界 | 脱敏规则 |
|---|---|---|---|
| API 密钥 / token | 本机凭证文件 | **只读、只在本机用**，绝不写入任何日志 / 状态文件 / 报告 | 展示一律掩码 `sk-****abcd`；用 key 拉模型列表时**只记"某源某时刻用过"这类使用事件（不含 key 本身）**——**边界澄清**：禁止的是"把 key 写进日志"，不是"记使用事实" |
| 会话正文 | 会话目录 / 归档目录 | 只取 `isError` + 错误签名，**不落全文** | 落库前剥：绝对路径→`<path>`、用户名→`<user>`、时间戳/PID/行号→`<ts>/<pid>/<ln>`；**「样本」字段 = 脱敏后的错误片段，单条 ≤200 字符、每签名 ≤3 条**——它是脱敏产物，不算"全文" |
| 文件内容 | 审计读到的文档 | 只算 hash + 结构检查，不复制内容到状态文件 | 存 hash 不存内容 |
| 路径 / 设备名 | 全部状态文件 | 允许（排障必需） | 跨机同步的状态文件保留原样（自用）；**对外报告脱敏** |
| 网络信息 | 拉取的模型列表 / 状态页 | 允许 | 无 |

> **铁律**：任何"把用户内容搬进状态文件"的设计都要先过这张表；**不在表内的字段默认不采集**。

### 5.2 审计介质（Markdown 不是权威源）

**问题**：早期把写动作审计追加到变更流水的 Markdown 文件——这是可写文本文件，且**被审计者自己每天都在写它**，"不可抵赖"语义不成立。

**改法**：
- **权威源 = append-only JSONL**：`<DSH 主目录>/steward-audit/YYYY-MM.jsonl`，每行 `{ts, actor, actionId, params, dryRun, result, affectedCount, rollbackScript, correlationId, prevHash, hash}`——`prevHash` 指向上一条 `hash`，形成**哈希链**。
- **防护能力如实说明**：哈希链**无签名** → ① 只能发现**意外损坏**；对有写权限的一方篡改一行后重算后续 hash，**外部无从发现**；② 链只保证"已写入的行没被改"，**不约束"该写没写"**（漏记不受限）。→ 因此定位为「**防损坏 + 便于追溯**」，**不宣称"不可抵赖"**。真抵赖需外部锚点（每日把链尾 hash 推到只读位置），列为可选增强。
- **Markdown 流水降级为渲染视图**：由 JSONL 生成（人工补写时标 `source:manual`），**不再作为审计依据**；回滚读取 JSONL 而非 Markdown。
- **完整性校验**：顺序重算哈希链，断链 → 🔴（进告警事件源）。
- **保留**：按月分文件永久保留（体积小）；30 天前只留摘要行。

### 5.3 actor 权限模型

| actor | 识别方式 | L1 无害 | L2 可回滚 | L3 涉意图/难撤销 | 逃生舱 force |
|---|---|---|---|---|---|
| 面板 | 面板请求（人操作） | ✅ | ✅ | ✅（需确认） | ✅ |
| 主会话 | 人直接对话 | ✅ | ✅ | ✅（需确认） | ❌ |
| 子代理 | 子代理 / 后台会话 | ✅ | ❌ | ❌ | ❌ |
| 计划任务 | 计划任务 | ✅（仅预登记动作） | ✅（仅预登记动作） | ❌ | ❌ |
| 自动 actor | **管家自身自动动作**（定时 / B 层回滚 / 组件自修复） | ✅（**仅动作注册表内 L1**） | ✅（**仅注册表内 L2，且强制备份**） | ❌ | ❌ |

> 缺 actor 字段 = **拒绝（fail-close）**；API 层强制，不依赖调用方自觉。
>
> **自动 actor 说明**：定时动作、B 层回滚与组件自修复都不属于原有四个 actor。**严禁复用"面板"身份**（它带 L3 + force 权限，等于让自动动作拿到人的权限）。硬约束：① 只能执行**动作注册表内已登记**的动作（未登记一律拒）；② 最大档位 L2，**永无 L3、永无 force**；③ 全局降档期一律转 S1 通知。

### 5.4 失败策略表（fail-open / fail-close 显式化）

| 组件 | 失败时 | 策略 | 理由 + 如何被发现 |
|---|---|---|---|
| 写入拦截器（宿主未加载 / 崩溃） | 写入放行 | **fail-open** | 拦死宿主比放过一次更糟；**必须告警"守门人不在岗"**——比拦截失败更危险的是不知道自己没在拦 |
| 告警巡检 tick 停摆 | 无告警 | fail-silent + 自曝 | 日报健康段显示「巡检停摆」🔴（自指闭环） |
| 重启协调器被拦 | 重启不执行 | fail-safe（不执行） | 请求 24h 过期作废 + 面板显示"请求未被处理" |
| 自动执行 | 暂停 | **fail-safe** | 读不到预算 / 上限 / 自治开关 → 一律不自动 |
| 自治降档状态丢失 | 默认 degraded | **fail-safe（保守）** | 状态文件持久化；启动时缺失即按 degraded 处理 |
| 采集器异常 | 跳过该采集 | fail-soft | 单项标「不可探测」，不中断整体 |

> **原则**：安全相关（写 / 自动执行 / 自治）一律 **fail-safe / fail-close**；可用性相关（拦截 / 采集）一律 **fail-open / fail-soft**，但**两者都必须能被检测到**。

### 5.5 爆炸半径与回滚验证

- 每个写动作注册项带 `maxAffected`（如合并卡 ≤10、归档 ≤50；批量操作必须显式 `allowBatch:true`）。
- 回滚脚本必须输出**校验结论**（hash / 字节 / 链接数对比），不许打印"已还原"就完事。
- 回滚本身写审计（谁 / 何时 / 把什么回滚了）。
- 单次操作影响 > `maxAffected` → 拒绝并提示分批；强制需 `allowBatch` + 审计标 `forced:true`。

### 5.6 并发与原子写契约（多个 tick 并发读写同一批状态文件）

**问题**：重启巡检（1 min）、告警巡检（5 min）、自动执行（6h）、坑库扫描、对账——**多个短命进程并发读写同一批 JSON**。Windows 上并发写同一文件会丢更新甚至写坏。

**三条强制契约（所有状态文件无例外）**：

1. **原子写**：一律「写同目录临时文件 → `fs.renameSync` 覆盖」。**禁止直接 `writeFileSync(目标)`**（半截写 = 文件损坏）。
2. **单例 tick 互斥**：每个 tick 启动先抢 `<DSH 主目录>/steward-locks/tick-<name>.lock`（内容 `{pid, startedAt, ttl}`；**放持久目录而非临时目录**——临时目录被清理 → 锁消失 → 双跑；抢锁必须 `fs.writeFileSync(lock, data, {flag:'wx'})` 原子创建，禁止"先 `existsSync` 再写"的 TOCTOU；**锁丢失后果照实写**：最多重复执行一次，由去重表兜底）——
   - 抢不到且锁未过期 → **静默退出（退出码 0）**，不排队不重试（下个周期还会来）
   - 锁过期（> `ttl`，默认 2× 周期）→ 视为持有者已死，**抢占并记一行审计**
3. **读-改-写唯一入口**：所有状态文件读写走共享模块（`readJson` / `updateJson(fn)`），`updateJson` 内部完成「加锁 → 读 → 变换 → 原子写 → 解锁」。**任何脚本不得自己 `JSON.parse(readFileSync(...))` 后写回。**

**运行开销（实测口径）**：
- **可忽略**：原子写多一次 `renameSync`（同目录元数据操作，毫秒级）；锁文件每 tick 一次创建/读取；`updateJson` 多一层函数调用 → **单次 tick 增量 <5 ms**，无常驻内存、无额外进程。
- **行为变化是正向的**：重叠 tick 被跳过（不再并发跑），CPU / IO 峰值反而下降。
- **真实成本在开发规范**（所有状态读写必须走共享模块），不在运行时；**新项目未动工时直接按此写 → 迁移成本 = 0**。
- **Windows 特有风险**：`fs.renameSync` 覆盖目标时，若目标正被其他进程打开（无 `FILE_SHARE_DELETE`）会 `EPERM` / `EBUSY`（实测踩过：同步盘锁、文件句柄占用）→ 内置**重试 3 次（间隔 50 / 150 / 400 ms）**，仍失败则记 🟡 并保留临时文件待下次覆盖；**绝不退化为直接写目标**。

**冲突语义**：同一文件被两个 tick 同时 `updateJson` → 串行（后到者拿到前者结果），不丢更新；跨文件事务不做（单文件粒度足够；确需多文件一致时用 `correlationId` 事后对账）。

**跨插件写契约**：任务账本由看板插件 Host 权威持有（铁律：绝不手改），而催办 / 改期与自动合并**都会改同一张卡** → 本节契约**不覆盖账本**。规定：① 所有账本写入一律走 Host API，**任何管家脚本不得直接读写账本文件**；② revision 乐观锁冲突 → 重新拉取 + 重放意图（最多 2 次），仍冲突则放弃并记审计；③ 同一张卡的动作串行化（同 `correlationId` 内不并发）。

**验收**：① 原子写：写入中途 kill 进程 → 目标仍是旧完整内容（不出现半截）② 互斥：并发起 10 个 tick → 只有 1 个真执行、其余退出码 0 ③ 过期抢占：伪造过期锁 → 新 tick 能抢占并记审计 ④ `updateJson` 并发 100 次自增 → 终值 = 100（无丢更新）。

### 5.7 可观测性与 SLO

#### 5.7.1 SLI / SLO 清单（阈值告警降为兜底）

| SLI | 定义 | SLO（建议） | 越界动作 |
|---|---|---|---|
| 守护进程可用率 | 1 −（异常分钟 / 总分钟），**排除维护窗口** | ≥99.5%/月 | 🟡 |
| 宿主服务可用率 | 服务端口可连比例（排除维护窗口） | ≥99.5%/月 | 🟡 |
| 会话成功率 | 正常收尾会话 / 总会话 | ≥95%/周 | 🟡 + 分析 |
| 管家数据新鲜度 | 各采集器最近成功时间 | 全部 ≤2× 周期 | 🟡「数据过期」 |
| **误报率** | 标记误报数 / 总告警数 | **<10%**（观测期放行门槛：<5% 才开自动执行） | 调阈 / 降档（**第一 KPI**） |
| 误报率 · 防自欺 | 分母必须够大，且**不能只靠人手动标记** | 累计告警 **≥50 条**才计入评估；未标记但**超时未 ack（>4h）**的告警计为**疑似误报**（单列，不算已确认） | 样本不足 → **不放行**；疑似误报占比 >10% → 同样不放行 |
| 自动动作成功率 | 成功 / 执行 | ≥99% | 自动暂停 L2 |
| 回滚成功率 | 成功回滚 / 需回滚 | **100%** | 任一失败 → 🔴 + 停用该动作 |

> 阈值告警（🔴/🟡）保留但定位为"兜底"：**SLI 越界 = 该关注，阈值越界 = 现在动手**。

#### 5.7.2 指标命名（跟业界语义约定，不自造）

- token 一律用 `gen_ai.usage.input_tokens` / `gen_ai.usage.output_tokens` / `gen_ai.usage.cache_read_tokens`；模型 `gen_ai.request.model`；操作 `gen_ai.operation.name`（`invoke_agent` / `tool_call` / `agent_run`）。
- 自定义指标前缀 `steward.*`（如 `steward.collector.duration_ms`、`steward.alert.count`）。
- 好处：将来接任何可观测后端零改动；与业界编码 Agent 遥测口径可比。

#### 5.7.3 关联 ID（correlationId）

- 一次"意图"贯穿全链路：重启请求 → 协调器 → 重启 → 通知 → 打断清单 → 审计行，全部带同一 `correlationId`。
- 自动动作、扫描批次、对账运行同样带 `runId`。
- 用途：出问题一条 grep 拿到全链路；跨进程也能串起来。

#### 5.7.4 数据新鲜度（asOf）

- 总览 / 日报的**每个状态行**都带 `asOf`（该数据采集时间）。
- 超过 `2 × 采集周期` → 该行标灰 + 附「数据 N 分钟未更新」。
- **显示旧数据比不显示更危险**：人看到"守护进程正常"而数据是 40 分钟前的，会得出错误结论。

#### 5.7.5 采集开销预算（硬指标，进验收）

| 项 | 预算 | 测量方式 |
|---|---|---|
| 管家常驻内存 | <50 MB（目标：无常驻，全 tick 短命） | 进程采样 |
| 单次 tick 耗时 | 轻量 <2 s / 重量 <10 s（30 min 一次） | tick 脚本自记时 + 落日志 |
| 管家总 CPU 占用 | <2%（日均） | 进程采样 |
| 坑库首扫 | 一次性 ≤30 s | 首跑计时并标 `bootstrap:true`；**首扫属 bootstrap 一次性，不占 tick 预算**（否则与"轻量 <2 s"自相矛盾）；仍超标 → 改后台异步分批 |
| 状态文件总增长 | <20 MB/年 | 周报统计。**复核结论**：坑库 + 审计 JSONL（永久保留）+ 误报反馈 + 外部源快照 → 预算偏紧，**超限优先降级为"坑库分片 + 快照留 10 份"** |

> **红线**：管家把宿主拖慢 = 产品失败。任一预算超标 → 降频采样或改增量，优先保主程序体验。

#### 5.7.6 告警可执行性模板

每条推送 / 日报条目固定四段：**现象**（一句话）/ **影响**（谁受影响）/ **建议动作**（可点的一步）/ **依据**（哪个文件、哪条判据）。缺任一段视为不合格告警——"CPU 高"式告警等于没告警。

### 5.8 面板信息架构

> 面板必须回答一个问题：**「今天有没有事，要我做什么」**——而不是让人翻六个 Tab 自己拼。

**首屏（默认落总览，五块自上而下）**：

| 序 | 块 | 内容 | 视觉 |
|---|---|---|---|
| 1 | **今日判断** | 一句话：「今天 2 件要你处理 / 3 件已自动办好」+ 建议前 3 条 | 大字，最高优先级 |
| 2 | **待处理** | 未 ack 的 🔴、待验收卡、L3 预览请求（可点开证据 + 一键处理） | 红/黄标 |
| 3 | **资产总览** | 按注册表分组（进程 / 配置 / 插件 / 会话 / 同步…），每行 `状态 + asOf` | 🟢🟡🔴 圆点 |
| 4 | **今日动态** | 事件流水（只报变化）+ 自动动作审计（可展开） | 灰底列表 |
| 5 | **健康底栏** | 管家自身：巡检心跳 / 协调器 / 采集器 + 数据新鲜度 + 误报率 | 一行 |

**Tab 结构**：总览 / 会话 / 任务 / 变更 / 记忆 / 报告 + **资产**（注册表 + 依赖图）+ **管家**（SLO / 校准 / 审计链校验 / 开关）。

**交互铁律**：
- 每个 🔴/🟡 **必须可点开看证据**（原始输出片段），不接受"只有结论"
- 每个写动作按钮 **先出 dry-run 预览**，预览里显示影响项 + 备份位置 + 回滚方式
- 探测失败显示「不可探测 + 原因」，**绝不显示假值**
- 危险动作（L3）视觉隔离（红区）并标注「需确认」

**空态与降级**：数据未就绪 → 显示「采集中（首次需 N 秒）」而不是空白；某模块崩 → 该块显示「模块未运行」+ 一键拉起入口。

---

## 6. 能力域

> **验收口径**：每个能力域的验收分两部分——**① 一次性实测** **② 回归项 R**（可重复执行的脚本 / 单测，改代码后必须能重跑，防"改 A 坏 B"）。判定类逻辑（活跃判定 / 签名归一化 / 重复卡判定 / S 档映射 / 抑制表）**必须有单测 + golden dataset**，不接受"人工跑一次正确"。

### P0 · 重启统筹（多会话并行下的重启闸门）

**问题**：多会话并行干活时经常要重启宿主（改插件 / 配置后生效），而重启脚本是**硬杀**（取服务端口 PID → `taskkill /F` → 守护进程拉起），**零闸门** → 一次重启打断所有正在跑的回合。

**三条硬约束（代码实证）**：
1. 重启 = 硬杀，无优雅退出
2. **「通知活跃会话快收尾」这条路禁止**：实证消息接口有"排队 / 插话"两种模式——`queue` 并不打断当前回合；但**发消息本身会让会话重新变活跃（产生新回合）→ 协调器永远等不到"空闲"**，而 `steer` 会真打断。→ 结论仍是禁止，**真正理由是"通知会制造新回合"而不是"会打断"**；实现者不得因为"queue 不打断"就恢复这条路
3. 发起重启的会话自身必然「在跑」（它正在执行这个请求）→ 活跃判定**必须排除发起者**

**架构（不动守护进程——它改代码风险高，有 crash-loop 熔断误伤的旧账）**：
- 面板 / 会话内 → 写请求文件到运行时目录
- 计划任务**每分钟**跑协调器脚本（无请求时 <100ms 退出，不常驻）；**窗口隐藏**：注册时用隐藏窗口包装，避免每分钟闪一次黑窗
- 协调器：读请求 → 检查活跃会话 → 空闲则调重启脚本 → 重启后就绪后**多播**通知 + 写打断清单
- **维护窗口**：协调器决定执行重启前，先写 `<DSH 主目录>/steward-maintenance.json` `{mode:'restart', correlationId, startedAt, until, reason}`（`until` 默认 = startedAt + 10 分钟）→ 告警巡检读到后只静默「**预期事件白名单**」：
  - **静默白名单**：守护进程存活异常 / 服务端口未监听 / 计划任务结果码非 0 / 宿主相关事件（重启期必然发生）
  - **不静默**：其余所有事件照常判定与推送——**维护窗口不是全局静音开关**（协调器崩 / `until` 写错 / 时钟跳变会导致真故障被吞 10 分钟）
  - **窗口清除判据**：协调器在窗口期内 tick 时探测「服务端口可连 + 一次成功 RPC」→ 判定就绪 → 清空窗口文件；窗口超时未清除 → 报一条 🔴「重启维护窗口未正常结束」（协调器自身有问题）。同一 `correlationId` 贯穿请求 → 重启 → 通知 → 打断清单 → 审计。

**策略（低水位 + 窗口兜底）**：

| 项 | 值 |
|---|---|
| 活跃判定 | 尾部事件 ∈ 回合中间白名单（`tool/call`、`assistant/chunk`…）**且** mtime < 5 分钟，**排除发起者** |
| 低水位 | **排除发起者后活跃 = 0 → 立即重启**（"≤1"与"排除发起者"组合会把 1 个真活跃会话也打断，与初衷相反） |
| 高并发 | 活跃 ≥ 2 → 排队（多请求**合并成一个批次**，不逐个重启） |
| 兜底窗口 | 03:00–06:00（排队请求在窗口内自然空闲时执行） |
| 超时 | 排队 > 30 分钟 → 桌面宠物 / 面板提醒拍板（强制 or 继续等） |
| 请求过期 | 24h 未执行自动作废（写日志，避免「幽灵重启」） |

**入口与强制**：
- 面板「排队重启」+ 会话内请求 → 走协调器排队
- 桌面一键重启入口 → **保持立即硬重启**（GUI 卡死急救通道，不能排队等空闲）
- 面板「立即强制重启」→ 跳过等待；重启前取快照，重启后通知被打断的会话（含「尾巴停在 X」）

**通知分工**：
- **协调器**（活在宿主之外）→ 重启完成通知 + 打断清单（**多播**：逐个发）——**实现路径**：守护进程的通知只挑单目标是它自己的策略，底层 `POST /api/session.prompt {sessionId, mode:'queue', content:[...]}` **支持指定任意会话** → 协调器循环调用即可多播（无需新 API；此时会话已被重启，`queue` 不存在"制造新回合干扰空闲判定"的问题）
- **面板** → 排队状态显示（等 N 个会话 / 已等 X 分钟）+ 审计流水
- 通道优先级：桌面宠物 → 会话 →（外部 IM 待云服务器）

**终端防护软件风险评估（已实测）**：
- 结论 = **低**：计划任务链路全过（Last Result=0、重启成功）；历史唯一真拦截 = 创建符号链接（改用 junction API 规避）
- 降风险三招：不新增常驻进程（每分钟短命 tick）、解释器绝对路径、脚本放已白名单的目录；**心跳文件 + 健康监控显示**（被拦立刻可见）；保留手动兜底

**验收清单**：
1. 无活跃会话 → 请求后 ≤1 分钟自动重启
2. 有活跃会话 → 排队不打断，活跃降下来后自动执行
3. 并发 ≥2 → 不插空（验证低水位生效）
4. 排队 >30 分钟 → 通知准确到达
5. 强制重启 → 打断清单正确，被打断会话收到通知
6. 协调器被停 / 被拦 → 健康监控显示心跳异常

**R · 回归**：活跃判定 / 低水位 / 合并批次三函数单测（构造会话事件序列 fixture）；维护窗口写入与清除单测；无请求时 tick 退出码与耗时冒烟测试。

### P1 · 会动手（写操作）

**形态**：面板里每个「问题」行旁边多一个「处理」按钮，点了先出预览，确认才执行。

**动作清单原则**（**能无脑判断的全自动，手动按钮只留给"需要人判断"的动作**）：
- ❌ 归档候选一键归档 → **划掉**：归档插件已全自动（每天多次扫、超时未活跃即归档）
- ❌ 空壳会话清理 → **移出，并入自动归档逻辑**（空壳无消息，无脑判断，自动跑时顺手清）
- 挂起任务催办 / 改期（涉意图，留手动）| 看板 API
- 失败卡重试（避免自动重试重复烧 token，留手动）| 看板重跑接口
- 升级项跑 L0 预检（只读审计；🟢🟡 级进自动升级链，🔴 级出方案报人，留手动入口）
- 中断会话转卡（已有）

**技术方案**：
- 统一路由 `POST /api/steward/v1/action`，body `{actionId, params, dryRun, actor}`；**默认 `dryRun:true`（缺省即预览），必须显式 `dryRun:false` 才真执行**
- 维护**动作注册表**：`{id, 标题, 前置检查, 级别(L1/L2/L3), 执行函数(ctx), maxAffected}`——**单一实现，不分预览 / 执行两个函数**（双函数必然漂移）；由 `ctx.dryRun` 决定返回"将做什么"还是真做，执行时**断言实际动作集 ⊆ 预览动作集**，超出即中止 + 🔴
- **TOCTOU 处理**：预览与确认之间目标状态可能被其他会话改动 → 断言失败时**不弹死报错**，而是**自动重新 dry-run + 返回新预览**，前端提示「状态已变化，请重新确认」；连续 2 次仍漂移 → 提示「目标频繁变动，建议稍后再试」并记审计（防死循环）
- **actor 校验**：子代理一律禁止 L2/L3；计划任务只允许预登记动作；**缺 actor 字段 = 拒绝（fail-close）**
- **爆炸半径**：注册表每项带 `maxAffected`；预览返回 `affectedCount`，超上限直接拒绝并提示分批
- **回滚可验证**：每个动作的回滚脚本必须输出校验结论（hash / 字节 / 链接数对比），回滚本身也写审计
- client 半：动作按钮 → 预览弹层（列影响项 + 备份 / 回滚提示）→ 确认按钮
- 审计：权威源改 append-only JSONL；Markdown 流水降级为渲染视图

**四个动作的实现细则**：

| 动作 | 前置检查 | 预览输出（dry-run） | 执行 | 回滚 / 安全 |
|---|---|---|---|---|
| **① 挂起任务催办 / 改期** | 账本里待办 / 待规划卡 `updatedAt` 超 3 天；**交互卡无定时，只出「催办」** | 「将把「X」的重复定时从 `<原 cron>` 改为 `<新 cron>`（或：将追加催办备注）」——⚠️ 看板定时接口只有 `{enabled, cron}`，**没有"单次日期改期"语义**，文案不得写"从 09-15 改到 09-20" | 调 Host API 改定时或改摘要追加催办备注 | 改期可再改回；催办备注可删。**注意"催办即续命"**：备注会刷新 `updatedAt` → 该卡 3 天计时重置，属预期但预览里要写明 |
| **② 失败卡重试** | 账本 `failed` 列卡片 | 「将重新运行「X」（上次失败于 <时间>，原因摘要）——消耗 token 约 ¥Y（按上次用量估）」 | 调 Host API 重跑 → 结算照常进「待验收」 | 重试只新建一次执行，不删原记录；token 预估超阈值拒绝 |
| **③ 升级项跑 L0 预检** | 生态面板有 🟡/🔴 项 | 「将对 X/Y/Z 跑只读符号审计（`npm pack` 到仓外隔离沙箱目录，不动生产），输出报告」 | 跑只读审计脚本 | 全程只读零风险；沙箱目录定期清 |
| **④ 中断会话转卡** | 已有 | — | — | — |

> 关键约束：**② token 预估**——执行前读失败卡上次执行的用量，预估成本 > 小额阈值弹强确认；> 更大阈值拒绝并建议人工开会话。

**风险与回滚**：所有动作沿用「备份 + 一键恢复脚本」；会话类动作复用两段式校验（不会出现"删了源没拷成"）。

**验收**：4 个手动动作各跑一次真流程（含 dry-run 与真执行），审计行正确写入；回滚脚本实测通过；token 预估拒绝路径实测一次。
**R · 回归**：四个动作的 `dryRun` 幂等测试（同参数重复预览结果一致）+「实际动作集 ⊆ 预览集」守卫单测 + actor 矩阵拒绝路径单测 + `maxAffected` 超限拒绝单测。

### P2 · 会盯人（主动告警）

**形态**：计划任务短命 tick（每 5 分钟），纯脚本 0 token，**只在状态变化时**通知（带冷却，避免刷屏）。

**运行形态细则——决策：计划任务短命 tick，不新增常驻进程**：
- **载体**：计划任务每 5 分钟跑包装脚本 → 巡检脚本。无异常时 <2s 退出，不占内存不常驻——与重启协调器同款模式
- **状态记忆**：`<DSH 主目录>/steward-watch-state.json`——记录每个事件类型的「上次通知时间 + 上次状态值」，用于**只报状态变化**（例如红项数 1→2 才报，持续 2 不重复报）+ 冷却计时
- **冷却与上限（默认值，可调）**：同类事件 4 小时内不重复推；每日全局推送 ≤ 10 条/天（超了只落报告）；免打扰 23:00–07:00 只落报告不推送
- **轻量事件（每 tick 查，毫秒级）**：计划任务结果码、守护进程数 / 服务端口监听——CIM / 端口探测，成本极低
- **重量事件（每 30 分钟查一次或按 mtime 增量）**：账本失败列新增、归档状态、中断堆积——用 mtime + size 缓存判断变化，没变不重扫
- **推送执行**：调桌面宠物 HTTP API + 守护进程通知（目标 = 新鲜的会话 id 或环境变量，**宁可不发不发错**）；报告文件兜底
- **失败降级**：巡检自身被停 / 被拦 → 下次日报的健康监控段显示「巡检停摆」🔴（自指闭环）
- **验收补充**：计划任务 tick 实测（Last Result=0、日志有 tick 行）；构造事件 → 5 分钟内推送到达；冷却 / 上限 / 免打扰各自实测一次

**事件源（主表）**：

| 事件 | 判定 | 级别 | 处置（🔴 推 / 🟡 进日报） |
|---|---|---|---|
| 自动归档出错 | 归档状态 `lastResult.errors > 0` | 🔴 | 主动推 |
| 归档定时器停摆 | `lastRunAt` 超过 `intervalMs × 2` | 🟡 | 进日报 |
| 升级体检新增 🔴 | 升级缓存里红项数增加 | 🟡 | 进日报 |
| 看板卡失败 | 账本 `failed` 列新增 | 🔴 | 主动推 |
| 挂起任务超时 | 待办 / 待规划卡 >3 天未动 | 🟡 | 进日报 |
| 会话中断堆积 | interrupted 数超过阈值（如 >5） | 🟡 | 进日报 |

**扩面事件源**：

| 事件 | 判定 | 级别 | 处置 |
|---|---|---|---|
| 计划任务结果码非 0 | 各巡检 / 日报任务 Last Result ≠ 0（终端防护拦截 / 脚本崩会静默） | 🔴 | 主动推 |
| 守护进程 / 宿主存活异常 | 进程数 ≠ 1 / 服务端口不在监听 / 心跳停（防双守护进程误杀事故重演） | 🔴 | 主动推 |
| 会话文件健康 | 定期扫 zstd 帧 magic + JSONL 完整性（防损坏积累） | 🟡 | 进日报（发现损坏 🔴 推） |
| 结构健康 | 链接目标存在性 / 未分组会话数 > N / profile 声明 vs 实体版本漂移 | 🟡 | 进日报 |
| 开工前预检 | 磁盘余量 / 代理可用性 / 本地推理栈就绪 | ⬜ | **进日报头部**（开工简报带一句，不告警） |

**学习项事件源（业界研究并入）**：

| 事件 | 判定 | 级别 | 处置 |
|---|---|---|---|
| **会话空转 / 工具死循环**（业界头号监控盲区：每轮调用全绿但整体烧钱） | 尾事件是工具调用或内容分片但 **事件序号 N 分钟不涨**（卡死）；同 callId 重试 ≥3 次 / 同工具同签名（去时间戳）短时 ≥5 次且无中间产出 | 🔴 | 主动推（事件解析复用 P0 活跃判定同款；同类后 5 分钟不重复推） |
| **超长 / 高轮会话**（轮数暴涨 = 走弯路） | 单会话未收尾但 >2h 或 >200 次工具调用 | 🟡 | 进日报「X 会话跑得异常久，可能走弯路」 |
| **定时卡成功率**（重复运行一致性） | 定时卡近 10 次执行成功率 <70% | 🟡 | 进日报（系统性缺陷非偶发，建议查该卡 prompt / 验收） |
| **会话上下文健康** | 会话累计输入超模型上限 70%（实测坑：小上下文模型装不下长会话会 400） | 🟡 | 进日报提示「该开新会话 / 切模型」（对应"干完提醒开新会话"的人工规矩自动化） |
| **公网隧道忘了关**（风险型：隧道在线 = 公网暴露面） | 隧道进程在 + 入口 >N 小时无请求（安全红线：用后即关） | 🔴 | 主动推「隧道还开着、N 小时没人用了」——隧道死了 = 安全态**不告警**（分类学：风险型反向盯"开"） |
| **备份加密 key 异常**（期望型：必须存在且有效） | key 文件缺失 / 0 字节 / mtime 异常（每日低频查，秒级） | 🔴 | 主动推「备份 key 异常——密文可能永久不可读」（灾难级项，每日查一次足够，勿高频） |

> **反面教材（实测）**：某调度器的 error 字段写着 `corrupt ledger was quarantined`，但实为上次残留的旧错（账本正常、tick 正常）——**报错要看时间戳新旧，见 error 就报 = 误报刷屏**，这是"会判断"的入门课。

**告警抑制 / 可延迟 / ack**：

- **依赖抑制表**（根因告警压住派生告警，避免一条链刷 3 条）：

| 根因（先到） | 被抑制的派生事件 |
|---|---|
| 维护窗口内 | 守护进程存活 / 端口监听 / 计划任务结果码 |
| 守护进程异常 | 端口未监听、计划任务失败、宿主相关事件 |
| 磁盘满 | 归档失败、备份失败、写入类失败 |
| 网络不可达 | 外部源失败、代码托管远端不可达 |
| 同类事件冷却窗口内 | 同类事件自身 |

- **可延迟维度（第三维，不能只看 🔴🟡）**：每条事件标 `deliverable: immediate | deferrable`——**安全类 / 数据丢失类**（隧道暴露、加密 key 异常、会话文件损坏、归档出错）**穿透免打扰**；其余按原规则只落报告。**修正**：穿透 ≠ 半夜发声——免打扰时段改为「**落报告 + 置顶到次日开机首句 + 面板红标**」，不推桌面宠物 / 不推会话（人睡着时推送无意义且扰民）。
- **ack 与升级**：每条推送带 `ackToken`，通知给「知道了」动作 → 写状态 `ackedAt` 字段；**同事件未 ack 且持续 >4h → 升级**（面板红标 + 下次开机第一句提醒）。既避免"推了没人管"，也避免"冷却期反复推"。
- **误报反馈**：推送 / 日报每条可标「误报」→ 写误报反馈文件 → 进干预统计 → 作为 S 档校准与规则调阈依据（**误报率是第一 KPI**）。

**通知通道**（按可行性排序）：
1. **桌面宠物**——⚠️ **实证：外部脚本调不到**。该插件注册的 HTTP 端点只有状态 / 资源 / 命令类，**没有"显示任意文本"的端点**；文本工具是宿主侧注册的 **agent 工具**（只有会话内 agent 能调），命令端点只能跑斜杠命令（需活跃会话）。→ **落地前提**：给插件打补丁加文本通道端点（走既有补丁体系：幂等脚本 + 维护清单登记 + 实体副本同步）；**补丁未落地前推送通道降级为「会话 + 报告文件」**
2. **会话内消息**（守护进程已有通知能力）——需指定目标会话，沿用「宁可不发也不发错」原则
3. **外部 IM**——卡在固定公网 IP（需云服务器采购）
4. **报告文件**（已有，兜底）

**风险**：通知刷屏 → 冷却窗口 + 每日上限 + 只报"状态变化"不报"状态本身"。

**验收**：构造各事件类型异常一次（主表 6 + 扩面 5 + 学习项 5 = 16 类，逐类抽验），通知准确到达且不重复；冷却与上限生效。
**R · 回归**：16 类事件判定函数单测（fixture 驱动）+ 抑制表覆盖测试（构造根因 + 派生同时出现，断言只发根因）+ 可延迟维度穿透免打扰单测 + 冷却 / 上限 / ack 状态机单测。

### P2.5 · 会判断（LLM 解读）

**形态**：日报生成后（当日首次开机自动触发，含开工预检头部），用**低成本模型**读日报 + 看板，输出「今天建议干什么 / 哪些活该砍 / 哪些活冲突」，追加到日报末尾。

**成本**：每天 1 次调用，走低成本渠道 ≈ ¥0；也可做成定时卡（有 token 成本，可随时停）。

**实现细则**：
- **载体**：不新写脚本进程——挂在日报管线尾部（生成之后串一个"建议"步骤），复用日报触发。读当日报告 + 账本（待办 / 待规划标题与 updatedAt）→ 组 prompt → 调低成本模型 → 输出 3-6 行「💡 建议」追加到日报尾部
- **降级**：LLM 调用失败 / 超时（安全审核拦截等）→ 追加一行「💡 今日解读不可用（原因）」，**不阻塞日报本身**（日报先行落盘成功）
- **幂等**：日报文件已含「💡 建议」段落 → 跳过（同日不重复解读）
- **可开关**：配置文件 `{enabled:true}`，可关

**验收**：①首开日报生成后自动出现建议段（实测真调一次）②解读失败时降级行正确 ③同日重复触发不重复解读 ④关闭后不生成。

### M3 · 日报管线改造（触发改开机 + 新段）

**目标**：把日报从「固定时刻计划任务」改为「**当日首次开机后自动跑一次**」，并在日报里增加**开工预检**与**结构健康**两个新段。

**改动 1：触发改开机（启动链）**
- **现状**：计划任务每天固定时刻跑日报脚本
- **改法**：
  1. 启动链末尾加「当日日报钩子」：读上次运行日期文件 → 非今日 → 分离进程跑日报（不阻塞启动链，几秒延迟）→ 写今日日期
  2. 包装脚本（纯 ASCII）延迟数秒（等宿主起来 + 系统稳定）→ 跑日报生成 → 成功后追加跑「建议」步骤
  3. 原计划任务**保留作兜底**（万一开机链路挂了当天还有一次机会；日报当天覆盖写幂等，双触发不重复）
- **幂等**：日报按日期覆盖写，重复跑无副作用；日期文件防多次触发
- **⚠️ 铁律遵守**：启动链是核心链路 → 动手前备份 + 写一键恢复脚本 + 实测才上生产；包装脚本全 ASCII
- **验收**：①模拟当日未跑（删日期文件）→ 跑启动链 → 8-15s 内日报出现且只一次 ②再跑启动链 → 不重复生成 ③日报脚本单独手动跑仍可用 ④原计划任务按选择删 / 留

**改动 2：新增「🛠 开工预检」段（头部）**
- 在摘要后插入预检段（纯只读，任何一项失败降级为「不可探测」不中断）：

| 检查 | 判定要点 | 输出 |
|---|---|---|
| 磁盘余量 | 取可用字节；**阈值 <15GB 标黄**（实测曾只剩 ~1GB，是危机前兆） | `系统盘余 120GB / 数据盘余 45GB`（<15GB 加 ⚠️） |
| 代理可用性 | 探测本地代理端口（或 500ms 超时连接） | `代理: 开/关`（只报状态不告警——按需开是明确语义） |
| 本地推理栈 | 探桥接端口与推理服务端口 | `生图栈: 桥开 / 服务关`（只报状态——按需启动） |
| 宿主本体 | 探服务端口 | `宿主: 运行中/未运行` |
| 📱 手机通道 | 探局域网代理 + 隧道进程 + 局域网 IP | `手机: 代理开/关 · 隧道 在线/离线 · 连 http://<IP>:3081`（**只报状态不告警**——均资源型或按需开；隧道 URL 每次重启随机变 → 由脚本写状态文件，日报 / 总览直接显示「当前手机访问地址」） |

**改动 3：新增「🏗 结构健康」段（尾部，低频重检查，每段 try/catch）**

| 检查 | 判定要点 | 级别 |
|---|---|---|
| 外移链接目标存在 | 外移链接清单硬编码（与自检脚本同一份）→ 验证重解析点目标存在；断链 → 标 🔴 并列出（提示跑迁移脚本预览） | 🔴 |
| 未分组会话数 | 复用 realpath 判定逻辑（读工作区配置 + 会话目录分组），未分组 >3 → 标 🟡（提示跑修复脚本） | 🟡 |
| profile 版本漂移 | 声明（profile `package.json` deps）vs 实体（`node_modules/<pkg>/package.json` version）不一致 → 列表（与生态面板逻辑共用） | 🟡 |

**改动 4：周报维持计划任务不动**（周报窗口语义依赖固定时刻跑）。

**改动 5：新增「📡 同步健康」段**

> **背景**：此前对同步只有零散链接检查；加密备份仓 + key + 多机交接 = **备份 / 交接可靠性层**，此前无监测对象。同步是天级节奏（收工 / 换机才 push），进日报段足够，不占 5 分钟 tick。

| 检查 | 判定要点 | 输出 / 级别 |
|---|---|---|
| 仓新鲜度 | 读各仓 `git log -1 --format=%ci` 与 status（落后 / 干净） | `仓1: 上次push <时间> · 干净 / 落后2`（>3 天无成功 push → 🟡「该 push / 换机前记得同步」） |
| 加密 key | key 文件存在 + 大小非零 + mtime | 缺失 / 0 字节 → 🔴（仓永久不可读 = 灾难级，提示冷备位置） |
| 远端可达 | 轻量探远端（HEAD，500ms 超时） | `远端: 可达/不可达`（只报状态不告警） |
| 多机交接提示 | Sync 时若远端有新会话且本机也有活跃会话 | 「疑似双开」提示进日报 |

> ⚠️ **对端心跳设计的迁移注记**：原心跳设计基于"共享实时盘"（心跳文件共享可读）；改为本地实体 + 代码托管仓后**无实时共享路径，心跳频率 = push 频率**——重建时需改设计（新机开机同步时检测双开，替代实时心跳）。

**改动 6：周报新增「📊 干活统计」段（0 token）**：本周完成卡数 / 失败卡数 / 中断会话数 / token 消耗环比 / 人工干预次数。看周报第一眼就知道体系本周干得好不好。
- 验收：周报含统计段且数字与面板 / 账本一致。

**验收总清单**：①开机触发当日首跑且只跑一次 ②预检段 5 项输出正确、盘 <15GB 标黄 ③结构健康 3 项正确（构造断链的沙盒测试 + 真实未分组 0 时显示正常）④原计划任务按选择删 / 留 ⑤备份 + 恢复脚本实测 + 变更流水记一行。

### P3 · 会派活（自动执行）

**形态**：管家发现的问题 → 按**后果严重度分级**决定处置（分界不是"机械 / 成本"，是"后果严重度"）→ L1/L2 自动干 → 结算进「待验收」→ 人点确认；L3 只通知或预览确认。

**自动执行三级**：

| 级 | 特征 | 例子 | 处置 |
|---|---|---|---|
| 🟢 L1 无害可逆 | 做错也毫发无损，随时还原 | 合并重复卡（挂起 + 备注可恢复）、空壳清理 | **全自动** + 审计留痕 |
| 🟡 L2 有损可回滚 | 动数据但有一键恢复脚本兜底 | 归档会话（物理移出，restore 脚本已有） | **自动执行 + 强制留备份** |
| 🔴 L3 可能丢 / 难撤销 | 删了就没 / 影响人的决策 | 删卡、改卡、催办改期（涉意图）、高风险升级 | **只通知 / 预览确认，绝不自动** |

> 判定口诀：会丢东西的、要人拿主意的 → 留给人；不丢东西的 → 管家自己干。

**token 用量监测**：自动执行与管家触发的 LLM 调用都记录 token 消耗，每日 / 每月汇总 + 阈值告警 + 超限自动停。
> **数据源已实测**：会话日志的内容分片事件带 `chunk.type="usage"` + `usage.{inputTokens,outputTokens,cacheReadTokens,reasoningTokens}`。扫会话日志按天 / 会话聚合即可，纯脚本 0 token。
> 口径：**按天汇总（input+output+reasoning）→ 阈值告警 → 超限自动停**。
> **归因升级（同一次扫描顺带算，0 token）**：①**按会话 / 按卡归因**（哪个会话烧得多 → "哪张卡最贵"）②**cache 读取占比单列**（占比高 = 上下文重复读，直接对应「该开新会话 / 压缩上下文」提示）③**推理 token 占比单列**（思考吃得多也是可优化点）④**周环比趋势**进周报。

**基础设施已就绪**：任务账本有 Host 定时 + 「待验收」人工闸门 + 定时 / 交互卡类型。
**缺口**：现在「谁发现问题 → 谁建卡」这一步靠人工。

**自动执行护栏（L1/L2 也必须遵守）**：
- **观测期门槛**：自动化开关默认关闭，**必须先在「只盯人、不派活」状态裸跑 ≥3 天且误报率 <5%** 才允许打开——防「一上来就自动合并 / 自动重试，把错的逻辑加速放大」
- 自动建卡去重（同问题 24h 内只建一次）+ 每日次数上限
- 执行会话权限限"工作区可写"，禁"完全访问"
- 每日 token / 次数预算上限，超了自动停——**精确措辞**："停" = **不再启动新的自动动作**；**已在跑的会话不会被强杀**（无 kill 通道，强杀亦有数据风险），靠上限前置拦截 + 面板告警
- 只对**分级表内 L1/L2** 的问题类型自动执行，L3 一律走通知 / 预览

**L1 首个自动动作：重复卡检测 + 合并**：
- **检测**：读账本 `tasks`，按「规范化标题」分组——规范化 = trim + 全角 / 半角统一（保守：不做语义模糊、不删括号差异，避免误并）；同组 ≥2 → 候选
- **判定分级**：同标题 **且** 同摘要（sha256 相等）→ **确定重复**（L1 直接合并）；同标题但摘要不同 → 🟡「疑似重复」**只通知不自动**（反例：模糊匹配必须人判——「云服务器方案B」与「云服务器入口打通」这种代码判不出来，给人看）
- **合并动作（L1，可逆）**：保留**先建**（updatedAt 更早）卡原样 → 对后建卡：摘要前插「⚠️ 与 <先建 id> 重复，已合并（<日期>），勿单独执行」+ 若带定时则**先把原 `schedule` 原值写进备注与审计行、再置空**（防双跑；原方案只说"置空"，回滚时 schedule 无处可查 → "人工可还原"不成立）→ 审计流水一行。**不删卡**（删 = L3）
- **执行载体**：面板 host 内定时（每 6h 查一次，挂巡检或独立脚本）→ 全部走同一套「去重 + 上限 + 审计」
- **去重**：`steward-auto-actions.json` 记 `{签名, 时间}`，同签名（标题 hash + 摘要 hash）24h 内不重复处理
- **上限**：`maxAutoActionsPerDay: 5`（每日 5 次，达上限停并记日志）；token 预算并入监测
- **验收（真样本）**：现存重复卡（同标题同摘要、updatedAt 差 17 秒）→ 检出「确定重复」→ 自动合并（后建卡挂备注 + 定时清空 + 审计）→ 24h 内重跑不重复处理 → 人工可还原（删备注即可，卡未删）

**L2 自动动作：归档出错自动重试**：
- 判定：归档状态 `lastResult.errors > 0` → 自动重跑一次 → 仍错 → 🔴 推 + 落报告（**不无限重试**）
- 审计：归档状态自带 lastResult（含批次时间戳）+ 补一行变更流水
- 上限 / 去重并入 5 次/日

**L1 自动动作：待验收自动预检——完成证据验证**（防 false completion，「验留下的状态，不听 agent 说」）：
- **形态**：任务卡结算进「待验收」时自动跑**只读证据预检**（非 LLM、0 token）——按卡类型验：改脚本类 → 语法检查 / 解析器；建档类 → 引用的文件存在 / 产物路径非空；迁移 / 备份类 → hash / 链接校验。**验不过 → 卡标 🟡「证据缺失」并附检查输出**，验收一眼见底
- **实现**：复用写后审计同款规则引擎，只换触发点——写后审计是「写入时」验，这里是「卡结算时」验；规则表抽共享
- **级别**：L1 无害只读（只标注不阻断，验收仍以人眼为准）
- **验收**：构造「报成功但证据缺失」的卡（如脚本改了但语法检查挂）→ 结算后自动标 🟡 附输出；证据齐的卡不误报

**真实验收样本**：账本现存两张同标题同摘要的历史卡被**保留不手动处理**，等自动检测实现后当第一个真实验收对象。

**R · 回归**：重复卡规范化函数单测（同标题不同摘要不得合并）+ 签名去重 24h 窗口单测 + 每日上限熔断单测 + L1/L2/L3 分级映射 golden dataset。
**风险**：自动执行花 token、执行会话可能干错事 → L2 强制备份、token 监测兜底、L3 永不自动。

### P4 · 会学习（翻车复盘 + 坑库自动化 + 阶梯升级）

**问题（使用者原话）**：
- 「每次你干活的时候遇到代码写错或者工具调用出问题，如果当时重试别的路径马上解决了，你其实是不会记录的」
- 「再开新会话遇到同样的问题其实你还会翻车，自我优化的机制只有在遇到大坑才会记」

**根因**：翻车-恢复的痕迹只活在当前会话上下文里，会话一关即蒸发；而「记」这个动作又依赖 agent 的自觉 → 两头夹击 = 小坑必然漏。

**数据可行性（实测已验证）**：会话日志 `tool/result` 事件带 `isError:true` + `error.name/code` + 错误原文，可用 `callId` 关联 `tool/call` 拿到工具名与参数。
> 活例子：查代码时 `grep` 指向一个**早已不存在的目录** → `SearchError / SEARCH_FAILED` → 秒改路径通过，**全程未记**——正是那种必然会再踩的坑。

**数据流**：
```
会话目录（含归档目录，两棵树都要扫）
  session.jsonl.zstd（多帧 zstd）
  → 增量扫 isError=true 的 tool/result（记录已扫 seq，避免重复）
  → 关联 tool/call（callId → 工具名 + 参数）
  → 签名 = error.name/code + 规范化错误文本（去路径/时间/PID/行号）
  → 聚类存坑库 JSON
  → 同签名 ≥2 次 → 自动写坑库（分主题，全自动）
  → ≥3 次且跨 ≥2 会话 → 生成「建议升级形态」候选（需批准）
  → 顺带：同批扫描时验每个会话文件 zstd 帧 magic + JSONL 完整性（零额外 IO）
```

> **扫描范围铁律**：**归档目录必须扫，而且是富矿**——归档批次正是"折腾最狠期"（迁移 / 同步 / 编码 / 插件崩溃现场），且自动归档每 6h 移入 = **持续增量源，不只首扫**。三个设计细节：
> ① **两棵树**：扫描范围 = 活跃会话目录 + 归档目录
> ② **记账按 sessionId 不按路径**：会话归档时文件路径变化，若 seq 记账绑路径会**归档后重复全扫**——记账键 = sessionId（+ 文件末次写时间），路径变了也不重扫
> ③ **健康检查同范围**：帧 / JSONL 完整性扫两棵树（损坏案修复后会话若已归档，健康也要覆盖）
> 成本：百余个归档会话百余 MB 全解首扫秒级～十秒级（一次性）；此后随归档批次增量，每批秒级

> **落点**：会话文件健康扫描**不做独立任务**——增量扫 isError 时顺带验帧 magic + 完整性（已解帧无额外成本，范围 = 两棵树含归档区）；发现损坏（帧数 0 / 尾帧解不开 / 撕裂 JSONL）→ 另写健康文件 → 🔴 通知（损坏 = 数据风险）+ 提示跑既有 restore 流程；正常则 🟡 级信息落日报结构健康段。

**坑库组织（索引法）**：
- 按子系统分主题文件（命令行 / 插件 / 会话 / 守护 / 同步 / 其他）——自动追加
  - **自指豁免**：坑库文件是写后审计的对象，而坑库自动写它会写它 → **审计器必须豁免「管家自身写入的路径」**（坑库 / 报告 / 流水渲染视图 / 审计 JSONL），否则自动写会被自己判违规并回滚，形成死循环
- 大而全的踩坑文档降级为**索引**：保留经典坑 + 高频自动条目摘要 + 指路
- 每条坑字段：现象 / 错误签名 / 触发工具 / 样本 / 次数 / 首末次 / 涉及会话
- 防噪音：签名去重 + 门槛 ≥2 次 + 排除业务性失败（无匹配、文件不存在等"正常查询结果"）
- **两条口径必须先落 fixture**：
  - **排除清单**：逐条给出"什么错误文本算业务性失败"的正反样本（无匹配 / 文件不存在 / 主动取消 → 排除；**权限拒绝不排除**——那是配置问题）→ 落 fixture 文件，实现者不许自己猜
  - **count 口径**：`count` = **同签名在扫描范围内出现的总次数**（同会话内重复也计、跨会话自然累加）；权重公式的"距上次踩坑天数"取**末次出现时间**——两条都进 golden dataset

**动态权重**：`score = count × 0.5^(距上次踩坑天数 / 30)`
- 反复踩 → 权重高 → 浮到坑库顶部 + 进开工 Top 3
- 30 天没再踩 → 权重减半、逐渐沉底，标「疑似已修复」（**不删除**，可查）
- 权重 Top 且跨 ≥2 会话 → 生成「建议焊入」候选

**「怎么想起来读」三层触发**：

| 层 | 机制 | 靠自觉吗 |
|---|---|---|
| L1 开工自动带 | 日报 / 记忆简报自动嵌入「高频坑 Top 3」 | ❌ 不靠 |
| L2 任务对口 | 各技能开头写死指针（改插件 → 先读插件坑库） | ❌ 不靠（技能加载即见） |
| L3 实时提醒 | 插件监听工具失败，命中坑库当场追加提醒 | ❌ 不靠（**待验证宿主 hook 能力**） |
| L4 长期约定指针 | 兜底一行 | ✅ 靠 |

**阶梯升级（按重复次数自动爬形态）**：

| 重复次数 | 处置 |
|---|---|
| 1 次 | 坑库记录 |
| 2 次 | 坑库 + 权重上浮 |
| ≥3 次且跨 ≥2 会话 | 生成候选：**①做成自动检查项（最优，坑不可能再犯）** / ②内联进相关技能 / ③铁律级才动长期约定。**产物归宿**：候选写 JSON + 面板「管家」Tab 新增「坑升级候选」段（一键采纳 / 忽略），**不自动改长期约定 / 技能**（采纳 = L3，仅人可点） |
| 30 天不再出现 | 降权沉底，标「疑似已修复」 |

> 核心洞察：坑的最高形态不是「记下来」而是「消灭掉」——能做成检查项（如"脚本漏补编码声明" → 自检脚本自动查）就做成检查项；技能只内联高频坑 + 指针，不塞全量。

**验收**：① 扫最近 7 天会话能提取 ≥N 条真实翻车签名 ② 坑库分主题文件正确生成 + 索引更新 ③ 权重排序正确（构造高频 / 低频坑验证衰减）④ 开工简报带 Top 3 ⑤ 达到 ≥3 次的坑生成升级候选（且**不自动**改长期约定 / 技能）。

**R · 回归**：签名归一化单测（同一坑不同路径 / 时间 / PID → 同签名；不同坑 → 不同签名，**两边都要测**）+ 权重衰减公式 golden 值 + 阶梯升级阈值单测 + 扫描记账按 sessionId 的幂等测试（移动路径后不重扫）。

**成本**：纯脚本 0 token；增量扫描单次数秒。

**历史 seed（防"前几天的坑覆盖不到"）**：坑库落地**首跑必须做历史 seed 三步**：
1. **全量历史扫**：首跑扫归档目录 + 当前全部现存会话的 isError（不止近 7 天）——原料上限 = 现存会话时间范围
2. **文字坑导入**：早期无会话原料的坑，从既有踩坑文档 + 长期约定 + 记忆库条目 **seed 进坑库**（标记 `source:seed`，不计 count、只进索引与权重衰减池，供 Top3 与命中提醒）
3. **此后增量**。诚实边界：更早的历史若只剩文档留存，**坑库不追索**（标注于坑库 README）

### P5 · 会审计（写入守门人）

**问题**：使用者追问「长期约定、记忆和各类文档的写入和优化，管家能不能干？」→ 澄清后 = **写入时对内容做审计**（审计 ≠ 代笔）。

**现有规范资产（散着 + 全靠手动跑）**：

| 规范 | 载体 | 检查工具 |
|---|---|---|
| 长期约定写入规范 | 专门技能（备份 → 编辑 → 校验 → 记忆 + 大改后条目审计）+ 长期约定的焊入纪律 | 校验脚本 |
| 文件保存归档规范 | 目录规范文档 | 自检脚本 / 收尾脚本 |
| 魔改登记规范 | 魔改维护清单 | 生态面板清单登记 |

**四个缺口**：①全靠人想起来才跑 ②长期约定只验语法、不验内容规范 ③备份-恢复脚本三件套没人查 ④变更流水漏记没人管。

**实测发现的规范漂移**：收尾脚本的根目录白名单**过时**（把合法的目录报成"混入"，且未登记新增的核心文件）；临时目录积压大量历史文件。
→ 启示：审计器不仅要查"文件是否违规"，还要能识别「**规范本身过时**」（白名单 / 检查项与现状不符）并提示更新，否则审计会天天误报、最终被无视。

**技术可行性（实测宿主）**：文件系统层暴露三个事件门，官方插件正是靠它们实现「改文件前必须先读过」：

| 事件 | 签名 | 能力 |
|---|---|---|
| `fs/write-intent` | `(target, actor, next)` | 可拦截（抛 `FsError`）/ 可改写意图 |
| `fs/edit-intent` | `(target, actor, next)` | 同上 |
| `fs/observed` | `(target, observation, actor)` | 记录已读状态 |

**关键限制**：只给**目标路径 + 意图**，**不给内容** → 内容审计必须走「拦截 + 强制先审计」的凭证模式。

**通道限制（阻断级）**：`fs/*` 事件门**只覆盖宿主的 write / edit 工具**。日常"写临时脚本再执行"的操作（命令行 + node `writeFileSync`）**完全不经过 fs 事件门** → A 层对这类写入零拦截，B 层的 `fs/observed` 同样触发不了。结论：**「守门人」定位必须改为「防手滑 + 事后审计」，不得宣称"防绕过"**。

**补强**：B 层增加**磁盘层兜底 watcher**——用文件系统监听（不依赖写入通道）监听保护区路径（长期约定 / 技能 / 核心脚本 / 关键文档）；检测到变更 → 跑同一套规则引擎 → 违规则用写前快照回滚 + 🔴。A 层保留（拦住工具路径上的高频手滑），但**A 层失效不再等于审计失效**。

**方案（分层）**：

| 层 | 对象 | 机制 |
|---|---|---|
| **A 写入前拦截** | 长期约定 / 技能 / 核心脚本（写坏 = 全局崩） | 拦 `fs/*-intent` → 无审计凭证抛 `AUDIT_NOT_PASSED` → 强制先跑审计工具 → 通过发凭证（内容 hash + 有效期）→ 放行；**留逃生舱**（可临时禁用 / 带原因 force） |
| **B 写后审计 + 回滚** | 文档 / 流水 / 普通文件 | 写后立即验（规范 + 结构）→ 违规告警 + 自动回滚到备份 |

**凭证模式实现细则**：
- **凭证形态**：拦截方维护内存 Map `path → {hash, expiresAt}`；凭证 = 「审计工具跑完后 POST `/api/steward/audit-pass {path, contentHash}`」→ 存 hash + 10 分钟有效期（`AUDIT_TOKEN_TTL: 600000`）→ 拦截器检查：目标 path 在 Map 且 hash 匹配 → 放行并删凭证（一次性）
- **hash 怎么对（关键论证修正）**：⚠️ 早期论证不成立——事件门只给 target 不给内容，而审计工具 hash 的是**磁盘上的旧内容**、agent 即将写入的是**新内容**，两者不是同一份东西 → 攻击路径：先跑审计工具审旧内容拿凭证 → 写入违规新内容 → 拦截器见 path 在 Map 就放行。**结论：A 层实际防护 ≈ 0，真正防线是 B 层写后审计。**
  - **定案（真防护）**：审计对象改为**待写入内容**——约定 agent 先落临时文件或传内容参数，审计工具 hash 待写入内容并发证；拦截器拿不到内容，故**放行后立即读回校验**（写后 1s 内 hash 比对），不一致 → 回滚 + 🔴
    - ⚠️ **副作用窗口**：读回校验发生在**写入之后**，而保护区恰恰是"新会话创建时注入"的长期约定与"热加载"的技能——这 1s 内违规内容**可能已被消费**，回滚文件**不能回滚已生效的副作用**（技能已注册 / 约定已进预设）
    - **修法**：保护区写入一律走「**写临时文件 → 审计 → 原子 `renameSync` 就位**」，不给中间态可见窗口（与并发契约同款）；写后读回校验保留作兜底。**局限照实写**：本机制保证「最终落盘内容合规」，不保证「违规内容从未被读到」——要真保证需上游写入方配合原子写，属流程约定
    - **配套约定**：审计 CLI 支持 `--staged <file>` 与 `--content-hash <sha256>` 两种入参；受保护路径的写入流程固定为「落临时文件 → 审计 → 拿凭证 → 写入」，并在文档头部写明
    - **回退方案（仅当真防护落地受阻）**：A 层降级为流程约束（凭证只含时间戳 + 路径，不含内容 hash），面板与文档明确标注「A 层不防内容替换，靠 B 层兜底」
  - **无论哪个方案，B 层写后审计 + 自动回滚都是必需的**，不得因 A 层存在而省
- **拦截器挂载**：注册 `fs/write-intent` + `fs/edit-intent` 监听；命中保护区路径（长期约定文件 / 技能文件 / 核心脚本清单）且无凭证 → 抛 `AUDIT_NOT_PASSED`（报错文案指引「先跑对应审计工具再写」）
- **审计工具**（各自独立，统一收口到一个 CLI 包装，跑完自动 POST 凭证）：长期约定 → 校验脚本 + 内容规则（长度 / 重复 / 引用存在 / "该不该焊"启发）；技能 / 脚本 → 自检项 + 语法检查
- **逃生舱**：① 面板 / 设置「暂停写入审计」开关（写状态文件，读到即不拦，每次暂停留审计行「暂停审计 HH:MM–HH:MM」）② 单次 force：错误文案提示可用 `confirm: "force: <原因>"` 重发（记 force 日志）。**逃生舱 = 人专用，Agent 不能 force 自己**（actor 判定：仅主会话 / 面板可 force）
- **B 层写后审计**：文档类路径监听 `fs/observed` 后延迟 1s 验文件（只读检查：白名单 / 结构 / 必填段）→ 违规 → 从写前自动备份回滚 + 通知。备份在拦截时同步做，零额外 IO 成本
- **验收补充**：逃生舱暂停后写入不被拦；force 带原因日志可查；子会话 force 被拒

**审计规则两类**：
- **长期约定审计**：语法校验通过 / 条目长度阈值 / 重复条目检测 / `详见 X` 引用文件是否存在 / 「该不该焊」判断（含日期流水、任务进度 → 告警）/ 大改后条目清单比对备份
- **归档审计**：根目录白名单 / 临时目录积压 / **备份 + 恢复脚本 + 桌面入口是否齐** / 归档命名与 README / 变更流水是否记 / 文档头部元信息

**处置**：违规 → 面板 + 桌面宠物提醒；低风险自动修；长期约定违规严重 → 告警 + 自动回滚。

**干预统计 → 分级校准数据**：
- 统计：面板新段展示**拒绝 / 改单 / 逃生舱**按动作类型聚合的次数（数据来自审计流水 + force 日志 + 预览确认记录，纯统计 0 token）
- 用途：**L1/L2/L3 分级第一次有校准数据**——某动作老被拒绝 → 该降级；某 L3 动作从未被拒 → 可考虑升档（升前仍需人批准）。「会判断」的自我进化闭环
- 验收：构造 3 次拒绝某动作 → 面板统计正确显示；校准建议文本正确生成（只建议不自动改分级）

**升级风险**：事件门是宿主 rc 版本 API，主程序升级必须复验（该版本已知删过符号）→ 同步写进升级决策表。

**验收**：① 改长期约定不走审计 → 被拦（`AUDIT_NOT_PASSED`）② 审计通过后写入成功 ③ 违规内容（超长 / 重复 / 引用不存在）→ 审计拒绝并给理由 ④ 文档类文件写后审计能发现违规并回滚 ⑤ 逃生舱可用 ⑥ 备份 + 恢复脚本实测通过。
**R · 回归**：凭证 TTL / 一次性 / 内容 hash 匹配单测 + 拦截路径清单单测 + 逃生舱权限单测（子代理 force 必须被拒）+ 哈希链审计完整性校验脚本。

### P6 · 会望风（外部信号感知）

**问题**：前几个能力域盯的全是**内部**（本机 / 本生态）；而 API 供应商的模型上新 / 下线、价格、故障、重要公告这些**外部世界变化**，现在全靠人逛到才知道——可能推荐一个已下线的模型。

**与升级体检区分**：升级体检盯 **npm 包**（软件供应链 / 主程序与插件版本）；本能力域盯 **API 供应商事实层**（模型 / 价格 / 公告 / 故障）——两个不同源、并列互不替代。

**数据源两分法（结构化自动理解 / 非结构化只提示）+ 状态页**：

| 源类 | 例子 | 怎么感知 | 自动化 |
|---|---|---|---|
| **结构化 · 模型列表** | 供应商 `/v1/models`（key 现成于本机凭证） | **模型清单 diff**：定期拉 → 对比本地快照 → 新增 / 下线 / 规格变化直接出结论 | 全自动：模型下线 🔴（影响现有配置 / 推荐链）、新增 🟡 进日报 |
| **结构化 · 状态页** | statuspage 系（均有 `api/v2/status.json`） | JSON → 故障 🔴 / 部分 🟡 / 恢复 🟢（恢复后自动解除告警） | 全自动 |
| **非结构化 · 公告 / 价格 / 更新日志** | 供应商公告页、定价页、模型文档 | **页面 hash 变化 = 「有更新」** 🟡 附链接，**不解析内容**（公告要人读，不猜语义）。**修正**：整页 hash 对动态元素（时间戳 / 广告 / 计数器）敏感 → 改为**只比对正文文本的归一化 hash**（剥 script/style/时间戳类文本）；同一源连续 3 次都"有变化"且被标过误报 → 该源自动降为"只存档不提示" | 半自动；可配 LLM 摘要（低成本模型，同 P2.5 降级 / 幂等 / 可开关三件套） |

**实现细则**：
- **载体**：独立低频扫描（**不是** 5 分钟的巡检 tick——外部源节奏不同：模型列表 / 公告 1 次/天、状态页 30 分钟）；计划任务每小时跑一次（内部按源各自节流），纯网络请求，0 token；通知 / 冷却 / 日上限复用告警通道机制
- **源注册表**：每源 `{id, url, kind: models-api|status-json|page-diff, key-ref?, interval, enabled}`——**新增源 = 加一行注册，不写代码**（监控注册表的对外版）
- **首批源**：主力供应商的 `/models` + 状态页 + 公告页（点名需求）；宿主主程序同源供应商、隧道依赖供应商按需加
- **防噪音**：模型下线告警只发一次（状态快照对比，重复下线不刷屏）；页面 diff 变化 24h 内同源只提示一次
- **降级**：任何源拉取失败（网络 / 被墙 / 超时）→ 静默跳过 + 连续 N 次失败才标 🟡「外部源 X 连续拉取失败」（单次失败是网络常态，不报——同"会判断"反面教材：报错看持续性）
- **风险**：全只读（拉公开数据 + 本地 key 拉自己账户的模型列表），零写风险；凭证只在本机用、不外发

**验收**：①构造模型列表少一个模型 → 检测出下线 🔴 且不重复 ②状态 JSON 构造降级态 → 🟡，恢复 → 自动解除 ③公告页内容改一个词 → 提示变化附链接 ④源注册表加新行 → 新源自动入扫 ⑤连续拉取失败 N 次 → 🟡 提示。
**R · 回归**：模型清单 diff 单测（新增 / 下线 / 字段变化三类 fixture）+ status JSON 状态映射单测 + page-diff 去噪（24h 同源一次）单测 + 连续失败计数单测。

### P7 · 会照应（跨载体关联发现）

**问题（实证）**：各能力域盯的是「运行时状态」和「执行层翻车」，但**规划层的跨载体耦合没人自动发现**——"某监控卡 ↔ 外部信号能力重叠"、"守护进程里程碑 ↔ 重启统筹共享活跃判定"、"云服务器卡 ↔ 告警通道"、"生态盘点 ↔ 守护进程文档"，**全部是人问出来才发现的**。重复卡检测只查账本内部同标题同内容（文本级）；监控注册表只管运行时对象。缺：**卡 ↔ 方案 ↔ 生态 ↔ 文档之间的实体级照应**。

**形态（按宪法：无 Agent 检测优先，0 token 确定性核心）**：
- **实体关联索引**：规范化实体表 + 别名归一——**首建即全量，源 = 六路**：①监控注册表的 CI 名 ②方案各节标题 ③任务卡标题 ④文档文件名（历史项目实体）⑤归档目录顶层名（历史部署 / 备份实体）⑥记忆库标题——**不留历史盲区**：新卡提到任何历史项目都能命中。**修正**：归档目录名多为 `<任务>-<时间戳>` → 建索引时**剥离时间戳 / 纯 hash / 纯日期段**，只留语义名（否则实体表被时间戳污染、IDF 过滤直接失效）
- **对账器**（挂巡检每日一次，或日报管线尾部；毫秒级纯文本匹配）——每次有新卡 / 新文档 / 新方案节落定后跑，三类输出：
  1. 🟡 **耦合预警**：「此卡 / 文档与 N 处既有内容同实体」
  2. 🟡 **落地缺口**：「方案说了 X 但无卡认领」
  3. 🟡 **脱管任务**：「有卡在做 X 但方案没提」
- 结果落日报「💡 统筹提示」段——**每天打开日报就看到谁跟谁可能重叠**，不用人问
- **后置自检流程（治"靠自觉"的根）**：建卡 / 建档 / 改方案成为**动作链固定一步**——每次落定必跑一次对账器再收尾（比自觉可靠，因为是流程不是习惯）
- **可选增强**：日报解读顺手读对账输出给语义级建议（低成本渠道，不开也行）
- **防噪音**：同实体同类型 24h 只提示一次；**阈值改为「耦合 ≥2 处且该实体非高频词」**（修正：在"十几张卡 + 数十份文档 + 上百条记忆标题"的规模下，守护进程 / 迁移 / 代码托管 这类高频实体几乎必然 ≥2 处 → 原阈值等于全量提示）；误报走「忽略并记录」沉淀忽略规则
- **IDF 过滤**：实体按出现频次分档——出现于 >10% 载体（约 >24 处）的实体**不触发耦合提示**（只用于索引，不用于告警）；**通过门槛 = 人工抽查 20 条误报 ≤20%**，超了回炉调阈值

**载体**：纯脚本 0 token；对账器与巡检共用调度；输出只落日报 / 文件不推送（统筹提示是日报语义不是告警语义，⬜ 级）。

**验收**：①新卡落定 → 对账器提示与方案 / 生态文档 N 处同实体 ②删掉某方案节但卡还在 → 报「方案无主」或「卡脱管」③同实体 24h 不重复提示 ④建卡后置自检实测触发（每次建卡必跑，收尾自查加一条）。
**R · 回归**：实体归一化单测（别名 / 大小写 / 路径形式）+ 对账三类输出 golden dataset + 阈值（耦合 ≥2 处）与 24h 去噪单测。

---

## 7. 推荐节奏与观测期

| 阶段 | 内容 | 工作量 | 前置 |
|---|---|---|---|
| **P0** | **重启统筹**（协调器 + 排队 + 通知 + 打断清单） | 1-2 会话 | 无 —— 痛点最直接、技术最独立，**先做** |
| **P4** | **会学习**（翻车复盘 + 坑库自动化 + 阶梯升级） | 1-2 会话 | 无 —— 与 P0 并列优先 |
| **P5** | **会审计**（写入守门人） | 1-2 会话 | 无 —— 依赖宿主 `fs/*` 事件门 |
| P1 | 会动手（催办 / 改期、失败卡重试、升级预检 + 预览确认） | 1-2 会话 | 无（归档 / 空壳已自动，不在 P1） |
| P2 | 会盯人（watch + 🔴推/🟡进日报） | 1 会话 | 与 P0/P1 共用通知通道 |
| P2.5 | 会判断（LLM 日报解读，低成本渠道） | 0.5 会话 | 日报 / 账本内容充实后更有意义 |
| P3 | 会派活（L1/L2 自动 + L3 预览 + token 监测） | 2-3 会话 | P1 的问题识别 + P2 通知通道 |
| P6 | 会望风（外部信号感知） | 0.5-1 会话 | 复用告警通知 / 冷却 / 上限 |
| P7 | 会照应（跨载体关联发现） | 0.5-1 会话 | 日报 / 账本内容充实后更有意义 |

**施工序列定稿时的三处修正**（原推荐顺序漏项，实测补正）：
1. **P2 必须前移到观测期之前**（观测期"开什么"含告警巡检，误报率 KPI 由它产出；P2 排在观测后则观测期无数据源）
2. **补日报管线改造**（日报是误报率逐日记录的载体，原顺序漏列）
3. **放行门槛补样本量**（累计告警 ≥50 条，非只看天数）

落地为 12 张任务卡（骨架 → P0 → P4/P5 并行 → P2 最小 watch + 日报改造 → 观测期 → P1 → P3 → P6 → P7 → P2.5）。

### 观测期（裸跑期）

> 外部审查意见原文：「P0 完工后，请先在生产环境『裸跑』3 天（只开 Watch 和 Audit，不开自动执行 P3），观察误报率。如果在『只盯人、不派活』的阶段，误报率就超过了 10%，说明判定规则还要调。等误报率降至 5% 以下，再放开 P3 的自动化开关。这能避免『一上来就自动合并 / 自动重试，结果把错的逻辑加速放大』的惨剧。」

| 项 | 规定 |
|---|---|
| **窗口** | P0 完工后 **≥3 个自然日**（含 1 个工作日 + 1 个周末，覆盖不同负载形态） |
| **开什么** | 告警巡检（只盯人）+ 写入审计（**A 层拦截 + B 层只告警不自动回滚**）+ 重启统筹（**仅"人主动请求"路径**） |
| **开关清单（逐项归属）** | 观测期一律关闭的"自动执行" = ①全部 L1/L2 自动动作 ②**低水位自动重启**（只接受显式请求，不做无人触发的自动执行）③**B 层的自动回滚**（降为人工确认）④**组件自修复的自动修复分支**（只告警等人批）。四项统一由开关控制，默认全 `false` |
| **关什么** | 自动执行全关 |
| **看什么** | 第一 KPI = **误报率**（标记误报数 / 总告警数），逐日记录进日报 |
| **放行门槛** | 误报率 **<5%** 才允许打开自动化开关；**10% 是警戒线**（>10% 必须回头调判定规则）。**样本量门槛**：累计告警 **<50 条**时**不得放行**（3 天 × 日上限 10 条 = 最多 30 条，比例统计不显著）→ 样本不足则**延长观测**，不许"靠样本少轻松达标" |
| **疑似误报口径** | 未标记但超时未 ack（>4h）的告警计入"疑似误报"单列；**疑似占比 >10% 同样不得放行**——防"不标就是 0%"的自欺 |
| **反面样本** | 实测：调度器 `error` 字段残留旧错被当故障 → 这类"看时间戳新旧"的规则正是观测期要调的对象 |
| **未达标怎么办** | 继续观测 + 调规则；**不许"先开自动再慢慢调"**（自动动作会把错误逻辑放大，且难追溯） |
| **降档联动** | 观测期内体系若触发降档 → 观测计时**重置**（异常期的误报率不可作放行依据） |

**MVP 砍单线**：
- **只做一条 = P0**（重启统筹）：最痛、技术最独立、不依赖其他能力域，且能立刻验证「协调器 + 通知 + 审计」三件套是否成立
- **单条做到一半的停止边界**：协调器 + 排队 + 通知可独立交付；打断清单与多播通知可延后（降级为"重启前落一份活跃会话清单到文件"）。**半成品一律不进生产**
- **依赖修正**：巡检 tick 被多个能力域共同挂载 → 建议先落一层**轻量调度器**（每个采集器独立注册 + 独立失败隔离 + 统一 tick 契约），再挂功能；否则巡检自身故障会连带拖垮一半能力
- **工作量口径修正**：按「实现 + 实测 + 文档」三段计，实际按 ×1.5～2 计；含面板改造 + 动作注册表 + 回滚脚本的，或含拦截器 + 磁盘 watcher + 规则引擎 + 凭证 CLI 的，按 ×2～2.5 计

**Non-goals（显式排除）**：
1. 不做云端控制面（外部通知通道另案，本轮只留适配器接口）
2. 不做跨机实时心跳（改"开机同步检测双开"）
3. 不做 LLM 质量 judge（单用户样本小 + 成本）
4. 不做"未知故障"的自动修复（只做预登记白名单内的确定性动作）
5. 不做资产来源的全自动判定（来源不明标 `unknown` 交人确认，不猜）
6. 不替代人的验收决策（L3 及涉意图动作只通知 / 预览）

### 7.1 管家自身交付与版本（多个组件是一个产品）

**问题**：各能力域落地为多个独立脚本 / 插件，却共同构成一个产品——怎么打包、怎么统一升级、怎么整体回滚、跨组件 schema 怎么协同。

**组件清单（一个版本号统管）**：

| 组件 | 形态 | 载体 |
|---|---|---|
| host | 宿主插件（服务端） | 路由 + 写入拦截器 |
| panel | 宿主插件（客户端） | 面板信息架构 |
| store | 共享库 | 状态文件读写唯一入口 |
| scheduler | 共享调度器 | 各 tick 统一注册 + 独立失败隔离 + 统一 tick 契约 |
| *-tick | 计划任务短命脚本 | 重启协调器 / 告警巡检 / 外部信号 |
| *-scan | 计划任务短命脚本 | 坑库扫描 / 对账 |
| audit-cli | CLI 包装 | 审计工具统一入口 + 发凭证 |

**版本策略**：
- **产品版本号**写在 `<DSH 主目录>/steward-version.json`；各组件启动时自检「我的版本 ⊆ 产品版本」
- **不匹配处置**：**跳过该组件、其余照常启动 + 告警 + 按宪法判自修复 / 重启**——"拒绝启动"会因一个组件掉队就让整套管家不干活：
  1. **跳过**：该组件不进工作状态，其余组件正常跑
  2. **告警**：面板 / 日报标 🟡「组件 X 版本不匹配，已跳过」——⚠️ **告警通道必须独立于管家自身组件**（日报文件 / 桌面宠物 / 守护进程通知，取可用者），否则面板自己掉队时人看不见
  3. **自修复 / 重启按宪法判定，不擅自动作**：「期望型 + S2」且修复动作已注册 → **可自动修复**，修完自检通过则回正常档；失败 → 🔴 + 保持跳过；「风险型 / S3 及以上」或修复动作未注册 → **只告警等人批**；需重启才生效 → **走重启闸门**，**绝不由组件自己杀进程**
  4. **降档联动**：任一组件因版本不匹配被跳过 → 按宪法视为「体系不稳」，全局临时降档，直到全部组件版本一致
- **状态文件 schemaVersion 与产品版本解耦**：产品升级不必然改 schema；改 schema 必须有迁移脚本
- **一键部署**：备份 → 铺实体 → 注册插件 → 重启 → 自检
- **一键回滚**：双向前滚 / 回滚 + 桌面入口

**交付验收（每个组件都要）**：standalone 单测 + 重启后自检 + 部署即自动备份 + 恢复脚本实测通过。

---

## 8. 风险与回滚

- 每个写动作：改前备份 + 一键恢复脚本 + 桌面入口 + **实测通过**才算交付
- 面板插件改动：部署脚本（自动备份）+ 回滚脚本（双向前滚 / 回滚）+ 重启后自检
- 会话类动作：两段式 copy → 目录相等校验 → 删源，绝不出现"删了源没拷成"
- 自动执行：后果严重度分级（L1 全自动 / L2 自动 + 备份 / L3 不自动）+ 每日次数 / token 上限 + 审计

### 8.1 状态文件清单（持久性 + retention + schemaVersion）

> 原则：**安全状态必须持久化，易失目录只放可重建的运行时状态**；每个文件带 `schemaVersion`，升级跑迁移；每项都要有保留策略，否则管家自己会把盘吃满。

| 文件 | 路径 | 持久性 | retention | schemaVersion | 丢失后果 |
|---|---|---|---|---|---|
| 监控注册表 | `<DSH 主目录>/steward-monitor-registry.json` | 持久 | 永久（CI 不删，标 retired） | v1 | 重新自动发现（S 档 / 期望值需重设） |
| 自治降档状态 | `<DSH 主目录>/steward-autonomy-state.json` | **持久（从临时目录迁出）** | 永久（保留最近 100 条迁移） | v1 | **fail-safe：默认 degraded**（放临时目录的方案重启 / 清理后会静默放行自动动作） |
| 维护窗口 | `<DSH 主目录>/steward-maintenance.json` | 持久 | 窗口结束即清 | v1 | 读不到 = 无维护窗口（可能误报一条重启期告警） |
| 告警状态 | `<DSH 主目录>/steward-watch-state.json` | 持久 | 每事件保留最近 1 条 + 90 天 | v1 | 冷却 / ack 重置 → 可能重复推一次 |
| 坑库数据 | `<DSH 主目录>/steward-pitfalls.json` | 持久 | 永久（沉底不删）+ 单文件 <10 MB | v1 | 重新积累（历史 seed 可重跑） |
| 会话健康 | `<DSH 主目录>/steward-sessions-health.json` | 持久 | 最近 90 天 | v1 | 重扫 |
| 实体索引 | `<DSH 主目录>/steward-entities.json` | 持久 | 全量重建（六路源） | v1 | 重建（秒级） |
| 外部源快照 | `<DSH 主目录>/external-sources.json` + 快照目录 | 持久 | 每源最近 30 份 | v1 | 首轮只报"有变化" |
| 审计流水 | `<DSH 主目录>/steward-audit/YYYY-MM.jsonl` | **持久 + append-only** | 永久；>30 天留摘要行 | v1 | **不可重建**（故必须随加密备份） |
| 误报反馈 | `<DSH 主目录>/steward-falsepositives.jsonl` | 持久 | 永久 | v1 | 校准数据缺失 |
| 自动动作去重 | `<DSH 主目录>/steward-auto-actions.json` | 持久 | 最近 30 天 | v1 | 可能重复执行一次（有上限兜底） |
| 重启请求 | `<运行时临时目录>/guard/restart-request.json` | **易失（可重建）** | 24h 过期 | v1 | 请求丢失（重发即可） |
| 守护进程状态 | `<运行时临时目录>/guard/guard-state.json` | 易失（运行时） | 进程重建 | **豁免** | 守护进程重新计数。**豁免说明**：守护进程是既有外部组件、状态文件由它自己写，不适用"无版本即拒启"的迁移契约——管家只读不写 |

> **跨机同步策略（阻断级遗漏）**：本表状态文件全在宿主主目录，而配置仓同步的正是宿主主目录的稳态部分 → **必须显式区分"持久"与"可同步"**，否则多台机器共享同一份冷却表 / 去重表 / ack 状态，互相压制对方的告警与自动动作。
> - **禁止随配置仓同步（单机运行时状态）**：告警状态（冷却 / ack）、自动动作去重、维护窗口、降档状态、会话健康、锁目录
> - **可同步（跨机共享语义）**：监控注册表（含设备短码，设计上支持多机）、坑库、实体索引、外部源清单、审计流水、误报反馈、升级候选
> - **判据**：文件内容带"设备短码 / 按会话 / 按设备"维度 → 可同步；带"时间窗口 / 冷却 / 去重 / 锁"→ 禁止同步

> **迁移契约**：任何状态文件 schema 变更 → 写 `migrate-<file>-vN-to-vM.mjs` + 启动时检测版本 + 迁移前备份；版本不匹配且无迁移脚本 → **拒绝启动该模块并告警**（不猜、不静默降级）。

---

## 9. 关键决策与理由

> 记录的是**决策本身与第一理由**，不含治理流水。

| 问题 | 决定 | 理由 |
|---|---|---|
| 重启时机策略 | **低水位 + 窗口兜底**（排除发起者后活跃 =0 立即重启、≥2 排队、凌晨兜底窗口、超 30 分钟提醒） | 硬杀无优雅退出，只能靠"等空闲"；"≤1"会把真活跃会话打断 |
| 通知分工 | **协调器发跨重启通知 + 面板管反馈**；强制重启的「打断清单」**要做** | 协调器活在宿主之外，重启期间只有它还能发消息 |
| 重启入口 / 强制 | 面板 + 会话内排队 / 桌面入口保持立即硬重启（急救）/ 面板加「立即强制重启」 | GUI 卡死时不能排队等空闲 |
| 终端防护是否拦截 | **实测无拦截**；降风险设计见 P0 | 计划任务链路可行，但建链接动作会被拦（改用 junction API） |
| 坑库组织方式 | **分主题目录 + 索引** | 单一大文件会膨胀且难定位 |
| 自动写坑库 | **是，全自动**（签名去重 + 门槛 ≥2 + 排除业务性失败） | 靠自觉必然漏（使用者的核心抱怨） |
| 动态权重 | **采纳** `score = count × 0.5^(距上次踩坑天数/30)`；30 天不踩降权沉底标「疑似已修复」 | 近期高频才是真风险 |
| 「想起来读」层次 | L1 开工自动带 Top3 + L2 技能指针先上，L3 实时提醒待验证宿主 hook | 把"记得读"变成流程而非习惯 |
| 坑的处置 | **阶梯升级**：1 次入库 / 2 次上浮 / ≥3 次跨会话生成升级候选（优先做自动检查项） | 坑的最高形态是消灭而非记录 |
| 写入审计定位 | **审计 ≠ 代笔**：管家当守门人，检查写入内容合不合规范 | 避免变成"代写"而失去把关作用 |
| 写入审计机制 | **分层**：长期约定 / 技能 / 核心脚本 → 写入前拦截（凭证 + 逃生舱）；文档 / 流水 → 写后审计 + 自动回滚 | 保护级别与写坏的后果对齐 |
| 外部推送通道 | **云端另案，不阻塞本轮**；本轮通知层做成**适配器架构**（本地三通道先实现，外部通道留标准接口） | 主动告警的主战场（人不在场时）需要外部通道，但采购不由本方案决定 |
| 人不在场认知 | 管家自动跑时**焦点不在 GUI** → 桌面宠物 / 会话消息对主动告警是废通道，只适合"人正开面板时"的低危提示 → 真告警靠外部通道 | 通道有效性取决于接收方是否在场 |
| 手动按钮清单 | 划掉已全自动的动作；手动只留：催办 / 改期、失败卡重试、升级预检、中断转卡 | 能无脑判断的全自动 |
| 自动执行门槛 | **按后果严重度分级，非成本论** | 成本可测，后果不可逆 |
| token 用量监测 | 新增（自动执行与管家触发的 LLM 调用都记），日 / 月汇总 + 阈值告警 + 超限自动停 | 自动执行的真实约束是预算 |
| 告警分级 | **🔴 主动推 / 🟡 进日报** | 避免刷屏；反面教材：残留旧错当故障 |
| 升级授权 | **分级授权**：体检后自动定方案；**低风险项自动备份后升级**；高风险（主程序 / 断裂面 / 魔改插件）仍报人批准后执行 | 风险与后果对齐 |
| 升级分级边界 | 🟢 无魔改自动升（快照→升→自检→失败自动回滚）；🟡 魔改 + 补丁重打自动升（快照→升→全量补丁重打→重启→验特征→失败自动回滚）；🔴 报人（主程序 / 禁升区 / 大跳版） | 同上 |
| 升级执行主体 | **计划任务脚本链（0 token）**，非建卡 LLM 会话 | 升级是确定性操作（跑命令 + 验证），不需要 LLM 判断 |
| 日报触发时机 | **当日首次开机后自动跑一次**；原计划任务保留作兜底 | 出报告的时间应跟着实际开工时间走 |
| 归档会话扫描范围 | **必须扫**（两棵树）+ 记账按 sessionId 不按路径 + 健康检查同范围 | 归档区是坑富矿且持续增量 |
| 多机同步监测 | 日报新增「📡 同步健康」段；告警低额加「加密 key 异常 🔴」（key 丢 = 仓永久不可读） | 此前只有零散链接检查 = 缺口 |

---

## 10. 假设与验证时机

> 规则：**任何"待验证"的能力都要在这里登记，排期时前置一个 spike**；验证失败的退路写清楚，避免开工后才发现路走不通。

| # | 假设 | 风险 | 验证方式 | 时机 | 失败退路 |
|---|---|---|---|---|---|
| A1 | 宿主支持插件监听工具失败事件（实时提醒） | 不支持则该层无法实现 | 查宿主 SDK / 写最小插件试 | 对应能力域开工前 | 只保留"开工自动带 + 技能指针"两层，删除实时提醒 |
| A2 | `fs/write-intent` / `fs/edit-intent` 可拦截 | 宿主编版本可能变 | 实测；主程序升级后复验 | 每次主程序升级 | 降级为"写后审计 + 回滚"（B 层），取消 A 层 |
| A3 | statuspage 类 `api/v2/status.json` 格式稳定 | 供应商改版 | 拉一次真实响应存 fixture | 对应能力域开工前 | 该源降级为 page-diff 模式 |
| A4 | 文本实体匹配误报率可接受 | 误报多 → 被无视 | 用现有文档 + 卡跑一次，人工抽查 20 条 | 对应能力域开工前 | 提高阈值 / 只做"卡↔方案"单类匹配 |
| A5 | 会话日志 usage 字段口径稳定 | 主程序改字段 | 已实测样本；每次升级复验 | 持续 | 回退为"按会话轮数估算" |
| A6 | 计划任务在终端防护环境下长期不被拦 | 被拦 → 自动化静默失效 | 已实测；每日查 Last Result | 持续 | **降级为日报级检测 + 显式 🔴**（"改启动钩子"不成立——钩子只在开机跑，替代不了 5 分钟 tick） |
| A7 | 代码托管仓可作为审计 JSONL 的备份载体 | 隐私 / 体积 | 评估单文件大小与加密开销 | 状态文件清单落地时 | 只备份摘要行，全文留本地 |
| A8 | 人会看"待验收"队列 | 自动动作堆积 | 上线后观察 2 周 | 自动执行之后 | 加"超 N 天未确认提醒 / 自动确认"策略 |
| A9 | **桌面宠物通道可被外部脚本调用** | 已实证端点清单**无文本能力** → 推送落不了地 | 打文本通道补丁后复验；日常探测端点 + 纳入健康底栏 | 告警能力域开工前（**阻断**） | 降级为"会话 + 报告文件"，面板红标「推送通道不可用」 |
| A10 | 拦截器不拖慢文件系统路径（每个事件都过一遍） | 宿主写文件变卡 | 加路径延迟预算（P95 < 5ms）+ 压测 | 审计能力域开工前 | 只拦保护区路径（白名单前置，非全量监听） |
| A11 | **观测期误报率能降到 <5%** | 降不下来 → 自动执行长期关闭，管家只能"盯人" | P0 完工后裸跑 ≥3 天，逐日记录误报率 | 自动执行开工前 | 继续调规则；若始终 >10%，降级为"只建议不执行"（等同长期关停） |
| A12 | 管家 host 能调任务看板 Host API（跨插件 HTTP + 鉴权） | 调不通 → 写操作 / 自动合并全废 | 读看板路由清单 + 最小请求试 | 写操作开工前 | 退化为"只读账本 + 一键跳转手动操作" |
| A13 | 任务卡执行可限制权限 | 不支持 → 护栏落不了地 | 查卡字段 + 跑一张受限卡实测 | 自动执行开工前 | 改用"卡 prompt 写死禁令 + 执行后审计 + 失败回滚" |
| A14 | 归档移动后会话文件 mtime 是否保留 | mtime 变了 → 归档后重复全扫 | 对归档批次抽样比对 mtime / 末帧 seq | 坑库能力域开工前 | 记账改"末帧 seq + 内容 hash" |
| A15 | 会话目录 → sessionId 映射稳定 | 映射不稳 → 活跃判定错、扫描重复 | 读目录结构 + 抽 3 个会话交叉验证 | 重启统筹开工前 | 从会话列表接口投影取映射（多一次 RPC） |
