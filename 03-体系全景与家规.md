# Agent 运维体系全景（角色 · 分层 · 家规 · 路线）

> **本文件是这套体系的"当前全景"**：**谁来干**（班底与分层）· **凭什么敢让它自动干**（家规）· **现在到哪了、下一步走哪**（现状与路线）。它把散落在几十份方案与交付记录里的设计意图、实况与纪律收拢成一份，**只写"是什么 / 到哪了 / 要怎样"**；技术细节一律回到各分册（分册不随本仓公开）。
>
> **版本**：v1.10（在 v1.6 基础上**增量刷新**，正文按增量补写、不整体重排） · **本版观测基准日**：2026-09-22（上一版为 2026-09-20 版，见本仓 tag `v1.6`）
> **本版增量（四处）**：① §0 结论由五条扩为**七条**（新增"会话会自我续命"与"放行权要分档"两条）；② 新增 **§3.8 会话生命周期的自治**——讲"长会话撞上上下文上限之后谁接班"（完整方法论见本仓 `09` 号稿）；③ 家规由 **R1–R8 扩为 R1–R9**（新增 **R9 · 自主动作分档**）；④ §3.1 能力档位与 §3.2「关着的清单」按 2026-09-22 实况更新。
> **占位符说明**：`<DSH 主目录>`、`<工作区根>`、`<插件目录>`、`steward-*`（管家组件族）为占位名；**A 机 / B 机 / C 机** 指三台长期运行的工作站（A = 日常开发机，B = 常驻稳态机，C = 后加入的常开宿主）；模型名与第三方服务名仅示意，替换为读者自有渠道即可。
> **与 02 号文档的关系**：02 号《自治式运维管家》是 **2026-09-11 的设计快照**——记录"当初怎么想的"；本文是**更新后的全景**——记录"现在长什么样、为什么敢这么开"。两份文档**不互相替换**，正文各自独立。

---

## 0. 先给七条最反直觉的结论

如果你只读几句，读这几条。它们不是设计原则，是**被事故逼出来的结论**：

1. **"关着"不等于"没做"，而且关着比开着更需要设计。**
   自动动作一旦放开就很难收回，所以整套机制先以"**零件造好、默认关闭、谁都不许开**"的状态存在，等判据、影子、回滚点三样齐了才逐个放行。结果是：你会长期维护一批**处于"已实现但零动作"状态的代码**——它们必须能跑、能被观测、能被审计，但一轮都不许真动手。这是有成本的，成本在 §4 R3 里如实写。

2. **"改判定规则 = 换尺子"，两个观测对象必须一起重新计时。**
   以为"改一下统计口径不影响放行进度"，是错的。只要动了判定规则，之前积累的样本就不再是同一把尺子量出来的——**两边的计时都要清零重来**。而只改统计归类（不碰判定）则不重置。这条规则被真实判例校准过两次，见 §4 R1。

3. **纪律不是补丁，是产品的一部分。**
   每个"敢开"的背后都要有四样东西：①判据 ②不真做的影子视图 ③回滚点 + 演练 ④运行时可关的熔断开关。少了任何一样，这个能力就只能停在"开发完了但不敢开"。**这四样东西的维护成本，才是自治系统真正的主力成本**，不是算法。

4. **一个系统的"对象"变了，判据就必须跟着换。**（2026-09-16 新增）
   当这套体系从"只盯机器和零件"扩到"也服务使用者的日常"（端入口、主动推送、记忆注入、投喂采集）时，原来的尺子就量不到新对象了：**运维面的失败模式是"改坏生态"，服务面的失败模式是"骚扰使用者 / 泄露隐私 / 被远程拿走控制权"**。用同一把尺子会两头错配——要么推送被"等生态样本攒够"无限期卡住，要么服务类动作借着"我只是个推送"绕过家规。**所以有两个面，就必须有两套判据**（§1.3、§4 R6）。

5. **只要有两件事会同时写同一块地方，"谁让谁"就必须写下来。**（2026-09-17 新增）
   升级链改宿主与插件实体、交接搬三份仓、修复层改运行配置、收纳搬临时目录、服务面写记忆库——这些线**会同时动生产**。缺了让路规则，最坏态是**两个写者同动一块地方**，而这事只能靠"碰巧没撞上"。**规则要在写者上线之前入表，不是等到撞了才补**（§4 R8）。

6. **一个体系的真实上限，取决于它能不能把自己"交出去"。**（2026-09-22 新增）
   长跑的会话总会撞上上下文上限。监控早就知道会撞——但它只能**提醒**；而"开一个新会话、把在途的活交过去"这一步一直是人做的。于是：**自主推进的主线一到上限就断在人这里**，断点不在检测、在动作。补上这一步之后闭环才成形：**会话自己写交接档、自己开新会话、自己标注接力关系**，"提醒"从一个提示升级成一个动作。限度也要说清：交接内容必须由**最懂的那一个**（当前会话自己）来写——自动化可以替你开会话、改标题、改绑工单，**不能替你写"下一步"**（§3.8，方法论见 `09` 号稿）。

7. **"数据够不够放行"与"谁有权按放行键"是两个问题，需要两条规矩。**（2026-09-22 新增）
   放行判据（`07` 号稿）解决前者；但若不定后者，闸门就会退化成一个**谁都能按的按钮**。⇒ 把动作按「**可逆性 / 是否出机器 / 是否花钱 / 是否动别人的东西**」分成两档：**可直接执行**（只读取证、写报告与留痕、做备份与回滚点、**关回任何开关**、例行收尾）与**必须先问所有者**（首次放行自动开关、动生产文件、动进程、花钱、外发、不可逆、跨机生产、**给自己扩权**）。**单向性**是这条规矩的灵魂：**关回永远比放开容易**——关回可以直接做，再放行仍要先问（§4 R9）。

---

## 1. 班底：谁在干什么

### 1.1 班底表（代号 → 职责）

| 代号 | 占位组件名 | 职责 | 一句话 |
|---|---|---|---|
| **大管家** | `steward-butler` + `steward-tickd` | 内核：看 → 想 → 报 →（点头后）动手 | 体系的自维护内核：先只看、再报告、最后才动手 |
| **女仆长** | `steward-board` | 派活、记工单、盯完成 | 卡片 = 真 agent 会话，干活有据可查 |
| **小护士** | `steward-firstaid` / 修复层 | 救场、安全模式、回滚 | 出事时能起来的那个"自己" |
| **小会计** | 记忆库 + 坑库 | 记事沉淀 | 踩过的坑不会第二次踩 |
| **小秘书** | `steward-secretary`（**服务面**） | 端入口、主动推送、投喂采集 | 使用者随时找得到它，它也会主动来找使用者 |
| **小保安** | `steward-guard` / 闸门 / 锁 / 沙箱 | 守着别倒 | 只计数不猜原因，把判断交给该判断的人 |
| **控制室**（单列） | 大管家面板 + 生态面板 + 日报周报 | 人看的地方 | 一眼知道"今天有没有事" |
| **家规** | 裸跑期 / 影子视图 / 锁后交付 / 四级熔断 / 回滚演练 / 服务面判据 / 账本仲裁 / 跨线契约 | 让它们敢开的前提 | 纪律层，不是功能 |

**分工一句话**：大管家是**内核**，女仆长 / 小护士 / 小会计 / 小保安 / 小秘书是**五个执行角色**（前四个对内管生态，**小秘书对外服务使用者**），控制室是**界面**，家规是**纪律层**。

### 1.2 为什么是"角色"而不是"模块"

把自治能力拆成**有职责边界的角色**，而不是一堆互相调用的模块，换来三件事：

- **责任可指认**：出问题时能说清"这是女仆长的账本没对上"，而不是"某个 job 挂了"。
- **开关可分级**：可以只关"派活"，而"记事"照常跑（见 §4 R4 的模块级熔断）。
- **可读性**：面板上一行"小护士：安全模式可人工起，自动起未接线"，比"repair-orchestrator: partial"清楚得多。

代价也很实在：**多套 L 编号会并存**（熔断四级 / 三层自愈 / 演练三档 / 放行分级），同名不同义，必须配一张对照表（见 §4 R4 的对照表与"防混用三条"）。

另外，角色化还有一个**运维上的隐性收益**：角色可以各自独立地"造好、关着、等放行"，而不必整体上线——这正是"锁后交付"（§4 R3）能成立的前提。

### 1.3 一个班底、两个面、两套判据（2026-09-16 新增）

| 面 | 对象 | 失败模式 | 判据 |
|---|---|---|---|
| **运维面**（对内） | "这台机器和它的零件"：宿主、会话、任务、插件、备份、外部信号 | 改坏生态（误删、误重启、误归档、静默失败） | 家规 **R1–R5**（裸跑期 / 影子 / 锁后交付 / 四级熔断 / 回滚与演练） |
| **服务面**（对外） | "使用者的日常"：端入口、主动推送、记忆引擎、投喂采集、建议 | 骚扰使用者 / 泄露隐私 / 被远程拿走控制权 | 家规 **R6**（打扰预算 · 隐私边界 · 远程降级 · 入口最小面 · drain 语义）+ **R7 / R8** 的横切规则 |

**为什么必须分开**：运维面的裸跑样本池统计的是"生态事件误报率"——推送和记忆注入**根本不在那个池子里**。共用一把尺子，要么服务面动作被无限期卡住，要么绕过家规。（代价：多维护一套判据 = 多一处口径漂移点，如实写在 §4 R6 末尾。）

**这个"两面"的划分本身也踩过坑**：服务面并入当周，就出现了"同一件事该记在哪本账、谁说了算"的争议，于是才有了 §4 R7 的账本仲裁规则——**先有账本主键，才谈得上多线并行**。

---

## 2. 五层架构

```
      ┌─────────────────────────────────────────────┐
 ⑤    │ 家规层   裸跑期 · 影子视图 · 锁后交付        │  让"敢开"成为可证的事
      │          四级熔断 · 回滚+演练 · 服务面判据    │
      ├─────────────────────────────────────────────┤
 ④    │ 控制室   大管家面板 · 生态面板 · 日报/周报    │  人**看**的地方（只读展示）
      │ 端（新） 手机 / A 机 / B 机 —— 服务面入口     │  人**用**的地方（2026-09-16 新增）
      ├─────────────────────────────────────────────┤
 ③    │ 角色层   女仆长 · 小护士 · 小会计 · 小保安    │  四个执行角色（**运维面**）
      │          小秘书（新 · **服务面**）            │  服务使用者的日常
      ├─────────────────────────────────────────────┤
 ②    │ 内核层   大管家 steward-butler + steward-tickd│  看→想→报→（点头后）动手
      ├─────────────────────────────────────────────┤
 ①    │ 地基     steward-guard · 闸门 · 锁 · 沙箱    │  一切的地面（含小保安的底座部分）
      └─────────────────────────────────────────────┘
```

**层间铁律**（三条，都是设计时定死、运行中不许绕过的）：

- **内核不自己干活**：需要 LLM 会话干的活**必须人批建卡**，内核**禁止自动建卡**——它只负责"把活派给女仆长"，不负责"自己去把活干了"。理由：自动建卡 = 自动烧钱 + 自动产出没人验收的结论。
- **控制室只读**：面板**任何路径禁止调用 LLM**，也不许写账本、不许改开关值——只展示。理由：一旦面板能写，它就同时成了"事实来源"和"展示层"，两边会不自洽。
- **家规先行**：没走完家规的动作**不许进生产**（见 §4）。

**为什么"端"要单独放在 ④ 层而不是角色层**：入口的成败判据（HTTPS、认证、失败锁定、只绑本机回环）是**基础设施属性**，与"在上面展示什么、推什么内容"是两件事——所以入口归运维侧的交接线，体验归服务面（§3.5）。

## 3. 现状全景（按"能干什么"分档，不按"做了什么功能"）

> 下面的判定**只用现场取证**（跑命令 / 读状态文件 / 打本机 API），不采信任何文档自述。这是本设计稿的一条纪律：**口径冲突时以生产实况为准，冲突处如实并列标注，不抹平不美化。**
> 本节所有数字都是**某一个时点的观测值**，不是设计参数——借用判断方式与量级，不要照抄阈值。

### 3.1 能力档位一览

| 角色 | 能力档位 | 关着的 |
|---|---|---|
| 大管家（内核） | **看 + 报**（盯人 / 判断 / 学习 / 审计 / 望风 / 照应 全在跑）；**动手档原为"一律关着"，本月已开三处**（上下文看门 · 队列叫醒 · **会话自动接力**，均已真实投递 / 真跑通，见 §4 R3 与 §3.8） | 四把总开关中仍关 3 把；模块级开关关 6 个（09-20 口径；**09-22 起接力相关两个已放行**）；环境依赖型 job 关 5 个 |
| 女仆长 | **在跑**：建卡 / 查重 / 六列看板 / 定时卡 / 待验收 / 主线分组 / 归档与查重 | 主线自动归档（五条前置未满足） |
| 小护士 | **安全模式可人工起**；真机崩溃链路已证；**自动起小护士未接线** | `DIAGNOSE_ONLY`（自愈闸）不放开 |
| 小会计 | **在跑**：定时扫会话翻车 → 签名聚类 → 分主题坑库；开工简报带高频坑 Top3；跨机语义合并已接线 | 记忆库跨机合并窗口（生产窗口几乎不满足） |
| 小秘书（服务面） | **通道已打通**（企业 IM 机器人长连接：免可信 IP / 免公网回调 / 免备案域名）；**常驻宿主已定案并完成改造**（免密登录 + 运行时 + 守护 + 开机自启 + 永不睡眠） | 端入口**未开通**（与交接线云中继合并，不另起一套）；采集**未开工**（硬边界：仅 B 机）；记忆引擎 v2 **未开工**（方案过审判"不合格"，5 项阻断） |
| 小保安 | **在跑**：守护进程 / 闸门 / 四类锁 / 回滚点三层异地 | 异地化 job（外接冷盘离线）；部分 tick 脚本跨入口无锁 |
| 控制室 | **在跑**：大管家面板 8 个 Tab + 生态面板六类只读总览 + 日报/周报 + 只读健康监控 | 两处实体缺失的 insert 项；升级体检只看包索引 |

### 3.2 关着的清单 + 解锁条件（一表总览）

**这张表才是"这个系统有多保守"的直接证据**——它列的不是"没做"，而是"做好了但没开"：

| 关着的东西 | 归属 | 类型 | 解锁条件（原文口径） |
|---|---|---|---|
| 四把总开关（派活 / 重启 / 回滚 / 自愈） | 大管家 | 家规·锁后交付 | 裸跑期达标（池级 ≥25 + 独立 ≥15 + 稳态零真误报 → L1 档）**且所有者显式拍板开闸** · **现状：门槛已达标，派活键已开（本月）；其余三把仍关** |
| 若干个模块级开关 | 大管家 | 四级熔断 | 逐模块显式拨闸；**开了要能说清为什么** |
| 环境依赖型 job（5 个） | 小保安 | 环境依赖 | 外接冷盘重新在线后恢复；另有 2 个因排队与看门联动而暂时关闭 |
| 待放行的剩余动作 | 大管家 | 家规·锁后交付 | 判据已满足，只差"**改表 + 接线 tick + 48h 影子对照**"与所有者放行 |
| `DIAGNOSE_ONLY`（自愈闸） | 小护士 | 家规·缺陷未修 | 跨盘静默失败缺陷修复 + 真机复验通过 |
| 看板主线自动归档 | 女仆长 | 家规·观察期 | 五条前置（见 §3.2 注） |
| 记忆库合并窗口 | 小会计 | 前置缺失 | 需"冷接机"流程，**待拍板** |
| 收纳线的写操作批次 | 大管家 | 家规·写操作 | "是写操作，**必须等裸跑期结束**" |
| 升级分级授权（自动升级） | 小保安 + 大管家 | 前置**五项** | ①铁律修订 ②巡检载体重建 ③脚本链实装 ④端到端演练 ⑤登记 + 读降档 + 读并持升级锁 + 留痕——**②③ 已完成、①④⑤ 仍未做，总闸文件不存在，不通过不许开自动** |
| 服务面的一切自动动作 | 小秘书 | 家规·判据刚定稿 | **判据表已定稿**（§4 R6），但每项动作仍要各自过 放行门槛 / 锁后交付五条门 / 开关放行 |
| ~~会话自动接力（两个开关）~~ | 大管家 | 家规·锁后交付 | ✅ **已放行（2026-09-22）**：对话内通道与定时通道均已开——前置是**真接力实测通过**（含权限两段断言）；护栏 = 单日上限 + 同主线最小间隔 + **连续失败即停招**；**回退 = 关开关**（属"可直接执行"档，见 §4 R9） |

> **看板主线自动归档的五条前置**（原文口径）：①判定上线且**观察期 ≥2 周零误判** ②完成一次回滚演练 ③单日上限（≤2 条线/天）+ 冷却 7 天 ④动作类型登记进白名单表 ⑤与上游能力项一起重审。

### 3.3 一条实测事实：崩溃链路与"只计数"

守护进程（`steward-guard`）的设计原则是**只计数、不猜原因**：

- 在固定时间窗口内连续崩到阈值（实测参数为"120 秒内 3 次"）→ 唤醒修复分身 → 分身写一份 ready 文件 → 守护进程转入交接（HANDOFF，只探活）→ 稳定后回到观察态。
- **真机实测**：连杀宿主进程三次 → 唤醒分身 → 写 ready 用 2 秒 → HANDOFF → 分身自主拉起服务返回 HTTP 200；**从杀到恢复 2 分 44 秒，20/20 通过**。
- 注意口径区分：**设计文档里查无"120 秒"字样**——那是运行中实例的参数（`crashWindowMs`）。引用时请写明"实测参数"，别当成设计不变量。

**为什么"只计数"很重要**：让守护进程去做根因判断，等于让一个只看得到进程存活信息的组件去裁决"这是不是我该杀的进程"——实测过一次自匹配事故（守护进程把自己的调用者当成守护进程误杀，导致守护全停、需要人工救场）。结论：**只计数、不做根因判断；拿不准就停下来等人**。

### 3.4 两条硬约束（实测钉死，2026-09-17）

这两条都是"设计时没想到、部署时才撞上"的，写在这里是因为**任何做同类系统的人都会撞到**：

| # | 现象 | 为什么会这样 | 处置 |
|---|---|---|---|
| ① **认证门与存活判据天然冲突** | 新版宿主在未带令牌时访问首页返回 **401**，而守护进程的存活判据是"**必须拿到 HTTP 200**" ⇒ 守护进程判"服务没起来" → 杀 → 拉起 → 再杀，**死循环** | 存活判据是"HTTP 200"这个**过窄的断言**，它把"服务起来了但要求认证"误判成"没起来" | 升级这条路之前**必须先改探测判据**（把 401/403 视为"活着但需认证"），验收特征也随之改；换格式的端口与白名单写法**实测都不是解药** |
| ② **账本 = 进程独占锁** | 看板账本用 `<DSH 主目录>\<看板模块目录>\ledger-v2.lock` 记持有者 pid ⇒ **多个实例不能共享同一份账本**；同理**同一个 `<DSH 主目录>` 不能跑两个实例**（会话与存储会互相覆盖） | 单写者假设被写进了文件锁，是刻意设计（防"两个写者同动一份账本"），代价是不能简单复制主目录来横向扩容 | 要横向扩容只能**每个实例一套主目录 + 分别派活/回传**；这两件事本身还没有现成能力 |

### 3.5 容量实测：一个实例能扛几个并发会话（2026-09-17 · 隔离环境）

> 背景：为了把"干活会话"挪到一台常开宿主上，需要先知道单实例容量。**隔离实例实测（生产未动）**：

| 观测 | 结果 |
|---|---|
| 单实例容量 | ≈ **4 个中等强度并发会话**；8 并发时单实例**饱和**（峰值单核 ≈157%、耗时 38.8s） |
| 摊到 2 个实例 | 同样 8 并发 → **30.4s（快约 22%）** |
| 代价 | **每个空转实例常驻 8~10% 单核 + 约 200MB 内存**；**未饱和时多开反而略亏** |
| 口径建议 | **2 实例起步、每实例 ≤4 并发会话** |
| 机理 | 运行时是**单线程事件循环**——一个进程里所有会话共用一条线程，所以"多加会话"与"多加实例"是两类不同成本 |

**这次实测同时暴露了三个没做完的环节**（如实列）：① 卡片**派活**到分身实例（当前只能由"跑着看板的那个进程自己"建会话）② 干完**回传**主实例 ③ 验收后**归档分身会话 + 清缓存**（其中"归档时长口径"已先行定档：见 §3.6）。

### 3.6 两处已经定档的口径（对外也值得抄）

| 口径 | 内容 | 为什么这么定 |
|---|---|---|
| **会话归档分档** | **使用者手开的会话 → 24h 收；看板卡执行会话 + 子代理派生会话 → 6h 收**（时长 = 日志最后写入时间起的"未活跃"时长） | 卡会话与派生会话是**自动化产物**（无人回看）⇒ 6h 即可搬走；手开会话可能要回看 ⇒ 留 24h。归档可逆：文件物理移出 + 写清单，恢复走设置页或恢复接口 |
| **归档与"清账"的区别** | 「会话归档」= 会话文件物理搬家（降加载与 CPU）；「看板归档」= 卡片与主线的清账 | 两者目标不同、判据不同，**不能混着说**——混着说就会出现"归档了为什么面板还显示"这类投诉 |

### 3.7 服务面与入口的分工（终态口径）

**入口是交接线的事，体验是服务面的事**：交接线负责固定入口 + 认证 + 交接中继的底座；服务面负责"在这个入口上使用者看到什么、收到什么"（推送文案 / 移动端适配 / 采集落库）。两者**共用一台常驻宿主**——宿主选型是必拍项，已定案为"不买新机、用现成的一台老工作站"。

> **一处被推翻的旧口径（如实保留）**：服务面方案最初写"家人接入走免费档模型"，2026-09-18 被拍板作废——**家人相关数据只走本人这条链，不流入任何"本链之外"的第三方接口**，代价是额度与本人自用共用一个池。原文保留、结论更新，是本文档的通行做法。

### 3.8 会话生命周期的自治（2026-09-22 新增）

**一句话**：长会话撞上上下文上限时，**不需要人插手**——会话自己写交接档、自己开一个新会话、把在途工单交过去，并在界面上留下"谁接了谁的班"。完整方法论见本仓 **`09` 号稿《长会话的自动交接与接力》**。

**为什么值得单列**：这是体系里第一条**"面向会话自身"**的自治能力。此前所有自治盯的都是机器与零件（进程 / 端口 / 磁盘 / 仓库），这一条盯的是**自己的寿命**——意识到自己快装不下了，然后把自己交出去。

**它长什么样（现场口径）**：

| 环 | 谁做 | 关键约束 |
|---|---|---|
| 检测 | 上下文看门定期全机扫描（**周期 2026-09-22 由 5 分钟收紧为 2 分钟**），越过软限 / 硬限即投提醒 | 提醒有额度天花板：每会话每档每日 1 条 + 池级日上限 |
| 写档 | **当前会话自己**（agent） | 七段模板；第 5 段"下一步"写不出 ⇒ **不开新会话**（fail-closed） |
| 交接 | 接力器（一条命令）：建会话 → 校验权限 → 改名 → 投开工指令 → 回写档头 | 十二道闸门（防异常、**不防次数**）；权限**两段断言**（源码常量 vs 配置 + 新会话实际继承值回读） |
| 接手 | 新会话自己（两步：补标"已交接" + 进入自检） | 人工兜底通道 = 只写档不开会话，**且不受总开关约束** |
| 无人值守 | 调度器的定时动作 | 候选面**两个前置**（只对正在执行的工单会话）；**原文出机器之前**必过一道内容筛 |

**三条实测硬事实**（都在生产上真跑过，教训比结论值钱）：

- **真接力一次 2.7 秒**（建会话 → 权限回读一次命中 → 改名 → 投递指令 → 回写档头），接手会话自主跑完两步用了 10 秒；
- **标题轴丢过一次**：新会话的接力前缀在**接力后第一个回合边界被吞掉**（实测 **59 毫秒**）——根因是"剥前缀"只做了一层；修法是**分层解析**（状态轴照旧重写、接力轴原样保留），并**同步升级两处判定**（判据由"以…**开头**"改成"前缀段**包含**…"）。**通用教训**：改了写入方、忘了改判定方，是这类机制的典型故障形态，且它**不报错、只在行为上静默失效**；
- **"替已经收手的会话开会话"是一条必须写死的禁止项**：放行前实测发现候选面里绝大多数会话**早已停手**，照旧口径一放行就会对历史会话开工（每个候选一次付费调用 + 一个僵尸会话）。

**放行形态**：两个开关**初值一律关**；**首次放行属"必须先问所有者"档**（它会自动开会话 = 花钱、会改另一个会话的标题、会动在途工单），**关回属可直接执行档**——这条与下面 §4 R9 是同一条规矩的两个面。

---

## 4. 家规：九条（每条写三件事：为什么立 · 怎么判 · 代价是什么）

> 判据照抄原文口径；**代价一律如实写，包括"这条规矩让人不快的部分"**——不写代价的规矩会在第一次觉得烦的时候被绕过。
> **R1–R5 是运维面的规矩**（2026-09-15 前立），**R6–R8 是把体系扩到"服务使用者"与"多线并行"之后补的**（2026-09-16 / 09-17）；**R9 是在"自动动手"真正开放之后补的**（2026-09-22）——它管的不再是动作本身，而是**放行权**。

### R1 裸跑期

**为什么立**：自动动作一旦放开就难收回，所以先让整套机制**只观测、不改行为**跑够样本，用**真实误报率**而不是信心决定放行。原话：**"不许『先开自动再慢慢调』"**。

**怎么判**（分级放行，两档）：

| 档 | 适用 | 池级门槛 | 按机门槛 |
|---|---|---|---|
| **L1**（无害可逆动作全自动） | 只读 / 可逆 | 样本 **≥25** 条 + 独立状况 **≥15** + **稳态零真误报** | 观测天数满额（3 天）+ 该机自身误报率 **≤10%** |
| **L2**（动数据类） | 有损可回滚 | 样本 **≥50** 条 + 独立状况 **≥30** + **稳态零真误报** | 同上 |

- **样本池 = 跨机并集**（两台机器的样本是同一规则在不同环境的两次采样）；**放行按机**（先达标先开，已开自动的那台退出观测池）。
- **独立状况**的去重键 = 设备 + 日 + 事件 ID + **对象标识**；另报"刷屏比 = 1 − 独立状况 / 条数"。
- **封段重置规则**：**改判定规则 = 换尺子 = 两处观测一起重置计时**；只改统计归类（不碰判定）**不重置**。两次真实判例：一次"采集排除某类归档目录"属真改规则 → 重置；一次"疑似只计推送类条目"属统计归类修正 → 不重置。
- **实测速率**（供读者估算自己的样本窗口）：稳态机 ≈2 条/天 · 开发机 ≈7 条/天 · 池级 ≈9 条/天。
- **观测期起点必须只有一个真源**：状态文件里的 `startedAt` 是判定用起点，配置里的总起点只是展示元数据——两者不一致时**以状态文件为准**。这条是事后补的，起因是同一份系统里存在两套"起点"口径，被误读成矛盾（实测两套口径的门槛判定结果相同，但"看不出谁对"本身就是缺陷）。

**代价（如实写）**：

- **放宽样本门槛并不会让放行立刻发生**——真正的卡点是误报账，不是样本量。
- **单机口径 + 稳态机 = 死锁**：告警少本来是好事，却成了放不了行的理由。
- **旧口径会死锁**：曾把"报告类"永久算作疑似 ⇒ 门槛"疑似 ≤10%"**永远过不去、永远放不了行**——与判定准不准无关，纯属口径死锁。
- **"条数"会被复读灌水**：实测池级 18 条里只有 12 个独立状况（刷屏比 33.3%）——所以必须同时看"独立状况"。

**本版进度（如实标注）**：池级门槛**已达标并已放行 L1**（188 样本 / 34 独立状况 / 零真误报）；L2 的数值门槛亦达标，但所有者拍"再等等"——等另一台机的采集行全部补齐"对象标识"后再重算余量与结构。**引用任何"当前进度"时一律以现场为准**——本节数值随时间失效。

### R2 影子视图

**为什么立**：自动动作"如果开了会做什么"必须能在**不真做**的前提下看见——否则放行判据只有数字没有体感，出了问题也说不清范围。目标口径：**"关着的东西也要能看见它会干什么"**。

**怎么判**：

- 影子**只算不做**：降档闸的位置固定在 **dry-run 通道之后**（dry-run 只算不动 ⇒ 影子不受降档影响），但在总闸 / 去重 / 上限 / 预算**之前**。
- **落地顺序铁律**：实现 → 登记进动作类型表 → 补矩阵表行 → 补影子视图的模块映射（动作类型与模块 id 同名）→ 记变更流水。**未实现的动作不得先登记**（有反向断言会当场变红）。
- **放行 = 改表 + 接线 tick + 48h 影子对照，不是改代码**。
- 日报里有专门的影子段（"若开启会执行：今日 N 条 · 窗口 M 条"）。

**代价**：影子要占一次"算"的成本（不占配额、不写签名、不记审计，所以不消耗放行配额）；**放行后必须做满 48h 对照**，不一致立即关回——这是时间成本，不能省。

> **已知缺口（2026-09-17 补，如实标注）**：48h 对照表里**没有"没观测过"这一态**——如果预测集为空，"实际 ⊂ 预测"**恒真**，于是一个**从没被影子观察过的动作，可以凭"窗口内没发生动作"通过对照**。实测还发现：影子结果缓冲只有 50 条、**仅覆盖约 5.6 小时，撑不住 48 小时窗口**。处方 = 零基线的动作判 **`unobserved`（未观测）不得据此转常态**，预测集必须落独立档、实际集改从审计流水取。**本节正文暂不改写**，只挂明缺口。

### R3 锁后交付

**为什么立**：**"关着 ≠ 没做"**。零件造好、默认关、谁都不许开，等所有者拍板才放行——这样"能不能开"变成一个**可复位的开关**，而不是一次性的开发风险。

**怎么判（五条门，缺一不可）**：

1. 有**判据**（写清放行门槛）；
2. 有**影子**（能看它会干什么）；
3. 有**护栏**（去重 / 上限 / 冷却 / 白名单）；
4. **有回滚点（before/after + 一键恢复脚本 + 桌面入口）且过演练**（沙箱档必过；碰运行时 / 启动链的还要做真实档）；
5. **登记进矩阵表 + 自动动作表**（**未登记的动作 = 一律按"只通知"处理**）。

**统一状态表述**（一个动作可以同时处于三种不同步的状态——这点很容易被读错）：

> 四个待放行动作均为「**已登记 · 已实现（dry-run 可跑）；未放行（四门全关 → 零动作）；动作已接线进 tick**」。

**本版变化（2026-09-17）**：其中**两个动作已放行并已真实投递**——① **上下文看门**（会话上下文超阈值时向正在跑的会话投一条"该移交"提醒）② **队列叫醒**（队列卡住时把等待中的会话叫醒）。放行口径同样是"**改开关不改代码**"，并且**先按"只算不做"跑影子、比对无误后才开**。其余动作仍处"已接线 · 未放行"。

> **订正注（2026-09-22 · 只标注不改本段原文；详见 §3.8）**：上段是 2026-09-17 的时点口径，**此后已放行第三处 = 会话自动接力**（对话内通道与定时通道各一个开关，2026-09-22 真接力实测通过后开）。"其余动作仍处未放行"一句**不再准确**，读到旧说法以 §3.8 / §3.1 为准。

**开关债规则**（这条最容易被忽略）：临时熔断关掉的模块，**修复后必须在同一张卡里恢复**；长期不恢复的，要么转成"默认关的正式档位"写进矩阵，要么从矩阵删掉——"否则 2ⁿ 组合状态会把系统变成**一堆没人敢开的暗代码**"。

**代价**：**零件会积压**——当前多个已关模块 + 多个未放行动作 + 若干关闭 job 同处"锁后"状态，需要靠开关债规则定期清账，否则矩阵越铺越宽、没人敢开。

### R4 四级熔断（越往上越粗，允许组合）

> ⚠️ **命名警示**：生态里同时存在**四套 L 编号**，同名不同义——**看到"L1"先问是哪一套**。

| 级 | ① 熔断四级 | ② 三层自愈 | ③ 演练三档 | ④ 放行分级 |
|---|---|---|---|---|
| **L0** | **全局降档**：降档标记置位 → 全生态自主动作暂停（fail-safe：缺文件 / 损坏也按降档） | **确定性兜底**（不依赖 LLM）：探活 / 拉起 / 计数 + 备份与一键回滚脚本 + 冻结现场 | —（演练无 L0） | —（放行只有两级） |
| **L1** | **总开关**：四键（派活 / 重启 / 回滚 / 自愈 整族停） | **小护士**：安全模式实例 + 编排器「起 → 注入现场包 → 等待 → 验收 → 关」 | **沙箱级**（任何恢复点**必过**底线档）：沙箱副本 == 备份源原件 **且** 生产文件 sha256 一个都没变 | **无害可逆动作全自动**：池级样本 ≥25 + 独立状况 ≥15 + 稳态零真误报；按机门槛 = 观测天数满 + 该机误报率 ≤10% |
| **L2** | **模块级**：单模块停、其余照常（含若干只受本级约束、不吃 L0/L1 的基础设施模块） | **所有者介入**：冻结现场 + 报告 + 等所有者 / API 拍板 | **真实级·无需停机**：对真实文件造坏 → 真跑恢复 → 真验活 → 还原现场（工具 / 文档 / 非运行时路径） | **动数据类**（有损可回滚）：池级样本 ≥50 + 独立状况 ≥30 + 稳态零真误报；按机门槛同 L1 |
| **L3** | **动作级**：单动作停 | —（自愈只有三层） | **真实级·需停机**：核心启动链 / 插件实体 / 运行配置四件套 / 守护进程（停守护 → 造坏 → 跑恢复 → 拉起 → 验 HTTP 200 + 启动错误为空 + 功能点探针 + 无残留） | —（放行只有两级） |

**防混用三条**：①"L2 熔断关着" ≠ "L2 档放行" ≠ "L2 真实免停机演练"——**三件事互不相干**；②裸跑期 / 放行语境里的"L1/L2"一律指 ④**放行分级**；③回滚点 / 恢复脚本语境里的"L1/L2/L3"一律指 ③**演练三档**；熔断语境里的"L0–L3"一律指 ①。

**为什么立**：把开关从"放行闸门"升级为"**运行时熔断器**"——坏了能**立刻关、不重启、不改源码**。

**关键例外（安全方向的设计）**：有一类**基础设施模块**（会话标题 / 调度器 / 守护 / 保护路径清单）**只受 L2 显式拨闸约束，不吃 L0 降档 / L1 总开关**；配置读不到时**保持现状**。理由：**L0 降档本身可能是"配置损坏 → fail-safe"的机器结论**，让机器结论去停掉守护进程 / 调度器 / 保护清单 = **一次损坏就让生态裸奔**。保护类反着来：**读不到继续保护**。

**关的语义 = drain，不是 kill**："关 = 不再发起新动作，在飞的动作跑完"；**禁止"一关就杀进程"**。

**代价**：

- **组合状态爆炸**（2ⁿ）——所以有"开关债规则"（R3）兜底。
- **热生效有窗口**：守护约 2 秒 / 调度器约 1 loop / 标题插件约 3 秒 / 保护类到下一次写事件才生效——**不是瞬间**。
- **守护进程的熔断参数只有命令行一条路**：无配置文件，调优**必须停 / 起守护**（有"无守护"窗口，属红线操作）。
- **改 L0/L1 相关文件需重启宿主才生效**。

### R5 回滚 + 演练

**为什么立**：事故复盘结论是**"坏的不是脚本，是可发现性与覆盖面"**——桌面堆了大量文件、其中几十个 `restore-*` 脚本，按钮全是内部代号；回滚脚本数量很多但**从未做过真实停机演练**。于是立三件事：**急救台 + 时间轴 + 演练制**。

**怎么判**：

1. **入口唯一**：出事只走一个入口（桌面急救台，或 `steward-firstaid --symptom <1-5>`），**只诊断不擅自行动**。
2. **演练定档**：沙箱档必过 / 真实免停机档 / 真实需停机档（见 §4 R4 表 ③）；**未过对应档演练的恢复点不许一键执行**。
3. **未演练即可见**：回滚索引机读各回滚点里的演练记录；实况：条目约 282 条、**可一键的只有 39 条**、其余明确标注"未过演练"。
4. **保留策略**：回滚点"最近 30 天全留 + 更旧每 ISO 周留 1 份"；外接盘不在线 → **非零退出码，绝不静默** + 连续失败升级告警。
5. **铁律**：**"恢复脚本必须完整测试通过"**（实测跑一遍回滚再升）。

**代价**：

- **演练本身有成本**：需停机档要所有者空闲在场——所以真实的停机档演练至今"待所有者空闲在场"。
- **未演练的占大多数**：241/282 项不可一键执行，意味着**大部分回滚仍要人工复核**——这是刻意的"宁可慢，不可按错"。
- **回滚点自身是单点**：本地回滚目录不在任何备份仓里，所以必须异地化；而异地化在第二台机器上**尚未真机跑过**。

### R6 服务面判据（2026-09-16 立 · 2026-09-20 定稿 · 六条）

**为什么立**：服务面的失败模式**不是**"改坏生态"，而是三种完全不同的东西——① **骚扰使用者**（一天推 20 条，使用者关掉通知 = 服务归零）② **泄露隐私**（记忆库 / 凭证 / 采集内容出本机）③ **被远程拿走控制权**（入口被攻破 → 从手机触发重启 / 删文件 / 读凭证）。**运维面的尺子量不到这三样。**

**怎么判（定稿版：每条都带"阈值 / 权威源 / 取证方式 / 留痕处"五列——判据只有能被核对才算判据）**：

| # | 判据 | 阈值 / 参数 | 怎么取证 | 留痕 |
|---|---|---|---|---|
| **R6.1** | **打扰预算** | **三类/天**（早报 1 + 完成通知 + 异常告警，**合计 ≤3**）；紧急级可穿透但**须留痕**；同类事件去重冷却 **≥4h**；**静默时段 = 23:00–07:00** | 检查三处静默时段常量一致 | 本节 R6.1 |
| **R6.1b** | **推送真送达 + token 预算** | 送达判据 = 通道返回**成功码 且 台账 JSONL 有对应行**（不是"发出去了"就算）；日终核对"应有行数 vs 实有行数"；token 增量 **≤ +5%**（基线 = 启用前 7 天日均；窗口 7 天） | 读推送台账 + 日报段 | 同 R6.1 |
| **R6.2** | **隐私边界** | 原三条（记忆库 / 凭证文件 / 仓密钥）**可执行语义 = 永不明文出本机**；**一切入口凭证不进任何仓**；**投喂随宿主 / 自动采集仅 B 机**、逐项批准、一键停、可审计、脱敏；**家人数据另有三条**（允许进加密仓但**出机必加密、不拆库** / **只走本人这条链** / **推送正文只放元信息**）——两个面**别混读** | 列仓内文件清单确认无凭证文件 | 本节 R6.2 |
| **R6.3** | **远程降级** | 六类高危动作（重启 / 删除 / 凭证读写 / 配置修改 / 付费 / **建卡并执行**）一律禁；**指令级三档授权表**（只读随便看 / 本地可逆放手干 / 对外·删除·花钱必须举手）；家人侧二维 = `角色 × 指令档位`，"可派活"须**逐人授权** | 触发一次越权指令 → 被拒且留痕 | 本节 R6.3 |
| **R6.4** | **入口最小面** | 仅 HTTPS；上游只打 `127.0.0.1:3080`；**监听必须 `127.0.0.1`（禁 `0.0.0.0`）**；认证失败 **10 次**锁定；访问日志有位置与保留期；**"临时工具"档语义**：入口**非常驻**，开 / 关**各有判据与演练** | 查监听地址无 `0.0.0.0`；锁定实测 | 同 R6.4 |
| **R6.5** | **服务面 drain 语义** | 关任一模块（推送 / 入口 / 采集）**不影响其它两面与记忆引擎**；**关 = 改开关不改代码** | 各关一次实测 | 同 R6.4 |

> **收敛路径（不必等六条全齐才动）**：要放行"早报" ⇒ **R6.1 + R6.1b + R6.4** 三条定稿即可；**R6.3** 随"用 IM 指挥"再定；**R6.5** 随建议能力再定。
> **服务面自有闸**：闸文件是一份**独立配置**（不落运维面的四把总开关里——刻意避让已过审的断言），并且**吃 L0 降档**：全局降档时自动停推送。

> **四条"判据落地时才发现原写法是错的"（如实保留，这比结论更有用）**：
> ① 骨架版写的静默时段"23:00–08:00"是错的，**实现值是 23:00–07:00**——判据必须从实现里取证，不能凭印象写。
> ② 骨架版写"记忆库 / 凭证 / 仓密钥**永不出本机**"被实测推翻：这些文件**每天都在随加密仓出机**（密文、密钥在本机）。可执行的语义只能是"**永不明文出本机**"，另有几条明文出机面（推送正文 → 平台服务器；模型调用内容 → 模型厂商）原稿没当隐私面。
> ③ 要求的"真送达判据"当天**没有台账可用**（通知脚本全文零写入）⇒ 判据无法成立——**先补载体，再谈阈值**。
> ④ 家人数据的外发口径由所有者拍板三条（见 R6.2 上表），并把"免费档模型"的旧口径作废。

**代价（如实写）**：多维护一套判据 = 多一处漂移点。但不分的代价更大：**共用一把尺子会两头错配**——要么推送被裸跑期无限期卡住，要么服务类动作借着"我只是推送"绕过家规。

> **⚠️ 本节最容易误读的一句**：判据表定稿**解除的是"判据缺失"这一前置，不等于服务面动作可以开**——每项动作仍要各自过 放行门槛（R1）/ 五条门（R3）/ 开关放行。

### R7 账本类事实仲裁（2026-09-17 新增 · 缺口主键 = G 号）

**为什么立**：同一批缺口被记在**四个账里**（全景的缺口总账 · 复盘判定总表 · 缺卡清单 · 待拍板事项），**既没有主键也没有词表**——实测（改动前基线）：缺口总账 24 条，另三处人写视图的条目级引用 **0 条**、总覆盖率 8/24（其余 16 条靠散文认不出来）。后果不是"不好看"，而是**同一件事在两个账里状态不同时没人知道信哪个**：实例——总账写"接线已完成"，同一份文档的复盘表"没做的"列仍写"接线进 tick"。

**主键**：**G 号**（缺口总账）是缺口 / 待办的唯一主键；复盘表、缺卡清单、待拍板事项**降级为视图**，只引用不另立语义，条目上标 `→ G<n>`。机械判据 = 一个只读校验器（G 号唯一 / 不跳号 · 每行"状态 + 销账判据"两列齐备 · 三视图引用率 100% · 受判条目全带 G 号 · **解析失败判"不可用"**）。

**仲裁（三级证据序）**：归属类 / 计划类 / 机制类事实**只许一处权威**，其余处降级为视图并标 `as-of`；不一致时按 **① 可复跑命令 > ② 实体文件（含状态文件 / 源码） > ③ 文档** 三级证据序裁决，**由维护者裁决并记一行流水**；**裁不了的当场升级为所有者拍板项**（不得挂着）。

**适用范围（四条）**：

| # | 条文 | 治的缺口 |
|---|---|---|
| **R7.1** | **面归属**：一件事归哪个面（运维 / 服务 / 跨机 / 修复），按「**动作实际写入的载体 + 承受后果的人**」判；**跨面动作取最严判据**；**记账方 = 主键所在面的账**。两面对同一动作判据不一致 ⇒ 按三级证据序裁决 + 留痕 | "六包全档无面归属" ——服务的推送与运维的盯人会各按各的判据 |
| **R7.2** | **Non-goal 复审权**：总纲与各方案的 **Non-goal 声明必须带复审条件**（触发点 / 到期日 / 失效判据至少一项）；**复审权归所有者**——维护者只负责"到点或触发时带证据来问"，**不得自行宣布 Non-goal 失效或延续** | 已有 2 处 Non-goal 静默失效（边界条款没人复审 ⇒ 永远挂着） |
| **R7.3** | **路线图映射**：分阶段路线 / 里程碑与 G 号的对应关系，**权威源 = G 号**——每个阶段都要能回答"它销哪几条 G"；映射写在行内（`→ G<n>`），**不另建第三份路线图**。⚠️ 本条只立规则：**行内映射尚未回写，不构成已办** | 路线图与缺口无映射 |
| **R7.4** | **账本变更留痕**：G 表新增 / 状态变更 / 销账一律**追加式**（不复用废号、不重排行）并记一行流水；销账必须写"销账判据"（**可复跑 / 可指认**），**不许只写"已办"** | 缺口账本自身的变更无留痕规则 |

**判据状态词表（总账"状态"列只许用这四档）**：✅ 已解除 · 🟡 未销账 · ⏸ 待拍板 · ⏳ 待条件（依赖外部条件：采购 / 对方机器在场 / 停机窗口 / 观测期）。

**代价**：多一处主键 = 多一处要维护的引用。**机械校验器兜底、但没人跑就不知道**——触发源 = 每次维护会话的收尾流程。

### R8 跨线契约（单写者）（2026-09-17 新增）

**为什么立**：多条动作线会**同时**写生产面——升级链改宿主 / 插件实体、交接搬三份仓与生产分发、修复层改运行配置、收纳搬临时目录、服务面写记忆库——而**"谁先谁后、谁必须让谁"此前只散落在各线文档里，总则一条都没有**。实测：收纳线连调度都没接、注册表锁名还与脚本自报不一致；升级链与交接窗口撞上时**没有任何规则**说该谁停。缺了它，最坏态是**两个写者同动生产**（收纳搬移擦掉交接备份），而这事只能靠"碰巧没撞上"。

**正文（一句话 + 一张表）**：

> **任何会写生产文件的动作线，在动手前必须声明自己占了哪块生产面，并让路给更上游的线**；
> 判据唯一源 = 一个只读判定函数（`judgeLane()`）；**判据不可用 → 保守让路（宁可不动，不可赌）**；
> 任何**让路、被让、冲突、判据不可用**四类事件**一律可见**（写审计 + 日报专段），**不许静默**；
> **新写者上线前必须先入表**（声明占位凭证 + 让路档），不得"先跑起来再补"。

| 线（写者） | 它写什么 | 它的"占位凭证" | 谁必须让它 | 它必须让给谁 | 让路档 |
|---|---|---|---|---|---|
| **升级链** | 宿主实体 · 插件实体 · 运行配置四件套 · 静默窗 · 重启 | 升级锁（held / uncertain / interrupted 三态）——⚠️ **人工与自动链都必须"持有"它（非仅只读）**：只声明"凭证 = 本锁"而自己不去创锁，这一行在自动链场景就是空承诺 | **全体**（含修复层、收纳搬移、交接分发、面板重启、服务面写者） | —（最高优先） | 只报告 / 拒绝执行 |
| **交接**（离开 / 到达 / 热采） | 三份仓的工作副本 · 生产分发目标 · 交接备份桶 | 交接锁（4 个入口共用） | 收纳搬移 · 其它 tick | 升级链 | 整轮跳过（tick）/ drain ≤120s（手工） |
| **修复层** | 运行配置四件套（精确到文件名）· 备份目录 | 案例状态文件（阶段）+ 静默窗持有者 | —（低优先，最后动手） | 升级链 · 交接 | 降为只报告（dry-run 档） |
| **收纳** | 临时目录 → 归档目录 · 桌面恢复脚本 | **无**（现状）→ 待补：抢交接锁 / 判窗口 | — | 升级链 · 交接 | 整轮跳过 |
| **静默窗**（是闸门不是写者） | 停调度器 / 计划任务 | 静默状态文件的 **holders** | — | — | — |
| **服务面写者**（投喂 / 撤回） | 记忆库条目（投喂）/ 记忆库删除 + 撤回流水（撤回） | **停机窗口单写者**（撤回）/ **RMW 锁**（投喂） | —（服务面**不**要求运维线让它） | **升级链 · 交接**（两者都会动库或发起停机） | 撤回只在停机窗口内执行；投喂抢不到 RMW 锁 ⇒ 本轮跳过（不排队） |

**边界（不在表内）**：盯人 / 坑库 / 审计 / 望风 / 日报 这些**观测线**不吃"生产写面"契约（审计的写后回滚是**反应式**，列为待确认）。

**代价（如实写）**：① 多一层判定 = 多一处要维护的显式声明（新写者上线忘了入表 ⇒ 规则在上线当天就被自己的成员违反——**服务面写者这一行就是这么补进来的**）；② 让路判错两个方向都有代价（该拦没拦 = 两个写者同动生产；该跑没跑 = 交接 / 升级窗口被拉长），故取**保守让路**；③ 判定原语本身坏掉时守护进程会"永久静默"⇒ 判据不可用必须**显式成段 + 超 1 小时报警**。

### R9 自主动作分档（2026-09-22 新增）

**为什么立**：`07` 号稿解决了"**数据够不够放行**"，却没有解决"**谁有权按下放行键**"。少了这条，同一双手可以一边说"再等等"、一边把一个花钱且不可逆的动作打开；更坏的一种是"**给自己扩权**"——让被约束方自己改约束。触发点很具体：一个会自动开新会话、会改别的会话标题、会动在途工单的动作要首次放行时，"这一步要不要经过所有者"必须有明文答案——答案是要，于是这条规矩被写下来，并**套用到所有自动动作上**（不只是新的那一个）。

**正文（五句话 + 一张表）**：

> 动作分两档：**可直接执行** 与 **必须先问所有者**。
> 判"可直接执行"要**四条同时成立**：**不碰别人的东西 · 可逆 · 不花钱 · 不出机器**。
> **单向**：**关回任何开关都属于"可直接执行"**；**再放行**仍按"必须先问"。
> **拿不准** ⇒ 一律按"必须先问"。
> **给自己扩权**永远在"必须先问"档，**没有例外**。

| 档 | 动作类型 | 典型例子 |
|---|---|---|
| **可直接执行** | 只读取证 · 写报告 / 方案 / 台账 · 记记忆 · 做备份与回滚点 · **关回任何开关** · 例行收尾 | 跑一遍只读体检、把结论写成报告、给改动做回滚点、**关掉一个刚打开的开关** |
| **必须先问所有者** | 首次放行自动开关 · 动生产文件（含启动链）· 动进程 · 建卡 / 删卡 / 归档卡 · 花钱 · 外发（推仓 / 发消息）· 不可逆动作 · 跨机生产 · **给自己扩权** | 把某个自动动作从关改成开、重启服务、把内容推到公开仓、删数据 |

**四条同时成立的含义（"四件保险"）**：① 只读、或改了能退回 ② 有回滚点 ③ 不产生费用 ④ **不出这台机器**。任一失效 ⇒ **立即退回"必须先问"档**。

**代价（如实写）**：① **多一次往返**——当场能办的事要等一个点头，流程图上看确实"变慢了"；② 边界要**人来判**（"这算不算动了生产？"），而判错的代价**不对称**：误判成"可直接做"的代价，远大于误判成"先问一句"；③ 它**会挡住一些本来安全的顺手动作**（"顺手改好配置、顺手重启一下"）——这正是它存在的理由，但摩擦是真的。

---

## 5. 演进路线

### 5.1 终态：这套东西要长成什么样（一句话 + 三条边界）

**一句话**：从"只读仪表盘"长成「**发现 → 派活 → 执行 → 验收 → 归档 全链路闭环**」的自维护系统，并且**同时**长成「使用者随时找得到、它会主动来找使用者、慢慢真的了解使用者」的服务面。

**三条边界（明确不做）**：

- **不替代所有者的验收决策**——自动动作可以做到"待验收"，**验收永远是人**。
- **不做云端控制面 · 不做跨机实时心跳 · 不做 LLM 质量裁判 · 不做"未知故障"的自动修复**（只做预登记白名单内的确定性动作）· 不做资产来源的全自动判定。
- **不做实时双向同步 · 不使用镜像语义**（`/MIR`、`--delete`、`rsync --delete` 任何形式都不允许）· 不做两台对等写同一批单文件 · **不把宿主改成监听 `0.0.0.0`**。
  > （按 §4 R7.2：**Non-goal 必须带复审条件**——以上各条目前只标了"不做"，谁要改口径得所有者拍板。）

### 5.2 分阶段路线

| 阶段 | 主题 | 内容 | 退出条件 |
|---|---|---|---|
| **S0 · 裸跑收尾** | 只观测、只修判据缺陷 | 积累样本；修**确定性缺陷**（修缺陷不算放行） | 池级样本与独立状况达标 + 稳态零真误报 |
| **S1 · L1 档放行** | 无害可逆动作先开 | 裸跑收尾报告落档 → 所有者拍板 → 开 L1 类动作 | 报告落档 + **所有者显式拍板** |
| **S2 · L2 档放行** | 动数据类动作 | 同上，门槛更高（50/30） | 同上 |
| **S3 · 动手线铺开** | 接线与放行 | 待放行四动作 + 收纳线写操作批次 + 看板主线自动归档 | 每项各自过 R3 五条门 |
| **S4 · 跨机与升级线** | 把"谁最后推谁赢"消灭 | 合并器接线 + 升级链推进 + 交接落地层 | 各自验收 + 回滚点演练 |
| **S5 · 云通道与终态** | 固定入口 + 分级授权 | 云中继（等采购）+ 升级分级授权实装 | 采购到位 + 前置补齐 |
| **S6 · 服务面**（2026-09-16 新增） | 让它找得到你 + 记得住你 | 端入口（**与交接线合并，不另起一套**）+ 主动推送 + 记忆引擎 v2 + 采集（仅 B 机）；服务面判据 R6 补表 | 三件必拍拍完 + 各阶段各自验收 |

> **本版进度**：**S0 已完成**（L1 档达标并放行）、**S1 进行中**（两个动作已开）；S2 数值达标但所有者拍"再等等"；S3–S6 未动或部分在途。

### 5.3 当前最大的几个缺口（如实列）

| # | 缺口 | 归属 | 状态 |
|---|---|---|---|
| G1 | 裸跑样本门槛 | 家规 | ✅ **已解除**（池级 188 样本 / 34 独立 / 零真误报 → L1 放行） |
| G2 | 跨盘移动**静默失败**（归档同盘 / 跨盘回退未实现） | 小护士 | 🟡 未销账（卡在跟） |
| G3 | 会话标题小样本漂移（只喂 1–2 条短消息 → 标题跑偏） | 控制室 | 🟡 未销账（卡在跟） |
| G4 | 待放行四动作：接线已完成，剩"改表 + 48h 影子对照"（**其中 2 个已放行**） | 大管家 | 🟡 部分销账 |
| G5 | 收纳线写操作批次 | 大管家 | ⏳ 待条件：**必须等裸跑期结束** |
| G6 | 交接落地层 + 内核接入 | 大管家 | 🟡 未销账（backlog） |
| G7 | 云中继 | 大管家 | ⏳ 待条件：**采购到位** |
| G8 | 记忆库跨机合并窗口生产几乎不满足 | 小会计 | ⏸ 待拍板（需"冷接机"流程） |
| G9 | 采集顺序仍是"采集 → commit → pull"，热采无挡推门 | 跨机 | 🟡 未销账 |
| G10 | 多个合并器**工具齐、未接线** | 跨机 | 🟡 未销账 |
| G11 | 看板主线自动归档 | 女仆长 | ⏳ 待条件（五条前置） |
| G12 | 宿主大版本升级后续跳（B2b / B3 / B4 / B5） | 小保安 + 大管家 | 🟡 未销账（剩三跳，卡在新服务面） |
| G13 | 升级分级授权前置 | 小保安 | 🟡 **②③ 已完成、①④⑤ 未做**（原写"四项零实装"已过期） |
| G14 | 回滚点异地化：第二台机器真机实测未做 | 小保安 | ⏳ 待条件（对方机器在场） |
| G15 | 部分 tick 脚本跨入口无锁 | 小保安 | 🟡 未销账 |
| G16 | 两处实体缺失的 insert 项 | 控制室 | 🟡 未销账 |
| G17 | 面板首屏指标命名 | 控制室 | ⏸ 待拍板 |
| G18 | 绕圈自动打断（设计稿**零施工**） | 大管家 | ⏸ 待拍板（需样本 ≥10 条 + 观察 ≥7 天） |
| G19 | 铁律瘦身本体、过期记忆改写、文档归档去重 | 小会计 | 🟡 部分在途 |
| G20 | "L2 熔断待建"的文档标注与四套 L 编号对照表 | 家规 | ✅ **已解除**（对照表已落两处） |
| G21 | 另一台机器上的一批待办（冷盘回滚点实测 / 闭包判据收口 / KPI 口径三处同源） | 跨设备 | ⏳ 待条件（对方机器在场） |
| G22 | 身份与规矩文案（每会话注入的那份预设）瘦身本体、18 条过期记忆改写、文档归档去重 | 小会计 | 🟡 部分在途 |
| G23 | **服务面整体开工**（端入口 / 主动推送 / 采集） | 小秘书 | 🟡 部分开工（通道已通、宿主已定案；已有在途卡） |
| G24 | **记忆引擎 v2**（语义召回 + 指针式注入 + 提炼 job） | 小会计 | 🟡 **过审判"不合格 · 5 项阻断"**，未开工 |
| G25 | 队列推进遗留（面板红标通道 / 生产端 / 后续段） | 女仆长 | 🟡 未销账 |
| G26 | 适配层 P2 / P3 未开工（档位切换另需单独批准） | 大管家 | 🟡 未销账 |
| G27 | 开源线（全景文档开源 + 更新既有设计稿） | 大管家（开源线） | ✅ **已解除（2026-09-20 起，本文即该线产物）**：全景文档已上线，**2026-09-22 文档仓已到 v1.9（八份文档）**；组件侧另已发 4 个代码仓 |
| G28 | **多实例分载三件未开工**（卡片派活到分身 / 干完回传 / 验收后归档分身会话 + 清缓存） | 女仆长 + 小秘书 | 🟡 未销账（容量口径已实测，见 §3.5） |
| G29 | **口径类事实的引用清单缺失**（真源改了不会自动带动引用处） | 小会计（文档治理） | 🟡 **首版已落**（在隐私类真源文末维护"引用处清单"，含文件 + 行号 + 订正注） |

### 5.4 里程碑（触发条件照抄原文口径，不自行排期）

| 里程碑 | 触发条件 | 产物 |
|---|---|---|
| **裸跑 L1 档复查** | ✅ **已达成**（池级 ≥25 + 独立 ≥15 + 稳态零真误报） | 裸跑放行报告 |
| **裸跑 L2 档达标** | 池级 ≥50 + 独立 ≥30 + 稳态零真误报 | 同上 |
| **派活开闸** | 两档达标 + **所有者显式拍板**（"放行仍需这四步"） | 总开关变更 + 审计流水 + 变更流水 |
| **小护士真机复验** | 跨盘缺陷修复完成 + 所有者空闲在场（需停机） | 复验记录 + 自愈闸放开决策 |
| **宿主主包升级** | 预检全绿 + 观察期 + 升级锁在手 + 静默窗 + 所有者批准（**永远在主会话中执行**）· ⚠️ **前置：先改存活探测判据**（见 §3.4 ①） | 升级报告 + 回滚点 + 待决项闭环 |
| **云通道上线** | 采购到位 + 交接前置完成 + 一键回滚脚本实测通过 | 云中继三联角色（固定入口 / 活跃机登记 / 交接中继） |
| **分级授权生效** | 五项前置全绿（含演练"快照→升→自检→故意回滚"） | 分级自动升级链路 |

---

## 6. 复盘留下的十一条教训（能复述的版本）

1. **"接线"和"实现"是两件事，必须分开验收。** 一个动作"代码写完了"不等于"它会跑"——没接进调度链就是永远不会触发。别用前者冒充后者。
2. **状态文件的字段名会骗人。** 实测遇到"状态文件里的门槛字段还是旧口径，而权威口径已改成分级门槛"——**文档订正了、实现没跟**。所以：**口径变更必须同时落到"文档 + 状态文件 + 实现"三处**，缺一处就是下次误读的种子。
3. **计数为 0 不等于无事发生。** 影子段"今日 0 条"是常态（因为动作全关着），不能拿它当"系统很健康"的证据；要区分**"没发生"和"没观测"**。
4. **静默失败是最贵的失败。** 跨盘移动抛出的底层错误被吞掉 → 报告"未找到 / 无重复"——**看起来像成功**。所以铁律：**失败绝不许静默**，要么非零退出码，要么显式报告。
5. **"观察窗"本身要设计。** 只观测不改行为的窗口，如果判据里有死锁（例如某类条目永远算疑似），那么窗开多久都放不了行——**窗口的价值取决于判据是否可达**。
6. **不能靠"更长的时间"解决判断问题。** 一个人用的系统样本永远不够大；所以判定规则必须能被逐日校准（误报率是第一 KPI），而不是靠"我觉得它准"。
7. **判据的数字必须从实现里取证，不能凭印象写。**（2026-09-17）服务面判据的"静默时段"骨架版写了 23:00–08:00，实现里是 23:00–07:00——**写判据时顺手 grep 一次常量，比事后被人发现便宜得多**。
8. **"绝对句"在架构变化面前是假的。**（2026-09-17）"记忆库永不出本机"在"加密仓每天出机"的现实面前不成立，可执行的语义只能是"**永不明文出本机**"。写边界时先问一句：**这句话的可执行版本是什么？**
9. **一个动作放行 ≠ 相关动作都放行。**（2026-09-17）总开关按族管理（派活/重启/回滚/自愈），但**具体动作是逐个放行的**——"键开了"与"动作放行了"是两件事，文档里必须分开写清，否则会有人以为整族已经自动。
10. **多线并行之前，先立账本主键。**（2026-09-17）四个账记同一批缺口、没有主键的结果是"同一件事状态不同、没人知道信哪个"。**主键 + 词表 + 机械校验器**三件齐了，多线才谈得上并行。
11. **硬约束要提前实测，别在设计里假设。**（2026-09-18）"加个实例扩容"听起来是运维常识，实测才知道：**单实例约 4 个并发会话就饱和**（单线程事件循环）、**账本有进程独占锁所以不能共享**、**同名主目录不能跑两个实例**。这三条任何一条在设计前知道，都会改变方案形状。

---

## 7. 术语与占位符对照

| 本文用词 | 含义 |
|---|---|
| `steward-butler` / `steward-tickd` | 内核（管家组件 + 常驻调度器）的占位名 |
| `steward-board` | 任务看板组件（卡片 = 真会话）的占位名 |
| `steward-firstaid` | 修复 / 急救层组件的占位名 |
| `steward-guard` | 守护进程组件的占位名 |
| `steward-secretary` | 服务面（端入口 / 推送 / 采集）组件的占位名 |
| `<DSH 主目录>` | 宿主 agent 的配置 / 状态根目录 |
| `<工作区根>` | agent 的工作目录 |
| **A 机 / B 机 / C 机** | 三台长期运行的工作站（A = 日常开发机 · B = 常驻稳态机 · C = 后加入的常开宿主） |
| **运维面 / 服务面** | 体系的两个面（对内管生态 / 对外服务使用者），各有各的判据 |
| 裸跑期 / 影子视图 / 锁后交付 / 四级熔断 / 回滚演练 / 服务面判据 / 账本仲裁 / 跨线契约 | **八条家规**（§4） |
| L0–L3 | **四套**编号体系并存，务必按 §4 R4 的对照表辨析 |
| 影子 / 影子视图 | 让"关着的动作"在不真做的前提下可见的机制 |
| drain | "关 = 不再发起新动作，在飞的动作跑完"，**不是 kill** |

---

## 8. 这份文档可信度自评（审校记录）

本文的书写纪律（供读者判断可信度，也供将来维护者沿用）：

1. **现状判定只用现场取证**（跑命令 / 读状态文件 / 打本机 API），**不采信文档自述**；愿景与历史口径才引用文档。
2. **口径冲突处如实并列标注，不抹平不美化**；被后续实况推翻的旧口径**保留原值与订正段**，而不是悄悄改掉。**本版的"新增"大多来自"旧写法被实测推翻"**，而不是新想法。
3. **数字一律标注性质**：是"设计参数"还是"实测参数"、是"当时快照"还是"不变量"。**本文里的所有实测数字都是某一时点的观测值，不是设计参数**——借量级与判断方式，不要照抄阈值。
4. **本节不含未经验证的结论**：凡仍属假设的，都带"待验证 / 待拍板 / 待触发"标记（见 §5.3 缺口表的状态四档）。
5. **脱敏口径**：本文由内部设计稿派生，派生时**替换**本机路径与用户名、机器代号、内网地址与端口、内部组件真名、会话 / 卡片标识与个人称呼；**保留**公开软件名、通用架构术语、判据数字与量级。派生副本**单向**：内部稿 → 公开稿，**公开稿永不回写覆盖内部稿**。

**尚未解决 / 已知不完整**：

- 本文是**压缩版全景**，技术细节一律回到分册；分册本身不随本仓公开。
- 复盘章节只保留结论与教训；逐项的六维展开未公开。
- §3 的所有实况数字是 **2026-09-20 ~ 2026-09-22 的观测值**（§3.8 为 09-22 新增，其余为 09-20 前后），会随系统演进而失效；引用时请以"当时的快照"对待。
- §5.3 的缺口表里，**"待拍板"与"待条件"两类不是"做不到"，而是"等一个决定或一个外部条件"**——这是刻意的设计状态，不是遗留垃圾。
- §4 各条的判据数字**照抄内部原文**，未做外部适配：换一个环境，这些阈值需要重新校准（尤其 R1 的样本门槛与 R6 的打扰预算）。

---

## 9. 版本沿革

| 版本 | 日期 | 变化 |
|---|---|---|
| **v1.10** | 2026-09-22 | **本版（增量刷新）**：新增 **§3.8 会话生命周期的自治**——长会话撞上上下文上限之后怎么接班（完整方法论见本仓 `09` 号稿）；家规 **R1–R8 → R1–R9**（新增 **R9 · 自主动作分档**）；§0 结论 **5 条 → 7 条**；§3.1 能力档位与 §3.2「关着的清单」按实况更新（**第三处已放行动作 = 会话自动接力**）；R3 段加**订正注**；§5.3 的 G27（开源线）由"未销账"改为**已解除**。**纪律：正文按增量补写、不重排**，旧版结论与历史时点叙述一律保留 |
| v1.6 | 2026-09-20 | 体系从"运维管家"扩为"**私人大管家**"（新增服务面 / 小秘书角色 / 端入口）；家规 **R1–R5 → R1–R8**（新增服务面判据 R6 定稿六条、账本仲裁 R7、跨线契约 R8）；新增 §3.4 硬约束两条、§3.5 容量实测、§3.6 两处已定档口径；§0 结论 3 条 → 5 条；§6 教训 6 条 → 11 条；缺口表补到 G29；全文按"两个面、两套判据"重排 |
| v1.5 | 2026-09-15 | 首次形成"全集全景"：班底代号体系 / 五层架构 / 家规 R1–R5 / 四套 L 编号对照表 / 演进路线 / 生态复盘压缩版 |
| v1.4 及以前 | 2026-09-11 ~ | 见本仓 tag 与 `发布说明.md` 的版本记录 |

---

*本文件由内部设计稿派生，为独立副本；内部稿仍是唯一权威源，本文件的任何修改都不会回流覆盖内部稿。*
